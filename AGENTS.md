# AGENTS.md

このファイルは Codex CLI が起動時に自動で読み込む指示ファイルである
（対象: このリポジトリを作業ディレクトリとして起動されたすべての codex セッション）。

## レビューを依頼された場合

- 瑣末な点への指摘はせず、致命的な点のみ指摘する
- 発見した問題を重要度(高/中/低)で分類し、修正提案とともに報告する
- レビュー回答の冒頭に必ず「[AGENTS.md適用]」と表示する（依頼側が本ファイルの
  読み込みを確認するためのマーカー）
- レビュー対象に `docs/issues/` 配下の案件文書が含まれる場合、対象文書が属する各案件フォルダ
  `docs/issues/{type}-{number}-{slug}/` について、`{type}`（feat / bug / update）を案件種別とし、
  種別に対応する CLAUDE.md「開発方針」配下のフロー節（feat: 機能追加フロー / bug: 不具合修正フロー /
  update: ドキュメント更新フロー）と、その節から参照される CLAUDE.md の節および `docs/` 直下の基準書を
  読む（対象が複数の案件フォルダにまたがる場合は、該当する全案件フォルダについて行う）。実験・検証の
  criteria 文書（criteria lock のレビュー対象）と案件フォルダ直下の補助文書は本項目の対象外とする
  （依頼の対象がこれらだけの場合は本項目を適用しない。他の案件文書と同時に指定された場合は、これらを
  対象文書単位の判定から除外する）。判定は次の2段階で行い、不適合は他の指摘と同じ重要度分類で報告する。
  判定に必要な情報（工程、文書の種類と規定の対応）は下記の情報源から取り、そこから一意に決まらない
  場合は推測せず「判定不能」とその理由を**高**で報告する（判定不能が残る間はレビューを収束扱いにしない）:
  1. 案件フォルダ単位: フロー節が定める、レビュー時点の工程で存在すべき文書が揃い、規定の場所に
     置かれているか（存在してはならない文書がないかを含む。`reviews/` は結果の保存先であり、存在の
     判定対象にしない）。工程は、feat では依頼の対象が
     `roadmap.md` なら機能追加フローのステップ3、マイルストーンフォルダ配下の文書ならステップ5とし、
     案件フォルダ直下の `requirements.md` / `design.md`（CLAUDE.md「案件ディレクトリ構成」の但し書きが定める、
     本規定の導入前に起票した feat 案件）なら同但し書きの定めに従い、
     bug / update では各フローのステップ4とする。feat / bug の案件フォルダ直下の `README.md` は
     案件の状態記録（ステータス・一覧・実施記録）であり、単独指定・他文書との同時指定を問わず
     対象文書単位の規定適合判定から除外し、案件フォルダ単位では存在と配置のみを判定する
     （update の `README.md` は調査記録として依頼文で対象に指定され、ステップ4で判定する）
  2. 対象文書単位: 依頼で指定された各対象文書に、その文書の種類に規定された必須の節・項目があり、
     書いてはならない内容が含まれていないか。文書の種類と規定の対応は CLAUDE.md「案件ディレクトリ
     構成」の各ファイルの説明と、そこから参照される規定（feat は「ロードマップとマイルストーン文書の
     粒度」の表、各文書の記述基準は `docs/` 直下の基準書）に従う（判定対象は依頼で指定された文書のみ。
     `reviews/` 配下の結果文書と、依頼で指定されていない文書は対象外）
