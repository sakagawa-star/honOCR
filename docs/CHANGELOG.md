# CHANGELOG

## リリース履歴

### 2026-09-08

- **update-010**: feat-024 で露出した運用上の規定の穴 8 件（F1・F3・F6〜F9・F15・F18）の見直し（ルール改訂の第 2 段）
  - **契機**: update-009（第 1 段）で式と次元の枠に吸収されなかった `rule_feedback_notes.md` の 8 件を、ユーザー決定（「改訂を2段に分ける」）に従い
    update-009 反映後の `CLAUDE.md` に照らして個別に調査し、規定で埋めるか記録にとどめるかを項目ごとに判断した（ユーザー「推奨を取り込む。その他は再度問題が発生したときに考える」）
  - **`CLAUDE.md` の変更（10 件。434 → 437 行）**: **F1** ステップ6 と Sonnet への指示 4 に「実装の委任には段の実行（実データの投入）を含めない。
    実行単位ごとに本体が予測を記録してコミットしてから 1 実行だけ行う」（bug 案件の修正計画に定めた動作確認は委任対象に残す）。
    **F3** 「実験・検証の進め方」項目 2 に「予測を記録したらその `experiment_log.md` をその場でコミット」、「git 操作の実行方法」に本体が直接行う例外
    （ステージ対象 1 ファイル・定型メッセージ・push なし・指示不要・失敗時は報告）。証跡が示すのはコミット時点の記録内容とその後の変更履歴だけで、
    予測と実行の前後関係は証明できない（実行時の遵守の機械的検証は未解決）と明記。**F6** 「パイプラインの切り方」末尾に「要調査事項の扱い」
    （補助文書に事実と典拠のみ。目標・段・想定値・マイルストーンを変えない）。**F7** 粒度表 `README.md` 行に「承認・実装開始・段の実行のたびにその場で更新」。
    **F8 (8-a)** 「収束」の定義のレビュー単位に `experiment_log.md` の実行後の書き換え（当該マイルストーンのステップ7 通過までを 1 単位。
    書き換えのたびに同じ段の再実行を含む次の実行またはステップ7 の前に収束。人レビューはステップ7 で兼ねる。通過後の書き換えは後続工程の進行状況によらず
    書き換え時にレビューを開始し、収束 → 訂正内容の人レビュー → 承認記録で終える）と、対応する依頼文を「依頼文の基準部分」に新設。
    **F9** 粒度表 `roadmap.md` 行に「状態欄の更新は改訂に当たらず、改訂履歴に書かない」
  - **取り込まなかった項目**（再発時に再検討）: F8 (8-b) 補助文書の新設・改版のレビュー単位（判定基準を含まない記録で、負荷が実利を上回る）、
    F15 補助文書の改版の単位（既存の「収束」の定義から導ける）、F18 トークン値の実測前記入（規定の穴ではなく遵守の問題。記録のみ）
  - レビュー: `gpt-6-astra low` で codex-01（A、中3）→ codex-02（B、中1）→ codex-03（B、ゼロ）→ `/new` → codex-04（**C**、中1）→ codex-05（B、中1 未解消。
    同じ指摘が 2 回続いたため**行き詰まり検出**を適用し、通過後の書き換えの期限を工程に結びつける方針を取り下げ）→ codex-06（B、ゼロ）で収束 → 人レビュー承認（「反映してください」）
  - 反映は Sonnet サブエージェントに委任し、10 件の各 `old` がちょうど 1 回出現することを確認して置換。想定外事象なし。
    反映後に Claude Code 本体が `git diff` で 10 項目を検証（変更ファイルは `CLAUDE.md` のみ・設計の模擬置換結果とバイト一致・`AGENTS.md` と `docs/REVIEW_CRITERIA.md` 無変更）

- **update-009**: 段の予想値を式から導出する規定への改訂（想定値の根拠の種別と梯子の廃止、2 段の再検討、外れ後の記述制限）
  - **契機**: update-008 の規定を feat-024 に初適用した結果、想定値が式から出ないまま lock され（`roadmap.md` 第3版 §6 の「推測」）、
    外れた後にエージェント（Opus）が解釈・区分・自作の選択肢を書き、再計画（第4版）が段の表・最終出力の成分に戻らず該当行の形式だけを直した
    （`rule_feedback_notes.md` F2・F4・F5・F11〜F14・F16・F17）。ユーザーは「式と次元の考え方が全く使われていないのが一番の問題」と目標を再設定し、
    2026-09-08 の壁打ちで枠を確定した（案件 README §1.2）
  - **`CLAUDE.md` の変更（14 件。426 → 434 行）**: 「パイプラインの切り方」の想定値を **`予想値 = 式（入力値）`** の書式（幅と幅を出した計算。
    根拠の種別は廃止。式の無い行は不適合。lock されるのは式と幅の計算方法で、値は前段の実測後に代入）に。「段の切り方の規則」に
    「式で予想が出せない量はそれを測る段を前に置く」（測る段は決める項「無し」＋測る項）を追加。「実行後の照合」を人の許可なしの再検討に変え、
    「式の精度は未検証」を記録。「ロードマップの改訂（再計画）」を書き換え: 再検討のトリガーは自動・改訂は人レビュー必須、
    **書いてよいもの**を 4 つ（事実・式の連鎖・取った手・外れ 2 回時の推論の取り下げ）に限定し判定・分類・自作の選択肢を禁止、
    梯子の手 1〜4 を**再検討の 2 手**（手 1 次元を固定して式を変える → 手 2 次元を変え、入力側なら前段・出力側なら後段に波及）に置換、
    diff を伴わない再検討の禁止、改訂履歴に式の連鎖。粒度表の `roadmap.md`・`experiment_log.md` の行、ステップ6 の直前予測、
    「実験・検証の進め方」項目 1〜3 の feat 読み替えを式ベースに。「行き詰まり検出」の外れ 2 回に理由（エージェントは事前の推論に執着するため、
    それを断ち切る外部トリガー）を明記し、feat の段の外れでは代替案を再検討の 2 手に限定
  - **採らなかった案**（案件 README §1.4）: 予想の二分法（停止条件／結果の予想）、根拠の種別の存続、外れの回数と再検討の水準の対応づけ、
    外れ 2 回ルールの廃止、外れへの解釈の付与、独立項目の同時取り込み。いずれも Claude Code（Fable）の提案をユーザーが退けた
  - **改訂は 2 段**: 本案件は壁打ちの枠のみ。独立項目 F1・F3・F6〜F9・F15・F18 の見直しは本案件の完了後に判断（第 2 段）。
    feat-024 は一時中断（第 2 段の後に feat-025 で目的のみコピーして新規定で実施し、完遂したら中止）
  - レビュー: codex-01（A、高1）→ codex-02〜04（B、各高1。codex-03 で**行き詰まり検出**を適用し P11-a の手順を P6 に一元化）→ codex-05（B、ゼロ）
    → モデルを `gpt-6-astra high` に変更して再起動 → `/new` → codex-06（**C**、高2: 手 1 の範囲／測る段の追加禁止）→ codex-07（B、ゼロ）で収束
    → 人レビュー承認（「反映してください」）。codex-06 後の B は利用上限で 1 回中断し、回復後に同じ会話で再送
  - 反映は Sonnet サブエージェントに委任し、14 件の各 `old` がちょうど 1 回出現することを確認して置換。想定外事象なし。
    反映後に Claude Code 本体が `git diff` で 12 項目を検証（変更ファイルは `CLAUDE.md` のみ・旧規定の語の残存 0 件・設計の模擬置換結果とバイト一致・
    `AGENTS.md` と `docs/REVIEW_CRITERIA.md` 無変更）

### 2026-09-07

- **update-008**: パイプラインの切り方の明文化（入出力を次元で把握し、段ごとに想定値を置き、外れたら梯子で見直す）
  - **契機**: update-006 で導入した feat 案件のマイルストーン規定を feat-024 に初適用した結果、3 日間で最終出力の成分が 0 個のまま
    文書 1,456 行・Codex レビュー 28 回に至った。ロードマップの分割は 4 境界の基準に形式上は適合し、Codex レビューも収束していたが、
    最終出力の式に項を持たない段（逆引き）の「検証の検証」に作業が集中した。ユーザーは「今のルールでは Opus がロードマップ方式で
    開発できない。馬鹿でも使えるルールにしたい」と目標を再設定し、feat-024 を実験台として**シミュレーション**を実施した
    （入力 10,352,855,692 次元 → 最終出力 6 次元のパイプラインを段ごとに次元・処理単位・決める項・想定値で再構成）
  - **`CLAUDE.md` の変更（14 箇所。418 → 426 行）**: 「マイルストーン分割（feat 案件の必須手順）」を「**パイプラインの切り方**」に置換
    （4 境界・併合 3 条件を削除。ステップ2 で書く 4 項目＝最終出力の成分／入力の次元／段の表〔処理単位・単位数・入出力の次元の式・手段・
    **決める項**・結合点〕／想定値〔根拠の種別・外れの幅〕、切り方の規則 4 つ、ID 規則は保持）。「ロードマップの改訂（再計画）」を
    **梯子**（手 1 段の見直し → 2 分割 → 3 構成の見直し → 4 目標変更。飛ばす・戻すにはユーザー指示。実行前の信号は手 3 から）に置換。
    粒度表の `roadmap.md` に上記 4 項目、`requirements.md` に**閾値の再掲・追加の禁止**、`experiment_log.md` を全マイルストーンに。
    「実験・検証の進め方」の feat 読み替えを「`roadmap.md` の想定値と幅の人レビュー通過＝criteria lock」に。
    「行き詰まり検出」に「想定値の幅を 2 回続けて外れたら」を追加。ディレクトリ構成ツリーのコメントを更新
  - **保持すべき制約からの逸脱**: update-006 README §5.1 (d) の「4 種類の境界と併合の 3 条件の内容」を、ユーザー判断 A(1)（2026-09-07）で削除。
    由来の目的（小さく作って動作確認し次へ／1 マイルストーン＝入力を与えて出力を確認できる単位／完了条件は実行結果／覆ったら中断して戻る）は保持
  - **採らなかった案**（案件 README §1.4）: 文書の行数上限／最終出力の確定数の停止規則／M1 の垂直スライス固定／受け入れ基準への典拠必須化／
    判断の境界の適用範囲の限定／4 境界の無根拠な削除。いずれも Claude Code の提案をユーザーが退けた
  - **記録の訂正待ち**: update-006 README §5.1 (a) とテンプレート update-011 留意事項 D の「feat-036 は規定導入後に判断の境界を全廃」は
    ユーザー口述（2026-09-07）により順序が逆（元形式で 6→3 に削った後にロードマップ形式を投入）。本案件では口述を典拠とし、訂正は範囲外
  - レビュー: codex-01（A、高1・中1）→ codex-02（B、ゼロ）→ `/new` → codex-03（**C**、高1: P5・P9 の `old` が範囲指定で置換不能）→
    codex-04（B、ゼロ）で収束 → 人レビュー承認（「こちらのレビューはok。実装を開始してください」）
  - 反映は Sonnet サブエージェントに委任し、P1〜P14 の各 `old` がちょうど 1 回出現することを確認して置換。想定外事象なし。
    反映後に Claude Code 本体が `git diff` で 9 項目（＋8-b・8-c）を検証（変更ファイルは `CLAUDE.md` のみ・旧規定の語の残存 0 件・
    新節と梯子の存在・粒度表と criteria lock と行き詰まり検出の更新・`AGENTS.md` 無変更）
  - コード変更なし・テスト不要（`docs/TECH_STACK.md`・`docs/PROJECT_KNOWLEDGE.md`・`AGENTS.md`・ルート `README.md` は更新していない）
  - **次**: feat-024 の `roadmap.md` を新規定で第 3 版に切り直す（判断 E(1)。feat-024 側の作業。新規定の初適用＝モデルケース）

- **update-007**: マイルストーン規定の適用上の欠落2件の解消（旧配置の対象範囲・スクリプトの置き場）
  - update-006 で導入した「feat 案件のマイルストーン分割」規定を進行中の feat-024 に適用する過程で見つかった欠落2件を、
    `CLAUDE.md`（5箇所）と `docs/issues/update-006-adopt-template-milestone-layout/README.md`（1箇所）で解消した
  - **欠落1（P1・P6）**: 「案件ディレクトリ構成」の但し書きが旧配置の対象を `feat-001〜feat-024` としていたが、
    **feat-024 は 2026-09-07 にユーザー判断で新フロー（`roadmap.md` ＋ マイルストーン）へ移行した**ため実態と食い違っていた。
    対象を **`feat-001〜feat-023`** に狭め、update-006 README §1.3 の案 D（「進行中の feat-024 を新フローへ移行する」を不採用とした判断）に
    **撤回の経緯**を追記した（判断が後で覆ったことを記録しないと、テンプレート還元時に誤った経緯が伝わるため）
  - **欠落2（P2〜P5）**: スクリプトの置き場の規定が「ジョブスクリプト・実験コード」と「プロダクトコード」を区別しておらず、
    限定の有無が箇所によって割れていた（「ドキュメント作成ルール」と粒度表の `design.md` 行は限定なし、ツリーのコメントと「細かさの上限」は限定あり）。
    **マイルストーンフォルダに置くのは「そのマイルストーンのためのジョブスクリプト・実験コード」、プロダクトコード（案件の完了後も他の入力・
    他の案件で使う CLI・モジュール）は `scripts/` 配下・テストは `tests/` 配下**と定め、**判定基準（「その案件が終わった後も使うか」）**と
    **判定と理由を当該 `design.md` の ADR に記録する義務**を明記した。4箇所（ドキュメント作成ルール・粒度表・細かさの上限・ツリーのコメント）で文言を揃えた
  - **起票の経緯（規定違反の是正）**: Claude Code が上記2件を「軽微な修正」として `CLAUDE.md` を直接編集し（1回目はユーザーに選択肢を提示して承認を得たが、
    2回目は推定で実施）、Codex が「アクティブな update 案件がない。`CLAUDE.md` は update 案件でのみ変更する規定」と**高**で指摘した。
    ユーザーの指摘を受けて違反を認め、**直接編集を `git checkout` で破棄したうえで本案件を正規フローで起票**した（経緯は案件 README §1.4）
  - **運用の変更**: ユーザー指示（2026-09-07）により「軽微なドキュメント修正は update 案件を省略可」という従来の運用を**廃止**し、
    **運用ドキュメントの変更は規模を問わず update 案件として起票する（省略不可）**こととした。
    これは `CLAUDE.md` の変更を伴わない（ドキュメント更新フローは既に「フローを厳守する」と定めており、省略の余地は規定上もともと存在しなかった）
  - レビュー: codex-01（A、ゼロ）→ `/new` → codex-02（C、ゼロ）で収束 → 人レビューで「update 案件の省略不可」の指摘 → 案件 README §1.3・§1.4 に反映 →
    codex-03（B、ゼロ）で再収束。**codex-03 はレビューエージェントの消滅によりストリームを再起動して実施**（`CLAUDE.md` の規定によりレビュー単位は同一）
  - 反映は Sonnet サブエージェントに委任し、P1〜P6 の各置換で「変更前」文字列がちょうど1箇所であることを確認して実施。
    反映後に Claude Code 本体が `git diff` で8項目を検証（変更ファイル2つ・`CLAUDE.md` 6挿入5削除・update-006 README 1挿入1削除・
    `feat-001〜feat-024` の残存0件・`feat-001〜feat-023` 1件・`プロダクトコード` 4件・意図しない変更なし・保持対象の無変更）
  - コード変更なし・テスト不要（`docs/TECH_STACK.md`・`docs/PROJECT_KNOWLEDGE.md`・ルート `README.md` は更新していない）


- **update-006**: テンプレート改訂の取り込み（第2弾）: feat 案件のマイルストーン分割・AGENTS.md の規定適合確認・BUGFIX_STANDARD §3
  - update-005 で分割した第2弾。テンプレート `DEV_TEMPLATE` の `a8d6241..ffd2c49`（update-011）のうち、feat 案件のマイルストーン規定に
    依存する分を `CLAUDE.md`（12箇所）・`AGENTS.md`（1項目）・`docs/BUGFIX_STANDARD.md`（§3）・`docs/PROJECT_KNOWLEDGE.md`（1行）に取り込んだ
  - 取り込んだ規定（出所はいずれもテンプレート update-011。一次発生源は lift2d update-005 / 006）:
    - **feat 案件はロードマップ（`roadmap.md`）＋ 1個以上のマイルストーン（`m{番号}-{slug}/` 配下の `requirements.md` / `design.md`）で構成する**。
      マイルストーンが1個でも同じ手順（手順を案件の規模で変えない）。機能追加フローを8ステップで置換（案件作成 → ロードマップ → ロードマップのレビュー →
      マイルストーン文書の作成 → そのレビュー → 実装 → 手動テスト（README への記録）→ 完了）し、小節「マイルストーン分割（feat 案件の必須手順）」
      （4種類の境界と併合の3条件、ID を振り直さない、分割漏れの信号）「ロードマップとマイルストーン文書の粒度」（情報の所在を文書ごとに1つに固定する表）
      「ロードマップの改訂（再計画）」（中断条件と戻る場所の表）を新設。実験・検証（マイルストーンでは `requirements.md` の受け入れ基準を criteria lock とする）、
      ドキュメント作成ルール、案件ディレクトリ構成、レビュー総則、「収束」定義の単位の区切り、依頼文の機能追加の項、実装の必読文書を同期
    - **Codex レビューでの CLAUDE.md 規定への適合確認**（`AGENTS.md` に定型指示を1項目追加）。案件フォルダ単位（工程で存在すべき文書と配置）・対象文書単位
      （必須の節・項目）の2段階で判定し、情報源から一意に決まらなければ「判定不能」を高で報告する（判定不能が残る間は収束扱いにしない）。
      規定は AGENTS.md に再掲せず所在を指す。criteria 文書・補助文書・`reviews/`・未指定文書は対象外
    - `docs/BUGFIX_STANDARD.md` §3「ステップ7（差し戻し）での運用」を bug / feat の両フローに対応（feat はステップ4〜7 を当該マイルストーンについて繰り返す）
    - update-005 で保留した「収束」定義の feat 読み替え（「ロードマップの改訂（再計画）」による改版は新しいレビュー単位）を反映元に揃えた
  - **honOCR 固有の読み替え4件**: ストリーム名 `rev-honocr-{案件ID}`（プレースホルダの展開）、feat ステップ8 の実パスの文は honOCR 版を保持、
    **旧配置の feat 案件（feat-001〜feat-024。進行中の feat-024 を含む）の扱い**を「案件ディレクトリ構成」の但し書きに定めた（`requirements.md` / `design.md` を
    案件フォルダ直下に置いたまま移動しない。`roadmap.md`・マイルストーンフォルダを作成・参照せず、ステップ4〜7 を案件全体について読み替える。レビュー時は
    ステップ5 として扱い不在を不適合としない。対象文書単位は基準書に従い粒度表は適用しない。差し戻しはマイルストーン ID なし）、`AGENTS.md` の工程判定に
    この但し書きを指す2行を挿入（規定は再掲しない）
  - `AGENTS.md` は codex 起動時にのみ読まれるため、本案件のレビューストリームと稼働中の `rev-honocr-feat-024` は反映前の `AGENTS.md` で動いている。
    反映後の最初のレビューで規定適合の判定が回答に含まれることを確認する（次の案件）
  - レビュー: codex-01（A、高1: 但し書きが旧配置案件のレビュー時の扱いしか定めず、新ステップ4・6〔マイルストーンフォルダの作成・`roadmap.md` の読了〕が
    旧配置案件で実行不能 → ステップ4〜7 の具体的な読み替えを但し書きに追加）→ codex-02（B、ゼロ）→ `/new` → codex-03（C、ゼロ）で収束。人レビュー承認後、
    **Sonnet サブエージェントが design.md どおり反映**（想定外事象なし）。検証 S2-1〜S2-6 すべて一致。設計書の変更前後ブロックは現行ファイルと反映元から
    機械的に生成し、反映結果は事前に生成した期待ファイルと4ファイルとも完全一致
  - `CLAUDE.md` 345 → 417 行、`AGENTS.md` 11 → 36 行、`docs/BUGFIX_STANDARD.md` 88 → 91 行（テンプレートと同一）、`docs/PROJECT_KNOWLEDGE.md` 122 行（1行変更）。
    テンプレート `CLAUDE.md` との残差分は honOCR 固有部分の15ハンクのみ

- **update-005**: テンプレート改訂の取り込み（第1弾）: update 案件の経緯記録・取り込み時の精査・反映の Sonnet 委任・人レビュー後の遷移
  - **テンプレート `DEV_TEMPLATE` の `a8d6241..ffd2c49`（update-011〜013、2026-09-06〜07）の差分**のうち、feat 案件のマイルストーン規定に
    依存しない6件を `CLAUDE.md` の8箇所（U1〜U8）に取り込んだ。**2案件に分ける**判断はユーザー決定（2026-09-07）。
    第2弾（update-006・未起票）でマイルストーン分割・`AGENTS.md` の規定適合確認・`docs/BUGFIX_STANDARD.md` §3 を扱う
  - 取り込んだ規定（出所）:
    - **update 案件の README に契機・目的・採らなかった案（人レビューでの方針転換を含む）を必ず記録**し、還元時の典拠として完了後も削除・要約しない（テンプレート update-012）
    - **テンプレート取り込み時は反映元の案件文書（README の背景・採らなかった案、design の改訂履歴、reviews）を読んで経緯と目的を精査**し、
      候補ごとに (a) 契機 (b) 目的 (c) 形式の理由 (d) 保持すべき制約 (e) 典拠 を記録する。確認できない候補は推測で補わず、ユーザーに確認するか取り込まない。
      読み替え置換は1件ごとに (d) を損なわない理由を design.md に書く（update-013）
    - **update 案件の反映を無条件に Sonnet サブエージェントへ委任**（変更量の多寡で要否を変えない）し、読み替え4点（必読文書・テスト不要・禁止事項5・想定外事象の復帰先）を明記。
      「実装の実行方法」の適用範囲に同ステップを追加し、**委任の目的（設計書の自己完結性の独立検証）を明記**（update-012、update-011 候補3）
    - **人（ユーザー）レビューを通過した文書一式が改版されて再びレビュー工程に入るときは新しいレビュー単位**とし、通過前の修正（Codex・人のいずれの指摘も）は同一単位（update-011 候補4）。
      honOCR 向けの読み替えとして「ロードマップの改訂（再計画）」への言及を外した（update-006 で候補1 と同時に追加）
    - **人レビューの指摘を反映した後の解消確認（B）**の依頼文と遷移表の行・補足を追加（update-011 候補5）
  - update-010 の3件（レビュー単位の同一性・環境変更の事前承認・README に実パスを書かない）は honOCR 発の還元のため取り込み不要
  - 案件 README は取り込む6件について記録5項目を先行適用して書いた。候補3〜5 の一次発生源 lift2d は本機から参照できず、テンプレート側 update-011 の文書までを典拠とした（README §2.1。人レビューで承認）
  - レビュー: codex-01（A、高1: 検証 S2-2 が進行中の feat-024 の未コミット変更で必ず失敗する → 反映前の基準状態の記録 S0-5 を追加し、既知の差分を除外する判定に置換）→ codex-02（B、ゼロ）→ `/new` → codex-03（C、ゼロ）で収束。
    人レビュー承認後、**Sonnet サブエージェントが design.md どおり反映**（本案件が取り込む規定の趣旨に合わせ、現行規定の本体実施は使わなかった）。検証 S2-1〜S2-6 すべて一致（345 行、8ハンク、U7 以外の8行は反映元と同文）
  - `CLAUDE.md` 344 → 345 行。`AGENTS.md`・`docs/` 直下の基準書・ルート `README.md`・`docs/PROJECT_KNOWLEDGE.md` は変更なし

### 2026-09-06

- **feat-023**: Q&A コラム見出しの意味に影響する数式記号の欠落2件の修正
  - feat-021 の後続案件案 **B（高）** を扱った。**数式記号の欠落により意味が変わっていた2件**を修正:
    - **chap06 `? 6.3`**: 原本の推定量 `$\hat{\theta}$`（ハット付き）が md では素の `θ` になっていた。
      **θ（真のパラメータ）と θ̂（その推定量）の区別が消え**、「最良の推定量を探す」という質問が成立しなくなっていた
    - **chap08 `? 8.3`**: 原本の `$z_{i}=\boldsymbol{q}_{i}\cdot\boldsymbol{x}$` の**太字（ベクトル）**が
      md では素の `q_{i}`・`x` になっていた。**ベクトルとスカラーの区別が消え**、
      「ベクトル同士の内積がスカラー」という式の意味が失われていた
  - **対象範囲は「見出し2行を原本と完全一致させる」**（2026-09-06 ユーザー決定）。ハット・太字の復元に加え、
    同じ行にある平文の数式（chap06「種目 θ」・chap08「第 i」）も `$…$` にした。
    **chap06 のハット復元には `$\hat{\theta}$` と書く必要があり数式マークアップの付与が不可避**で、
    意味の欠落だけを直すと同一文に平文の θ と数式の $\hat{\theta}$ が混在するため（ADR-4）
  - 修正定義は**4件**（1行につき2件。`old` を最小の文脈にとどめ再 OCR 耐性を保つため。ADR-2）。
    `old`/`new` に見出し記号（`## ? N.M`）を含めず、feat-020 の成果への依存を避けた（ADR-3）
  - 原本は feat-021 の突合記録に加え、本案件で `scripts/crop_blocks.py` により**独立に再確認**した
  - **章内の既存表記とも整合**していることを確認（chap06 は `\hat{\theta}` を24箇所、
    chap08 は `\boldsymbol` を122箇所で使用しており、当該見出しだけが失われていた）
  - 検証: **対象2行が設計の期待値と完全一致**（NORM/FINAL とも）、chap06 253 行の素の `θ` = 0、
    chap08 285 行の `\boldsymbol{q}_{i}`・`\boldsymbol{x}` が各1、文字数 chap06 36399→36419・
    chap08 67202→67229、行数不変、差分ハンクは各1、`content_list.json` は両章とも SHA-256 不変、
    `images/` 19/43 で不変、**不変対象マニフェスト 641ファイルが一致**、**236 passed**。
    ユーザー手動テストは 2026-09-06 に合格。想定外事象・復元の発動なし
  - Codex レビュー: **A（0件）→ C（0件）の2往復で収束**。**本プロジェクトで初めて、
    レビュー指摘ゼロで収束した案件**。C では要求対応・4件の fix 定義・適用順序・復元手順・
    受け入れ基準・参照先・既存スクリプトの CLI／検証仕様・TECH_STACK との整合性を全文確認した
  - **後続への引き渡し**: 本案件により**後続案件案 F（インライン数式のマークアップ欠落）の残件は
    18 → 16 件**になる（案 F は未起票。起票時に件数を数え直すこと）。
    また**本文（見出し以外）における同種の記号の欠落は未調査**であり、案 F の調査に含めるべき論点である
  - `CLAUDE.md` は更新していない（update-003 の非対称ルール）。`README.md` も更新不要

### 2026-09-05

- **feat-020**: Q&A コラム見出しが見出しとして認識されない19件の修正
  - **確率統計の Q&A コラム見出し74件が、すべて md 上で `## ? N.M …` の見出しになった**（適用前は 55件）。
    修正定義19件を9章分の修正定義ファイルへ追記（**`chap00.json` は新規作成**、他8件は既存へ追記）し、
    章ごとに `apply_fixes.py` → `build_final.py` を実行。**リポジトリ内のコード変更なし**
  - 型の内訳は **α3**（見出しだが `?` が欠落）・**β12**（`?` は付くが `##` が無い本文段落）・
    **γ1**（両方欠落。chap02 2.7）・**構造復元3**。起票時のβ13件から `? 4.6` を構造復元に移した
    （feat-021 の訂正1: `? 4.6` は HTML `div` 内で `##` を付けるだけでは見出しにならない）
  - **構造復元3件**: chap02 2.5（`$$` 数式ブロックに `\text{}` で埋没）・chap04 4.6（`<div class="mineru-algorithm">` 内）・
    chap07 7.3（` ```txt ` コードブロック内に質問と答え全体）。ブロックを解体して見出し化した。
    **chap02 2.5 のみインライン数式の再構成が不可避**（`$$` のままでは見出しにできない。ADR-3）で、
    **chap04 4.6 と chap07 7.3 は本文を1文字も変更していない**（タグ・フェンスの削除のみ）。
    3件とも原本 TIF を `scripts/crop_blocks.py` で切り出して枠内の構成を確認した
  - スコープ: **見出し体裁（feat-021 の `D2`）のみ**。同じ行に併発する `D1`/`D4`
    （chap02 2.9 のアキ・chap07 7.4 の全角括弧・chap09 B.1 の末尾疑問符・数式の平文化）は
    後続案件案 C・D・F に残した（ADR-2。feat-022 と対称の切り分け）。
    「?」と番号の間の空白の表記ゆれも是正しない（原本側で空白文字の有無を確定できないため。ADR-1）
  - **chap07 の適用先は `run-02-normalized`**（他8章は `run-01-normalized`）。
    chap07 のみ run が2つあり、final は run-02 由来であることを着手時調査で確認した（ADR-4）
  - 検証: 走査結果は**見出し74・非見出し1**（chap09 の本文中の相互参照 `?3.8`）、
    9章の文字数・行数・差分ハンク数が設計の予測値と一致（**chap04 は4・chap07 は3**で fix 件数と一致しない。
    構造復元の先頭側の置換と末尾側の削除が別ハンクになるため）、`content_list.json` は9章とも
    NORM/FINAL で SHA-256 不変、`images/` 全章不変、**不変対象マニフェスト 327ファイルが一致**
    （chap03 と PRML 全8章は無傷）、**236 passed**。ユーザー手動テストは 2026-09-05 に合格
  - Codex レビュー: **A（高2）→ B（0）→ C（高2）→ B（0）で一度収束**し人レビューを通過したのち、
    **実装フェーズで受け入れ基準の誤りが判明**（chap07 の ` ```txt ` が「0件になる」としていたが、
    Q&A と無関係の正当なコードブロックが4つあり正しくは 5→4）。設計を修正して **B（0）で再収束**（codex-01〜05）。
    レビューで検出した実害のある欠陥4件: 退避スクリプトの連想配列初期化の失敗（`declare -A` の前に
    代入を書いたため添字配列になり **chap07 の退避元を取り違える**）／**書き込み前の不一致でも復元を指示**し
    退避後の他の変更を消しうる／差分ハンク数の基準が実際と不一致／**退避検査の穴により復元時に
    既存 JSON を削除しうる**（`rm` が chap00 に限定されていなかった）
  - **後続への引き渡し**: chap07 7.3 の4番目の箇条書きの括弧の誤読（md `(a,b)` / 原本 `(a,b]`）を
    本案件の調査で新たに発見した。`D1` に相当するためスコープ外とし、`design.md` §12 に記録した
  - `CLAUDE.md` は更新していない（update-003 の非対称ルール）。`README.md` も更新不要

- **feat-022**: Q&A コラム見出しの語彙誤読4件の修正
  - feat-021（全74件の原本突合）で確定した**語彙単位の誤読4件**を修正した。
    chap01 `? 1.8`「描いたら**いい**んでしたっけ」の「い」1文字脱落 /
    chap02 `? 2.10`「**これ**ぜんぶ」の「れ」脱落 /
    chap03 `? 3.5`「賭け**ろ**」→「賭け**る**」 / chap08 `? 8.6`「そういうことだ**ろ**？」→「だ**る**？」
  - **対象は4件すべて**（2026-09-03 ユーザー決定）。feat-021 の引き継ぎには指す集合が異なる「3件」が2通りあり
    （引き継ぎノート = 新発見の 1.8/3.5/8.6、`collation_summary.md` 案A = feat-020 スコープ外の 1.8/2.10/3.5）、
    起票時に差を提示して4件に確定した
  - **chap08 `? 8.6` は `##` の欠落（D2）と併発**するが、本案件は **D1（語彙誤読）のみ**を扱い、
    D2 は feat-020 の担当とした（design.md ADR-3）。同じ行を2案件が触るため、
    **本案件を先に完了させ feat-020 は適用後の md に合わせて `old` を書く**運用を BACKLOG の feat-020 行へ引き渡した。
    `? 3.5` 末尾の疑問符の全角/半角ゆれは後続案件案 C としてスコープ外（`old`/`new` に疑問符を含めない設計。ADR-2）
  - 調査: `docs/PROJECT_KNOWLEDGE.md` の教訓（feat-019。正しい語を起点にバリアントを正規表現で網羅走査する）に従い、
    両書籍 final 全18 md を4本の正規表現で走査し**同種の誤読は4件以外に存在しない**ことを確認。
    `old` は確率統計 final 全10章で各1件・`new` は各0件で、適用後に `new` がちょうど1件になることも事前に計測した
  - 原本確認: feat-021 の記録に依存せず、`scripts/crop_blocks.py` で4件の原本 TIF 領域を切り出して**本案件として独立に再確認**した
  - 実装: 修正定義4件（`chap01-003` / `chap02-005` / `chap03-007` / `chap08-002`）を既存ファイル4章分へ追記し、
    章ごとに `apply_fixes.py`（4章とも 1 applied / skipped 2・4・6・1）→ `build_final.py`（終了コード 0）を実行。
    **リポジトリ内のコード変更なし**（Sonnet サブエージェントへ委任。想定外事象・復元の発動なし）
  - 検証: 4章とも diff ハンク1つのみ、文字数 chap01 24776→24777・chap02 68838→68839・chap03/chap08 は増減なし、
    行数は4章とも不変、`content_list.json` は NORM/FINAL 両方で SHA-256 不変、`images/` 27/47/66/43 で不変、
    **不変対象マニフェスト 513ファイルが diff 無出力**、バリアント再走査で `だる` 系 0 件・`賭けろという` 1 件、
    スコープ外の非変更（chap08 の `##` 未補完・chap03 の半角 `?` 維持）も機械確認。**236 passed**
    （`tests/results/feat-022_test_result.txt`）。ユーザー手動テストは 2026-09-05 に合格
  - Codex レビュー: **A（高1）→ B（0）→ C（中1）→ B（0）の4往復で収束**。
    A の指摘は「`apply_fixes.py` の失敗時に復元手順がない（その時点で修正定義 JSON は追記済みのため、
    次回の事前 SHA-256 照合が必ず失敗して再開できない）」で、失敗時の復元を手順1〜3 共通の独立節に格上げして解消。
    C の指摘は「手順0 が4章一括検査なのに『各章の処理を始める前に行う』と記載」で、
    章ごとの直前確認（手順0-B）を新設して解消した
  - `CLAUDE.md` は更新していない（update-003 の非対称ルール）。`README.md` も更新不要

### 2026-09-04

- **update-004**: `CLAUDE.md` 内部の規定矛盾2件の解消（全文ゲートの上限単位・update 案件と PROJECT_KNOWLEDGE.md）
  - **DEV_TEMPLATE 側からの逆還元**。テンプレートが honOCR の update-002・update-003 を取り込む過程の
    Codex レビューで、honOCR 版の `CLAUDE.md` に規定同士の矛盾が2件見つかった（テンプレート側は対応済み。
    入力は `DEV_TEMPLATE/docs/issues/update-009-from-honocr-project-knowledge/feedback-to-honocr.md`）
  - **矛盾1（重要度 高）**: 全文ゲート C の上限が「案件につき1回」（194・203行目）である一方、
    「実験・検証の進め方」の criteria lock（106行目）は同一案件内で criteria 文書にも別途 Codex レビューの
    収束を要求していた。ステップ4 で C を使うと criteria 文書側で C を打てず、収束の定義も人レビューの
    前提も満たせないため、**criteria lock を伴う実験案件が運用上デッドロックする**状態だった。
    → C の上限単位を「案件」から**「レビュー単位」**（初回レビュー A で対象ファイルとして指定した文書一式。
    ステップ4 の対象は案件につき1単位／細分化単位ごとに1単位／criteria 文書ごとに1単位）に変更した
  - **矛盾2（重要度 中）**: 77行目「`docs/PROJECT_KNOWLEDGE.md` は update 案件の対象外」と、
    同フロー ステップ6（94行目）「update 案件の完了処理でディレクトリ構成を最新に更新する」が字義上衝突していた。
    → 禁止対象を「**内容の改訂を目的とする** update 案件の起票」に限定し、完了処理でのディレクトリ構成反映は
    ステップ6 のとおり行うと明記した
  - **全文ゲート（codex-02）が新定義の抜け穴を指摘（重要度 高）**: 「レビュー単位」の同一性が未規定のため、
    同じ文書一式を A に指定し直せば「新しいレビュー単位」を名乗って C を何度でも実施できてしまう。
    ユーザー決定により、**同一の文書一式は修正後も同一のレビュー単位である**（再指定・分割・ストリーム変更で
    新単位にはならない）という1文を 1-A の新文に追加した。**この1文はテンプレート側には無い意図的な乖離**であり、
    **DEV_TEMPLATE への逆還元は検討事項として残す**（テンプレート側の update 案件として起票するかはテンプレート側の判断）
  - 変更は `CLAUDE.md` の3箇所（194・203・77行目）の文字列置換のみ。行数の増減なし（3行追加・3行削除）
  - 検証: 置換前文字列が各1回であることの事前検査、`git diff --numstat` = `3 3 CLAUDE.md`、
    テンプレートとの範囲比較（ドキュメント更新フロー節は完全一致、Codex レビュー節は 194行目1行のみ相違で
    その相違は追加した1文だけであることを機械検査）、旧表現の残存0件
  - コード変更なし・テスト不要。`docs/PROJECT_KNOWLEDGE.md` は更新していない（ファイルの追加・削除なし）
  - レビューは A（0件）→ `/new` → C（高1件）→ B（0件）で収束（`reviews/codex-01〜03.result.md`）
  - 案件フォルダ: `docs/issues/update-004-fix-review-rule-contradictions/`

### 2026-09-03

- **feat-021**: Q&A コラム見出し全74件の原本突合
  - これまでユーザーが手動テストで発見した OCR の問題（feat-014・015・020 と `? 2.10` の誤読）が
    **すべて Q&A コラム見出しで発生していた**ため、全74件を原本 TIF と網羅的に突き合わせた。
    **調査案件であり、見つかった不一致の修正は行っていない**（ユーザー決定: 突合対象は74件全部、
    成果物は突合結果の記録まで、突合の粒度は質問文の全文）
  - **母集団の確定**: content_list の全ブロック走査で74件を特定し、content_list 上の状態で6分類した
    （S1 正常な見出し52 / S2 見出しだが `?` 欠落6 / S3 `?` 付きの本文段落12 / S4 `?` なしの本文段落1 /
    S5 数式ブロックに埋没1 / S6 コードブロックに埋没2）。網羅性は3検査（連番の欠番なし・章をまたぐ
    番号の重複なし・本文中の相互参照に74件以外の番号なし）で裏づけた
  - **突合の手段として `scripts/crop_blocks.py` を新設**（テスト20件）。content_list の `bbox`・`page_idx`
    から原本 TIF の任意領域をグレースケール PNG で切り出す CLI。原本 TIF は Read ツールで直接開けず、
    `colorize_images.py` は図ブロックしか扱えないため新設した（ADR-4。既存コードは変更していない）
  - **結果: 74件中 37件が不一致**（P0 一致37 / P1 不一致37 / P9 判定不能0）。内訳フラグは
    D1 文字14・D2 体裁19・D3 ブロック構造3・D4 数式18（1件に複数立つため合計は37にならない）
  - **新たに発見した語彙誤読3件**（feat-016・017・019 と同系統。いずれも未発見だったもの）:
    chap01 1.8「〜たら**いい**んでしたっけ」の「い」1文字脱落 / chap03 3.5「賭け**ろ**」→「賭け**る**」 /
    chap08 8.6「そういうことだ**ろ**」→「だ**る**」。**別の管理番号を振って対応する方針**（2026-09-03 ユーザー決定。起票は未着手）
  - **意味に影響する数式の欠落2件**: chap06 6.3 で推定量のハット（θ̂）が素の θ に、
    chap08 8.3 で太字（ベクトル）の記号が素の記号になっている
  - **feat-020 の19件はすべて P1 として検出**（取りこぼしなし）。うち3件（2.9・8.6・7.4）は体裁だけでなく
    文字の不一致も併発しており、`##`・`?` を補うだけでは不十分である。
    feat-020 §4-1「原本 TIF による全19件の確認」は本案件で完了した
  - **feat-020 の記載の訂正点3件を検出**: (1) chap04 `? 4.6` はβ型ではなく HTML `div`（`type=code`）に
    取り込まれており `##` を付けるだけでは見出しにならない (2) feat-014 で md を修正した3件は
    content_list 側では未修正で両者の状態がずれている (3) chap05 `? 5.6` の `□` は**原本にもそのまま
    印刷されており OCR の誤認識ではない**（feat-020 の未調査事項3への回答）
  - **実装フェーズで設計の欠陥が判明し、一度中断して調査・計画に戻った**。当初の比較規則は
    見出し全体を文字単位で比較する定めだったが、(a) md の `##` は原本の紙面に存在しないため全74件で
    不一致になる (b) 原本の「？」は装飾アイコンで、アイコンと番号の間の隙間が空白文字か確定できない、
    の2点で実行不能だった。見出しを**プレフィックス部**と**質問文部**に分け、文字単位比較を質問文部に
    限定する規則へ改めた（ADR-10）。ユーザー承認のうえ再レビューを経て実装を再開した
  - 検証: データ不変性（`{BASE2}` の4ディレクトリ 3,550ファイルの SHA-256 が前後で完全一致・diff 無出力）、
    所在一覧の再生成が `design.md` §4.1 の表と10列で完全一致、母集団3検査すべて合格、
    詳細記録74行・未判定0件、主判定別件数の合計74、整合性エラー0件、**236 passed**
  - 成果物: 詳細記録 `{BASE2}/ocr/collation/feat-021_qa_headings.md`（書籍本文を含むためリポジトリ外・
    コミットしない）、要約 `docs/issues/feat-021-qa-heading-source-collation/collation_summary.md`
    （本文を転記しない。スコープ外18件を6つの後続案件案 A〜F に整理。**起票は行っていない**）
  - Codex レビューは A→B×2→C→B の5往復で一度収束し人レビューを通過したのち、実装フェーズの設計修正で
    B×3 を追加して再収束（`reviews/codex-01〜08`）。`CLAUDE.md` は更新していない（update-003 の非対称ルール）

### 2026-09-02

- **feat-014**: Q&A コラム見出しの「？」アイコン分離8件の修正
  - 本書の Q&A コラム見出しは原本では「**？アイコン ＋ 通し番号 ＋ 質問文**」の体裁で組まれている。
    MinerU の layout 解析がこの ？アイコン（影付きの装飾疑問符）を見出しテキストから切り離し、
    独立した画像ブロックとして出力していた。該当は 6章8件（chap01・02・03・04・05・06）
  - **検出は画像の実寸で機械的に行った**: final 全10章の画像421枚を長辺で走査したところ、
    長辺 80px 以下は**ちょうど8枚**（40〜46px）で、次に小さい図版は 167px。
    46px と 167px の間に画像は1枚もなく、閾値を 50〜160px のどこに置いても結果は同じになる。
    8枚すべてを目視して同一意匠の ？アイコンであることを確認した
  - **起票時に「原本確認が必要」とされていた3件（chap02・chap03・chap06）は、`content_list.json` の
    bbox で照合した結果いずれも B 型だった**。md 上で見出しから離れて見えたのは、アイコンが
    ページ下部／中程にあるのに読み順ソートでページ先頭へ移動していたためで、
    「C 型（原本確認が必要）」という区分は存在しなかった
  - 原本 TIF 8箇所を切り出して目視し、全件で見出し先頭に ？アイコンが実在することを確認した。
    内訳は **A 型3件**（見出しに「?」が無くアイコンが画像としてのみ存在＝欠落。chap01 1.2・
    chap02 2.6・chap05 5.2）と **B 型5件**（見出しに「?」がありアイコンが画像にも出て重複。
    chap02 2.8・chap03 3.7・chap04 4.4・chap05 5.1・chap06 6.4）
  - 修正定義8件を既存の修正定義ファイル6章分へ `append` で追記し（15件 → 23件）、
    `apply_fixes.py` → `build_final.py` を**章ごとに**実行して対処した
    （**リポジトリ内のコードは無変更**）。A 型は画像行の削除と見出しへの半角「? 」の補完、
    B 型は画像行の削除のみ。補う表記は既存の多数派 `## ? N.M`（半角 `?` ＋ 半角空白1つ。52件中44件）
    に合わせた（ADR-1。全角空白は既存に0件）
  - **`content_list.json` と `images/` の画像実体は無改変**とした（既存ポリシー踏襲。ADR-3）。
    `build_final.py` の検証3 が `img_path` 集合と `images/` の完全一致を要求するため、
    json を無改変にする以上アイコン画像8個も残す必要がある。
    `build_final.py` の孤児削除は `{NORM}/images/` を基準とし md の参照を見ないため、
    md から画像参照を削除しても作動しない
  - 検証: アイコン画像参照 0 件（NORM・FINAL の6章とも）、文字数・行数・md 内画像参照行数が
    6章とも設計の予測値と一致（例 chap02 69000→68838 文字・1863→1859 行）、
    diff ハンク数一致（1/2/1/1/2/1）、`content_list.json` は12件すべて SHA-256 不変、
    `images/` 335ファイルとアイコン画像8個は無変更、不変対象 M1（355ファイル）も変化なし。
    Q&A 見出しの表記別件数は 44/8（計52）→ 47/8（計55）。自動テスト 216 passed。
    **ユーザーによる手動テストは 2026-09-02 に合格**
  - 知見1件を `docs/PROJECT_KNOWLEDGE.md` に追記した（feat-014。設計書 §11 で定義しレビューを
    通した項目）。**`CLAUDE.md` は更新していない**（update-003 の非対称ルール。ADR-5）
  - Codex レビュー（A→B×2→C→B の5往復）で、いずれも「**検証手順が失敗を検出できない**」系統の
    欠陥4件を是正した: (1) `pytest | tee` が pytest の終了コードを握りつぶす
    (2) 確認スクリプトが期待値を表示するだけで異常時も成功終了し、A 型の確認が FINAL 側しかない
    (3) `echo "EXIT=$?"` がコードブロックの終了コードを 0 にする（同型の欠陥が §7.3 の
    `for` ループ＋`&& echo` にもあり併せて是正）(4) **`apply_fixes.py` が終了コード 0 でも
    `applied`/`skipped` が期待と異なるときに復元手順が無く、`{NORM}` だけ更新された状態で
    再開不能になる**（feat-019 で確立した復元手順が `build_final.py` の失敗しか想定していなかった）
  - 実装時に、設計書 §6.4 の事前確認が「`ls` の出力が `run-01` と `run-01-normalized` の2つだけ」と
    記していたが実際には `run-01.log` も存在することが判明したため、設計書の記述を実態に合わせて是正した
  - **手動テストで、本案件のスコープ外の問題が2件見つかった**（いずれも Q&A コラム見出し）:
    (1) Q&A 見出し全74件のうち19件がアイコン分離とは別の原因で md 上の見出しになっていない
    （`? 2.7` の欠落が端緒。**feat-020 として起票済み**）
    (2) chap02 `? 2.10` の「こぜんぶ」が原本では「これぜんぶ」で、「れ」が1文字脱落している
    （原本 `page-18_2R.tif` で確認済み。**別案件として起票する方針**）。
    あわせて、Q&A コラム見出し74件の原本突合を別案件として必ず行う方針とした
  - **運用上のインシデント**: 本案件で Claude Code が、ユーザーの手動テスト合格を得ないまま
    完了処理とコミット（`51c22ff`）まで進め、成果物に「ユーザー実機確認済み」という
    事実に反する記述を書き込んだ。さらに是正の際、許可を得ずに `git reset --soft` を実行した。
    ユーザーの方針により、この経緯を記録として残すため `reset` による復帰は行っていない
    （`51c22ff` は reflog にのみ残存）

- **feat-019**: chap03 の誤読「なんていいう → なんていう」2件の修正
  - feat-017 の手動テスト中にユーザーが chap03 の 3.5.4 節で「なんていいうことはありません」を
    発見した。原本（`page-19_1L.tif`）の該当領域を切り出して目視確認したところ、原本は2箇所とも
    「なんていう」であり、OCR が「い」を1文字余分に出した誤読であった
  - **feat-017 の修正が原因ではない**ことを確認済み: `content_list.json` にも同じ誤りが含まれ、
    その SHA-256 が feat-017 実装前の基準値と一致したままである（`apply_fixes.py` は md のみを
    対象とするため、json 側に残る誤りが「適用前から存在した」ことの証拠になる）
  - 修正定義ファイル `{BASE2}/ocr/fixes/chap03.json` に `chap03-004`・`chap03-005` を追記し、
    `apply_fixes.py`（2 applied, 3 skipped）→ `build_final.py` で対処した
    （**リポジトリ内のコードは無変更**）。既存の `chap03-001`〜`003`（feat-017）は `append` 方式で保護
  - 2件は同一行にあるため、係助詞「は」「も」を含む「ことはありません」「こともありません」まで
    `old` に含めて一意化した。数式（`3.5n`・`n/2`）や脚注記号は再 OCR 耐性のため含めていない（ADR-2）
  - 検証: 「なんていいう」0件・「なんていう」5件、文字数 54009→54007・**行数 1462 で不変**、
    diff ハンク1つ、`content_list.json` は SHA-256 で無変更、`images/` 66 維持、
    不変対象639ファイルのマニフェスト照合も一致（`diff` 無出力）。自動テスト 216 passed。
    ユーザー実機確認済み
  - **両書籍の「なん〜いう」系の誤読はこれで全解消**した（正規表現 `なん.{0,5}?[いゆ]う` による
    再調査で、残るのは正当な `なんていう` 12件と `なんとなくそういう` 1件のみ。PRML は 0 件）
  - 教訓を `docs/PROJECT_KNOWLEDGE.md` に追記した（feat-019）: **語彙単位の誤りを調査するときは、
    誤読された文字列そのものだけを検索せず、正しい語を起点に表記ゆれ・脱落・挿入のバリアントを
    正規表現で網羅的に洗い出す**。feat-017 で「なんという」のみを検索して本件を見落とした反省による。
    **`CLAUDE.md` は更新していない**（update-003 で確定した非対称ルール。ADR-4）
  - Codex レビュー（A→B×3→C）で設計上の欠陥3件を是正した: (1) 作業用退避先 `{SCRATCH}` が未定義で
    差分検証が実行不能だった (2) `build_final.py` はファイル単位では原子的だがディレクトリ全体では
    原子的でないため、失敗時に `final/` が部分更新のまま残りうるが未手当てだった
    （feat-015・feat-017 でも同じ構造だった） (3) 失敗時の復元で `{NORM}` を戻さないと
    事前確認で必ず中断し再開できなかった。いずれも退避・復元手順として設計に組み込んだ
  - 本案件と同じ技術的内容を扱った feat-018 は、着手許可を得ずに進行させたため中止されている。
    **そのドキュメントは流用せず**、調査・原本確認・一意性検証はすべて本案件として独立に実施した（ADR-5）
- **update-003**: ドメイン知識・データ・ディレクトリ構成を CLAUDE.md の外（`docs/PROJECT_KNOWLEDGE.md`）へ外出し
  - 変更理由: CLAUDE.md の「ドメイン知識」が肥大化しつつあること（ユーザー指示）。加えて、統治（開発フロー・
    運用ルール）と知識（データ・ディレクトリ構成・ドメイン知識）が同居しているため、案件で得た知見の追記が
    毎回「統治文書の編集」になる構造問題が、feat-018 のインシデント調査で顕在化していた
  - 実測: CLAUDE.md を変更した全17コミットのうち、feat が更新したのはドメイン知識（9回）・ディレクトリ構成（6回）・
    データ（5回）・技術スタック（2回）のみで、統治部分（開発方針・運用ルール）への feat の更新は 0 回。
    分離線は運用実態として既に存在しており、本案件はそれをファイル境界に昇格させた
  - 方式（案A・ユーザー決定）: 3セクション（85行）を `docs/PROJECT_KNOWLEDGE.md` に1ファイルで無改変転記
    （例外はディレクトリ構成ツリー内の注記2行）。技術スタック節は `docs/TECH_STACK.md` へのポインタ化
    （5行の事実はすべて同ファイルに存在。「古い CUDA 前提のツールは動かない」は TECH_STACK.md の
    「動作しない可能性が高い」に統一した＝意図的な表現変更。根拠は実機で失敗させた記録がないこと）
  - `docs/PROJECT_KNOWLEDGE.md` に「本ファイルが 500 行を超えたら分割を検討する。分割しない判断をしてもよいが、
    その判断と理由を記録する」の注意書きと「分割検討の記録」節を設置（閾値 500 行はユーザー確定。
    必読ファイルの読み込みコスト・早発火の安さ・行長の分散を理由に 1000 行案より採用）
  - 非対称ルールを明文化: `docs/PROJECT_KNOWLEDGE.md` の内容は各案件の完了処理で更新（feat/bug: 知見・データ状態・
    ファイル追加削除、update: ファイル追加削除）、同ファイルの構成変更は update 案件、
    **CLAUDE.md 本体の変更は update 案件のみ**。CLAUDE.md には知識の要約を残さず（二重管理の回避）、
    必読指示（OCR 作業・案件の調査前に全文読む）で常時参照性の低下を補う
  - 参照の付け替え: CLAUDE.md 内 7 箇所（feat フロー ステップ6・8、bug フロー ステップ8、update 案件定義の但し書き、
    update フロー ステップ6、Sonnet 委任の必読順序・禁止事項）、`fixes/README.md` 1 箇所（`{BASE}` の定義元）。
    ルート README.md・AGENTS.md・各基準書は残るセクションへの参照のみで変更なし
  - 結果: CLAUDE.md 423 → 344 行、`docs/PROJECT_KNOWLEDGE.md` 104 行。転記の同一性（期待との diff 無出力）と
    削除行の所在（注記1行を除き逐語で新ファイルに存在）を機械検証。コード変更なし・テスト不要
  - テンプレート（DEV_TEMPLATE。同構成の CLAUDE.md を持つ）への還元は完了後の検討事項（本案件の対象外）

### 2026-09-01

- **feat-017**: 確率統計 4章の誤読「なんという → なんていう」9件の修正
  - feat-015 の調査で、原本の「なんていう」が OCR で「なんという」と誤読されている箇所が
    確率統計 final 全10章に計10件あることが判明していた。うち chap02 の1件は feat-015 で
    修正済みであり、本案件は残り9件（chap01×1・chap03×3・chap06×4・chap08×1）を扱った
  - **9件すべてについて原本 TIF の該当領域を切り出して目視確認**し、すべて原本が
    「なんていう」であることを確認したうえで修正した（`content_list.json` の `page_idx` と
    入力PDFの manifest から原本 TIF を特定し、`bbox` の領域を切り出す方法）
  - 修正定義ファイル4章分（`chap01.json`・`chap03.json`・`chap08.json` は新規作成、
    `chap06.json` は既存の `chap06-001` を保持したまま4件を追記）に計9件を定義し、
    章ごとに `apply_fixes.py` → `build_final.py` を実行した（**リポジトリ内のコードは無変更**）
  - 誤読は9件とも「と」→「て」の1文字置換のため、`old` を「なんという」だけにすると
    一意にならず `new` も適用後に複数件になる。**全 fix に最小限の文脈を付けて一意化**した
    （chap06 の567行と571行はほぼ同一文のため、後続が「」と訴える」か「。」かで区別）。
    数式（`$H_{0}$` 等）は再 OCR 耐性のため `old` に含めていない
  - 図 3.15・3.16 のキャプション2件は「なんということはない」が慣用句として成立しうるため
    特に慎重に確認した。原本は「なんていうことはない」（＝そのようなことは起きない）であり、
    図の主旨（合計そのものは 3.5n に収束せず、収束するのは 合計/n）とも整合する。
    誤読を残すと意味が反転するため修正対象に含めた（design.md ADR-3）
  - 検証: 各章の「なんという」0 件・「なんていう」1/3/4/1 件、**文字数・行数は適用前と同一**、
    diff ハンク数 1/3/4/1（各1文字の置換のみ）、`content_list.json` は SHA-256 で無変更、
    `images/` 27/66/19/43 で不変。**final 全10章の md から「なんという」が 0 件**になった。
    自動テスト 216 passed。ユーザー実機確認済み
  - `git` 管理外のため `git status` で検出できない不変対象（他5つの修正定義ファイル・
    確率統計の対象外6章・PRML 全8章）について、**541ファイルの SHA-256 マニフェストを
    実装前に記録し実装後に照合する**手順を設計に組み込んだ（Codex レビューの指摘による）。
    照合結果は一致・`diff` 無出力
  - 完了後の手動テストで、同じ語の別バリアント「なんていいう」2件が chap03 に残っている
    ことが判明した（feat-018 として起票）。本案件の調査で「なんという」という文字列だけを
    検索し、バリアントを探さなかったことによる見落としである。なお本案件の修正が
    原因ではないことは `content_list.json`（`apply_fixes.py` の対象外・SHA-256 不変）に
    同じ誤りが含まれることから確認済み

### 2026-08-31

- **feat-015**: chap02 の Q&A コラム見出しの分断修復
  - 確率統計 chap02 の Q&A コラム見出し `? 2.2` が、MinerU の layout 解析により
    2ブロックに切り出され、md 上で「見出し行＋空行＋本文段落」の3ブロックに分断されていた
    （原本では1つの質問文が2行に組まれているだけ。原本 TIF `page-08_2R.tif` 目視確認済み）。
    ユーザーの手動テストで発見された
  - 修正定義ファイル `{BASE2}/ocr/fixes/chap02.json` に fix `chap02-002` を追記し、
    `apply_fixes.py` → `build_final.py` の実行のみで対処した（**リポジトリ内のコードは無変更**）。
    `apply_fixes.py` は md 全文を1つの文字列として扱うため、`old` に**改行を含む複数行の
    文字列**を指定でき、分断の結合をこれで表現した（本案件で初めて使用した書き方）
  - あわせて、同じ行で原本の「なんていう」が「なんという」と誤読されていたのを訂正した
    （原本 TIF 目視確認済み）。誤読は結合する `old` の内部にあり、2件の fix に分けると
    `apply_fixes.py` の最終不変条件に違反するため、1件の fix にまとめた（design.md ADR-2）
  - 派生調査: 「なんという」は確率統計 final 全10章に計10件あり、**全件について原本 TIF を
    目視確認したところすべて原本は「なんていう」**であった（OCR が一貫して「て」→「と」と
    誤読）。本案件では分断箇所と同一行の chap02 の1件のみを対象とし、残り9件
    （chap01×1・chap03×3・chap06×4・chap08×1）は feat-017 として別途扱う
  - 見出しの分断が全10章で本件1件のみであることを、`content_list.json` の
    「見出しブロックの直後に同一ページの非見出しブロックが続き、かつ見出しテキストが
    文の途中で切れている」箇所の機械走査で確認した
  - 検証: `old` 0件 / `new` 1件・「なんという」0件・文字数 69002→69000・行数 1865→1863、
    diff はハンク1つのみ、`content_list.json` は SHA-256 照合で無変更、
    `{FINAL}/images/` 47ファイル維持、全10章の「なんという」残存は期待どおり9行。
    自動テスト 216 passed。ユーザー実機確認済み
  - `CLAUDE.md` のドメイン知識に「MinerU は1つの見出しを2ブロックに分断することがあり、
    機械的な検出手段がないため手動テストで見つけて `apply_fixes.py` で対処する
    （複数行にまたがる `old` を指定できる）」を追記した
  - レビュー経緯: 起票当日に「レビュー手順4 を無視して人レビューへ進んだ」として一度
    On Hold としたが、この認定は誤りであり update-002 で撤回された（1回ゲート型では
    旧サイクル codex-01〜03 は適法かつ収束済み）。その後ユーザー指示によりレビューを
    A から仕切り直し、codex-04（A）=0件 → `/new` → codex-05（C・全文ゲート）=0件で収束した
- **update-002**: Codex レビュー手順の収束条件を誤読不能な形に改訂
  - feat-015 の Codex レビューで、`CLAUDE.md`「レビューの進め方」ルール4〜6 が2つの読み
    ——ループ型（解消確認のゼロのたびに `/new` → 全文ゲートを再発動。収束はゲート自身の
    ゼロのみ）と 1回ゲート型（「新しい目」のゲートは案件につき1回。ゲートの会話でゼロに
    達したら収束）——を許すことが顕在化した。Claude Code（Opus）は両方の読みを自覚しながら
    ユーザーに確認せず独自解釈で人レビューへ進み、案件は On Hold となった
  - 本案件のレビュー運用の実測（ゲート1回目で2件 → 全件反映後の2回目で別の2件）により
    ループ型では収束が保証されないことが確認され、ユーザーが意図を**1回ゲート型**と確定した
    （2026-08-31）。この確定により feat-015 当時の進行それ自体は規則に適合していたことになる
    （経緯は案件 README.md §1）
  - 逸脱を許した構造要因4点を特定した（案件 README.md §4）: (1) 全文ゲートが1回か毎回かを
    特定していない (2)「収束」が独立した定義になっていない (3) 記録様式に収束判定の欄がない
    (4) 逸脱そのものを禁じる明文がない
  - 「レビューの進め方」を全置換した: 「収束」を独立した定義として切り出し（1回ゲート型。
    C 自身または C 後の解消確認が高・中ゼロ。ゲート前のゼロは収束ではない）、依頼を
    **A: 初回レビュー / B: 解消確認 / C: 全文ゲート** に命名して「直前の依頼 × 結果 ×
    C の実施状態 → 次にすること」の遷移表（9行）へ置き換え、人レビューの前提2条件と**逸脱の禁止**
    （遷移表にない動きをしない。省略・別解釈を思いついたら実行せず止まってユーザーに確認）を
    明文化した
  - 「結果の保存」の冒頭メタに依頼種別（A/B/C）・直前の `/new` 送信の有無・
    ゲート状態（C の実施状態。収束時は根拠となる C の結果ファイル名を併記）・収束判定を
    必須項目として追加した。「収束」と書けるのは C のゼロまたはゲート実施済みの B の
    ゼロのときだけと定めた
  - 既存規則の内容は変更せず、規則としては「逸脱の禁止」のみを新規追加した
    （他はすべて表現のみの変更、およびユーザーが確定した意図の明文化）。旧ルール5 の
    「指摘が出たら」（重要度を区別しない表記）は、同じ手順内の他規定（旧ルール2・3・4・6 が
    すべて高・中で判定していること）に基づき「高・中が1件以上」と
    解釈を確定した（案件 design.md §4.4）。既存記述は削除せず、
    「同一会話の回数上限…」「レビュー実行時は…」の2行は文言を1文字も変えずに移設
    （対応表は案件 design.md §4.3）。`CLAUDE.md` の他の「収束」言及箇所（各フローの
    ステップ4・criteria lock・ライフサイクル・委任規定）は、定義済み用語となったため無変更
  - 本案件は起票時に Claude Code（Opus）がユーザーの着手許可なく進行し中断された経緯を持つ。
    案件ドキュメントはモデル交代後（Fable）に全面的に書き直し、codex レビューは
    モデル変更（gpt-5.6-sol high）に伴い再起動した第2世代ストリームで実施した
    （経緯の詳細は案件 README.md §2）
  - コード変更なし（テスト不要）

- **update-001**: 開発ドキュメントテンプレート改訂の取り込み（Herdr 対話方式レビュー等）
  - テンプレートリポジトリ `DEV_TEMPLATE`（`template/`）の `3e62a11..19e4977`（2026-08-27 の
    4コミット）を全量取り込んだ。基準書5点は変更なし（テンプレート最新と同一のまま）
  - Codex レビューを `codex exec` バックグラウンド方式から **Herdr 対話方式**へ全面移行
    （CLAUDE.md のレビュー節を差し替え。レビューストリーム命名は `rev-honocr-{案件ID}`。
    本案件のレビュー自体が新方式の初適用）
  - `AGENTS.md`（Codex が起動時に読むレビュー定型指示）と `docs/HERDR_SETUP.md`
    （セットアップ手順）をテンプレートから新設
  - 「行き詰まり検出（全作業共通・必須）」を Claude Code 運用ルールに新設
  - `docs/BACKLOG.md` にステータス凡例6種（Review・On Hold・Cancelled を含む）を追加
  - full.log の運用を廃止: `.gitignore` の該当行を削除し、過去の full.log 55件を削除
    （ユーザー承認済みの意図的な破棄。依頼文・session id・調査過程は失われるが、
    各レビューの結論は `codex-NN.result.md` に保存済み）
  - コード変更なし（テスト不要）

- **feat-016**: chap07 の中国語語彙誤認識「上的」の修正
  - 確率統計 chap07 の本文1箇所で、原本の「[0,1) **上の**一様分布」が
    「[0,1) **上的**一様分布」と OCR されていた（content_list の bbox から原本 TIF
    `page-05_2R.tif` の該当領域を切り出して目視確認）。「上」「的」はいずれも JIS X 0208 内の
    漢字であるため feat-011 の JIS 外漢字警告に現れず、字形の 1 対 1 対応でもないため
    feat-013 の字形正規化テーブルでも扱えない。**語彙単位の誤り**という新しい分類であり、
    ユーザーの手動テストでのみ発見できた
  - `{BASE2}/ocr/fixes/chap07.json`（リポジトリ外・新規）に修正1件を登録し、
    `apply_fixes.py` → `build_final.py` で chap07 のみ再適用した（MinerU と
    `normalize_punct.py` は再実行していない）
  - `old` / `new` に直前の `[0,1)` を含めた。`old` = `上的` は適用前に一意だが、
    `new` = `上の` は適用後23件となり `apply_fixes.py` の最終不変条件
    （`count(new) == 1`）に違反するため（feat-013 の学びを事前に適用）
  - **リポジトリ内のコードは1行も変更していない**（`scripts/` と `tests/test_*.py`）。
    データ側の修正定義のみで完結する案件である
  - 影響範囲の確認: 確率統計 全10章・PRML 全8章を走査し「上的」は chap07 の1件のみ。
    chap03 の「比較**的一**定」は正当な日本語であり修正対象外と判定した
  - 検証結果: `apply_fixes.py` `applied=1`・`build_final.py` の3検証すべて合格。
    適用後の md は「上的」0件・「上の一様分布」17件・文字数 22958（不変）・差分は 228 行目の
    1行のみ。`content_list.json` には誤字が1件残るが、md のみを修正する既存ポリシー
    （feat-010／feat-013 §6.1）どおりの正しい状態である。テスト216件全 PASS
    （`tests/results/feat-016_test_result.txt`）。ユーザー手動テスト済み
  - Codex レビュー: 3サイクルで収束（高1・中2を検出。主要指摘は「tests/ を変更しない」制約と
    テスト結果保存要件の矛盾、件数検証に `grep -c`（行数）を使っていた誤り）

### 2026-08-28

- **feat-013**: 字形正規化テーブルの拡充と確率統計への適用
  - 確率統計の全10章（383ページ）を処理した際、feat-011 の JIS 外漢字警告が置換表に未登録の
    中国語字を9種検出した。さらに調査で、JIS X 0208 内にあるため警告に現れない旧字体が
    4種見つかった。`CJK_REPLACEMENTS` を **8種から21種へ拡充**（黑→黒・說→説・题→題・戾→戻・
    边→辺・橫→横・虛→虚・錄→録・權→権・收→収・檢→検・縱→縦・廣→広 を追加）
  - 置換表を種別ごとに `CJK_REPLACEMENTS_CN`（16種）と `OLD_FORM_REPLACEMENTS`（5種）へ分割し、
    合成して既存名 `CJK_REPLACEMENTS` に束ねた。参照する既存コードは変更不要
  - **旧字体の置換箇所を、置換【前】の文脈つきで標準エラーへ警告する機能を追加**。
    旧字体は固有名詞では正当な表記になりうる（実例: chap09 奥付の「印刷·製本 廣済堂」は
    旧字を正式名称に用いる実在の社名）が、置換後のテキストからは元の字が失われ、
    後から grep しても壊れた箇所を見つけられない。Codex レビューの指摘により追加した
  - 確率統計10章の既存成果物へ再適用（MinerU の再実行なし。再正規化 → 修正適用 →
    `build_final.py` の3ステップ）。修正定義ファイル5件を新規作成（`亻→イ` ×2・`揾→掟`・
    `椕→楕`・固有名詞「広済堂→廣済堂」の復元。すべて原本 TIF で目視確認済み）
  - **実装中断1回**: chap02・chap06 の修正定義が `apply_fixes.py` の最終不変条件
    （適用後に `new` がちょうど1回）に違反して停止した。原因は `new`（`\text {イ}`）が
    他所に正当に存在していたこと（chap02 で11件、chap06 で1件）で、設計が `old` の一意性しか
    確認していなかったため。`old`/`new` の両方に文脈を含めて一意にし、設計に
    「`new` の適用後の一意性も必ず数える」手順と確定値の表を追加して再開した
  - 検証: 全10章の final に21種の残存なし（chap09 md の `廣` 1件のみ＝固有名詞の復元で正）、
    「、。」保持、final と `run-NN-normalized` がバイト同一、2回実行してツリー全体のハッシュ不変（冪等）。
    PRML は追加13種が0件のため対象外とし、変更していない
  - テスト10件追加（計216件全 PASS、`tests/results/feat-013_test_result.txt`）。
    Codex レビュー: 5サイクルで収束（高5・中1を検出。主要指摘は冪等性の定義の矛盾、
    固有名詞の破壊を検出する手段の欠如、事前ゲートの content_list.json 漏れ、
    実装再開手順における修正定義ファイルの更新工程の欠落）
  - 付随して、設計文書の記述を1点訂正: `touten` スタイルは「、。→，．」の置換を行わないだけであり、
    原本が正当に用いる「，．」を除去するものではない（実測21件はすべて原本由来。
    参考文献リスト・箇条書き記号「イ．ロ．ハ．」・文字そのものへの言及）

- **feat-012**: final ディレクトリ構築の自動化
  - `scripts/build_final.py` を追加。正規化済みディレクトリ（`run-NN-normalized/`）から
    最終成果物 `final/{name}/`（md＋content_list.json＋images/）を組み立て、
    feat-005 design.md §4.4 で定義された3種類の機械検証（コピー元とのバイト同一・
    md の画像参照がすべて `images/` に存在すること・content_list の `img_path` の basename 集合が
    `images/` のファイル集合と完全一致すること）を実行する CLI。コピーは一時ファイル経由の
    原子的置換で行い、再構築時は `images/` の孤児ファイルを削除する（集合の完全一致を保つため）
  - `scripts/ocr_dir.py` に `--final` を追加（オプトイン）。指定すると修正適用の直後に
    「カラー再切出（`colorize_images.py`）→ final 構築（`build_final.py`）」を実行する。
    **章単位構築**（1章の処理が終わるたびにその章の final を作る）とし、
    後続の章が FAIL しても完成済みの final は残る。PASS サマリに `images=` を追加
  - 背景: `ocr_dir.py` は図画像を扱わないため、正規化済み出力には md と content_list.json しか
    置かれず `![](images/…)` が解決できない。final の構築は feat-005 以来「カラー再切出 →
    コピー → 3種類の検証」の手作業だった。次に処理する『プログラミングのための確率統計』は
    10章あり、同じ手作業を10回繰り返すことになるため自動化した（ユーザー要望）
  - **安全機構**: 出力先がコピー元と同一・入れ子になる指定、出力先やその `images/` が
    シンボリックリンクである場合を、`--overwrite` の有無によらず書き込み前に拒否する
    （コピー元の成果物を上書き・削除しうる唯一の経路を入口で塞ぐ）。
    入力検証は計10項目、構築中の `OSError` はトレースバックを出さずに終了コード 1 で報告する
  - 実データ検証（PRML の既存成果物は読み取りのみ）: chap00〜07 の 8 章を一時ディレクトリへ
    構築し、md・content_list 計16ファイルが既存 final と**バイト同一**、`images/` のファイル名集合も
    全章一致（3/55/55/36/27/41/25/0 枚。chap07 は図 0 件で空ディレクトリ）。
    `--overwrite` での再実行でツリー全体のハッシュが不変（冪等）。性能は chap01 で 0.19 秒
  - テスト36件追加（新規 `tests/test_build_final.py` 28件・`tests/test_ocr_dir.py` 8件。
    計206件全 PASS、`tests/results/feat-012_test_result.txt`）。
    Codex レビュー: 3サイクルで収束（高3・中1を検出。3件の「高」はいずれもコピー元を破壊しうる
    経路（出力先の重なり・`validate_inputs` のシグネチャ不足・シンボリックリンクの素通り））
  - ユーザー実機テストで、確率統計 chap07 の final 構築と Markdown プレビューでの図表示を確認

- **feat-011**: 他書籍対応 — 正規化・脚注処理の一般化
  - 第2の書籍『プログラミングのための確率統計』（全10ディレクトリ・補正済み TIF 383枚）の OCR 化要望を受けた調査で、既存パイプラインが PRML 専用の前提を含むことが判明。chap07（18ページ）のパイロット実行（所要1分45秒・ページ欠落なし・独立数式33件、数式品質は原本 p.263 と照合して一致）で課題を実測し、書籍ごとに異なる部分をパラメータ化した
  - `scripts/normalize_punct.py`: `--punct-style {comma,touten}` を追加（既定 `comma` = 従来動作の「、→，」「。→．」。`touten` は句読点を置換しない）。確率統計は「、。」表記のため、従来の無条件正規化では chap07 だけで 501 箇所が原本と異なる出力になっていた。あわせて**中国語字8種の字形正規化**（值→値・变→変・单→単・对→対・图→図・换→換・徵→徴・樣→様）をスタイルによらず常時適用し、**正規化後に残る JIS X 0208 外の漢字を標準エラーへ警告**する（`str.encode("shift_jis")` の可否で判定。規則化できない誤認識を `apply_fixes.py` に回すための検出）
  - `scripts/ocr_dir.py`: `--punct-style` を追加して `normalize_punct.py` へ伝播。`check_normalized` の許可差分・残存検査を `normalize_punct.build_replacements(style)` から自動導出する構造に変更し（置換表の二重管理を解消）、正規化サブプロセスの標準エラーを素通しするようにした
  - `scripts/insert_footnotes.py`: 脚注番号プレフィックスに `*N` / `\*N` / `$^{N}$` / `$^{*N}$` を追加（確率統計は `*1` 形式。従来は別々の脚注が1行に連結されていた）。断片判定の比較キーから `$`・`\` を除去し、本文が数式化された断片（`[0,1)` と `$[0,1)$`）も除去できるようにした。比較キーが空になるブロックは断片判定の対象外とする（`"" in "任意の文字列"` が真になり内容が消えるため）
  - **中国語字は PRML 側にも混入していた**ことが判明（final の Markdown 8章で 13 件、content_list.json で 23 件）。字形正規化は PRML にとっても改善であり、`--punct-style` 省略時の既存の呼び出しでも出力が変わる意図的な差分として要求仕様に明記した
  - 実データ検証（成果物は書き換えずに一時ディレクトリで実施）: PRML final の再正規化差分が md 13件・json 23件・句読点差分 0件で期待値と完全一致、JIS 外漢字の警告が `跺` 2件・`濵` 1件、脚注の冪等性が全8章で 0 inserted かつバイト同一。確率統計 chap07 では `--punct-style touten` で「、」241件・「。」260件が保持され、字形置換 9件（樣6・图1・变1・换1）、脚注 p4 が2行の blockquote に分割、p10 の断片残留が解消
  - テスト27件追加（計170件全 PASS、`tests/results/feat-011_test_result.txt`）。性能は `normalize_text` 1.13倍・`insert_notes` 0.99倍（要件は2倍未満）。Codex レビュー: 4サイクルで収束（高3・中3を検出。主要指摘は回帰の期待件数の自己矛盾、MVP 範囲と FR 間の依存の矛盾、任意項目 FR-003 不採用時にテスト要求が満たせない矛盾）
  - **付随して発見**: PRML chap01 の訳注1（`page_idx` 83「Bayesian inference は…」）が final の md に挿入されていない。該当ページでアンカーが見つからず feat-009 の設計どおりスキップされたもので、変更前スクリプトでも同一結果（本案件による回帰ではない）。成果物の欠落として別途対処を要する
  - ユーザー手動テストで `--punct-style` の表示と、確率統計 chap07 の実行で句読点が「、。」のまま保たれることを確認

### 2026-08-25

- **feat-010**: 手動修正の永続化 — 修正定義ファイルの機械適用
  - `scripts/apply_fixes.py` を追加。OCR 誤りの手動修正を「old→new の完全一致文字列ペア」として章ごとの修正定義ファイルに記録し、md に機械適用する CLI。old ちょうど1回一致で置換、old 不在でも new ちょうど1回なら適用済みスキップ（冪等）、それ以外（不在・複数一致・曖昧）は全件エラーで出力を書かない。出力前に最終不変条件（全 fix で old=0回・new=1回）を検査し、再実行が冪等にならない定義を初回に検出する
  - **公開分離**: 定義ファイルの実体（書籍本文の文字列を含む）はリポジトリ外 `{BASE}/ocr/fixes/{name}.json` にのみ置く（GitHub 公開時に露出しない。ユーザー要件）。リポジトリには `fixes/template.json`（架空文字列のみ）と `fixes/README.md`（書式仕様・適用規則）のみ
  - `scripts/ocr_dir.py` に `--fixes-dir` オプションを追加: 脚注挿入の直後に `{name}.json` があれば適用（なければ何もしない）。PASS サマリに `fixes=` を追加
  - 初期データ: MinerU の式番号誤結合5件（chap01 の (1.25)/(1.26)、chap02 の (2.218)/(2.219)、chap05 の (5.98)〜(5.100)・(5.106)/(5.107)・(5.159)/(5.160)。全て原本 TIF 目視確認済み）を定義し、final / run-01-normalized 両系統に適用（applied 1/1/3、両系統バイト同一・content_list 無改変・迷子番号 0 件・各タグちょうど1回・冪等性を検証）。統合型3件は array を独立 `$$` ブロックに分割（構文要素のみ除去）
  - テスト34件追加（計143件全 PASS、`tests/results/feat-010_test_result.txt`）。Codex レビュー: 3サイクルで収束（高2・中2を検出。主要指摘は要求仕様内の公開分離違反、array 分割と alignment marker の衝突、適用済み判定の甘さ、冪等性の最終不変条件不足）
- **feat-009**: MinerU 出力から欠落する脚注（訳注）の Markdown 挿入
  - `scripts/insert_footnotes.py` を追加。MinerU が Markdown に出力しない `page_footnote` 型ブロック（chap01 の訳注1〜13 を含む計31ブロック。ユーザー報告: 式 (1.45) 後の注釈4・5 が md に見当たらない）を content_list.json からページ単位で組み立て、該当ページ本文末尾の直後に blockquote（`> 4 訳注：…`）として挿入する CLI。組み立ては「浮遊断片除去（空白除去後の部分文字列判定）→ bbox (y0, x0) 読み順ソート → 脚注番号プレフィックスで結合」。アンカー探索は単調増加カーソルの完全一致（table は `table_body` → `convert_table` 再生成パイプテーブル → `img_path` の3候補）。冪等（既挿入はスキップ）、content_list は無改変
  - `scripts/ocr_dir.py` に組み込み: HTML 表変換の直後に正規化済み md へインプレース適用（`insert_footnotes` / `parse_footnote_summary` を新設。非0終了・サマリ解析不能は FAIL）。PASS サマリに `footnotes=` を追加
  - 既存データ適用: 全8章 × final / run-01-normalized 両系統。挿入は chap01 = 14件＋1件スキップ（第2章冒頭が写った最終ページ。アンカーなしで正）、chap02 = 3件、chap06 = 2件、他5章 = 0件（設計の期待値と完全一致）。適用後も final = run-01-normalized のバイト同一・content_list 16ファイルの sha256 不変を検証。実データで冪等性も確認（再実行 0件挿入・md 不変）。ユーザー手動テストで注釈4・5 の表示を確認
  - テスト24件追加（計109件全 PASS、`tests/results/feat-009_test_result.txt`）。Codex レビュー: 2サイクルで収束（高1・中1を検出。主要指摘は feat-008 変換後 md で table アンカーが `img_path` では一致しない矛盾）

### 2026-08-24

- **feat-008**: MinerU 出力の HTML 表を Markdown パイプテーブルに変換
  - `scripts/html_table_to_md.py` を追加。MinerU が1行の HTML `<table>` として出力する表（CommonMark の HTML ブロックとなり `$…$` が数式描画されない。VS Code プレビューで chap01 表 1.1/1.2 の数式が LaTeX ソースのまま表示される問題の原因）を GFM パイプテーブルに変換する CLI。表タグ6種・属性なし・列数一致の単純表のみ変換し、`colspan` 等の複雑な表は壊さずスキップして警告（忠実性優先）。数式中の `<`（`$a<b$`）はセルテキストとして保持。改行コード（LF/CRLF）を含め表行以外はバイト不変
  - `scripts/ocr_dir.py` に組み込み: 機械確認合格後に正規化済み md をインプレース変換（`convert_tables` / `parse_table_summary` を新設。合計行を読み取れない場合は FAIL）。PASS サマリに `tables=` を追加。content_list.json は MinerU スキーマ維持のため無改変
  - 既存データ適用: chap01（2表）・chap06（1表）の final / run-01-normalized 計4ファイルを変換（スキップ 0、変更行は表行の置換のみ、content_list・images の sha256 不変）。ユーザー手動テストで chap01 表 1.1/1.2 の数式描画を確認
  - **付随して発見・復旧**: run-01-normalized の md 2件が feat-005 の final 構築（8/21 15:05、バイト同一 PASS 記録あり）後に外的要因で損傷していた（chap06 末尾49行欠落・chap07 先頭28行欠落。mtime 8/21 15:12、原因プロセス特定不能）。final は全8章とも生出力＋正規化とバイト一致（正）であることを確認の上、final からのコピーで復旧。復旧後、全8章で final = run-01-normalized のバイト同一・`<table` 行 0 を検証（調査記録は案件 README「実装中の発見」）
  - テスト29件追加（計85件全 PASS、`tests/results/feat-008_test_result.txt`）。Codex レビュー: 5サイクルで収束（高2・中5を検出。うち1件は Sonnet 実装時検出の設計書の名前衝突 `convert_table(html)` × `import html`）。実装中断2回（設計書矛盾・データ損傷）はいずれも調査→設計改訂→レビュー→承認を経て再開

### 2026-08-21

- **feat-005**: 全スキャンデータの本処理（フェーズ4 完了）
  - chap00〜07 の8ディレクトリ・補正済み TIF 全396枚（除外なし・辞書順）を OCR し、最終成果物 `{BASE}/ocr/final/chapNN/`（Markdown＋content_list.json＋カラー images/）を確立。**書籍スキャン分の LLM 用 Markdown 化が完了**
  - 経緯: 当初「章単位の再構成・重複/白紙除外」を計画したが、ユーザーレビューで「ゴールは入力への忠実な高精度 OCR」と指摘され全面是正（2026-08-18）。入力範囲も chap01〜04 → chap00〜07 に拡大
  - 実測: chap00〜03 は手動＋ocr_dir.py、chap04〜07 は ocr_dir.py 一括（PASS。計178ページ約16分、モデル追加DLなし）。全8章で page_idx 検査（白紙位置のみ欠落許容）・正規化のコードポイント機械確認に合格。図はカラー化済み（計207枚。chap07 は図なし）
  - final 構築の機械確認: コピー16ファイルのバイト同一、md 参照画像の存在、content_list の img_path ユニーク集合 = images 一致（全8章）。回帰テスト56件 PASS（`tests/results/feat-005_test_result.txt`）
  - Codex レビュー: 8サイクル（高2・中7を検出。スコープ2転換を含む）

### 2026-08-18

- **feat-007**: 図画像のカラー再切出スクリプト
  - `scripts/colorize_images.py` を追加。MinerU content_list の図ブロック（img_path を持つ image/chart/table）の bbox（0–1000 正規化。実データで座標系を検証）と page_idx から、原本 TIF（600dpi カラー）の同一領域を切り出してグレー画像と同名で差し替える CLI。md・content_list は無改変（確認済みテキストに手を付けない）
  - 手動テスト差し戻し1回（イテレーション1）: 原寸出力で表示サイズが約3倍になったため、既定 1/3 縮小（200dpi 相当 = 旧画像と同等の表示サイズ、差1%以内）＋ `--scale` オプションを追加
  - 動作確認: chap00〜03 の全図ブロック（3/55/55/36）を再生成し、カラー（彩度指標 0.00→6.15）・サイズ 630×944（旧 631×946 比 0.2%差）を確認。ユーザー手動テストでカラー表示・表示サイズを確認
  - テスト15件追加（計56件全 PASS、`tests/results/feat-007_test_result.txt`）。Codex レビュー: 6サイクルで収束（高2・中6を検出。主要指摘は feat-005 final 構築とのコピー元矛盾、JPEG 非可逆とテスト完全一致の矛盾、1/3 丸めと「同一サイズ」表現の矛盾）。feat-005 の FR-004・§4.4 も併せて改訂（final の images コピー元を正規化側に変更、カラー再切出完了を前提条件化）
- **feat-006**: OCR 一括実行スクリプト
  - `scripts/ocr_dir.py` を追加。TIF ディレクトリを指定するだけで「PDF 生成 → MinerU 変換（no_proxy 設定・run 連番・ログ保存・60分タイムアウト）→ 句読点正規化 → 機械確認（ページ数・page_idx（白紙位置のみ欠落許容）・コードポイント比較）」を一括実行し、ユーザーが Claude Code を介さず OCR できるようにした（ユーザー要望）。複数ディレクトリの逐次処理・PASS/FAIL サマリ付き
  - 安全設計: PDF 生成時に対象TIF のパス・サイズ・mtime を manifest（JSON）として記録し、既存 PDF の再利用は manifest 完全一致時のみ（Codex 指摘・高）。name 重複の事前エラー、ページ数検証は MinerU 実行前
  - 依存変更: pypdf 6.16.1 を dev group から通常依存へ移動（新規インストールなし）
  - 動作確認: chap03 実データで PASS（44ページ・505ブロック・置換330箇所・4分35秒）。feat-005 の chap03 処理を兼ねる。テスト17件追加（当時計41件全 PASS、`tests/results/feat-006_test_result.txt`）。Codex レビュー: 2サイクルで収束（高1・中3を検出）

### 2026-08-17

- **feat-004**: 句読点正規化の後処理と再判定
  - `scripts/normalize_punct.py` を追加。MinerU 出力（md / content_list.json）の「、→，」「。→．」を無条件全文置換する CLI（原本が「、。」を不使用のため安全。JSON は文字列値のみ置換し構造を保存）
  - run-02 出力を正規化（md 247 + json 258 = 505箇所置換）。コードポイント位置比較で「差分は許可置換のみ・長さ一致・残存『、。』ゼロ」を機械確認し、feat-003 の項目A（数式 19/20）・項目C（読み順 5/5）の引き継ぎを成立させた
  - 再判定（criteria lock 済みの事前基準・feat-003 と同一10段落）: 本文 **10/10 合格**（feat-003 は 7/10）→ 総合 **Go**。残存誤りは「;→；」1文字と「ようやく→よやく」1文字脱落のみ。疑義の括弧2件は原本TIF（600dpi）切り出しで確認し誤りではないと判定。ユーザー二次確認済み
  - テスト8件追加（計24件全 PASS、`tests/results/feat-004_test_result.txt`）。Codex レビュー: 2サイクルで収束（高2・中1を検出。主要指摘は `str.translate` に dict を渡す設計不備、A/C 引き継ぎの機械確認アルゴリズム未定義）
  - フェーズ4（全章の本処理）へ進行可

- **feat-003**: MinerU 試行（章単位PDFの品質確認）
  - 章単位PDF を生成（chap-00: 17ページ 6.9MB / chap-01: 64ページ 54.5MB。章対応はユーザー回答＋機械検証で確定し案件 README に記録）
  - MinerU 3.4.4（hybrid-engine 既定）で chap-01 を変換。初回モデルDL 2.4GB、所要 6分26秒、独立数式157件。プロキシ環境で localhost ヘルスチェックが 502 になる想定外事象が発生し、`no_proxy` への localhost 追加を設計に反映して解決（run-01 失敗 → run-02 成功）
  - 品質判定（criteria lock 済みの事前基準）: 数式 19/20 合格・本文 7/10 **不合格**・読み順 5/5 合格 → **No-Go**。不合格の主因は句読点「，．」→「、。」の系統的置換（全ブロックの15%・241箇所）。純粋な文字誤認識は10段落で1文字のみ。対策として feat-004（句読点正規化の後処理）を起票
  - 回帰テスト16件 PASS（`tests/results/feat-003_test_result.txt`）。Codex レビュー: 4サイクル（高3・中4を検出、criteria lock 含む）

- **feat-002**: TIF → OCR用可逆PDF生成スクリプト
  - `scripts/make_ocr_pdf.py` を追加。原本TIF（600dpi）をグレースケール化・1/2縮小（300dpi相当）し、Flate 可逆で複数ページPDFに格納する CLI（入力TIF列 + `-o` + `--overwrite`。章対応は呼び出し時のファイル指定で表現）
  - 安全設計: 変換前の全件検証（存在・モード・`im.load()` による実デコード）、一時ファイル経由の原子的書き込み（no-clobber は `os.link`、上書きは `os.replace`）
  - 依存追加: `img2pdf==0.6.3`（新規）、`pillow==12.3.0`・`pypdf==6.16.1`（ロック済み版の明示固定）
  - 検証結果: テスト10件追加（計16件全 PASS、`tests/results/feat-002_test_result.txt`）。実データ確認: 4件サンプルで gray/8-bit/Flate/300ppi を確認、全84件の一括変換が 22.4秒・62.3MB（`.../chap01/out/ocr/` に `sample4.pdf`・`all-pages.pdf` を残置）。ユーザーの目視で文字・数式の可読性を確認
  - Codex レビュー: 3サイクルで収束（高1・中3を検出）。主要指摘は非原子的書き込みによる不完全PDF残留、壊れたTIFの事前検出漏れ、`--overwrite` なし時の競合上書き、書き込み失敗テストの実効性

- **feat-001**: uv 環境構築と MinerU 導入
  - uv プロジェクトを初期化（`pyproject.toml` / `.python-version` / `uv.lock`）。`mineru[core]==3.4.4`、`torch==2.13.0+cu130`・`torchvision==0.28.0+cu130`（PyTorch 公式 cu130 インデックスから固定）、`pytest==9.1.1` を導入
  - `[tool.uv]` で Python 自動ダウンロード禁止（`python-downloads = "never"`）とシステム Python 限定（`python-preference = "only-system"`）を設定
  - 検証結果: スモークテスト6件全件 PASS（Python 3.12 / CUDA 認識 / CUDA ビルド 12.8 以上 / compute capability (12,0) / CUDA カーネル実行 / mineru バージョン）。結果は `tests/results/feat-001_test_result.txt`。ユーザーの手動テストでも全件 PASS を確認
  - Codex レビュー: 4サイクルで収束（高3・中3を検出）。主要指摘は torch の推移的依存任せによるバージョン不定、Python 自動ダウンロードの禁止漏れ、cu130 インデックスのネットワーク要件記述漏れ、TECH_STACK.md 更新計画の漏れ
