# CHANGELOG

## リリース履歴

### 2026-08-31

- **update-002**: Codex レビュー手順の収束条件を誤読不能な形に改訂
  - feat-015 の Codex レビューで、`CLAUDE.md`「レビューの進め方」ルール4〜6 が2つの読み
    ——ループ型（解消確認のゼロのたびに `/new` → 全文ゲートを再発動。収束はゲート自身の
    ゼロのみ）と 1回ゲート型（「新しい目」のゲートは案件につき1回。ゲートの会話でゼロに
    達したら収束）——を許すことが顕在化した。Claude Code（Opus）は両方の読みを自覚しながら
    ユーザーに確認せず独自解釈で人レビューへ進み、案件は On Hold となった
  - 本案件のレビュー運用の実測（ゲート1回目で2件 → 全件反映後の2回目で別の2件）により
    ループ型では収束が保証されないことが確認され、ユーザーが意図を**1回ゲート型**と確定した
    （2026-08-31）。この確定により feat-015 当時の進行それ自体は規則に適合していたことになる
    （経緯は案件 README.md §1）
  - 逸脱を許した構造要因4点を特定した（案件 README.md §4）: (1) 全文ゲートが1回か毎回かを
    特定していない (2)「収束」が独立した定義になっていない (3) 記録様式に収束判定の欄がない
    (4) 逸脱そのものを禁じる明文がない
  - 「レビューの進め方」を全置換した: 「収束」を独立した定義として切り出し（1回ゲート型。
    C 自身または C 後の解消確認が高・中ゼロ。ゲート前のゼロは収束ではない）、依頼を
    **A: 初回レビュー / B: 解消確認 / C: 全文ゲート** に命名して「直前の依頼 × 結果 ×
    C の実施状態 → 次にすること」の遷移表（9行）へ置き換え、人レビューの前提2条件と**逸脱の禁止**
    （遷移表にない動きをしない。省略・別解釈を思いついたら実行せず止まってユーザーに確認）を
    明文化した
  - 「結果の保存」の冒頭メタに依頼種別（A/B/C）・直前の `/new` 送信の有無・
    ゲート状態（C の実施状態。収束時は根拠となる C の結果ファイル名を併記）・収束判定を
    必須項目として追加した。「収束」と書けるのは C のゼロまたはゲート実施済みの B の
    ゼロのときだけと定めた
  - 既存規則の内容は変更せず、規則としては「逸脱の禁止」のみを新規追加した
    （他はすべて表現のみの変更、およびユーザーが確定した意図の明文化）。旧ルール5 の
    「指摘が出たら」（重要度を区別しない表記）は、同じ手順内の他規定（旧ルール2・3・4・6 が
    すべて高・中で判定していること）に基づき「高・中が1件以上」と
    解釈を確定した（案件 design.md §4.4）。既存記述は削除せず、
    「同一会話の回数上限…」「レビュー実行時は…」の2行は文言を1文字も変えずに移設
    （対応表は案件 design.md §4.3）。`CLAUDE.md` の他の「収束」言及箇所（各フローの
    ステップ4・criteria lock・ライフサイクル・委任規定）は、定義済み用語となったため無変更
  - 本案件は起票時に Claude Code（Opus）がユーザーの着手許可なく進行し中断された経緯を持つ。
    案件ドキュメントはモデル交代後（Fable）に全面的に書き直し、codex レビューは
    モデル変更（gpt-5.6-sol high）に伴い再起動した第2世代ストリームで実施した
    （経緯の詳細は案件 README.md §2）
  - コード変更なし（テスト不要）

- **update-001**: 開発ドキュメントテンプレート改訂の取り込み（Herdr 対話方式レビュー等）
  - テンプレートリポジトリ `DEV_TEMPLATE`（`template/`）の `3e62a11..19e4977`（2026-08-27 の
    4コミット）を全量取り込んだ。基準書5点は変更なし（テンプレート最新と同一のまま）
  - Codex レビューを `codex exec` バックグラウンド方式から **Herdr 対話方式**へ全面移行
    （CLAUDE.md のレビュー節を差し替え。レビューストリーム命名は `rev-honocr-{案件ID}`。
    本案件のレビュー自体が新方式の初適用）
  - `AGENTS.md`（Codex が起動時に読むレビュー定型指示）と `docs/HERDR_SETUP.md`
    （セットアップ手順）をテンプレートから新設
  - 「行き詰まり検出（全作業共通・必須）」を Claude Code 運用ルールに新設
  - `docs/BACKLOG.md` にステータス凡例6種（Review・On Hold・Cancelled を含む）を追加
  - full.log の運用を廃止: `.gitignore` の該当行を削除し、過去の full.log 55件を削除
    （ユーザー承認済みの意図的な破棄。依頼文・session id・調査過程は失われるが、
    各レビューの結論は `codex-NN.result.md` に保存済み）
  - コード変更なし（テスト不要）

- **feat-016**: chap07 の中国語語彙誤認識「上的」の修正
  - 確率統計 chap07 の本文1箇所で、原本の「[0,1) **上の**一様分布」が
    「[0,1) **上的**一様分布」と OCR されていた（content_list の bbox から原本 TIF
    `page-05_2R.tif` の該当領域を切り出して目視確認）。「上」「的」はいずれも JIS X 0208 内の
    漢字であるため feat-011 の JIS 外漢字警告に現れず、字形の 1 対 1 対応でもないため
    feat-013 の字形正規化テーブルでも扱えない。**語彙単位の誤り**という新しい分類であり、
    ユーザーの手動テストでのみ発見できた
  - `{BASE2}/ocr/fixes/chap07.json`（リポジトリ外・新規）に修正1件を登録し、
    `apply_fixes.py` → `build_final.py` で chap07 のみ再適用した（MinerU と
    `normalize_punct.py` は再実行していない）
  - `old` / `new` に直前の `[0,1)` を含めた。`old` = `上的` は適用前に一意だが、
    `new` = `上の` は適用後23件となり `apply_fixes.py` の最終不変条件
    （`count(new) == 1`）に違反するため（feat-013 の学びを事前に適用）
  - **リポジトリ内のコードは1行も変更していない**（`scripts/` と `tests/test_*.py`）。
    データ側の修正定義のみで完結する案件である
  - 影響範囲の確認: 確率統計 全10章・PRML 全8章を走査し「上的」は chap07 の1件のみ。
    chap03 の「比較**的一**定」は正当な日本語であり修正対象外と判定した
  - 検証結果: `apply_fixes.py` `applied=1`・`build_final.py` の3検証すべて合格。
    適用後の md は「上的」0件・「上の一様分布」17件・文字数 22958（不変）・差分は 228 行目の
    1行のみ。`content_list.json` には誤字が1件残るが、md のみを修正する既存ポリシー
    （feat-010／feat-013 §6.1）どおりの正しい状態である。テスト216件全 PASS
    （`tests/results/feat-016_test_result.txt`）。ユーザー手動テスト済み
  - Codex レビュー: 3サイクルで収束（高1・中2を検出。主要指摘は「tests/ を変更しない」制約と
    テスト結果保存要件の矛盾、件数検証に `grep -c`（行数）を使っていた誤り）

### 2026-08-28

- **feat-013**: 字形正規化テーブルの拡充と確率統計への適用
  - 確率統計の全10章（383ページ）を処理した際、feat-011 の JIS 外漢字警告が置換表に未登録の
    中国語字を9種検出した。さらに調査で、JIS X 0208 内にあるため警告に現れない旧字体が
    4種見つかった。`CJK_REPLACEMENTS` を **8種から21種へ拡充**（黑→黒・說→説・题→題・戾→戻・
    边→辺・橫→横・虛→虚・錄→録・權→権・收→収・檢→検・縱→縦・廣→広 を追加）
  - 置換表を種別ごとに `CJK_REPLACEMENTS_CN`（16種）と `OLD_FORM_REPLACEMENTS`（5種）へ分割し、
    合成して既存名 `CJK_REPLACEMENTS` に束ねた。参照する既存コードは変更不要
  - **旧字体の置換箇所を、置換【前】の文脈つきで標準エラーへ警告する機能を追加**。
    旧字体は固有名詞では正当な表記になりうる（実例: chap09 奥付の「印刷·製本 廣済堂」は
    旧字を正式名称に用いる実在の社名）が、置換後のテキストからは元の字が失われ、
    後から grep しても壊れた箇所を見つけられない。Codex レビューの指摘により追加した
  - 確率統計10章の既存成果物へ再適用（MinerU の再実行なし。再正規化 → 修正適用 →
    `build_final.py` の3ステップ）。修正定義ファイル5件を新規作成（`亻→イ` ×2・`揾→掟`・
    `椕→楕`・固有名詞「広済堂→廣済堂」の復元。すべて原本 TIF で目視確認済み）
  - **実装中断1回**: chap02・chap06 の修正定義が `apply_fixes.py` の最終不変条件
    （適用後に `new` がちょうど1回）に違反して停止した。原因は `new`（`\text {イ}`）が
    他所に正当に存在していたこと（chap02 で11件、chap06 で1件）で、設計が `old` の一意性しか
    確認していなかったため。`old`/`new` の両方に文脈を含めて一意にし、設計に
    「`new` の適用後の一意性も必ず数える」手順と確定値の表を追加して再開した
  - 検証: 全10章の final に21種の残存なし（chap09 md の `廣` 1件のみ＝固有名詞の復元で正）、
    「、。」保持、final と `run-NN-normalized` がバイト同一、2回実行してツリー全体のハッシュ不変（冪等）。
    PRML は追加13種が0件のため対象外とし、変更していない
  - テスト10件追加（計216件全 PASS、`tests/results/feat-013_test_result.txt`）。
    Codex レビュー: 5サイクルで収束（高5・中1を検出。主要指摘は冪等性の定義の矛盾、
    固有名詞の破壊を検出する手段の欠如、事前ゲートの content_list.json 漏れ、
    実装再開手順における修正定義ファイルの更新工程の欠落）
  - 付随して、設計文書の記述を1点訂正: `touten` スタイルは「、。→，．」の置換を行わないだけであり、
    原本が正当に用いる「，．」を除去するものではない（実測21件はすべて原本由来。
    参考文献リスト・箇条書き記号「イ．ロ．ハ．」・文字そのものへの言及）

- **feat-012**: final ディレクトリ構築の自動化
  - `scripts/build_final.py` を追加。正規化済みディレクトリ（`run-NN-normalized/`）から
    最終成果物 `final/{name}/`（md＋content_list.json＋images/）を組み立て、
    feat-005 design.md §4.4 で定義された3種類の機械検証（コピー元とのバイト同一・
    md の画像参照がすべて `images/` に存在すること・content_list の `img_path` の basename 集合が
    `images/` のファイル集合と完全一致すること）を実行する CLI。コピーは一時ファイル経由の
    原子的置換で行い、再構築時は `images/` の孤児ファイルを削除する（集合の完全一致を保つため）
  - `scripts/ocr_dir.py` に `--final` を追加（オプトイン）。指定すると修正適用の直後に
    「カラー再切出（`colorize_images.py`）→ final 構築（`build_final.py`）」を実行する。
    **章単位構築**（1章の処理が終わるたびにその章の final を作る）とし、
    後続の章が FAIL しても完成済みの final は残る。PASS サマリに `images=` を追加
  - 背景: `ocr_dir.py` は図画像を扱わないため、正規化済み出力には md と content_list.json しか
    置かれず `![](images/…)` が解決できない。final の構築は feat-005 以来「カラー再切出 →
    コピー → 3種類の検証」の手作業だった。次に処理する『プログラミングのための確率統計』は
    10章あり、同じ手作業を10回繰り返すことになるため自動化した（ユーザー要望）
  - **安全機構**: 出力先がコピー元と同一・入れ子になる指定、出力先やその `images/` が
    シンボリックリンクである場合を、`--overwrite` の有無によらず書き込み前に拒否する
    （コピー元の成果物を上書き・削除しうる唯一の経路を入口で塞ぐ）。
    入力検証は計10項目、構築中の `OSError` はトレースバックを出さずに終了コード 1 で報告する
  - 実データ検証（PRML の既存成果物は読み取りのみ）: chap00〜07 の 8 章を一時ディレクトリへ
    構築し、md・content_list 計16ファイルが既存 final と**バイト同一**、`images/` のファイル名集合も
    全章一致（3/55/55/36/27/41/25/0 枚。chap07 は図 0 件で空ディレクトリ）。
    `--overwrite` での再実行でツリー全体のハッシュが不変（冪等）。性能は chap01 で 0.19 秒
  - テスト36件追加（新規 `tests/test_build_final.py` 28件・`tests/test_ocr_dir.py` 8件。
    計206件全 PASS、`tests/results/feat-012_test_result.txt`）。
    Codex レビュー: 3サイクルで収束（高3・中1を検出。3件の「高」はいずれもコピー元を破壊しうる
    経路（出力先の重なり・`validate_inputs` のシグネチャ不足・シンボリックリンクの素通り））
  - ユーザー実機テストで、確率統計 chap07 の final 構築と Markdown プレビューでの図表示を確認

- **feat-011**: 他書籍対応 — 正規化・脚注処理の一般化
  - 第2の書籍『プログラミングのための確率統計』（全10ディレクトリ・補正済み TIF 383枚）の OCR 化要望を受けた調査で、既存パイプラインが PRML 専用の前提を含むことが判明。chap07（18ページ）のパイロット実行（所要1分45秒・ページ欠落なし・独立数式33件、数式品質は原本 p.263 と照合して一致）で課題を実測し、書籍ごとに異なる部分をパラメータ化した
  - `scripts/normalize_punct.py`: `--punct-style {comma,touten}` を追加（既定 `comma` = 従来動作の「、→，」「。→．」。`touten` は句読点を置換しない）。確率統計は「、。」表記のため、従来の無条件正規化では chap07 だけで 501 箇所が原本と異なる出力になっていた。あわせて**中国語字8種の字形正規化**（值→値・变→変・单→単・对→対・图→図・换→換・徵→徴・樣→様）をスタイルによらず常時適用し、**正規化後に残る JIS X 0208 外の漢字を標準エラーへ警告**する（`str.encode("shift_jis")` の可否で判定。規則化できない誤認識を `apply_fixes.py` に回すための検出）
  - `scripts/ocr_dir.py`: `--punct-style` を追加して `normalize_punct.py` へ伝播。`check_normalized` の許可差分・残存検査を `normalize_punct.build_replacements(style)` から自動導出する構造に変更し（置換表の二重管理を解消）、正規化サブプロセスの標準エラーを素通しするようにした
  - `scripts/insert_footnotes.py`: 脚注番号プレフィックスに `*N` / `\*N` / `$^{N}$` / `$^{*N}$` を追加（確率統計は `*1` 形式。従来は別々の脚注が1行に連結されていた）。断片判定の比較キーから `$`・`\` を除去し、本文が数式化された断片（`[0,1)` と `$[0,1)$`）も除去できるようにした。比較キーが空になるブロックは断片判定の対象外とする（`"" in "任意の文字列"` が真になり内容が消えるため）
  - **中国語字は PRML 側にも混入していた**ことが判明（final の Markdown 8章で 13 件、content_list.json で 23 件）。字形正規化は PRML にとっても改善であり、`--punct-style` 省略時の既存の呼び出しでも出力が変わる意図的な差分として要求仕様に明記した
  - 実データ検証（成果物は書き換えずに一時ディレクトリで実施）: PRML final の再正規化差分が md 13件・json 23件・句読点差分 0件で期待値と完全一致、JIS 外漢字の警告が `跺` 2件・`濵` 1件、脚注の冪等性が全8章で 0 inserted かつバイト同一。確率統計 chap07 では `--punct-style touten` で「、」241件・「。」260件が保持され、字形置換 9件（樣6・图1・变1・换1）、脚注 p4 が2行の blockquote に分割、p10 の断片残留が解消
  - テスト27件追加（計170件全 PASS、`tests/results/feat-011_test_result.txt`）。性能は `normalize_text` 1.13倍・`insert_notes` 0.99倍（要件は2倍未満）。Codex レビュー: 4サイクルで収束（高3・中3を検出。主要指摘は回帰の期待件数の自己矛盾、MVP 範囲と FR 間の依存の矛盾、任意項目 FR-003 不採用時にテスト要求が満たせない矛盾）
  - **付随して発見**: PRML chap01 の訳注1（`page_idx` 83「Bayesian inference は…」）が final の md に挿入されていない。該当ページでアンカーが見つからず feat-009 の設計どおりスキップされたもので、変更前スクリプトでも同一結果（本案件による回帰ではない）。成果物の欠落として別途対処を要する
  - ユーザー手動テストで `--punct-style` の表示と、確率統計 chap07 の実行で句読点が「、。」のまま保たれることを確認

### 2026-08-25

- **feat-010**: 手動修正の永続化 — 修正定義ファイルの機械適用
  - `scripts/apply_fixes.py` を追加。OCR 誤りの手動修正を「old→new の完全一致文字列ペア」として章ごとの修正定義ファイルに記録し、md に機械適用する CLI。old ちょうど1回一致で置換、old 不在でも new ちょうど1回なら適用済みスキップ（冪等）、それ以外（不在・複数一致・曖昧）は全件エラーで出力を書かない。出力前に最終不変条件（全 fix で old=0回・new=1回）を検査し、再実行が冪等にならない定義を初回に検出する
  - **公開分離**: 定義ファイルの実体（書籍本文の文字列を含む）はリポジトリ外 `{BASE}/ocr/fixes/{name}.json` にのみ置く（GitHub 公開時に露出しない。ユーザー要件）。リポジトリには `fixes/template.json`（架空文字列のみ）と `fixes/README.md`（書式仕様・適用規則）のみ
  - `scripts/ocr_dir.py` に `--fixes-dir` オプションを追加: 脚注挿入の直後に `{name}.json` があれば適用（なければ何もしない）。PASS サマリに `fixes=` を追加
  - 初期データ: MinerU の式番号誤結合5件（chap01 の (1.25)/(1.26)、chap02 の (2.218)/(2.219)、chap05 の (5.98)〜(5.100)・(5.106)/(5.107)・(5.159)/(5.160)。全て原本 TIF 目視確認済み）を定義し、final / run-01-normalized 両系統に適用（applied 1/1/3、両系統バイト同一・content_list 無改変・迷子番号 0 件・各タグちょうど1回・冪等性を検証）。統合型3件は array を独立 `$$` ブロックに分割（構文要素のみ除去）
  - テスト34件追加（計143件全 PASS、`tests/results/feat-010_test_result.txt`）。Codex レビュー: 3サイクルで収束（高2・中2を検出。主要指摘は要求仕様内の公開分離違反、array 分割と alignment marker の衝突、適用済み判定の甘さ、冪等性の最終不変条件不足）
- **feat-009**: MinerU 出力から欠落する脚注（訳注）の Markdown 挿入
  - `scripts/insert_footnotes.py` を追加。MinerU が Markdown に出力しない `page_footnote` 型ブロック（chap01 の訳注1〜13 を含む計31ブロック。ユーザー報告: 式 (1.45) 後の注釈4・5 が md に見当たらない）を content_list.json からページ単位で組み立て、該当ページ本文末尾の直後に blockquote（`> 4 訳注：…`）として挿入する CLI。組み立ては「浮遊断片除去（空白除去後の部分文字列判定）→ bbox (y0, x0) 読み順ソート → 脚注番号プレフィックスで結合」。アンカー探索は単調増加カーソルの完全一致（table は `table_body` → `convert_table` 再生成パイプテーブル → `img_path` の3候補）。冪等（既挿入はスキップ）、content_list は無改変
  - `scripts/ocr_dir.py` に組み込み: HTML 表変換の直後に正規化済み md へインプレース適用（`insert_footnotes` / `parse_footnote_summary` を新設。非0終了・サマリ解析不能は FAIL）。PASS サマリに `footnotes=` を追加
  - 既存データ適用: 全8章 × final / run-01-normalized 両系統。挿入は chap01 = 14件＋1件スキップ（第2章冒頭が写った最終ページ。アンカーなしで正）、chap02 = 3件、chap06 = 2件、他5章 = 0件（設計の期待値と完全一致）。適用後も final = run-01-normalized のバイト同一・content_list 16ファイルの sha256 不変を検証。実データで冪等性も確認（再実行 0件挿入・md 不変）。ユーザー手動テストで注釈4・5 の表示を確認
  - テスト24件追加（計109件全 PASS、`tests/results/feat-009_test_result.txt`）。Codex レビュー: 2サイクルで収束（高1・中1を検出。主要指摘は feat-008 変換後 md で table アンカーが `img_path` では一致しない矛盾）

### 2026-08-24

- **feat-008**: MinerU 出力の HTML 表を Markdown パイプテーブルに変換
  - `scripts/html_table_to_md.py` を追加。MinerU が1行の HTML `<table>` として出力する表（CommonMark の HTML ブロックとなり `$…$` が数式描画されない。VS Code プレビューで chap01 表 1.1/1.2 の数式が LaTeX ソースのまま表示される問題の原因）を GFM パイプテーブルに変換する CLI。表タグ6種・属性なし・列数一致の単純表のみ変換し、`colspan` 等の複雑な表は壊さずスキップして警告（忠実性優先）。数式中の `<`（`$a<b$`）はセルテキストとして保持。改行コード（LF/CRLF）を含め表行以外はバイト不変
  - `scripts/ocr_dir.py` に組み込み: 機械確認合格後に正規化済み md をインプレース変換（`convert_tables` / `parse_table_summary` を新設。合計行を読み取れない場合は FAIL）。PASS サマリに `tables=` を追加。content_list.json は MinerU スキーマ維持のため無改変
  - 既存データ適用: chap01（2表）・chap06（1表）の final / run-01-normalized 計4ファイルを変換（スキップ 0、変更行は表行の置換のみ、content_list・images の sha256 不変）。ユーザー手動テストで chap01 表 1.1/1.2 の数式描画を確認
  - **付随して発見・復旧**: run-01-normalized の md 2件が feat-005 の final 構築（8/21 15:05、バイト同一 PASS 記録あり）後に外的要因で損傷していた（chap06 末尾49行欠落・chap07 先頭28行欠落。mtime 8/21 15:12、原因プロセス特定不能）。final は全8章とも生出力＋正規化とバイト一致（正）であることを確認の上、final からのコピーで復旧。復旧後、全8章で final = run-01-normalized のバイト同一・`<table` 行 0 を検証（調査記録は案件 README「実装中の発見」）
  - テスト29件追加（計85件全 PASS、`tests/results/feat-008_test_result.txt`）。Codex レビュー: 5サイクルで収束（高2・中5を検出。うち1件は Sonnet 実装時検出の設計書の名前衝突 `convert_table(html)` × `import html`）。実装中断2回（設計書矛盾・データ損傷）はいずれも調査→設計改訂→レビュー→承認を経て再開

### 2026-08-21

- **feat-005**: 全スキャンデータの本処理（フェーズ4 完了）
  - chap00〜07 の8ディレクトリ・補正済み TIF 全396枚（除外なし・辞書順）を OCR し、最終成果物 `{BASE}/ocr/final/chapNN/`（Markdown＋content_list.json＋カラー images/）を確立。**書籍スキャン分の LLM 用 Markdown 化が完了**
  - 経緯: 当初「章単位の再構成・重複/白紙除外」を計画したが、ユーザーレビューで「ゴールは入力への忠実な高精度 OCR」と指摘され全面是正（2026-08-18）。入力範囲も chap01〜04 → chap00〜07 に拡大
  - 実測: chap00〜03 は手動＋ocr_dir.py、chap04〜07 は ocr_dir.py 一括（PASS。計178ページ約16分、モデル追加DLなし）。全8章で page_idx 検査（白紙位置のみ欠落許容）・正規化のコードポイント機械確認に合格。図はカラー化済み（計207枚。chap07 は図なし）
  - final 構築の機械確認: コピー16ファイルのバイト同一、md 参照画像の存在、content_list の img_path ユニーク集合 = images 一致（全8章）。回帰テスト56件 PASS（`tests/results/feat-005_test_result.txt`）
  - Codex レビュー: 8サイクル（高2・中7を検出。スコープ2転換を含む）

### 2026-08-18

- **feat-007**: 図画像のカラー再切出スクリプト
  - `scripts/colorize_images.py` を追加。MinerU content_list の図ブロック（img_path を持つ image/chart/table）の bbox（0–1000 正規化。実データで座標系を検証）と page_idx から、原本 TIF（600dpi カラー）の同一領域を切り出してグレー画像と同名で差し替える CLI。md・content_list は無改変（確認済みテキストに手を付けない）
  - 手動テスト差し戻し1回（イテレーション1）: 原寸出力で表示サイズが約3倍になったため、既定 1/3 縮小（200dpi 相当 = 旧画像と同等の表示サイズ、差1%以内）＋ `--scale` オプションを追加
  - 動作確認: chap00〜03 の全図ブロック（3/55/55/36）を再生成し、カラー（彩度指標 0.00→6.15）・サイズ 630×944（旧 631×946 比 0.2%差）を確認。ユーザー手動テストでカラー表示・表示サイズを確認
  - テスト15件追加（計56件全 PASS、`tests/results/feat-007_test_result.txt`）。Codex レビュー: 6サイクルで収束（高2・中6を検出。主要指摘は feat-005 final 構築とのコピー元矛盾、JPEG 非可逆とテスト完全一致の矛盾、1/3 丸めと「同一サイズ」表現の矛盾）。feat-005 の FR-004・§4.4 も併せて改訂（final の images コピー元を正規化側に変更、カラー再切出完了を前提条件化）
- **feat-006**: OCR 一括実行スクリプト
  - `scripts/ocr_dir.py` を追加。TIF ディレクトリを指定するだけで「PDF 生成 → MinerU 変換（no_proxy 設定・run 連番・ログ保存・60分タイムアウト）→ 句読点正規化 → 機械確認（ページ数・page_idx（白紙位置のみ欠落許容）・コードポイント比較）」を一括実行し、ユーザーが Claude Code を介さず OCR できるようにした（ユーザー要望）。複数ディレクトリの逐次処理・PASS/FAIL サマリ付き
  - 安全設計: PDF 生成時に対象TIF のパス・サイズ・mtime を manifest（JSON）として記録し、既存 PDF の再利用は manifest 完全一致時のみ（Codex 指摘・高）。name 重複の事前エラー、ページ数検証は MinerU 実行前
  - 依存変更: pypdf 6.16.1 を dev group から通常依存へ移動（新規インストールなし）
  - 動作確認: chap03 実データで PASS（44ページ・505ブロック・置換330箇所・4分35秒）。feat-005 の chap03 処理を兼ねる。テスト17件追加（当時計41件全 PASS、`tests/results/feat-006_test_result.txt`）。Codex レビュー: 2サイクルで収束（高1・中3を検出）

### 2026-08-17

- **feat-004**: 句読点正規化の後処理と再判定
  - `scripts/normalize_punct.py` を追加。MinerU 出力（md / content_list.json）の「、→，」「。→．」を無条件全文置換する CLI（原本が「、。」を不使用のため安全。JSON は文字列値のみ置換し構造を保存）
  - run-02 出力を正規化（md 247 + json 258 = 505箇所置換）。コードポイント位置比較で「差分は許可置換のみ・長さ一致・残存『、。』ゼロ」を機械確認し、feat-003 の項目A（数式 19/20）・項目C（読み順 5/5）の引き継ぎを成立させた
  - 再判定（criteria lock 済みの事前基準・feat-003 と同一10段落）: 本文 **10/10 合格**（feat-003 は 7/10）→ 総合 **Go**。残存誤りは「;→；」1文字と「ようやく→よやく」1文字脱落のみ。疑義の括弧2件は原本TIF（600dpi）切り出しで確認し誤りではないと判定。ユーザー二次確認済み
  - テスト8件追加（計24件全 PASS、`tests/results/feat-004_test_result.txt`）。Codex レビュー: 2サイクルで収束（高2・中1を検出。主要指摘は `str.translate` に dict を渡す設計不備、A/C 引き継ぎの機械確認アルゴリズム未定義）
  - フェーズ4（全章の本処理）へ進行可

- **feat-003**: MinerU 試行（章単位PDFの品質確認）
  - 章単位PDF を生成（chap-00: 17ページ 6.9MB / chap-01: 64ページ 54.5MB。章対応はユーザー回答＋機械検証で確定し案件 README に記録）
  - MinerU 3.4.4（hybrid-engine 既定）で chap-01 を変換。初回モデルDL 2.4GB、所要 6分26秒、独立数式157件。プロキシ環境で localhost ヘルスチェックが 502 になる想定外事象が発生し、`no_proxy` への localhost 追加を設計に反映して解決（run-01 失敗 → run-02 成功）
  - 品質判定（criteria lock 済みの事前基準）: 数式 19/20 合格・本文 7/10 **不合格**・読み順 5/5 合格 → **No-Go**。不合格の主因は句読点「，．」→「、。」の系統的置換（全ブロックの15%・241箇所）。純粋な文字誤認識は10段落で1文字のみ。対策として feat-004（句読点正規化の後処理）を起票
  - 回帰テスト16件 PASS（`tests/results/feat-003_test_result.txt`）。Codex レビュー: 4サイクル（高3・中4を検出、criteria lock 含む）

- **feat-002**: TIF → OCR用可逆PDF生成スクリプト
  - `scripts/make_ocr_pdf.py` を追加。原本TIF（600dpi）をグレースケール化・1/2縮小（300dpi相当）し、Flate 可逆で複数ページPDFに格納する CLI（入力TIF列 + `-o` + `--overwrite`。章対応は呼び出し時のファイル指定で表現）
  - 安全設計: 変換前の全件検証（存在・モード・`im.load()` による実デコード）、一時ファイル経由の原子的書き込み（no-clobber は `os.link`、上書きは `os.replace`）
  - 依存追加: `img2pdf==0.6.3`（新規）、`pillow==12.3.0`・`pypdf==6.16.1`（ロック済み版の明示固定）
  - 検証結果: テスト10件追加（計16件全 PASS、`tests/results/feat-002_test_result.txt`）。実データ確認: 4件サンプルで gray/8-bit/Flate/300ppi を確認、全84件の一括変換が 22.4秒・62.3MB（`.../chap01/out/ocr/` に `sample4.pdf`・`all-pages.pdf` を残置）。ユーザーの目視で文字・数式の可読性を確認
  - Codex レビュー: 3サイクルで収束（高1・中3を検出）。主要指摘は非原子的書き込みによる不完全PDF残留、壊れたTIFの事前検出漏れ、`--overwrite` なし時の競合上書き、書き込み失敗テストの実効性

- **feat-001**: uv 環境構築と MinerU 導入
  - uv プロジェクトを初期化（`pyproject.toml` / `.python-version` / `uv.lock`）。`mineru[core]==3.4.4`、`torch==2.13.0+cu130`・`torchvision==0.28.0+cu130`（PyTorch 公式 cu130 インデックスから固定）、`pytest==9.1.1` を導入
  - `[tool.uv]` で Python 自動ダウンロード禁止（`python-downloads = "never"`）とシステム Python 限定（`python-preference = "only-system"`）を設定
  - 検証結果: スモークテスト6件全件 PASS（Python 3.12 / CUDA 認識 / CUDA ビルド 12.8 以上 / compute capability (12,0) / CUDA カーネル実行 / mineru バージョン）。結果は `tests/results/feat-001_test_result.txt`。ユーザーの手動テストでも全件 PASS を確認
  - Codex レビュー: 4サイクルで収束（高3・中3を検出）。主要指摘は torch の推移的依存任せによるバージョン不定、Python 自動ダウンロードの禁止漏れ、cu130 インデックスのネットワーク要件記述漏れ、TECH_STACK.md 更新計画の漏れ
