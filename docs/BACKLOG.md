# BACKLOG

## ロードマップ

- フェーズ1: 環境構築（uv プロジェクト初期化、MinerU 導入）
- フェーズ2: TIF → OCR用可逆PDF（300dpiグレースケール・章単位1ファイル）生成スクリプト
- フェーズ3: MinerU 試行（chap-00 の数ページ）→ 日本語本文・数式LaTeXの品質確認
- フェーズ4: 全ページ処理 → 章単位 Markdown 生成（LLM学習用の主成果物）
- フェーズ5（任意）: 座標付きJSONを使い、既存PDFへ不可視テキスト層を挿入

## 案件一覧

| ID | 概要 | ステータス | 備考 |
|---|---|---|---|
| feat-001 | uv 環境構築と MinerU 導入 | Closed | 2026-08-17 完了。案件フォルダ: feat-001-env-setup-mineru |
| feat-002 | TIF → OCR用可逆PDF生成スクリプト | Closed | 2026-08-17 完了。案件フォルダ: feat-002-tif-to-ocr-pdf |
| feat-003 | MinerU 試行（章単位PDFの品質確認） | Closed | 2026-08-17 完了（判定 No-Go、対策は feat-004）。案件フォルダ: feat-003-mineru-trial |
| feat-004 | 句読点正規化の後処理と再判定 | Closed | 2026-08-17 完了（再判定 Go、ユーザー二次確認済み）。feat-003 No-Go 対策。案件フォルダ: feat-004-punct-normalize |
| feat-005 | 全スキャンデータの本処理（ディレクトリ単位の高精度 OCR） | Closed | 2026-08-21 完了。chap00〜07 計396枚を OCR・正規化・カラー化し final を構築（全8章 PASS、ユーザー確認済み）。2026-08-18 にスコープを「入力への忠実な OCR」に是正。案件フォルダ: feat-005-full-conversion |
| feat-006 | OCR 一括実行スクリプト（ocr_dir.py） | Closed | 2026-08-18 完了。PDF生成→MinerU→正規化→機械確認を1コマンド化（Claude Code 不要）。案件フォルダ: feat-006-ocr-pipeline-cli |
| feat-007 | 図画像のカラー再切出（colorize_images.py） | Closed | 2026-08-18 完了（差し戻し1回: 既定1/3縮小を追加）。bbox で原本TIF からカラー再切出。案件フォルダ: feat-007-colorize-images |
| feat-008 | MinerU 出力の HTML 表を Markdown パイプテーブルに変換（html_table_to_md.py） | Closed | 2026-08-24 完了。VS Code プレビューで表内数式が描画されない問題（chap01 表 1.1/1.2）の対処。既存データ適用済み・ocr_dir.py 組み込み済み。付随して normalized md 2件の損傷を発見し final から復旧。案件フォルダ: feat-008-html-table-to-md |
| feat-009 | MinerU 出力から欠落する脚注（訳注）の Markdown 挿入（insert_footnotes.py） | Closed | 2026-08-25 完了。MinerU が md に出力しない page_footnote 型（chap01=訳注1〜13 等）を content_list から組み立てて挿入。既存データ適用済み・ocr_dir.py 組み込み済み。ユーザー手動テストで注釈4・5 の表示を確認。案件フォルダ: feat-009-insert-footnotes |
| feat-010 | 手動修正の永続化: 修正定義ファイルの機械適用（apply_fixes.py） | Closed | 2026-08-25 完了。修正を old→new の定義ファイル（{BASE}/ocr/fixes/、リポジトリ外）として管理しパイプライン最終段で機械適用。初期データとして式番号誤結合5件を修正・適用済み。ユーザー実データ確認済み。案件フォルダ: feat-010-apply-fixes |
| feat-011 | 他書籍対応: 正規化・脚注処理の一般化（句読点スタイル選択・中国語字正規化・脚注 `*N` 対応） | Closed | 2026-08-28 完了。第2の書籍『プログラミングのための確率統計』の OCR 化に向けた一般化。`--punct-style {comma,touten}`・中国語字8種の字形正規化・JIS 外漢字の警告・脚注 `*N` 対応。ユーザー手動テスト済み。案件フォルダ: feat-011-multi-book-normalization |
| feat-012 | final ディレクトリ構築の自動化（build_final.py） | Closed | 2026-08-28 完了。feat-005 で手作業だった「カラー再切出 → final/chapNN/ への集約 → 検証」を CLI 化し、ocr_dir.py に `--final` として章単位で組み込んだ。PRML 8章で既存 final とバイト同一を確認。ユーザー実機テスト済み。案件フォルダ: feat-012-build-final |
| feat-013 | 字形正規化テーブルの拡充と確率統計への適用（中国語字13種の追加・fixes 5件・旧字体警告） | Closed | 2026-08-28 完了。置換表を8→21種に拡充し、旧字体の置換箇所を置換前の文脈つきで警告する機能を追加。確率統計10章へ再適用（MinerU 再実行なし）。実装中に apply_fixes の最終不変条件違反で1度中断し、old/new の一意性を是正して再開。案件フォルダ: feat-013-expand-cjk-table |
| feat-014 | Q&A コラム見出しの「？」アイコン分離8件の修正 | Closed | 2026-09-02 完了（同日起票）。原本 TIF で8件すべて「？」アイコンの実在を目視確認のうえ、修正定義8件（chap01-002 / chap02-003・004 / chap03-006 / chap04-002 / chap05-002・003 / chap06-006）を既存の修正定義ファイル6章分へ追記し、`apply_fixes.py` → `build_final.py` を章ごとに実行して対処。**A型3件**（chap01 1.2・chap02 2.6・chap05 5.2）は見出しに半角「? 」を補完、**B型5件**（chap02 2.8・chap03 3.7・chap04 4.4・chap05 5.1・chap06 6.4）は重複した画像行を削除。8件すべてのアイコン画像行を md から削除した。検証: アイコン画像参照 0 件、文字数・行数・画像参照行数が6章とも設計の予測値と一致、diff ハンク数一致、content_list.json は SHA-256 不変、`images/` 335ファイルとアイコン画像8個は無変更、不変対象 M1（355ファイル）変化なし、216 passed。**ユーザーによる手動テストは 2026-09-02 に合格**（この一文は当初、合格を得る前に「ユーザー実機確認済み」と書いてしまったものを是正した）。Q&A 見出しの表記別件数は 44/8（計52）→ 47/8（計55）。コード変更なし・`CLAUDE.md` は更新していない（update-003 の非対称ルール）。レビューは A→B×2→C→B の5往復で収束し、「検証手順が失敗を検出できない」系の欠陥4件（`pytest | tee` の終了コード、確認スクリプトが表示のみ、`echo` が終了コードを 0 にする、`apply_fixes.py` が終了コード 0 でも `applied`/`skipped` 不一致のときの復元手順の欠落）を是正した。**完了時に別問題を発見**: Q&A コラム見出し全74件のうち19件がアイコン分離以外の原因で見出しになっていない（feat-020 として起票）。以下は起票時の記録: 2026-09-02 起票。確率統計の Q&A コラム見出し先頭の「？」アイコン（影付きの装飾疑問符）が、MinerU の layout 解析で見出しテキストから切り離され独立した画像ブロックとして出力された箇所が6章8件ある（chap01・02・03・04・05・06）。原本 TIF 目視で8件すべてに「？」アイコンの実在を確認済み。内訳は2型: **A型3件**（chap01 1.2・chap02 2.6・chap05 5.2）は見出しテキストに「?」が無く画像のみ、**B型5件**（chap02 2.8・chap03 3.7・chap04 4.4・chap05 5.1・chap06 6.4）は見出しに「?」があり画像が重複。起票時に「原本確認が必要」とされていた3件（chap02・chap03・chap06）は、bbox 照合の結果アイコンが見出しと同一行にあり読み順ソートでページ先頭へ移動していただけと判明し、実体は B 型だった。方針（2026-09-02 ユーザー決定）: A型は見出しに「? 」を補い（既存多数派 `## ? N.M` に合わせる）、8件すべての画像参照行を md から削除する。`content_list.json` と `images/` の画像実体は無改変（既存ポリシー）。既存の正常な Q&A 見出しの表記ゆれ（`## ?N.M` 8件）はスコープ外。修正定義ファイル6章分（chap01・02・03・04・05・06。すべて既存へ追記）＋ `apply_fixes.py` → `build_final.py` で対処予定。コード変更なしの見込み。案件フォルダ: feat-014-column-heading-icon |
| feat-015 | chap02 の Q&A コラム見出しの分断修復 | Closed | 2026-08-31 完了。MinerU の layout 解析により2ブロックに分断された Q&A コラム見出し `? 2.2` を1行に結合し、同一行の誤読「なんという」→「なんていう」も同時に訂正（修正定義 `chap02-002`。改行を含む複数行の `old` を使用）。コード変更なし（`apply_fixes.py` → `build_final.py` の実行のみ）。テスト 216 passed、ユーザー実機確認済み。経緯: 起票当日に「レビュー手順4 を無視して人レビューへ進んだ」として一度 On Hold にしたが、**この認定は誤りであり update-002（収束条件を1回ゲート型として明文化）で撤回された**。その後ユーザー指示でレビューを A から仕切り直し、codex-04（A）=0件 → `/new` → codex-05（C）=0件で収束。案件フォルダ: feat-015-chap02-split-heading |
| feat-016 | chap07 の中国語語彙誤認識「上的」の修正 | Closed | 2026-08-31 完了。確率統計 chap07 の本文1箇所で原本の「[0,1) 上の一様分布」が「上的」と OCR された（原本 TIF 確認済み）。JIS X 0208 内の字による語彙単位の誤りのため既存の警告・字形正規化では検出も補正もできず、修正定義ファイル `{BASE2}/ocr/fixes/chap07.json`（リポジトリ外・新規）と `apply_fixes.py` → `build_final.py` の再適用で対処した。リポジトリ内のコード変更なし。ユーザー手動テスト済み。案件フォルダ: feat-016-chap07-chinese-vocab |
| feat-017 | 確率統計 4章の誤読「なんという → なんていう」9件の修正 | Closed | 2026-09-01 完了（2026-08-31 起票）。9件すべて原本 TIF で「なんていう」を目視確認のうえ、修正定義ファイル4章分（chap01・chap03・chap08 は新規、chap06 は既存へ追記）＋ `apply_fixes.py` → `build_final.py` で対処。コード変更なし、文字数・行数とも不変（「と」1文字→「て」1文字）。final 全10章の md から「なんという」が 0 件になった。テスト 216 passed、ユーザー実機確認済み。以下は起票時の記録: feat-015 の調査で判明した同一誤読 計10件のうち、chap02 の1件（feat-015 で修正済み）を除く残り9件（chap01×1・chap03×3・chap06×4・chap08×1）を扱う。9件すべて原本 TIF で「なんていう」を目視確認済み。図 3.15/3.16 のキャプションは「なんということはない」が慣用句として成立しうるため特に慎重に確認し、原本が「なんていうことはない」（＝そのようなことは起きない、の意。図の主旨とも整合）であることを確認した。修正定義ファイル4章分（chap01・chap03 は新規、chap06 は追記、chap08 は新規）＋ `apply_fixes.py` → `build_final.py` で対処予定。コード変更なしの見込み。案件フォルダ: feat-017-nanteiu-misread |
| feat-018 | chap03 の誤読「なんていいう → なんていう」2件の修正 | Cancelled | 2026-09-01 起票・同日**中止**（ユーザー指示）。中止理由: Claude Code が着手許可を得ずに起票・進行させたため（インシデント1）。あわせて、自分の調査漏れの対策として `CLAUDE.md` の改訂を計画していたことも判明した（インシデント2。実際には `CLAUDE.md` を編集していない）。詳細は案件フォルダの `README.md` §0。**案件フォルダはインシデントの記録として残す**（ドキュメントは当時のまま保存し修正しない）。実データ（`{BASE2}`）は一切変更していない。技術的内容（chap03 の「なんていいう」2件の修正）は**別案件 feat-019 で扱う方針**（2026-09-01 ユーザー決定。起票は未着手）。本案件のドキュメントをそのまま流用してはならない。以下は起票時の記録: feat-017 の手動テスト中にユーザーが 3.5.4 節で「なんていいうことはありません」を発見。当初は feat-017 の悪影響が疑われたが、content_list.json（apply_fixes.py の対象外・SHA-256 不変）に同じ誤りが含まれることから**適用前から存在した別の誤読**と確定（原本 TIF 目視確認済み。原本は「なんていう」で、OCR が「い」を1文字余分に出した）。feat-017 の調査で「なんという」のみを検索し同じ語の別バリアントを探さなかった見落としが原因。正規表現 `なん.{0,5}?[いゆ]う` で両書籍を再調査し、残る誤読は本件2件のみと確定。修正定義ファイル `chap03.json` への2件追記＋ `apply_fixes.py` → `build_final.py` で対処予定。コード変更なしの見込み。案件フォルダ: feat-018-nanteiiu-misread |
| feat-019 | chap03 の誤読「なんていいう → なんていう」2件の修正 | Closed | 2026-09-02 完了（同日起票）。原本 TIF 目視確認のうえ修正定義 `chap03-004`・`chap03-005`（係助詞「は」「も」で一意化）を `chap03.json` に追記し、`apply_fixes.py`（2 applied, 3 skipped）→ `build_final.py` で対処。文字数 54009→54007・行数 1462 で不変、diff ハンク1つ、content_list は SHA-256 不変、images 66 維持、不変対象639ファイルのマニフェスト照合も一致。**両書籍の「なん〜いう」系の誤読はこれで全解消**（残るは正当な `なんていう` 12件と `なんとなくそういう` 1件）。テスト 216 passed、ユーザー実機確認済み。教訓（正しい語を起点にバリアントを正規表現で網羅調査する）は `docs/PROJECT_KNOWLEDGE.md` に追記し、**`CLAUDE.md` は更新していない**（update-003 の非対称ルール）。レビューで設計上の欠陥3件（`{SCRATCH}` 未定義・`build_final.py` 失敗時の final 部分更新・復元時の `{NORM}` 未復元）を是正。以下は起票時の記録: feat-017 の手動テスト中にユーザーが 3.5.4 節で「なんていいうことはありません」を発見。調査の結果 feat-017 の修正が原因ではなく（content_list.json は apply_fixes.py の対象外で SHA-256 も不変）、**適用前から存在した別の誤読**と確定（原本 TIF 目視確認済み。原本は「なんていう」で、OCR が「い」を1文字余分に出した）。同一内容を扱った feat-018 は着手許可を得ずに進行させたため中止されており、**そのドキュメントは流用しない**（調査・検証は本案件として独立に実施）。修正定義ファイル `chap03.json` への2件追記（係助詞「は」「も」で一意化）＋ `apply_fixes.py` → `build_final.py` で対処予定。文字数 54009→54007・行数不変の見込み。コード変更なし・`CLAUDE.md` は更新しない（update-003 の非対称ルール。教訓は docs/PROJECT_KNOWLEDGE.md へ）。案件フォルダ: feat-019-nanteiiu-misread |
| feat-020 | Q&A コラム見出しが見出しとして認識されない19件の修正 | Closed | **2026-09-05 完了**（2026-09-02 起票・2026-09-05 着手）。**Q&A コラム見出し74件がすべて md 上で `## ? N.M …` の見出しになった**（適用前は 55件）。修正定義19件を9章分の修正定義ファイルへ追記（**`chap00.json` は新規作成**、他8件は既存へ追記）し、章ごとに `apply_fixes.py`（9章とも終了コード 0）→ `build_final.py`（同）で対処。コード変更なし。型の内訳は**α3（`?` 欠落）・β12（`##` 欠落）・γ1（両方欠落）・構造復元3**（起票時のβ13件から `? 4.6` を構造復元に移した。feat-021 の訂正1）。**構造復元3件**は chap02 2.5（`$$` 数式ブロックに `\text{}` で埋没）・chap04 4.6（HTML `div` 内）・chap07 7.3（` ```txt ` コードブロック内に質問と答え全体）で、ブロックを解体して見出し化した。chap02 2.5 のみインライン数式の再構成が不可避（ADR-3）、他2件は本文を1文字も変更していない。検証: 走査結果は**見出し74・非見出し1**（chap09 の相互参照 `?3.8`）、9章の文字数・行数・差分ハンク数（chap04 は4・chap07 は3で fix 件数と不一致。構造復元の先頭側と末尾側が別ハンクになるため）が設計の予測値と一致、`content_list.json` は9章とも NORM/FINAL で SHA-256 不変、`images/` 全章不変、**不変対象マニフェスト 327ファイルが一致**（chap03 と PRML 全8章は無傷）、**236 passed**（`tests/results/feat-020_test_result.txt`）。**ユーザーによる手動テストは 2026-09-05 に合格**。**chap07 の適用先は `run-02-normalized`**（他8章は `run-01-normalized`。chap07 のみ run が2つあり final は run-02 由来。ADR-4）。レビューは A（高2）→ B（0）→ C（高2）→ B（0）で一度収束・人レビュー通過 → **実装フェーズで受け入れ基準の誤りが判明**（chap07 の ` ```txt ` を「0件になる」としていたが、Q&A と無関係の正当なコードブロックが4つあり正しくは 5→4）→ 設計を修正して B（0）で再収束（codex-01〜05）。レビューでは実害のある欠陥を4件検出（退避スクリプトの連想配列初期化の失敗で chap07 の退避元を取り違える／書き込み前の不一致でも復元を指示し他の変更を消しうる／差分ハンク数の基準が実際と不一致／退避検査の穴により復元時に既存 JSON を削除しうる）。`CLAUDE.md` は更新していない（update-003 の非対称ルール）。`README.md` も更新不要。以下は起票時の記録: 2026-09-02 起票（feat-014 の完了時にユーザーが `? 2.7` の欠落に気づいたことを端緒に発覚）。確率統計の Q&A コラム見出しは全**74件**あり、うち**19件**が md 上で見出し（`## ? N.M …`）になっていない。内訳は4型: **α型3件**（見出しだが「?」が欠落。chap00 0.2 / chap01 1.3 / chap06 6.2）、**β型13件**（「?」は付くが `##` でない本文段落。1.10 / 2.1 / 2.9 / 2.11 / 2.12 / 4.6 / 4.7 / 4.8 / 5.6 / 5.10 / 7.4 / 8.6 / B.1）、**γ型1件**（「?」も無い本文段落。chap02 2.7）、**δ型2件**（別ブロックに取り込まれ「?」も欠落。chap02 2.5 は数式 `$$` 内に `\text{}` で埋没、chap07 7.3 はコードブロック ```txt 内）。feat-014 が扱ったアイコンの画像ブロック化とは別の現象で、本19件にはアイコン画像が出ていない。**着手は後回し**（2026-09-02 ユーザー決定。起票と調査記録のみ）。調査の詳細と全19件の一覧は案件フォルダの README.md を参照。**feat-022 からの引き渡し（2026-09-05）**: chap08 `? 8.6` は `##` の欠落（D2）と語彙誤読（D1）が同じ行で併発していたが、**D1 は feat-022 で修正済み**（「そういうことだ**る**？」→「だ**ろ**？」）。本案件で `##` を補う際の `old` は、**feat-022 適用後の md**（末尾が「そういうことだ**ろ**？」）に合わせて書くこと。また feat-021 が指摘した訂正点3件（`? 4.6` はβ型でなく HTML `div` 内で `##` を付けるだけでは見出しにならない／feat-014 修正済み3件（1.2・2.6・5.2）は content_list 側が未修正／`? 5.6` の `□` は原本にも印刷されており誤認識ではない）を着手前に README へ反映すること。§4-1「原本 TIF による全19件の確認」は feat-021 で完了済み。案件フォルダ: feat-020-qa-heading-not-recognized |
| feat-021 | Q&A コラム見出し全74件の原本突合 | Closed | 2026-09-03 完了（2026-09-02 起票）。**調査案件であり不一致の修正は行っていない**。content_list の全ブロック走査で74件を特定し S1〜S6 に分類（52/6/12/1/1/2）、網羅性を3検査（連番・重複・相互参照）で裏づけたうえで、新設した `scripts/crop_blocks.py` で原本 TIF から領域を PNG 切り出しして全件を目視突合した。**結果は P0 一致37 / P1 不一致37 / P9 判定不能0**（内訳フラグ D1 文字14・D2 体裁19・D3 ブロック構造3・D4 数式18。1件に複数立つため合計は37にならない）。**新たに語彙誤読3件を発見**（chap01 1.8「いい」→「い」/ chap03 3.5「賭けろ」→「賭ける」/ chap08 8.6「だろ」→「だる」）。これらは**別の管理番号を振って対応する方針**（2026-09-03 ユーザー決定。起票は未着手）。**意味に影響する数式の欠落2件**（chap06 6.3 のハット θ̂ 消失 / chap08 8.3 の太字ベクトル消失）も検出。**feat-020 の19件はすべて P1 として検出**され取りこぼしは無く、うち3件は体裁だけでなく文字の不一致も併発。feat-020 §4-1「原本 TIF による全19件の確認」は本案件で完了した。**feat-020 の記載の訂正点3件**（`? 4.6` は β型でなく HTML div 内 / feat-014 修正済み3件は content_list 側が未修正 / `? 5.6` の `□` は原本にも印刷されており誤認識ではない）を要約に記録した。実装フェーズで比較規則が実行不能と判明し（md の `##` が原本に無い・？アイコンと番号の間の空白を確定できない）、一度中断して設計を修正（ADR-10。見出しをプレフィックス部と質問文部に分け文字単位比較を質問文部に限定）し、ユーザー承認のうえ再レビューを経て再開した。検証: データ不変性 3,550ファイル SHA-256 一致（diff 無出力）、所在一覧の再生成が design.md §4.1 と10列一致、母集団3検査合格、詳細記録74行・未判定0件、236 passed。成果物は詳細記録 `{BASE2}/ocr/collation/feat-021_qa_headings.md`（書籍本文を含むためリポジトリ外）と要約 `collation_summary.md`（スコープ外18件を後続案件案 A〜F に整理。起票は未実施）。レビューは A→B×2→C→B で収束後、設計修正で B×3 を追加して再収束（codex-01〜08）。コード追加あり（`scripts/crop_blocks.py`・`tests/test_crop_blocks.py`）、`README.md` を更新、`CLAUDE.md` は更新していない（update-003 の非対称ルール）。以下は起票時の記録: 2026-09-02 起票。これまでユーザーが手動テストで発見した OCR の問題（見出しの2ブロック分断 feat-015、「？」アイコン分離 feat-014、見出し不認識19件 feat-020、`? 2.10` の誤読）が**すべて Q&A コラム見出しで発生している**ことを受け、全74件を原本 TIF と網羅的に突き合わせる。ユーザー決定（2026-09-02）: **突合対象は74件全部**（feat-020 の19件を含む）、**成果物は突合結果の記録まで**（修正の実施は型ごとに別案件）、**突合の粒度は質問文の全文**（見出し体裁だけでなく文字列を1字ずつ照合する。`? 2.10` の「こぜんぶ→これぜんぶ」のような語彙単位の誤読は体裁確認では検出できないため）。起票時の実測: final 全10章の走査で見出し55件・「?」付き本文段落14件を検出し、うち chap09 L941 の `?3.8` は本文中の相互参照（p.101 への参照）であって見出しではないため除外。55 + β13 + α3 + γ1 + δ2 = 74 で feat-020 の内訳と一致することを確認した。未実施: 74件の原本ページ特定、突合の実施方法と記録様式の設計、不一致の型の分類体系、判定基準の事前定義（criteria lock の要否判断）、後続案件への引き渡し設計。feat-020 §4-1「原本 TIF による全19件の確認」は本案件が担い、feat-020 は修正の実施に専念する。案件フォルダ: feat-021-qa-heading-source-collation |
| feat-022 | Q&A コラム見出しの語彙誤読4件の修正 | Closed | **2026-09-05 完了**（2026-09-03 起票）。修正定義4件（`chap01-003` / `chap02-005` / `chap03-007` / `chap08-002`）を既存の修正定義ファイル4章分へ追記し、章ごとに `apply_fixes.py`（4章とも 1 applied / skipped 2・4・6・1）→ `build_final.py`（終了コード 0）で対処。コード変更なし。検証: 4章とも diff ハンク1つのみ、文字数 chap01 24776→24777・chap02 68838→68839・chap03 と chap08 は増減なし（る→ろ）、行数は4章とも不変、`content_list.json` は NORM/FINAL 両方で SHA-256 不変、`images/` 27/47/66/43 で不変、**不変対象マニフェスト 513ファイルが diff 無出力**（非対象6章・fixes 5件・PRML 全8章）、バリアント再走査で `だる` 系 0 件・`賭けろという` 1 件、**236 passed**（`tests/results/feat-022_test_result.txt`）。**ユーザーによる手動テストは 2026-09-05 に合格**。スコープ外の非変更も確認済み（chap08 520行は `? 8.6` のまま＝`##` 未補完、chap03 585行の末尾は半角 `?` のまま）。レビューは A（高1）→ B（0）→ C（中1）→ B（0）の4往復で収束（根拠 C: `reviews/codex-03.result.md`、収束: `reviews/codex-04.result.md`）。C の指摘は「手順0 が4章一括検査なのに章ごとに実行と記載」で、章単位の直前確認（手順0-B）を新設して解消した。`CLAUDE.md` は更新していない（update-003 の非対称ルール）。`README.md` も更新不要（コマンド・CLI オプション・入出力形式・既定値・実行環境のいずれも不変）。以下は起票時の記録: 2026-09-03 起票（feat-021 の完了を受けて）。確率統計の Q&A コラム見出しの質問文に**語彙単位の誤読が4件**ある（feat-021 の原本突合で確定。内訳フラグ D1 の一部）。内訳: chap01 1.8「描いたら**いい**んでしたっけ」→「い」1文字脱落 / chap02 2.10「**これ**ぜんぶ」→「れ」脱落 / chap03 3.5「賭け**ろ**」→「賭け**る**」 / chap08 8.6「そういうことだ**ろ**？」→「だ**る**？」。**対象は4件すべて**（2026-09-03 ユーザー決定）。feat-021 の引き継ぎには指す集合が異なる「3件」が2通りあり（引き継ぎノート = 新発見の 1.8/3.5/8.6、collation_summary 案A = feat-020 スコープ外の 1.8/2.10/3.5）、起票時に差を提示して4件に確定した。**chap08 8.6 は `##` 欠落（D2）と併発**するが、本案件は **D1（語彙誤読）のみ**を扱い、**D2 は feat-020 の担当**とする。同じ行を2案件が触るため、**本案件を先に完了させ feat-020 は適用後の md に合わせて `old` を書く**。`? 3.5` の末尾疑問符の全角/半角ゆれは後続案件案 C としてスコープ外。調査済み（2026-09-03 実測）: 両書籍 final 全18 md をバリアント走査し**同種の誤読は4件以外に存在しない**ことを確認。修正定義の `old` は全10章で各1件・`new` は各0件で、適用後に `new` がちょうど1件になることも確認済み。新 ID は chap01-003 / chap02-005 / chap03-007 / chap08-002 の予定（4章とも既存ファイルへ追記）。`{BASE2}/ocr/fixes/chapNN.json` への追記＋ `apply_fixes.py` → `build_final.py` で対処予定。コード変更なし・`CLAUDE.md` は更新しない見込み（update-003 の非対称ルール）。案件フォルダ: feat-022-qa-heading-vocab-misread |
| update-004 | CLAUDE.md 内部の規定矛盾2件の解消（全文ゲートの上限単位・update 案件と PROJECT_KNOWLEDGE.md） | Closed | 2026-09-04 完了（同日起票・ユーザー指示）。**DEV_TEMPLATE 側からの逆還元**。テンプレートが honOCR の update-002・update-003 を取り込む過程の Codex レビューで、honOCR 版の `CLAUDE.md` に規定同士の矛盾が2件見つかった（テンプレート側は修正済み・再取り込み不要）。**矛盾1（重要度 高）**: 全文ゲート C の上限が「案件につき1回」なのに、同一案件内で criteria lock も別途 Codex レビューの収束を要求するため、criteria lock を伴う実験案件が運用上デッドロックする。→ C の上限単位を「案件」から**「レビュー単位」**（初回レビュー A で対象指定した文書一式）に変更する。**矛盾2（重要度 中）**: 77行目「PROJECT_KNOWLEDGE.md は update 案件の対象外」と 94行目「update 案件の完了処理でディレクトリ構成を更新する」が字義上衝突。→ 禁止対象を「内容の改訂を**目的とする** update 案件の起票」に限定し、完了処理でのディレクトリ構成反映は行うと明記する。変更は `CLAUDE.md` の3箇所（194・203・77行目）の文字列置換のみで行数の増減なし。他ファイル（`docs/PROJECT_KNOWLEDGE.md`・`AGENTS.md`・`docs/` 直下の基準書）は変更不要。コード変更なし・テスト不要。反映担当は Claude Code 本体。**全文ゲート（codex-02）が新定義の抜け穴を「高」で指摘**したため（同じ文書一式を A に指定し直せば「新しいレビュー単位」を名乗って C を何度でも実施できる）、ユーザー決定により**同一の文書一式は修正後も同一のレビュー単位である**旨の1文を 1-A の新文に追加した。**この1文はテンプレートに無い意図的な乖離**であり、DEV_TEMPLATE への逆還元は検討事項として残す。検証: `git diff --numstat` = `3 3 CLAUDE.md`、テンプレートとの範囲比較（フロー節は完全一致・レビュー節は 194行目1行のみ相違で、その相違が追加1文だけであることを機械検査）、旧表現の残存0件。レビューは A（0件）→ C（高1件）→ B（0件）で収束。案件フォルダ: update-004-fix-review-rule-contradictions |
| update-003 | ドメイン知識・データ・ディレクトリ構成を CLAUDE.md の外（docs/PROJECT_KNOWLEDGE.md）へ外出し | Closed | 2026-09-02 完了（同日起票・ユーザー指示）。CLAUDE.md からデータ・ディレクトリ構成・ドメイン知識の3セクション（85行）を `docs/PROJECT_KNOWLEDGE.md` に無改変で外出し（例外はディレクトリ構成ツリー内の注記2行）、技術スタック節は `docs/TECH_STACK.md` へのポインタ化（事実の喪失なし。「古い CUDA 前提のツールは動かない」は TECH_STACK.md の「動作しない可能性が高い」に統一＝意図的な表現変更）。CLAUDE.md は 423 → 344 行、PROJECT_KNOWLEDGE.md は 104 行（分割検討の閾値 500 行、末尾に「分割検討の記録」節）。CLAUDE.md 内の参照 7 箇所と `fixes/README.md` 1 箇所を付け替え。非対称ルール（PROJECT_KNOWLEDGE.md の内容は各案件の完了処理で更新／構成変更は update 案件／CLAUDE.md 本体は update 案件のみ）と必読指示を明文化し、CLAUDE.md には知識の要約を残さない。転記の同一性（diff 無出力）と削除行の所在を機械検証。コード変更なし・テスト不要。テンプレートへの還元は完了後の検討事項。以下は起票時の記録（2026-09-02）: CLAUDE.md の肥大化対策として、feat/bug 案件が更新してきた4セクション（ドメイン知識・データ・ディレクトリ構成・技術スタック要約）を統治部分から分離する。データ・ドメイン知識・ディレクトリ構成の3セクションを `docs/PROJECT_KNOWLEDGE.md` に1ファイルで外出し（案A）、技術スタックは `docs/TECH_STACK.md` へのポインタ化。外出し先には「500行を超えたら分割を検討する」注意書きを必須とする。あわせて feat/bug フロー完了ステップの更新先の付け替え、必読指示、非対称ルール（外部ファイル＝feat/bug の完了処理で更新可／CLAUDE.md 本体＝update 案件のみ）を明文化する。反映担当は Claude Code 本体。コード変更なし。案件フォルダ: update-003-externalize-project-knowledge |
| update-002 | Codex レビュー手順の収束条件を誤読不能な形に改訂 | Closed | 2026-08-31 完了。feat-015 のレビューで「レビューの進め方」ルール4〜6 が2つの読み（ループ型/1回ゲート型）を許すことが顕在化し、Claude Code（Opus）が曖昧さを自覚しながら確認せず独自解釈で人レビューへ進んだことを受けた改訂。ユーザーが意図を1回ゲート型（`/new` の「新しい目」の全文ゲートは案件につき1回。ゲートの会話——C 自身または C 後の解消確認——で高・中ゼロに達したら収束）と確定し、これを明文化した。「収束」を独立した定義として切り出し、依頼を A: 初回レビュー / B: 解消確認 / C: 全文ゲート に命名して「直前の依頼 × 結果 × C の実施状態 → 次にすること」の遷移表（9行）へ置き換え、人レビューの前提2条件と「逸脱の禁止」（遷移表にない動きをしない・迷ったら止まってユーザーに確認）を明文化した。低の指摘は遷移判定に使わないことも明文化（旧手順内の他規定がすべて高・中で判定していることに基づく解釈の確定）。「結果の保存」の冒頭メタに依頼種別・`/new` の有無・ゲート状態（C の実施状態と根拠ファイル名）・収束判定を必須化。既存規則の内容は変更せず、規則としては「逸脱の禁止」のみを新規追加（他はすべて表現のみの変更、およびユーザーが確定した意図の明文化）、既存記述の削除なし。対象は `CLAUDE.md` の2箇所のみ、コード変更なし（テスト不要）。案件フォルダ: update-002-clarify-review-convergence |
| update-001 | 開発ドキュメントテンプレート改訂の取り込み（Herdr 対話方式レビュー等） | Closed | 2026-08-31 完了。テンプレート `3e62a11..19e4977` の4コミットを全量取り込み。Codex レビューを Herdr 対話方式へ移行（本案件が初適用）し、AGENTS.md・docs/HERDR_SETUP.md を新設、行き詰まり検出・BACKLOG ステータス凡例を追加、full.log 運用を廃止（過去55件は承認のもと削除）。コード変更なし。案件フォルダ: update-001-adopt-dev-template |

## ステータス凡例

- **Open**: 起票済み・未着手
- **In Progress**: 調査・実装中
- **Review**: レビュー中
- **On Hold**: 一時中止（凍結）。再開する場合も、そのまま中止する場合もある。On Hold にする際は、本表の備考に日付・理由・再開点を記録し、案件フォルダの README.md のステータスも On Hold に更新する（README.md が未作成の案件では、概要と現在のステータスを記した README.md を作成して記録する）
- **Closed**: 完了
- **Cancelled**: 取りやめ・破棄（ドキュメントは履歴として残す）
