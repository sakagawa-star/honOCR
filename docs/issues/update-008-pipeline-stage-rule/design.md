# update-008 反映設計書: パイプラインの切り方の明文化

- **案件**: update-008（`docs/issues/update-008-pipeline-stage-rule/`）
- **作成日**: 2026-09-07
- **位置づけ**: ドキュメント更新フロー ステップ3「設計・保存」。調査と判断は `README.md`（§1〜§5）
- **反映担当**: Sonnet サブエージェント（ステップ5。読み替えは CLAUDE.md「ドキュメント更新フロー」ステップ5 に従う）
- **テスト**: コード変更が無いため自動・手動とも**不要**

**本書だけで反映作業ができる**（`/clear` 後の Sonnet が、本書の「変更前」を検索して「変更後」に置換すれば完了する）。変更方式はすべて**完全一致の文字列置換**（`old` は各変更対象ファイル内で**ちょうど1回**出現する。出現回数が 0 または 2 以上なら中断して報告する）。P5・P9 は節全体を 1 つの `old` 文字列として扱う（コードブロック内の全文。改行を含めて完全一致）。

---

## 1. 変更対象ファイルと変更の一覧

| # | ファイル | 箇所（行番号は 2026-09-07 時点。`CLAUDE.md` 418 行） | 方式 |
|---|---|---|---|
| P1 | `CLAUDE.md` | 43 行「開発方針」2 項目目 | 置換 |
| P2 | `CLAUDE.md` | 48 行「機能追加フロー」導入文 | 置換 |
| P3 | `CLAUDE.md` | 51 行 ステップ2 | 置換 |
| P4 | `CLAUDE.md` | 55 行 ステップ6 末尾の1文 | 置換 |
| P5 | `CLAUDE.md` | 62〜86 行「マイルストーン分割（feat 案件の必須手順）」節**全体** | 置換（節ごと） |
| P6 | `CLAUDE.md` | 93 行 粒度表 `roadmap.md` の行 | 置換 |
| P7 | `CLAUDE.md` | 94 行 粒度表 `requirements.md` の行 | 置換 |
| P8 | `CLAUDE.md` | 96 行 粒度表 `experiment_log.md` の行 | 置換 |
| P9 | `CLAUDE.md` | 103〜126 行「ロードマップの改訂（再計画）」節**全体** | 置換（節ごと） |
| P10 | `CLAUDE.md` | 172 行「実験・検証の進め方」項目1 末尾の feat 読み替え | 置換 |
| P11 | `CLAUDE.md` | 173 行 同 項目2 の括弧書き | 置換 |
| P12 | `CLAUDE.md` | 268 行「収束」定義の適用先の列挙 | 置換 |
| P13 | `CLAUDE.md` | 380 行「行き詰まり検出」1 項目目の直後 | 挿入（置換で表現） |
| P14 | `CLAUDE.md` | 203 行「案件ディレクトリ構成」ツリーの `experiment_log.md` のコメント | 置換 |
| P15 | `docs/BACKLOG.md`・`docs/CHANGELOG.md`・本案件 `README.md` | 完了処理 | 追記・更新（Claude Code 本体がステップ6 で行う） |

**変更しないファイル**（理由つき）:

- `AGENTS.md`: 規定を再掲せず「ロードマップとマイルストーン文書の粒度」の表を名前で参照している。表の**内容**が変わっても参照先の名前は変わらないため、判定は自動的に新しい表に従う（update-006 §5.2 (c) の設計どおり）
- `docs/BUGFIX_STANDARD.md` §3: feat のステップ番号（4〜7）を参照するのみで、本案件でステップ番号は変わらない
- `docs/PROJECT_KNOWLEDGE.md`: ファイルの追加・削除は案件フォルダのみで、ディレクトリ構成は `docs/issues/` を個別に列挙していない
- `docs/REQUIREMENTS_STANDARD.md` / `docs/DESIGN_STANDARD.md`: 判断 B(1) により変更しない
- feat-024 の文書: 判断 E(1) により、本案件の完了後に feat-024 側で `roadmap.md` を第3版に切り直す（本案件の範囲外）

**保持すべき制約からの逸脱**: update-006 README §5.1 (d) の「4 種類の境界と併合の 3 条件の内容」を削除する。ユーザー判断 A(1)（2026-09-07）による。由来の**目的**（(b): 小さく作って動作確認し次へ／1 マイルストーン＝入力を与えて出力を確認できる単位／完了条件は実行結果／覆ったら中断して戻る）と、(d) のうち「マイルストーンが 1 個でも同じ手順」「粒度表」「ID を振り直さない」「再計画の中断条件と戻る表（構造）」「ステップ1・2 の README の扱い」「経過措置」は**保持**する。

---

## 2. 変更の内容（変更前 → 変更後）

各項目の `old` / `new` は**行全体**（P5・P9 は節全体の全文）である。Markdown の強調記号・全角括弧・読点・改行を含めて完全一致で置換する。コードブロックの開始・終了のフェンス（```）自体は `old` / `new` に含めない。

### P1: 開発方針（43 行）

old:
```
- 大きな機能を一度に作らない。小さく作って動作確認し、次の機能へ進む（feat 案件では、後述「マイルストーン分割（feat 案件の必須手順）」の分割基準で機械的に担保する）
```
new:
```
- 大きな機能を一度に作らない。小さく作って動作確認し、次の機能へ進む（feat 案件では、後述「パイプラインの切り方（feat 案件の必須手順）」で機械的に担保する）
```

### P2: 機能追加フロー 導入文（48 行）

old（行の一部。この文字列は行内に1回出現する）:
```
分割の基準は後述「マイルストーン分割（feat 案件の必須手順）」、各文書に書く内容の範囲は後述「ロードマップとマイルストーン文書の粒度」、途中で計画を見直す手順は後述「ロードマップの改訂（再計画）」に従う。
```
new:
```
段（マイルストーン）の切り方は後述「パイプラインの切り方（feat 案件の必須手順）」、各文書に書く内容の範囲は後述「ロードマップとマイルストーン文書の粒度」、途中で計画を見直す手順は後述「ロードマップの改訂（再計画）」に従う。
```

### P3: ステップ2（51 行）

old（行の一部）:
```
通常モードで既存コードを調査し、「マイルストーン分割」の基準で案件をマイルストーンに分割し、ロードマップを
```
new:
```
通常モードで既存コードを調査し、「パイプラインの切り方」に従って最終出力の成分・入力の次元・段の表・想定値を定めて案件を段（マイルストーン）に分割し、ロードマップを
```

### P4: ステップ6 末尾（55 行）

old（行の一部）:
```
数値判定を伴うマイルストーンは「実験・検証の進め方（予測→実行→照合）」に従う
```
new:
```
すべてのマイルストーンで、実行の直前に `roadmap.md` の想定値の枠に値を入れ、実行後に実測と照合して `experiment_log.md` に記録する（「実験・検証の進め方（予測→実行→照合）」に従う）
```

### P5: 「マイルストーン分割（feat 案件の必須手順）」節の全体（62〜86 行）

old（節の全文。見出し行から、次の見出し `#### ロードマップとマイルストーン文書の粒度` の直前の空行まで。末尾の空行 1 行を含む）:
```
#### マイルストーン分割（feat 案件の必須手順）

開発方針「大きな機能を一度に作らない。小さく作って動作確認し、次の機能へ進む」を機械的に守るための手順。

**分割の単位**: 1マイルストーン = 入力を与えて出力を確認できる、単独で動作確認できる機能のまとまり。完了条件は必ず実行結果の確認（動くもの・測れるもの）とし、文書の完成をゲートにしない。

**分割の基準**（実装すべき機能の構成に基づく。ステップ2 の調査で次の4種類の境界を列挙し、該当する境界で分ける。列挙と判定の結果を `roadmap.md` の「分割の根拠」に記録する。境界がなくマイルストーンが1個の場合も、4種類とも該当しないことを記録する）:

1. **生成物の境界**: あるステージの出力（ファイル・データ）が次のステージの入力になり、そのステージ単独で実行して出力を確認できるところ（パイプラインのステージ単位）
2. **判断の境界**: Go/No-Go 判定・パラメータの採否・ユーザー承認など、その結果で後段の設計内容が変わる判断があるところ（判断の前後で分ける）
3. **新規コンポーネントの境界**: 新しいスクリプト・モジュール・外部ツール連携（subprocess で呼ぶ CLI 等）を追加する場合、そのコンポーネントの単体疎通を1マイルストーンにする
4. **環境の境界**: 新しい実行環境・ライブラリ・ジョブ基盤の初回疎通は、本体の実装より前の独立したマイルストーンにする（リスクの高い前提の先行検証）

**分けない条件**: 上記の境界がない単一機能の追加（既存スクリプトへのオプション追加・入力形式の追加等）は1マイルストーンとする。中間生成物が内部データにすぎず、後段なしに確認する意味がないものは境界にしない。

**併合の規則**（複数の境界が同時に該当する場合）: 次の3条件を**すべて**満たすときに限り、複数種の境界に該当する一連の作業を1マイルストーンにまとめる。1つでも満たさなければ分ける。

1. 判断の境界を含まない（判断の結果で後段の設計内容が変わるため、判断の前後は常に分ける）
2. 各作業が単独では、指定済みの入力から検証可能な出力を生成できない（例: ジョブスクリプトは実行して初めて出力が出る。設定ファイルはそれを読むドライバなしに検証できない）
3. 共通の完了条件が、それらを同一の1回の実行で初めて満たされる（例: 新規ジョブスクリプトの作成・実行環境の初回疎通・処理本体の疎通は、1回のジョブ投入で出力ファイルが出て初めて完了条件を満たす。バッチのドライバとそのジョブスクリプト・設定ファイルは1回のバッチ投入で初めて完了条件を満たす）

**マイルストーン ID とフォルダ**: ID は `M1`, `M2`, … と順に振り、フォルダ名は `m{番号}-{slug}`（例: `m1-env-smoke/`）とする。**ID は振り直さない**。ロードマップの改訂でマイルストーンを挿入する場合は枝番（例: `M3-1`, `M3b`。フォルダは `m3-1-{slug}/`, `m3b-{slug}/`）、追加する場合は末尾の番号を進める。作成済みのフォルダは案件フォルダと同様に削除・改名しない（中止したマイルストーンは `README.md` に中止と理由を記録して残す）。

**分割漏れの信号**: 設計書が肥大する、Codex レビューが収束しない、実装中に不要な機能や設計の欠陥が判明する、といった症状が出たら、微修正で押し通さず（「行き詰まり検出」と同じ扱い）、上記4種類の境界を再評価する。境界の見落としが確認された場合は「ロードマップの改訂（再計画）」に従って分割し直す。不要と判明した機能は分割ではなく削除する。境界に影響しない設計の欠陥は、影響範囲に応じて当該 `design.md` の修正または `requirements.md` からの作り直しに戻る。

```

new:
```
#### パイプラインの切り方（feat 案件の必須手順）

開発方針「大きな機能を一度に作らない。小さく作って動作確認し、次の機能へ進む」を機械的に守るための手順。案件を「入力から最終出力までのパイプライン」として捉え、その**段**をマイルストーンにする。

**段の単位**: 1マイルストーン = パイプラインの1段 = 入力を与えて出力を確認できる、単独で動作確認できる処理のまとまり。完了条件は必ず実行結果の確認（段の出力が出て、想定値との照合が `experiment_log.md` に記録されていること）とし、文書の完成をゲートにしない。

**ステップ2 で書くもの**（すべて `roadmap.md` に書く。書き方は「ロードマップとマイルストーン文書の粒度」の表）:

1. **最終出力の成分**: 案件完了時にユーザーが受け取る出力を成分で列挙し、**独立な成分**と**従属な成分**（独立な成分からの式で決まるもの。式を添える）に分ける。次元数（独立な成分の数）を明記する
2. **入力の次元**: 各入力の成分数を、数え方の規則（数値は1成分、文字列は1文字1成分、画像は1画素1成分×チャネル数）を明記したうえで、コマンドで数えた**実測値**で書く
3. **段の表**: 段ごとに次の列を埋める
   - 処理単位（1章・1標本・1ページ等）と単位数
   - 単位あたりの入力の次元 → 出力の次元（**式**で書く。定数は実測値）
   - 手段（既存ツールのパス／手作業／**無し**）
   - **決める項**（最終出力の式のどの成分を決めるか。無ければ「無し」と書く）
   - 結合点か（前段の全単位が揃うのを待つ段か）
4. **想定値**: 段ごとに、出力の値または割合の想定と、**根拠の種別**（実測／式から導出／推測）と、**外れとみなす幅**を数値で書く。値は当該段の実行直前に確定してよい（前段の実測を使う）が、想定する項目と幅の枠は `roadmap.md` の人レビュー通過で固定する（「実験・検証の進め方」の criteria lock）

**段の切り方の規則**:

- 出力の次元を式で書けない作業のまとまりは1段にしない。式で書ける単位まで分ける
- 手段が「無し」の作業（新しいスクリプト・モジュール・外部ツール連携・実行環境）は、それを1単位の入力で通す単体疎通を1段にし、その手段を使う段より前に置く
- 決める項が「無し」の段には、その段の出力を次段に正しく渡すこと以上の作業（出力の検証の検証、判定文書の作成等）を積まない
- 段が1個の案件も同じ手順を踏む（手順を案件の規模で変えない）

**マイルストーン ID とフォルダ**: ID は `M1`, `M2`, … と順に振り、フォルダ名は `m{番号}-{slug}`（例: `m1-env-smoke/`）とする。**ID は振り直さない**。ロードマップの改訂でマイルストーンを挿入する場合は枝番（例: `M3-1`, `M3b`。フォルダは `m3-1-{slug}/`, `m3b-{slug}/`）、追加する場合は末尾の番号を進める。作成済みのフォルダは案件フォルダと同様に削除・改名しない（中止したマイルストーンは `README.md` に中止と理由を記録して残す）。

**実行後の照合**: 段の実行後に想定値と実測を照合して `experiment_log.md` に記録する。想定の幅を外れたら、微修正で押し通さず「ロードマップの改訂（再計画）」の梯子に従う（「行き詰まり検出」と同じ扱い）。

```

（`new` の末尾は空行1行で終わり、その直後に既存の `#### ロードマップとマイルストーン文書の粒度` が続く。）

### P6: 粒度表 `roadmap.md` の行（93 行）

old:
```
| `roadmap.md` | 案件全体の計画。人レビュー通過後は固定し、変更は「ロードマップの改訂（再計画）」に従う | 目標。前提（各項目に典拠を付ける: ユーザー確定 / 調査 / M-n で実証 / 未検証〔M-n で検証する〕）。現状（実証済みの事実の要約と参照）。分割の根拠（4種類の境界の列挙と併合の判定）。マイルストーン一覧（ID・名称・フォルダ名・状態）と順序。各マイルストーンの5項目: 目的・依存（前提となるマイルストーンと前提の項目）・入力と出力の概要・開始条件・完了条件（判断の境界を持つマイルストーンは6項目目として「判断結果ごとの次の行き先」を加える。失敗時の扱い・実験の位置づけ・範囲などの補助節を加えてよい）。改訂履歴（改訂理由）。任意: 保留リスト（今はやらないが候補として残すもの） | 実施結果（`README.md` とマイルストーンフォルダに置く）。実行手順・CLI オプション・ジョブ構成・スクリプト・判定に使う閾値や測定式・確認サンプル・フォールバック候補（マイルストーン文書に置く） |
```
new:
```
| `roadmap.md` | 案件全体の計画。人レビュー通過後は固定し、変更は「ロードマップの改訂（再計画）」に従う | 目標と**最終出力の成分**（独立な成分・従属な成分と式・次元数）。前提（各項目に典拠を付ける: ユーザー確定 / 調査 / M-n で実証 / 未検証〔M-n で検証する〕）。現状（実証済みの事実の要約と参照）。**入力の次元**（数え方の規則と実測値）。**段の表**（段ごとの処理単位・単位数・入出力の次元の式・手段・決める項・結合点）。**段ごとの想定値**（根拠の種別・外れの幅）。マイルストーン一覧（ID・名称・フォルダ名・状態）と順序（段の順）。各マイルストーンの4項目: 目的・依存（前提となるマイルストーンと前提の項目）・開始条件・完了条件（段の出力が出て想定値との照合が記録されていること。失敗時の扱い・範囲などの補助節を加えてよい）。改訂履歴（改訂理由）。任意: 保留リスト（今はやらないが候補として残すもの） | 実施結果（`README.md` とマイルストーンフォルダに置く）。実行手順・CLI オプション・ジョブ構成・スクリプト・測定式・確認サンプル・フォールバック候補（マイルストーン文書に置く） |
```

### P7: 粒度表 `requirements.md` の行（94 行）

old:
```
| `{マイルストーンID}-{slug}/requirements.md` | そのマイルストーンの「何を・なぜ・合否」 | 機能要求（受け入れ基準に判定の具体値・チェック項目・確認対象を書く。数値判定を伴う場合はここが criteria lock の対象）。非機能要求。制約。範囲外の明示 | 手順と実装。他のマイルストーンの要求。`roadmap.md` の再掲 |
```
new:
```
| `{マイルストーンID}-{slug}/requirements.md` | そのマイルストーンの「何を・なぜ・合否」 | 機能要求（受け入れ基準にチェック項目・確認対象を書く。**判定の閾値は `roadmap.md` の当該段の想定値と外れの幅を参照し、再掲も追加もしない**）。非機能要求。制約。範囲外の明示 | 手順と実装。他のマイルストーンの要求。`roadmap.md` の再掲。`roadmap.md` に無い判定基準 |
```

### P8: 粒度表 `experiment_log.md` の行（96 行）

old:
```
| `{マイルストーンID}-{slug}/experiment_log.md` | 予測と実測の記録（数値判定を伴うマイルストーンのみ） | 直前予測。実測。照合と乖離の原因 | 判定基準（`requirements.md` を参照） |
```
new:
```
| `{マイルストーンID}-{slug}/experiment_log.md` | 予測と実測の記録（全マイルストーン） | 直前予測（`roadmap.md` の想定値の枠に値を入れる）。実測。照合と乖離の原因 | 判定基準（`roadmap.md` の想定値と外れの幅を参照） |
```

### P9: 「ロードマップの改訂（再計画）」節の全体（103〜126 行）

old（節の全文。見出し行から、節の最後の項目の行末まで）:
```
#### ロードマップの改訂（再計画）

ロードマップが真価を発揮するのは複雑な実装のときで、その場合はマイルストーンの途中で中断して計画を見直すことが多い。次のいずれかが起きたら、**その場で回避策を取らずに中断**し、何が起きたか・どこまで完了したかをユーザーに報告する。

- 設計書どおりに実行できない
- ドキュメントと実装の記述が矛盾する
- 完了条件を満たせない、または判定できない
- ロードマップの前提が実際と違うと判明した（前提の典拠が覆った）
- 分割漏れの信号（「マイルストーン分割」参照）が出た

中断後は、影響範囲に応じて戻る場所を選び、再レビューを経て再開する。「行き詰まり検出」は案件全体に適用する。

| 影響範囲 | 戻る場所 |
|---|---|
| そのマイルストーンの手順だけの問題 | 当該 `design.md` の修正 → ステップ5 |
| そのマイルストーンの目的・完了条件の問題 | 当該 `requirements.md` から作り直し → ステップ5 |
| 前提・マイルストーンの分割・順序の問題 | `roadmap.md` の改訂 → ステップ3 |

`roadmap.md` の改訂は次のとおり行う。

- 同じファイルを上書きで改訂し、改訂履歴に改訂理由（覆った前提、変更した分割・順序、削除・吸収したマイルストーン）を書く。旧版は git に残るため複製しない
- マイルストーン ID は振り直さない（「マイルストーン分割」の ID 規則）。完了済みマイルストーンの記録（`README.md`・マイルストーンフォルダ）は変更しない
- 前提の典拠を更新する（実証された項目は「M-n で実証」に、覆った項目は新しい値と典拠に）
- 改訂後の `roadmap.md` をステップ3（Codex 収束 → 人レビュー）に通してから、次のマイルストーンに着手する
```

new:
```
#### ロードマップの改訂（再計画）

ロードマップが真価を発揮するのは複雑な実装のときで、その場合はマイルストーンの途中で中断して計画を見直すことが多い。次のいずれかが起きたら、**その場で回避策を取らずに中断**し、何が起きたか・どこまで完了したかをユーザーに報告する。

- 設計書どおりに実行できない
- ドキュメントと実装の記述が矛盾する
- 完了条件を満たせない、または判定できない
- ロードマップの前提が実際と違うと判明した（前提の典拠が覆った）
- 段の実測が想定値の幅を外れた（「パイプラインの切り方」の照合）
- 実行前の信号が出た: 設計書が肥大する、Codex レビューが収束しない、実装中に不要な機能や設計の欠陥が判明する。不要と判明した機能は分割ではなく削除する

中断後は、次の**梯子**を 1 から順に上がって見直し、再レビューを経て再開する。**手を飛ばす、または手を戻すには、ユーザーの指示を要する。** ただし実行前の信号（外れた段が無い）では手 1・2 に対象が無いため、**手 3 から入る**（これは飛ばしではなく規定の入口である）。「行き詰まり検出」は案件全体に適用する。

| 手 | 見直し | 戻る場所 |
|---|---|---|
| 1 | 外れた段を見直す（手段・手順の修正。段の入出力の次元と決める項は変えない） | 当該 `design.md`（必要なら `requirements.md`）の修正 → ステップ5 |
| 2 | それでも外れる → 精度が悪い → 段を細かく分ける（枝番のマイルストーンとして挿入） | `roadmap.md` の改訂 → ステップ3 |
| 3 | 分けられない → パイプラインの構成を見直す（段の追加・削除・順序・入力・手段の変更） | `roadmap.md` の改訂 → ステップ3 |
| 4 | 見直しも無理 → 最終ゴールを変更する（妥協案・中止。ユーザーの決定） | `roadmap.md` の目標と最終出力の成分の改訂 → ステップ3 |

`roadmap.md` の改訂は次のとおり行う。

- 同じファイルを上書きで改訂し、改訂履歴に改訂理由（覆った前提、外れた段と実測、変更した段・順序、削除・吸収したマイルストーン、上がった手）を書く。旧版は git に残るため複製しない
- マイルストーン ID は振り直さない（「パイプラインの切り方」の ID 規則）。完了済みマイルストーンの記録（`README.md`・マイルストーンフォルダ）は変更しない
- 前提の典拠を更新する（実証された項目は「M-n で実証」に、覆った項目は新しい値と典拠に）
- 改訂で段を足す場合は、足す段の決める項を書く。決める項が「無し」の段を足せるのは手3 に限り、その理由を改訂履歴に書く
- 改訂後の `roadmap.md` をステップ3（Codex 収束 → 人レビュー）に通してから、次のマイルストーンに着手する
```

### P10: 「実験・検証の進め方」項目1 末尾（172 行）

old（行の一部）:
```
feat 案件のマイルストーンでは、criteria 文書を別立てせず、判定基準を当該マイルストーンの `requirements.md` の受け入れ基準に数値で書き、その `requirements.md` のレビュー収束（機能追加フローのステップ5）を criteria lock とする
```
new:
```
feat 案件では、criteria 文書を別立てせず、段ごとの想定値と外れの幅を `roadmap.md` の段の表に書き、その `roadmap.md` の人（ユーザー）レビュー通過（機能追加フローのステップ3）を criteria lock とする（値は各段の実行直前に確定してよいが、想定する項目と幅の枠は変えない）
```

### P11: 「実験・検証の進め方」項目2 の括弧書き（173 行）

old（行の一部）:
```
criteria 文書（feat 案件のマイルストーンでは当該 `requirements.md`）に事前定義してよい
```
new:
```
criteria 文書（feat 案件では `roadmap.md` の段の表）に事前定義してよい
```

### P12: 「収束」定義の適用先の列挙（268 行）

old（行の一部）:
```
「マイルストーン分割」の分割漏れの信号
```
new:
```
「パイプラインの切り方」の実行後の照合
```

### P13: 「行き詰まり検出」（380 行の直後に 1 項目を挿入）

old（380 行全体）:
```
- **同じ指摘の再発、または同じ原因での失敗が2回続いたら、事前の想定自体が間違っている**。3回目の試行に進んではならない
```
new（2 行。1 行目は old と同一）:
```
- **同じ指摘の再発、または同じ原因での失敗が2回続いたら、事前の想定自体が間違っている**。3回目の試行に進んではならない
- **feat 案件では、段の実測が想定値の幅を2回続けて外れたら**（同じ段の再実行でも、連続する2つの段でも）、原因が同じかの分類を経ずに同じ扱いとする（「ロードマップの改訂（再計画）」の梯子に従う）
```

### P14: 「案件ディレクトリ構成」ツリーの `experiment_log.md` のコメント（203 行）

old（行全体。先頭の空白と罫線文字を含む）:
```
    │   ├── experiment_log.md  # 予測と実測の照合記録（数値判定を伴うマイルストーンのみ）
```
new:
```
    │   ├── experiment_log.md  # 予測と実測の照合記録（全マイルストーン。想定値は roadmap.md）
```

---

## 3. 実施手順（Sonnet サブエージェント）

1. `CLAUDE.md` を読み、P1〜P14 の各 `old` が**ちょうど1回**出現することを確認する（`grep -c` または Python の `str.count`）。1つでも 0 または 2 以上なら**中断して報告**する（本書の行番号は参考であり、一致判定は文字列で行う）
2. P1〜P14 を順に置換する（P5・P9 は節全体。`old` の先頭行から末尾行までを1つの文字列として扱う）
3. 置換後、次を確認する
   - `grep -n "マイルストーン分割\|4種類の境界\|併合の規則\|分割の根拠\|分割漏れの信号\|判断の境界\|数値判定を伴うマイルストーン" CLAUDE.md` が**無出力**であること（旧規定の語が残っていない）
   - `grep -c "パイプラインの切り方" CLAUDE.md` が **6 以上**であること（P1・P2・P3・P5・P9〔2 箇所〕・P12）
   - `CLAUDE.md` の行数が 418 → **約 430**（P5 で +2 行前後、P9 で +7 行前後、P13 で +1 行、P14 は不変）
4. `git diff CLAUDE.md` を出力し、変更箇所が P1〜P14 の 14 箇所だけであることを報告する
5. git 操作・`docs/BACKLOG.md`・`docs/CHANGELOG.md`・`docs/PROJECT_KNOWLEDGE.md`・本案件 `README.md` は**変更しない**（ステップ6 で Claude Code 本体が行う）

---

## 4. 反映後の検証（Claude Code 本体が `git diff` で行う）

| # | 確認項目 |
|---|---|
| 1 | 変更ファイルが `CLAUDE.md` のみである |
| 2 | 削除された行に、他所に存在しない情報が含まれていない（4 境界・併合 3 条件の本文は update-006 README §5.1 と DEV_TEMPLATE update-011 に由来が残る。ID 規則・経過措置・粒度表の他の行は保持されている） |
| 3 | 旧規定の語（「マイルストーン分割」「4種類の境界」「併合の規則」「分割の根拠」「分割漏れの信号」「判断の境界」「数値判定を伴うマイルストーン」）が `CLAUDE.md` に残っていない |
| 4 | 「パイプラインの切り方（feat 案件の必須手順）」節が存在し、ステップ2 で書くもの 4 項目・切り方の規則 4 項目・ID 規則・実行後の照合を含む |
| 5 | 「ロードマップの改訂（再計画）」節に梯子の表（手 1〜4）があり、「手を飛ばす、または手を戻すには、ユーザーの指示を要する」の文がある |
| 6 | 粒度表の `roadmap.md` 行に「最終出力の成分」「入力の次元」「段の表」「段ごとの想定値」があり、`requirements.md` 行に閾値の再掲・追加の禁止がある |
| 7 | 「実験・検証の進め方」の feat 読み替えが `roadmap.md` の人レビュー通過＝criteria lock になっている |
| 8 | 「行き詰まり検出」に「想定値の幅を2回続けて外れたら」の項目がある |
| 8-b | 「案件ディレクトリ構成」ツリーの `experiment_log.md` のコメントが「全マイルストーン」になっており、粒度表（P8）と一致する |
| 8-c | 「ロードマップの改訂（再計画）」の中断条件に実行前の信号（設計書の肥大・レビュー不収束・不要機能の判明）が残り、手 3 から入る旨がある |
| 9 | `AGENTS.md` が変更されていない（参照先の名前「ロードマップとマイルストーン文書の粒度」は不変） |

---

## 5. 完了処理（ステップ6。Claude Code 本体）

1. `docs/BACKLOG.md` の update-008 の行をステータス **Closed** にし、備考に完了日・レビュー回数・反映内容の要約を追記する
2. `docs/CHANGELOG.md` に 2026-09-07 の項として、変更の要旨（4 境界・併合 3 条件を「パイプラインの切り方」に置換、判断の境界と criteria lock を段の想定値に統合、再計画を梯子に、行き詰まり検出に想定値の外れ 2 回を追加）と案件フォルダを記録する
3. 本案件 `README.md` のステータスを **Closed** に更新し、レビューの経過（`reviews/codex-NN`）と反映の検証結果（§4 の 9 項目）を記録する
4. `docs/PROJECT_KNOWLEDGE.md` は更新しない（ファイルの追加・削除が案件フォルダのみ）
5. **feat-024 の `roadmap.md` 第3版への切り直しは本案件の完了後、feat-024 側で行う**（判断 E(1)）。その際、feat-024 は新規定の初適用（モデルケース）として、実施記録に「新規定で切り直した」旨を書く

---

## 6. 設計判断の記録

| # | 判断 | 理由 |
|---|---|---|
| 1 | 4 境界・併合 3 条件を残さず置換する（並存させない） | 判断 A(1)。並存すると Claude Code が両方を書いて肥大させる（README §1.2 の癖）。旧 4 境界のうち生成物・新規コンポーネント・環境の境界は「段」「手段が無しの段」として新規定に表現される |
| 2 | 判断の境界を廃止し、Go/No-Go を「想定値と外れの幅」に統合する | 判断 C(1)。判定の対象に形が無いと十分の基準が無く、criteria lock が肥大した（feat-024 M2 検査4）。想定値は段の出力（形のあるもの）に対する数値で、幅も先に固定する |
| 3 | `requirements.md` に閾値を書かせない（`roadmap.md` を参照させる） | 閾値を 2 か所に置くと Claude Code が `requirements.md` 側で足す（feat-024 M2 の 2-b は `roadmap.md` に無い基準だった）。閾値の所在を人レビューを通る `roadmap.md` の 1 か所に限定する |
| 4 | 梯子の手を飛ばす・戻すにはユーザーの指示を要する | Claude Code は手 1 を飛ばして手 4（目標変更）を提案し、別の場面では手 1 で済むものを拡大した（README §1.2 (d)）。順序を機械的に固定する |
| 5 | 行き詰まり検出に「想定値の外れ 2 回」を追加し、既存の「同じ原因 2 回」は残す | 判断 D(1)。既存条件は原因の分類を Claude Code が行うため発動しなかった。分類を経ない条件を並置する |
| 6 | `DESIGN_STANDARD.md` / `REQUIREMENTS_STANDARD.md` は変えない | 判断 B(1)。まず `roadmap.md` の 1 か所で効くかを feat-024 で見る |
