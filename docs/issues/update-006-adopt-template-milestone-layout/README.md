# update-006: テンプレート改訂の取り込み（第2弾）: feat 案件のマイルストーン分割・AGENTS.md の規定適合確認・BUGFIX_STANDARD §3

- ステータス: **Closed**（2026-09-07 起票・同日完了。Codex レビュー3回で高・中ゼロ収束〔codex-03（C）〕、人レビュー承認済み、design.md どおり反映・検証済み）
- 起票日: 2026-09-07
- 種別: update（ドキュメント更新。テンプレート取り込み）
- 起票の指示: ユーザー（2026-09-07「第2段を着手してください。それが完了後にpushする予定です」）
- 反映担当: **Sonnet サブエージェント**（CLAUDE.md ドキュメント更新フロー ステップ5〔update-005 で取り込んだ規定〕に従う）
- 本ファイルの位置づけ: ドキュメント更新フロー ステップ2「調査」の記録。反映設計は `design.md`
- 反映先: `CLAUDE.md`（12箇所）、`AGENTS.md`（1項目挿入）、`docs/BUGFIX_STANDARD.md`（§3 ブロック置換）、`docs/PROJECT_KNOWLEDGE.md`（1行）
- 前提案件: update-005（第1弾。2026-09-07 完了、コミット `8c9189e`）。本案件は update-005 の README §4 で定めた「第2弾」である

## 1. 経緯・目的

### 1.1 契機

update-005 の調査（2026-09-07）で、テンプレート `DEV_TEMPLATE` の `a8d6241..ffd2c49`（update-011〜013）の差分を「feat 案件のマイルストーン規定に依存しない改訂（第1弾）」と
「マイルストーン規定とそれに依存する改訂（第2弾）」の2案件に分けることをユーザーが決定した。第1弾の完了を受け、本案件で残りを取り込む。

### 1.2 目的

- feat 案件を「ロードマップ（`roadmap.md`）＋ 1個以上のマイルストーン」で構成する規定を honOCR に導入し、開発方針「小さく作って動作確認し、次へ進む」を機械的に担保する
- Codex レビューで案件文書が CLAUDE.md の規定に適合しているか（案件フォルダ単位・対象文書単位の2段階）を判定させ、規定の穴を「判定不能・高」として表面化させる
- 上記に伴い、`docs/BUGFIX_STANDARD.md` §3 の差し戻し運用を bug / feat の両フローに対応させ、update-005 で保留した「収束」定義の feat 読み替え（「ロードマップの改訂（再計画）」への言及）を反映元に揃える
- **honOCR 固有の課題**: 過去 24 件の feat 案件（feat-001〜feat-024。進行中の feat-024 を含む）は `requirements.md` / `design.md` を案件フォルダ直下に置く旧配置である。
  テンプレートの経過措置（「導入前に起票した feat 案件は遡及して移動しない」）を honOCR の実態に即して具体化し、AGENTS.md の規定適合確認が旧配置の案件文書に対して「判定不能・高」を出さないよう、判定方法を一意に定める

### 1.3 採らなかった案と不採用の理由

| 案 | 内容 | 判断 | 理由 |
|---|---|---|---|
| **採用** | 候補1（マイルストーン分割一式）・候補2（AGENTS.md の規定適合確認）・BUGFIX §3・PROJECT_KNOWLEDGE の1行・「収束」定義の feat 読み替えを1案件で取り込み、旧配置の feat 案件の扱いを CLAUDE.md の但し書きと AGENTS.md の工程判定に明記する | 採用 | 候補2 の文面が候補1 のステップ番号・`roadmap.md` を参照し、相互依存する（update-005 README §1.3。テンプレート側 update-011 README「留意事項 B」）。旧配置の扱いを決めないと、feat-024 の残工程や過去案件の bug 修正時のレビューで判定不能が常態化する |
| A | 候補2（AGENTS.md）を取り込まず、規定適合確認は導入しない | 不採用 | テンプレート側 update-011 で Claude Code が一度推奨して誤りと訂正された案そのもの（update-012 README「契機」）。「判定不能・高」は規定の穴を表面化させる設計上の信号であり、対処は参照先を本プロジェクトの規定に合わせること |
| B | 旧配置の feat 案件を新配置（`m1-{slug}/` 配下）へ移動して統一する | 不採用 | テンプレートの経過措置（遡及して移動しない）と「案件フォルダは完了後も削除・移動しない」（命名規則）に反する。24 件の移動は情報の喪失リスクが大きく、進行中の feat-024 の作業を止める |
| C | 旧配置の扱いを CLAUDE.md の但し書きだけに書き、AGENTS.md は反映元と同文にする | 不採用 | AGENTS.md の工程判定は「`roadmap.md` ならステップ3、マイルストーンフォルダ配下ならステップ5」と文書の所在で決めており、案件フォルダ直下の `requirements.md` / `design.md` はどちらにも当てはまらず「判定不能・高」になる。判定に必要な情報の所在（CLAUDE.md の但し書き）を AGENTS.md から指す1文が要る |
| D | 進行中の feat-024 を新フローへ移行する（`roadmap.md` を後付けする） | 不採用 | feat-024 は調査案件で criteria lock（codex-01〜04）を終えており、後付けのロードマップは実施済みの工程と食い違う。旧配置のまま残工程（実装・手動テスト・完了）を進める |

## 2. 反映元

- リポジトリ: `/home/sakagawa/git/DEV_TEMPLATE`（`template/` 配下が反映元の実ファイル）
- 反映元コミット: `ffd2c49`（2026-09-07、update-013 完了）。update-005 と同じ。2026-09-07 時点で `ffd2c49` が HEAD であることを再確認済み
- 前回の同期点: update-005（honOCR コミット `8c9189e`。第1弾の6件を反映済み）
- 差分の範囲: `git -C /home/sakagawa/git/DEV_TEMPLATE diff a8d6241..ffd2c49 -- template/` のうち update-005 で取り込まなかったもの（§3）
- 反映元の案件文書（本案件で読んだもの）:
  - `docs/issues/update-011-from-lift2d-milestone-layout/README.md`（「概要」「調査結果1・2」「反映対象の選別」候補1・候補2、「留意事項」A〜E、「反映元への逆還元候補」）、同 `design.md`（P1〜P7・P10・P12〜P14 と各注記）、`reviews/codex-01〜04.result.md`
  - `docs/issues/update-012-import-provenance-rule/README.md`「契機（update-011 で起きたこと）」（候補2 の誤った扱いとその訂正）
- honOCR 側の現状: HEAD `8c9189e`（2026-09-07）。`CLAUDE.md` 345 行、`AGENTS.md` 11 行、`docs/BUGFIX_STANDARD.md` 88 行、`docs/PROJECT_KNOWLEDGE.md` 122 行。作業ツリーの4ファイルは HEAD と同一。
  作業ツリーには進行中の feat-024（`docs/BACKLOG.md` の行追加、`docs/issues/feat-024-inline-math-markup-survey/` 未追跡）が反映前から存在する

### 2.1 典拠の到達範囲（確認できない範囲の明示）

候補1・2 の一次発生源は lift2d リポジトリ（`/Private/sakagawa/git/lift2d-to-3d-keypoints`、fmcs2 上）の update-005 / update-006 だが、**本機からは参照できない**（update-005 README §2.1 と同じ）。
本案件の精査は、テンプレート側 update-011 の案件文書（README の候補ごとの「内容」「汎用性の判断」「転記時に除くプロジェクト固有語」、「留意事項」A・B・D、design.md の変更前後全文と注記、reviews）までを典拠とする。
テンプレート側は lift2d の update-005〜008 の README と design.md・reviews を精読したうえで取り込み（update-011 README「調査方法」5）、Codex 4回（高3件を反映）・人レビューを経て確定している。
反映元（テンプレート）の文書から経緯と目的は確認できると判断した。lift2d の原文（人レビューで3回差し戻された経緯の詳細、feat-036 の実運用記録）が必要になった場合はユーザーに確認する。

## 3. 差分の全量（update-005 で取り込まなかったもの）

| ファイル | 差分 | 出所 | 本案件 |
|---|---|---|---|
| `template/CLAUDE.md` | 候補1（11箇所）＋候補2（1箇所）＋「収束」定義の feat 読み替え | update-011 | **取り込む**（`design.md` P1〜P12） |
| `template/AGENTS.md` | +23 行（規定適合確認の項目） | update-011 候補2 | **取り込む**（P13。honOCR 向けに旧配置の工程判定を1文追加） |
| `template/docs/BUGFIX_STANDARD.md` | §3「ステップ7（差し戻し）での運用」の bug / feat 両フロー対応 | update-011 候補1（1-11） | **取り込む**（P14） |
| `template/docs/PROJECT_KNOWLEDGE.md` | ディレクトリ構成の `AGENTS.md` 行のコメント | update-011 候補2（2-3） | **取り込む**（P15） |

`template/CLAUDE.md` の候補1・2 の箇所（テンプレート側 update-011 README「候補1」の表 1-1〜1-10 と「候補2」2-2 に対応）と honOCR の現状:

| # | 箇所（honOCR `CLAUDE.md` 345 行時点） | 現状 | 変更 | 読み替え |
|---|---|---|---|---|
| P1 | 43行「開発方針」2項目目 | 「大きな機能を一度に作らない。…次の機能へ進む」 | 末尾に「（feat 案件では、後述「マイルストーン分割（feat 案件の必須手順）」の分割基準で機械的に担保する）」を追加 | なし（3項目目の環境変更の文は honOCR 版を保持） |
| P2 | 48〜60行「機能追加フロー」の導入文とステップ1〜8 | 案件直下に `requirements.md` / `design.md` を1組作る8ステップ | 導入文を追記し、8ステップを置換 | ステップ3 のストリーム名 `rev-{{プロジェクト略称}}-{案件ID}` → `rev-honocr-{案件ID}`。ステップ8 の実パスの文は honOCR 版「**`README.md` にはローカルの実パスを書かない**（公開リポジトリのため。…）」を保持 |
| P3 | 60行の後（機能追加フローの末尾、「### 不具合修正フロー」の前） | なし | 小節「マイルストーン分割（feat 案件の必須手順）」「ロードマップとマイルストーン文書の粒度」「ロードマップの改訂（再計画）」を新設 | なし |
| P4 | 106〜107行「実験・検証の進め方」項目1・2 | criteria 文書を別立てする記述のみ | feat 案件のマイルストーンでは `requirements.md` の受け入れ基準を criteria lock とする文を追加 | なし |
| P5 | 116行・121行「ドキュメント作成ルール」 | feat の必須文書は `requirements.md` と `design.md`。置き場は案件フォルダ直下 | `roadmap.md` とマイルストーンごとの文書、置き場を明記 | なし |
| P6 | 129〜139行「案件ディレクトリ構成」のツリーと補足 | 案件直下のツリー | feat のツリーに差し替え、経過措置の但し書きを追加 | **但し書きを honOCR の実態に具体化**（§5.1 (d) と §6） |
| P7 | 148行「Codexによるレビューの実行方法」総則 | 「機能追加・不具合修正・ドキュメント更新フローのステップ4（レビュー）では」 | 「機能追加フローのステップ3・5、不具合修正・ドキュメント更新フローのステップ4（レビュー）では」 | なし |
| P8 | 194〜195行「収束」の定義（レビュー単位の区切り・人レビュー通過後の括弧内・適用先の列挙） | update-005 U7 で「（ステップ7 の差し戻し）」とした読み替え版 | 反映元と同文に揃える（単位の区切りの feat 読み替え、括弧内に「ロードマップの改訂（再計画）」、適用先の列挙） | なし（update-005 の読み替えを解消） |
| P9 | 244行「依頼文の基準部分」定型指示の段落 | 定型指示4点 | 規定適合確認を追加し「案件種別固有のレビュー観点は従来どおり依頼文に書く」を追加 | なし |
| P10 | 246行 同 機能追加の依頼文 | `{requirements.md と design.md のパス}` の1文 | ロードマップ（ステップ3）・マイルストーン文書（ステップ5）の2文 | なし |
| P11 | 276行「サブエージェントへの指示」項目1 | 「機能追加は `requirements.md` と `design.md`」 | 「機能追加は `roadmap.md` と当該マイルストーンの `requirements.md` / `design.md`」 | なし |
| P12 | （P2 に含む）ステップ6 のテスト結果ファイル | — | 「結果ファイルは案件単位の `tests/results/feat-{number}_test_result.txt`」 | なし（honOCR「テスト」節と同じ命名） |

## 4. 案件の分割（update-005 からの引き継ぎ）

update-005 README §4 のとおり、本案件は第2弾として上表の全件を1案件で取り込む。案件内の細分化・並列レビューは行わない（レビュー単位は本 README と `design.md` の1単位）。

## 5. 取り込む候補の精査（経緯と目的）

CLAUDE.md ドキュメント更新フロー ステップ2（update-005 で取り込んだ規定）に従い、候補ごとに (a) 契機 (b) 目的 (c) 形式の理由 (d) 保持すべき制約 (e) 典拠 を記録する。

### 5.1 候補1: feat 案件のマイルストーン分割（P1〜P8・P10〜P12・P14。テンプレート update-011 候補1。一次発生源は lift2d update-005）

- (a) 契機: lift2d で feat-026 / feat-035 が規定なしに場当たり的にロードマップ運用を行い、honOCR でも設計書が肥大化した案件でレビューを打ち切った実績があった（テンプレート側 update-011 README 候補1「汎用性の判断」）。
  lift2d update-005（2026-09-04）で規定化され、人レビューで3回差し戻された末に確定した（任意の別方式 → 1個の場合が従来手順になる統一手順、行数基準 → 機能構成の4境界、ロードマップは常に作る、粒度は再計画のしやすさで定める）。
  lift2d feat-036 で実運用され、ロードマップを第7版まで改訂（判断の境界の全廃、環境の境界による先頭マイルストーン M6 の挿入、M4 の第4版）した実績がある（同「留意事項 D」）
- (b) 目的: 開発方針「大きな機能を一度に作らない。小さく作って動作確認し、次の機能へ進む」を機械的に守る。1マイルストーン = 入力を与えて出力を確認できる単位とし、完了条件を実行結果の確認に置く（文書の完成をゲートにしない）。
  途中で前提が覆ったら回避策を取らず中断し、影響範囲に応じて戻る場所を決めて再レビューを経る
- (c) 形式の理由: マイルストーンが1個でも同じ手順を踏む（手順を案件の規模で変えない）。分割基準は行数ではなく機能構成の4種類の境界（生成物・判断・新規コンポーネント・環境）と併合の3条件で機械的に判定し、`roadmap.md` の「分割の根拠」に記録する。
  情報の所在を文書ごとに1つに固定する粒度表で、粒度の逸脱（別の文書に属する情報を書く）を防ぐ。マイルストーン ID は振り直さない。
  テンプレート側で汎用化した点: 由来注記「（feat-036 で導入。2026-09-04）」を削除、例示フォルダ名 `m1-render-smoke/` → `m1-env-smoke/`、併合の規則の例文「レンダリングの疎通は…連番 PNG が出て」→「処理本体の疎通は…出力ファイルが出て」、経過措置「update-005（2026-09-04）より前に起票した feat 案件は…」→「本規定の導入より前に起票した feat 案件は…」（design.md P3・P6 の注記）。
  レビュー指摘で確定した文言: ステップ1 は README の「概要・ステータス・実施記録の枠のみ」、ステップ2 で「`roadmap.md` 保存と同時にマイルストーン一覧と初期状態を README に反映」（codex-03 高: 分割確定前のステップ1 で一覧を書かせる矛盾。反映元 lift2d と同じ穴として逆還元候補 #3）
- (d) 保持すべき制約: 4種類の境界と併合の3条件の内容、「マイルストーンが1個でも同じ手順」、粒度表の「書くこと・書かないこと」、ID を振り直さない規則、再計画の中断条件と戻る場所の表、ステップ1・2 の README の扱い（codex-03 で確定）。
  経過措置の「遡及して移動しない」と「bug 案件で元の feat 案件の文書を変更する場合は元の案件フォルダ内の当該ファイルを更新する」
- (e) 典拠: `DEV_TEMPLATE/docs/issues/update-011-from-lift2d-milestone-layout/README.md`「候補1」「留意事項 B・D・E」「反映元への逆還元候補 #3」、同 `design.md` P1〜P7・P10〜P12 と注記、`reviews/codex-03.result.md`（高1: P2 ステップ1・2）・`codex-04.result.md`（解消）。lift2d update-005 の原文は本機から参照不能（§2.1）

### 5.2 候補2: Codex レビューでの CLAUDE.md 規定への適合確認（P9・P13・P15。テンプレート update-011 候補2。一次発生源は lift2d update-006）

- (a) 契機: lift2d update-006（2026-09-04）。Codex レビューが案件文書の内容は見るが「文書が規定どおりに揃い、規定の場所にあり、規定された節・項目を持つか」を見ていなかった。
  lift2d のレビューで「判定が決定論的でない」という同じ指摘が codex-03〜05 で続き、行き詰まり検出を発動して「規定を AGENTS.md に再掲せず、所在（案件種別に対応する CLAUDE.md のフロー節と参照先）を指定し、情報源から一意に決まらなければ判定不能を高で報告させる」形に切り替えた（テンプレート側 update-011 README「留意事項 A」、update-012 README「契機」）。
  テンプレート側 update-011 では Claude Code がこの経緯を読まず「判定不能・高が出続けて収束しない」ことを不具合と誤認して検査を外す案を推奨し、ユーザーの指摘で訂正された（update-012 の契機）
- (b) 目的: 案件文書の規定適合を Codex に機械的に判定させる。「判定不能・高」は規定の穴を裁量で埋めさせず CLAUDE.md 側の欠陥として表面化させる信号であり、判定不能が残る間は収束扱いにしない
- (c) 形式の理由: AGENTS.md は規定そのものを持たず、規定の所在と判定手順（案件フォルダ単位・対象文書単位の2段階）だけを指定する（規定が増減しても AGENTS.md を直さない。Codex が提案した「対応表を AGENTS.md に置く」案は規定の再掲・二重管理になるため不採用）。
  テンプレート側で加えた2点（codex-01 高2件。反映元 lift2d への逆還元候補 #1・#2）: 実験・検証の criteria 文書と案件直下の補助文書を対象外とする文（工程が一意に決まらず判定不能になる穴）、`reviews/` を存在の判定対象にしない文（初回レビューでは必ず不在になる）。
  「定型指示は AGENTS.md、案件種別固有の観点は依頼文」の原則を維持し、CLAUDE.md の定型指示の列挙にだけ適合確認を加える
- (d) 保持すべき制約: 2段階判定の構造、「判定不能は高で報告し、残る間は収束扱いにしない」、criteria 文書・補助文書・`reviews/`・未指定文書の除外、feat / bug の README を対象文書単位の判定から除外し存在と配置のみ判定すること、規定を AGENTS.md に再掲しないこと。
  **honOCR 向けの読み替え（旧配置の feat 案件の工程判定）は、規定の所在を指す1文の追加にとどめ、判定手順・除外・判定不能の扱いは変えない**（§6）
- (e) 典拠: `DEV_TEMPLATE/docs/issues/update-011-from-lift2d-milestone-layout/README.md`「候補2」「留意事項 A」「反映元への逆還元候補 #1・#2」、同 `design.md` P10・P13・P14 と注記、`reviews/codex-01.result.md`（高2）・`codex-02.result.md`（解消）、
  `DEV_TEMPLATE/docs/issues/update-012-import-provenance-rule/README.md`「契機（update-011 で起きたこと）」。lift2d update-006 の原文（codex-03〜06 の行き詰まり検出の記録）は本機から参照不能（§2.1）

### 5.3 「収束」定義の feat 読み替え（P8。update-005 U7 で保留した部分）

- (a) 契機: update-005 は候補1 を取り込まないため、テンプレート update-011 README 候補4 が明記した読み替え（「候補1 を取り込まない場合は『ステップ7 の差し戻し』のみにする」）を適用した。本案件で候補1 を取り込むため、反映元の文面に揃える
- (b) 目的: レビュー単位の区切りを feat のステップ3（`roadmap.md`）・ステップ5（マイルストーンごと）で定め、「ロードマップの改訂（再計画）」による改版を新しいレビュー単位とする。「収束」の適用先の列挙に新設の小節を加える
- (c) 形式の理由: テンプレート側 design.md P8 の変更後全文（候補1 と候補4 を同時に反映した文）。update-005 U7 で保持した3点（人レビューの通過が単位の区切り・通過前の修正はすべて同一単位・Codex と人のいずれの指摘も）はそのまま
- (d) 保持すべき制約: update-005 U7 の (d) の3点。単位の区切りの列挙（feat ステップ3 / 5、bug / update ステップ4、細分化単位、criteria 文書）
- (e) 典拠: `docs/issues/update-005-adopt-template-update-flow-provenance/design.md` U7 の読み替え表、`DEV_TEMPLATE/docs/issues/update-011-from-lift2d-milestone-layout/design.md` P8

## 6. honOCR 向けの読み替え: 旧配置の feat 案件の扱い（本案件の独自判断。design.md P6・P13 に反映）

### 6.1 事実

- honOCR の feat 案件は feat-001〜feat-024 の 24 件すべてが `requirements.md` / `design.md` を案件フォルダ直下に置く（実測: `ls docs/issues/feat-*/requirements.md | wc -l` = 24、`roadmap.md` は 0）。criteria 文書は `experiments/{実験名}/criteria.md`（feat-024 で使用）
- feat-024 は In Progress（2026-09-06 起票。調査案件。Codex レビュー codex-01〜04 済み、設計確定、実装は未着手）。残工程は実装（ステップ6）・手動テスト（ステップ7）・完了（ステップ8）で、新フローでも番号は同じ。レビューストリーム `rev-honocr-feat-024` が稼働中（反映前の `AGENTS.md` で起動）
- テンプレートの経過措置は「本規定の導入より前に起票した feat 案件は、`requirements.md` / `design.md` を案件フォルダ直下に置いている（遡及して移動しない）。bug 案件で元の feat 案件の `requirements.md` / `design.md` を変更する場合は、元の案件フォルダ内の当該ファイルを更新する。」の2文で、レビュー時の工程判定と差し戻し時の運用には触れていない
- AGENTS.md の工程判定は「feat では依頼の対象が `roadmap.md` なら…ステップ3、マイルストーンフォルダ配下の文書ならステップ5」で、案件フォルダ直下の `requirements.md` / `design.md` はどちらにも当てはまらない。旧配置の文書をレビュー対象にすると（feat-024 の差し戻し、過去案件を bug 案件で変更する場合）「判定不能・高」になり、判定不能が残る間は収束扱いにできない

### 6.2 決定（design.md に反映する読み替え）

1. **CLAUDE.md「案件ディレクトリ構成」の但し書き（P6）**: テンプレートの2文を保持したうえで、honOCR の実態を具体化する: 「本規定の導入（update-006、2026-09-07）より前に起票した feat 案件（feat-001〜feat-024）」と対象を特定し、
   これらの案件では `roadmap.md` とマイルストーンフォルダを作成・参照しないことを明記し、機能追加フローのステップ4〜7 を案件全体について次のとおり読み替える（codex-01 の指摘で具体化）: ステップ4 = 案件フォルダ直下の `requirements.md` / `design.md` の作成・更新（差し戻し時は `investigation.md` の追記を含む）、ステップ5 = それらのレビュー（依頼文は旧規定の「{requirements.md と design.md のパス}」形。工程はステップ5 として扱い、`roadmap.md` とマイルストーンフォルダの不在を不適合としない。対象文書単位の判定は `docs/REQUIREMENTS_STANDARD.md` / `docs/DESIGN_STANDARD.md` に従い、「ロードマップとマイルストーン文書の粒度」の表は適用しない）、ステップ6 = 必読の案件ドキュメントを直下の `requirements.md` / `design.md` として実装、ステップ7 = 手動テスト（差し戻しは BUGFIX_STANDARD §3 の feat の項を、マイルストーン ID を付けず案件全体について繰り返すものとして適用）。ステップ8 は変更なし
2. **AGENTS.md の工程判定（P13）**: 反映元の「工程は、feat では依頼の対象が `roadmap.md` なら機能追加フローのステップ3、マイルストーンフォルダ配下の文書ならステップ5とし、」の直後に
   「案件フォルダ直下の `requirements.md` / `design.md`（CLAUDE.md「案件ディレクトリ構成」の但し書きが定める、本規定の導入前に起票した feat 案件）なら同但し書きの定めに従い、」を挿入する。判定に必要な情報の所在（CLAUDE.md の但し書き）を指すだけで、規定は再掲しない

### 6.3 保持すべき制約を損なわない理由

- 候補2 の設計思想（規定を AGENTS.md に再掲せず所在を指す。一意に決まらなければ判定不能）は変えていない。旧配置は honOCR に現に存在する既知の状態であり、その扱いを CLAUDE.md に一意に定めることは「規定の穴を CLAUDE.md 側で塞ぐ」という設計どおりの対処である（テンプレート側 update-011「留意事項 A」の「対処は検査を外すことではなく、参照先を本リポジトリの規定に合わせること」）
- 候補1 の経過措置の2文（遡及して移動しない・bug 案件は元フォルダの文書を更新）はそのまま保持し、追加は honOCR に固有の具体化（対象案件の範囲・工程・差し戻し）に限る。新規に起票する feat 案件には新配置を適用する（例外を設けない）
- 粒度表を旧配置の文書に適用しないのは、旧配置の文書が粒度表の導入前の基準（`docs/` 直下の基準書）で書かれ、粒度表の「書かないこと」（`roadmap.md` の再掲等）が成立しないため。基準書による判定は従来どおり行う

### 6.4 運用上の注意

- `AGENTS.md` は codex 起動時にのみ読まれる。稼働中の `rev-honocr-feat-024` は反映前の `AGENTS.md` で動いており、feat-024 の残工程でレビューが必要になった場合は当該ストリームを起動し直す必要がある（本案件では feat-024 のストリームに一切触れない。feat-024 を担当するセッションが判断する）。
  本案件のレビューストリーム `rev-honocr-update-006` も反映前の `AGENTS.md` で回り、完了処理で `/quit` する
- 反映後の最初のレビュー（次の案件）で、「[AGENTS.md適用]」マーカーと規定適合の判定が回答に含まれることを確認する（テンプレート側 update-011「留意事項 A」の反映時の注意。本案件のスコープ外）

## 7. 取り込まないもの

| 内容 | 理由 |
|---|---|
| 「開発方針」3項目目の「大容量データ・モデルのダウンロード」の語 | honOCR 発（update-010）の文をテンプレートが一般化したもの。honOCR は「モデルのダウンロード」のままでよい（update-005 §6 と同じ判断） |
| feat / bug ステップ8 の「リポジトリを公開する場合、」の前置き | honOCR は公開リポジトリのため honOCR の文面を保持（update-005 §6） |
| ストリーム命名規則の `{{プロジェクト略称}}`、テンプレート利用ガイド、プロジェクト概要・技術スタック・テスト・コーディング規約のプレースホルダ | テンプレートの雛形機能・プロジェクト固有部分。honOCR は展開済み |
| `template/CLAUDE.md`「プロジェクト知識」の「実作業・案件の調査に入る前に」の語 | honOCR は「OCR 作業・案件の調査に入る前に」で具体化済み |

## 8. 現状の実測（2026-09-07・HEAD `8c9189e`）

- `CLAUDE.md` 345 行、`AGENTS.md` 11 行（テンプレートの先頭 11 行と同一）、`docs/BUGFIX_STANDARD.md` 88 行（§3 は 73〜88 行。テンプレートの変更前と同一）、`docs/PROJECT_KNOWLEDGE.md` 122 行（39 行目が `AGENTS.md` の行）
- `docs/` 直下の基準書（REQUIREMENTS / DESIGN / REVIEW_CRITERIA）、ルート `README.md` に `requirements.md` のパスや feat フローのステップ番号への参照はない（`grep` 無出力）。`.claude/commands/` は存在しない
- `docs/PROJECT_KNOWLEDGE.md` のディレクトリ構成は `docs/issues/` を1行で示し案件フォルダを列挙していないため、本案件でのフォルダ追加による更新は不要。変更は `AGENTS.md` 行のコメントのみ
- 置換前のアンカー文字列（`design.md` §2 S0-3）はそれぞれちょうど1回出現する

## 9. レビューの経過（ドキュメント更新フロー ステップ4）

ストリーム `rev-honocr-update-006`（ペイン wH:p13、新規起動。反映前の `AGENTS.md` で稼働。モデルは `~/.codex/config.toml` の既定 gpt-5.6-terra medium）。

| # | 依頼 | 結果 | 対応 |
|---|---|---|---|
| codex-01 | A（初回） | 高1 / 中0 / 低0。P6b の但し書きが旧配置案件のレビュー時の扱いしか定めず、変更後のステップ4・6（マイルストーンフォルダの作成・`roadmap.md` の読了）が旧配置案件で実行不能 | 但し書きに「`roadmap.md`・マイルストーンフォルダを作成・参照しない」とステップ4〜7 の具体的な読み替えを追加（§6.2） |
| codex-02 | B（解消確認。同じ会話） | 高0 / 中0 / 低0。前回指摘は解消、P2・P11・P14 との衝突なし | `/new` → C |
| codex-03 | C（全文ゲート。`/new` 後） | 高0 / 中0 / 低0 | **収束**。人レビューへ |

Codex 3回、案件累計 total 1,039,510 トークン。

## 11. 実施記録

- 2026-09-07: 起票（ユーザー指示「第2段を着手してください。それが完了後にpushする予定です」）。調査（本ファイル）と `design.md` を作成。design.md の変更前後ブロックは現行ファイルと反映元から機械的に生成し、模擬適用で期待ファイルを再現できることを確認
- 2026-09-07: Codex レビュー（§9）: codex-01（A、高1）→ codex-02（B、ゼロ）→ `/new` → codex-03（C、ゼロ）で収束
- 2026-09-07: 人レビュー承認（「こちらはokです。反映作業を開始してください」。§6 の旧配置 feat 案件の扱い・読み替え4件・§2.1 の典拠の到達範囲を含めて承認）
- 2026-09-07: 本体が S0-1〜S0-4 を確認後、Sonnet サブエージェントが P1〜P15 を反映（想定外事象なし）。本体で S2-1〜S2-6 を再実行しすべて一致（417 / 36 / 91 / 122 行、変更4ファイルのみ、ハンク 14 / 1 / 4 / 1、テンプレートとの残差分 15 ハンク、AGENTS.md の差分は R4 の2行のみ、BUGFIX_STANDARD.md はテンプレートと同一）。反映結果は事前に生成した期待ファイルと4ファイルとも完全一致
- 2026-09-07: 完了処理（BACKLOG・CHANGELOG・本ファイルを更新。ストリーム `rev-honocr-update-006` を `/quit` しペイン wH:p13 を撤去。Opus サブエージェントでコミットし、ユーザー指示に基づき `origin/main` へ push）

## 10. 参照

- テンプレート差分: `git -C /home/sakagawa/git/DEV_TEMPLATE diff a8d6241..ffd2c49 -- template/`
- テンプレート側案件: `DEV_TEMPLATE/docs/issues/update-011-from-lift2d-milestone-layout/`、`update-012-import-provenance-rule/`
- 第1弾: `docs/issues/update-005-adopt-template-update-flow-provenance/`（README §4「案件の分割」、design.md U7）
- 反映設計: `design.md`（本フォルダ）
