# feat-004 実験ログ: 正規化後の再判定

criteria: `criteria.md`（codex-02 で指摘ゼロに収束、criteria lock 済み）
前提: `scripts/normalize_punct.py` 実装済み・テスト24件 PASS（`tests/results/feat-004_test_result.txt`）

---

## フェーズ1: 正規化の実行（design.md §4.4）

### 予測（実行前に記録）

- `chap-01_gray300.md` の置換件数: **約240件**（根拠: feat-003 実測で content list のテキストブロック中の「、。」計241箇所。Markdown は同じテキストの結合であり同水準と見積り）
- `chap-01_gray300_content_list.json` の置換件数: **約240件**（根拠: 同上。キャプションブロックの分で数件の差があり得る）
- 引き継ぎの妥当性確認（コードポイント位置ごと比較）: **2ファイルとも合格**（根拠: 置換実装は `str.translate` による1対1置換で長さ・位置が保存される）

### 実測（2026-08-17）

- `chap-01_gray300.md`: **247件** 置換（予測 約240件に対し +3%）
- `chap-01_gray300_content_list.json`: **258件** 置換（予測 約240件に対し +7.5%）
- 出力先: `.../mineru-trial/run-02-normalized/`。スクリプト終了コード 0

### 照合

- 置換件数: 予測根拠の241は「テキストブロックのみ」の集計で、Markdown・JSON には図キャプションブロックの句読点が加わるため予測をやや上回った。方向性の説明がつき、前提を疑う乖離ではない
- 引き継ぎの妥当性確認（criteria §2 のアルゴリズム）: **両ファイル合格** — 長さ一致（md 94,251 / json 247,213 コードポイント）、許可置換のみ（md 247 / json 258）、不許可差分 **0**、残存「、」「。」**0** → **項目A = 19/20・項目C = 5/5 を引き継ぐ**

---

## フェーズ2: 項目B′ の再判定

### 予測（判定作業前に記録）

- 項目B′: 合格 **10/10**（根拠: feat-003 で記録した既知残存誤りは P5 の「；」1文字と P9 の「よやく」1文字のみで、いずれも合格圏内（≤2）。P1・P7・P8 の不合格要因だった句読点置換は正規化で全量解消済み（残存0を機械確認）。ただし feat-003 で不合格だった段落は句読点超過の時点で不合格が確定していたため、句読点以外の見落としが残っているリスクを認識した上で全誤りを数え直す）

### 判定結果（一次判定: Claude Code、2026-08-17）

#### 項目B′: 正規化後の日本語本文 — **10/10 合格（100%）→ 合格**（合格ライン 9/10）

同一10段落を正規化後テキストで原本と再突き合わせ（全誤り種別を数え直し）。判定にあたり、疑義箇所2件は原本TIF（600dpi）から該当領域を切り出して高解像度で確認した:

- P9 の「(知識) 利用」の括弧: 原本は英語部と同じ半角スタイル → MD の半角出力と一致（誤りに数えない）
- P2 の「（発見的方法）」の括弧: 原本は全角（前後に字送りの余白あり）→ MD の全角出力と一致（誤りに数えない）

| # | 段落 | 判定 | 残存誤り |
|---|---|---|---|
| P1 | データに潜むパターンを… | 合格 | 0（句読点16箇所は正規化で解消） |
| P2 | 例として，図1.1の… | 合格 | 0 |
| P3 | 一方，機械学習の… | 合格 | 0 |
| P4 | 機械学習のアルゴリズムに… | 合格 | 0 |
| P5 | 実際の応用ではほとんどの… | 合格 | 1（半角「;」→全角「；」） |
| P6 | 前処理は計算の高速化の… | 合格 | 0 |
| P7 | 訓練データが，入力ベクトル… | 合格 | 0（句読点5箇所は正規化で解消） |
| P8 | その他に，訓練データが… | 合格 | 0（句読点3箇所は正規化で解消） |
| P9 | 最後に，強化学習… | 合格 | 1（「ようやく」→「よやく」の1文字脱落） |
| P10 | さて，今まで述べた… | 合格 | 0（末尾の「。」は正規化で解消。継続先の原本4ページ目と照合済み） |

#### 照合

- 予測 10/10 → 実測 **10/10** ✓（乖離なし）

### 総合 Go/No-Go 判定（criteria §2 の事前定義どおり）

- 項目A: **19/20 合格**（引き継ぎ。機械確認合格済み）
- 項目B′: **10/10 合格**
- 項目C: **5/5 合格**（引き継ぎ。同上）

→ **Go**（フェーズ4: 全章の本処理 = MinerU 変換＋句読点正規化の後処理、へ進んでよい）

ユーザーの二次確認: 実施済み（2026-08-17 承認。Go 確定）
