# update-010 反映設計書: 運用上の規定の穴 6 件の反映（実装と実行の分離、予測記録時のコミット、要調査事項の受け皿、状態更新の時点、実験ログの書き換えのレビュー、状態欄の更新）

- **案件**: update-010（`docs/issues/update-010-operational-rule-gaps/`）
- **作成日**: 2026-09-08
- **位置づけ**: ドキュメント更新フロー ステップ3「設計・保存」。調査と判断は `README.md`（§1〜§5）
- **反映担当**: Sonnet サブエージェント（ステップ5。読み替えは CLAUDE.md「ドキュメント更新フロー」ステップ5 に従う）
- **テスト**: コード変更が無いため自動・手動とも**不要**

**本書だけで反映作業ができる**（`/clear` 後の Sonnet が、本書の「変更前」を検索して「変更後」に置換すれば完了する）。変更方式はすべて**完全一致の文字列置換**（`old` は `CLAUDE.md` 内で**ちょうど1回**出現する。出現回数が 0 または 2 以上なら中断して報告する）。Q1・Q3・Q8・Q10 は行の一部、Q2・Q4・Q5・Q6・Q7・Q9 は行全体（Q6・Q9 は 1 行を複数行にする）。コードブロックのフェンス（```）自体は `old` / `new` に含めない。

---

## 1. 変更対象ファイルと変更の一覧

| # | ファイル | 箇所（行番号は 2026-09-08 時点。`CLAUDE.md` 434 行、コミット `b09725a`） | 方式 | README の判断 |
|---|---|---|---|---|
| Q1 | `CLAUDE.md` | 55 行 機能追加フロー ステップ6 末尾 | 置換（行の一部） | A（F1） |
| Q2 | `CLAUDE.md` | 367 行「実装の実行方法」サブエージェントへの指示 4 | 置換（行全体） | A（F1） |
| Q3 | `CLAUDE.md` | 188 行「実験・検証の進め方」項目 2 末尾 | 置換（行の一部） | B（F3） |
| Q4 | `CLAUDE.md` | 409 行「git 操作の実行方法」導入文 | 置換（行全体） | B（F3 (a)） |
| Q5 | `CLAUDE.md` | 421 行 同「委任しない作業」1 項目目 | 置換（行全体） | B（F3 (a)） |
| Q6 | `CLAUDE.md` | 92 行「パイプラインの切り方」の「未検証の事項」段落（直後に段落を追加） | 置換（行全体。1 段落を 2 段落にする） | C（F6） |
| Q7 | `CLAUDE.md` | 104 行 粒度表 `README.md` の行 | 置換（行全体） | D（F7） |
| Q8 | `CLAUDE.md` | 282 行「収束」の定義のレビュー単位の列挙 | 置換（行の一部） | E（F8 (8-a)） |
| Q9 | `CLAUDE.md` | 336 行「依頼文の基準部分」ドキュメント更新の項（直後に 1 項目を追加） | 置換（行全体。1 行を 2 行にする） | E（F8 (8-a)） |
| Q10 | `CLAUDE.md` | 100 行 粒度表 `roadmap.md` の行の役割の列 | 置換（行の一部） | F（F9） |
| Q11 | `docs/BACKLOG.md`・`docs/CHANGELOG.md`・本案件 `README.md` | 完了処理 | 追記・更新（Claude Code 本体がステップ6 で行う） | — |

**変更しないファイル**（理由つき）:

- `AGENTS.md`: 粒度表を名前で参照しており、表の内容の変更（Q7・Q10）は自動的に判定に反映される。Q8 で加える `experiment_log.md` の書き換えのレビューは、`AGENTS.md` の案件フォルダ単位の判定では「マイルストーンフォルダ配下の文書」としてステップ5 の工程で判定され（当該マイルストーンの `requirements.md` / `design.md` は書き換え時点で存在する）、対象文書単位の判定では粒度表の `experiment_log.md` の行に従う。既存の規定で判定できるため変更しない
- `docs/REVIEW_CRITERIA.md`・`docs/BUGFIX_STANDARD.md`・`docs/REQUIREMENTS_STANDARD.md`・`docs/DESIGN_STANDARD.md`: 対象の規定は `CLAUDE.md` のみに置かれている
- `docs/PROJECT_KNOWLEDGE.md`: ファイルの追加は案件フォルダのみ
- feat-024 の文書: 一時中断中。遡及適用しない（feat-025 から適用）
- Claude Code のメモリ（リポジトリ外）: 対象外

**保持するもの**: update-008・009 の規定（段の表・想定値の式・再検討の 2 手・書いてよいもの・行き詰まり検出）、Sonnet 委任の目的（設計書の自己完結性の独立検証）と禁止事項、Opus 委任の規定（本案件で加えるのは予測記録のコミットの例外のみ）、レビュー単位の既存の区切りと「人レビュー通過が単位の区切り」の規定。**削除するもの**: 無し（すべて追記または語句の置換）。

---

## 2. 変更の内容（変更前 → 変更後）

### Q1: 機能追加フロー ステップ6 末尾（55 行。行の一部）

old（この文字列は `CLAUDE.md` 内に 1 回出現する）:
```
すべてのマイルストーンで、実行の直前に `roadmap.md` の想定値の式に前段の実測を代入して予測値を出し、実行後に実測と照合して `experiment_log.md` に記録する（「実験・検証の進め方（予測→実行→照合）」に従う）
```
new:
```
すべてのマイルストーンで、実行の直前に `roadmap.md` の想定値の式に前段の実測を代入して予測値を出し、実行後に実測と照合して `experiment_log.md` に記録する（「実験・検証の進め方（予測→実行→照合）」に従う）。**実装の委任には段の実行（実データの投入）を含めない。** 段の実行は、実行単位ごとに、その直前に Claude Code 本体が予測を `experiment_log.md` に記録してコミットしてから（同 項目2）、その 1 実行だけを本体が行う（複数の実行の予測をまとめて先に書かない。依存の有無・実行が決定的かどうかを理由に一括記録を正当化しない）。実行単位ごとの予測の記録は当該 `design.md` に手順として書く
```

### Q2: 「実装の実行方法」サブエージェントへの指示 4（367 行。行全体）

old:
```
4. **検証まで実施**: テストの全件実行（「テスト」のルールに従う）、`tests/results/{type}-{number}_test_result.txt` への出力保存、ドキュメントに定義された動作確認（実データ実行等）
```
new:
```
4. **検証まで実施**: テストの全件実行（「テスト」のルールに従う）、`tests/results/{type}-{number}_test_result.txt` への出力保存、ドキュメント（設計書・修正計画）に定義された動作確認。ただし **feat 案件の段の実行（実データの投入。`roadmap.md` の想定値との照合の対象になる実行）は委任に含めない**（機能追加フローのステップ6。本体が実行単位ごとに予測を記録してから行う）
```

### Q3: 「実験・検証の進め方」項目 2 末尾（188 行。行の一部）

old（この文字列は `CLAUDE.md` 内に 1 回出現する）:
```
feat 案件では、直前予測は lock された式に前段の実測を代入した値であり、式に無い項目や幅の無い点予測を足さない
```
new:
```
feat 案件では、直前予測は lock された式に前段の実測を代入した値であり、式に無い項目や幅の無い点予測を足さない。**予測を記録したら、その `experiment_log.md` をその場で git にコミットする**（ステージ対象は当該ログ 1 ファイルのみ。実行前の内容が改変されていないことを git 履歴で独立に検証するため。実施は「git 操作の実行方法」の予測記録の例外に従う）。この証跡が示すのは、コミット時点の記録内容と、その後の変更の有無・履歴だけである。予測と実行の前後関係（予測が実行の前に書かれたこと）はコミット単体では証明できない（実行後に書いた予測をコミットしても区別できない。実行時の手順の遵守を機械的に検証する手段は未解決。update-010）
```

### Q4: 「git 操作の実行方法」導入文（409 行。行全体）

old:
```
git のコミット・プッシュは、Claude Code 本体が直接実行するのではなく、Agent ツールで **model: opus** を指定したサブエージェントに委任する。
```
new:
```
git のコミット・プッシュは、Claude Code 本体が直接実行するのではなく、Agent ツールで **model: opus** を指定したサブエージェントに委任する。**例外（予測記録のコミット）**: feat 案件で段の実行直前の予測を `experiment_log.md` に記録したときのコミット（「実験・検証の進め方」項目2）は、Claude Code 本体が直接行う。ステージ対象は当該 `experiment_log.md` の 1 ファイルのみ、コミットメッセージは定型 `{案件ID}: {マイルストーンID} {実行単位の識別} の予測を記録`（末尾トレーラーは本体が稼働しているモデルの `Co-Authored-By`）、push はしない。ユーザーの指示は不要（同 項目2 の規定が指示に当たる）。ステージ対象に他のファイルが含まれる場合や、コミットが失敗した場合は、対処せずユーザーに報告する。この例外以外のコミット・プッシュは本節の規定どおり委任する。
```

### Q5: 「git 操作の実行方法」委任しない作業 1 項目目（421 行。行全体）

old:
```
- コミット可否の判断・タイミング（ユーザーの指示を受けて Claude Code 本体が起動する）
```
new:
```
- コミット可否の判断・タイミング（ユーザーの指示を受けて Claude Code 本体が起動する。予測記録のコミット〔本節の例外〕はユーザーの指示なしに本体が行う）
```

### Q6: 「パイプラインの切り方」の「未検証の事項」段落（92 行。行全体。直後に段落を追加）

old:
```
**未検証の事項**: 式から出した幅の精度（外れの発生頻度、幅の広さ）は未検証である（update-009。ユーザー 2026-09-08「実際に運用してみないと分からない」）。運用では各段の照合結果（`experiment_log.md`）を材料に、幅が広すぎて外れが出ない段と、狭すぎて毎回外れる段を記録し、規定の見直しに使う。
```
new:
```
**未検証の事項**: 式から出した幅の精度（外れの発生頻度、幅の広さ）は未検証である（update-009。ユーザー 2026-09-08「実際に運用してみないと分からない」）。運用では各段の照合結果（`experiment_log.md`）を材料に、幅が広すぎて外れが出ない段と、狭すぎて毎回外れる段を記録し、規定の見直しに使う。

**要調査事項の扱い**: 段の実行中に出た、最終出力の成分に無い事項（要調査事項）は、案件直下の補助文書に事実と典拠のみを記録し（対処方針・判定基準は書かない。後続案件が決める）、本案件の目標・段・想定値・マイルストーンを変えない。変えるなら「ロードマップの改訂（再計画）」の手 2 として `roadmap.md` を改訂し、人レビューを通す。
```

### Q7: 粒度表 `README.md` の行（104 行。行全体）

old:
```
| `README.md` | 案件の状態 | ステータス。マイルストーン一覧と状態。実施記録（日付・結果の1行要約・判定・承認・詳細への参照） | 計画の内容（`roadmap.md` を参照）。結果の詳細（マイルストーンフォルダを参照） |
```
new:
```
| `README.md` | 案件の状態 | ステータス。マイルストーン一覧と状態。実施記録（日付・結果の1行要約・判定・承認・詳細への参照）。**状態は、人レビューの承認・実装の開始・段の実行のたびにその場で更新する**（記録が遅れると、文書上は未着手のまま実行済みの記録が存在する状態になり、手続き違反と区別できなくなる） | 計画の内容（`roadmap.md` を参照）。結果の詳細（マイルストーンフォルダを参照） |
```

### Q8: 「収束」の定義のレビュー単位の列挙（282 行。行の一部）

old（この文字列は `CLAUDE.md` 内に 1 回出現する）:
```
実験・検証の criteria 文書のレビュー（criteria lock）は criteria 文書ごとに1単位とする。
```
new:
```
実験・検証の criteria 文書のレビュー（criteria lock）は criteria 文書ごとに1単位、feat 案件の `experiment_log.md` の実行後の書き換え（実測・照合の追記を除く。記述の訂正・撤回・再構成）は、当該マイルストーンのステップ7 の人レビュー通過までを1単位とする（同じマイルストーンで書き換えが複数回起きても同一単位であり、C は1回。書き換えのたびに、次の実行〔同じ段の再実行を含む〕または当該マイルストーンのステップ7 に進む前に、遷移表に従って高・中ゼロに収束させる〔同一単位で C 実施済みなら B で収束〕。人レビューは当該マイルストーンのステップ7 の手動テストで兼ね、別途は行わない。ステップ7 通過後の書き換えは新しい単位とし、後続工程の進行状況によらず書き換えた時点でレビューを開始し、遷移表に従って高・中ゼロに収束させたうえで訂正内容の人レビューを受け、承認を `README.md` の実施記録に日付つきで記録する〔単位の終了はこの承認。収束と承認までは書き換え後の記述を確定扱いにしない〕。依頼文は「依頼文の基準部分」の実験ログの書き換えの項）。
```

### Q9: 「依頼文の基準部分」ドキュメント更新の項（336 行。行全体。直後に 1 項目を追加）

old:
```
- **ドキュメント更新**: 「以下のドキュメントをレビューせよ: {README.md と design.md のパス} 。レビュー観点は次の3点: (1) 反映計画の自己完結性 (2) 情報の喪失 (3) 変更後のドキュメント間整合性。」
```
new:
```
- **ドキュメント更新**: 「以下のドキュメントをレビューせよ: {README.md と design.md のパス} 。レビュー観点は次の3点: (1) 反映計画の自己完結性 (2) 情報の喪失 (3) 変更後のドキュメント間整合性。」
- **実験ログの書き換え**（feat 案件の `experiment_log.md` の実行後の書き換え）: 「docs/REVIEW_CRITERIA.md の基準に従い、以下の実験ログの書き換えをレビューせよ: {experiment_log.md のパス} 。関連ドキュメントとして {roadmap.md のパス} を参照し、書き換え後の記述が『ロードマップの改訂（再計画）』の『書いてよいもの』に収まり、予測の記述が改変されておらず、事後解釈（『実験・検証の進め方』手順4）を含まないことを確認せよ。」
```

### Q10: 粒度表 `roadmap.md` の行の役割の列（100 行。行の一部）

old（この文字列は `CLAUDE.md` 内に 1 回出現する）:
```
| `roadmap.md` | 案件全体の計画。人レビュー通過後は固定し、変更は「ロードマップの改訂（再計画）」に従う |
```
new:
```
| `roadmap.md` | 案件全体の計画。人レビュー通過後は固定し、変更は「ロードマップの改訂（再計画）」に従う（マイルストーン一覧の状態欄の更新は改訂に当たらず、改訂履歴に書かない） |
```

---

## 3. 実施手順（Sonnet サブエージェント）

1. `CLAUDE.md` を読み、Q1〜Q10 の各 `old` が**ちょうど1回**出現することを確認する（Python の `str.count`）。1つでも 0 または 2 以上なら**中断して報告**する（本書の行番号は参考であり、一致判定は文字列で行う）
2. Q1〜Q10 を順に置換する
3. 置換後、次を確認する
   - `grep -c "予測記録のコミット" CLAUDE.md` が **2** であること（Q4・Q5。Q3 は「予測記録の例外」と書く）
   - `grep -c "要調査事項の扱い" CLAUDE.md` が **1** であること（Q6）
   - `grep -c "実験ログの書き換え" CLAUDE.md` が **2** 以上であること（Q8・Q9。`grep -c` は行数）
   - `grep -c "段の実行（実データの投入" CLAUDE.md` が **2** であること（Q1・Q2）
   - `CLAUDE.md` の行数が 434 → **437**（Q6 で +2 行、Q9 で +1 行、他は不変。2026-09-08 に置換を模擬して実測）
4. `git -C <path> diff CLAUDE.md` を出力し、変更箇所が Q1〜Q10 の 10 件だけであることを報告する
5. git 操作・`docs/BACKLOG.md`・`docs/CHANGELOG.md`・`docs/PROJECT_KNOWLEDGE.md`・本案件 `README.md` は**変更しない**（ステップ6 で Claude Code 本体が行う）

---

## 4. 反映後の検証（Claude Code 本体が `git diff` で行う）

| # | 確認項目 |
|---|---|
| 1 | 変更ファイルが `CLAUDE.md` のみである |
| 2 | 削除された情報が無い（すべて追記・語句の置換。Q2 の「ドキュメントに定義された動作確認（実データ実行等）」は「ドキュメント（設計書・修正計画）に定義された動作確認」のまま維持され、feat の段の実行を除く旨だけが加わる。bug 案件の `investigation.md` に定めた動作確認は委任対象に残る） |
| 3 | ステップ6 に「実装の委任には段の実行（実データの投入）を含めない」と、実行単位ごとの予測記録・コミット・1 実行の手順がある（Q1）。「実装の実行方法」項目 4 が段の実行を委任に含めない（Q2）。両者が矛盾しない |
| 4 | 「実験・検証の進め方」項目 2 に予測記録時のコミットと、証跡が示す範囲の限定（記録内容とその後の変更履歴のみ。実行との前後関係は証明できない。未解決の明記）がある（Q3）。README §2 F3 の記述と一致する |
| 5 | 「git 操作の実行方法」に予測記録のコミットの例外（本体が直接、ステージ対象 1 ファイル、定型メッセージ、push なし、指示不要、失敗時は報告）があり、「委任しない作業」と整合する（Q4・Q5） |
| 6 | 「パイプラインの切り方」に「要調査事項の扱い」の段落があり、手 2 と人レビューへの参照がある（Q6） |
| 7 | 粒度表の `README.md` の行に状態更新の時点があり、`roadmap.md` の行に状態欄の更新が改訂に当たらない旨がある（Q7・Q10）。両者が矛盾しない（README の状態と roadmap の状態欄は同じ時点で更新できる） |
| 8 | 「収束」の定義のレビュー単位に `experiment_log.md` の書き換え（当該マイルストーンのステップ7 通過までを 1 単位、書き換えのたびに次の実行〔同じ段の再実行を含む〕またはステップ7 の前に収束、人レビューはステップ7 で兼ねる、ステップ7 通過後は新しい単位で、後続工程の進行状況によらず書き換え時にレビューを開始し、収束と人レビュー・承認の記録で終える）があり、「依頼文の基準部分」に対応する依頼文がある（Q8・Q9）。粒度表の `experiment_log.md` の行（update-009）の「書かないこと」と矛盾しない |
| 9 | `AGENTS.md`・`docs/REVIEW_CRITERIA.md` が変更されていない |
| 10 | 反映後の `CLAUDE.md` が、本書の `old`/`new` を順に適用した模擬置換の結果と一致する |

---

## 5. 完了処理（ステップ6。Claude Code 本体）

1. `docs/BACKLOG.md` の update-010 の行をステータス **Closed** にし、備考に完了日・レビュー回数・反映内容の要約を追記する
2. `docs/CHANGELOG.md` に完了日の項として、変更の要旨（実装と実行の分離、予測記録時の本体コミットの例外、要調査事項の受け皿、状態更新の時点、実験ログの書き換えのレビュー単位と依頼文、状態欄の更新は改訂でない）と、取り込まなかった項目（F8 (8-b)・F15・F18。再発時に再検討）と案件フォルダを記録する
3. 本案件 `README.md` のステータスを **Closed** に更新し、レビューの経過（`reviews/codex-NN`）と反映の検証結果（§4 の 10 項目）を記録する
4. `docs/PROJECT_KNOWLEDGE.md` は更新しない（ファイルの追加が案件フォルダのみ）
5. Codex のレビューストリーム `rev-honocr-update-010` に `/quit` を送り、ペインを撤去する
6. feat-025 の起票（feat-024 の目的のコピー。新規定の初適用）はユーザーの判断を待つ（本案件の範囲外）

---

## 6. 設計判断の記録

| # | 判断 | 理由 |
|---|---|---|
| 1 | 段の実行を委任から外し、本体が実行単位ごとに行う（Q1・Q2） | README §2 F1。実装と実行が同じ委任に入ると予測が必ず実行の手前に前倒しになる（`procedure_deviation_record.md` §1.5-2）。委任の目的（設計書の自己完結性の検証）は実装の部分で保たれる |
| 2 | 予測記録のコミットは本体が直接行う例外とし、ステージ対象・メッセージ・push なし・失敗時の扱いを固定する（Q3〜Q5） | README §5 判断 B (a)。Opus 委任を実行単位ごとに繰り返すのは重い（update-009 の完了コミットで約 5.6 万トークン・39 秒）。例外の範囲を 1 ファイル・定型メッセージに限定し、判断の余地を残さない |
| 3 | 証跡が示す範囲（コミット時点の記録内容とその後の変更履歴のみ。実行との前後関係は証明できない）を規定に明記する（Q3） | `procedure_deviation_record.md` §3-1・3-3、codex-01 中2。証跡が「予測が実行の前に書かれた」「実行時の遵守」まで示すと誤読させない |
| 4 | 要調査事項の受け皿を「パイプラインの切り方」に置く（Q6） | README §2 F6。実行中の規定であり、外れの規定（再計画）とは別の入口。「最終出力の成分に無い」は式で判定できる |
| 5 | 状態更新の時点を粒度表の `README.md` の行に置く（Q7） | README §2 F7。`README.md` の「書くこと」の定義の一部として読ませる（ステップ7 に書くと承認時だけの規定に読める） |
| 6 | 実験ログの書き換えをレビュー単位に加え、実施時点（書き換えのたびに、同じ段の再実行を含む次の実行またはステップ7 の前）・通過条件（Codex 高・中ゼロ。人レビューはステップ7 で兼ねる）・単位（当該マイルストーンのステップ7 通過までを 1 単位。既存の「人レビュー通過が単位の区切り」と同じ構造）・通過後の書き換えの終了条件（書き換え時にレビューを開始し、収束と訂正内容の人レビュー・承認の記録で終える）と依頼文を用意する（Q8・Q9） | README §5 判断 E (8-a)、codex-01 中3、codex-02 中、codex-04 中、codex-05 中。単位を加えるだけでは依頼文・実施時点・再開条件が無く自己完結しない。観点は「書いてよいもの」への適合・予測の不改変・事後解釈の不在に限定する |
| 7 | 補助文書の新設・改版はレビュー単位に加えない | README §5 判断 E (8-b)・G。補助文書は判定基準を含まない記録。再発時に再検討 |
| 8 | `AGENTS.md` を変えない | 粒度表の名前参照で判定が追随する。実験ログの書き換えの工程判定は既存の「マイルストーンフォルダ配下の文書ならステップ5」で行える |
| 9 | ステップ7 通過後の書き換えのレビューを工程に結びつけず、書き換え時に開始して承認で終える | codex-04・05 で同じ指摘が 2 回続いた（期限を「次の実行」「ステップ7」「次のマイルストーンのステップ4・ステップ8」と工程に結びつけるたびに、その工程を過ぎた後の訂正が扱えない）。「行き詰まり検出」に従い、工程を期限にする方針を取り下げ、レビューの開始点を書き換えの事象そのものに置いた |
