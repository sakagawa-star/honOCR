# update-009 反映設計書: 段の予想値を式から導出する規定への改訂

- **案件**: update-009（`docs/issues/update-009-derive-predictions-from-formula/`）
- **作成日**: 2026-09-08
- **位置づけ**: ドキュメント更新フロー ステップ3「設計・保存」。調査と判断は `README.md`（§1〜§5）
- **反映担当**: Sonnet サブエージェント（ステップ5。読み替えは CLAUDE.md「ドキュメント更新フロー」ステップ5 に従う）
- **テスト**: コード変更が無いため自動・手動とも**不要**

**本書だけで反映作業ができる**（`/clear` 後の Sonnet が、本書の「変更前」を検索して「変更後」に置換すれば完了する）。変更方式はすべて**完全一致の文字列置換**（`old` は `CLAUDE.md` 内で**ちょうど1回**出現する。出現回数が 0 または 2 以上なら中断して報告する）。P1・P2・P3・P7・P8・P9・P10・P11 は行の一部または行全体、P4・P5 は表の行全体（P2・P4・P11 はそれぞれ a・b の 2 件）、P5 は節全体（見出し行から末尾の箇条書き行まで。改行を含めて完全一致）を 1 つの `old` 文字列として扱う。コードブロックのフェンス（```）自体は `old` / `new` に含めない。

用語: 本書では `CLAUDE.md` の既存の用語「想定値」をそのまま使う（欄の名前は変えず、欄の中身を「予想値 = 式（入力値）」の書式に変える）。「予想値」「予測値」は `roadmap.md` の想定値の式に値を代入した結果を指す。

---

## 1. 変更対象ファイルと変更の一覧

| # | ファイル | 箇所（行番号は 2026-09-08 時点。`CLAUDE.md` 426 行） | 方式 | 対応する README の判断 |
|---|---|---|---|---|
| P1 | `CLAUDE.md` | 76 行「パイプラインの切り方」ステップ2 で書くもの 4「想定値」 | 置換（行全体） | A・E |
| P2 | `CLAUDE.md` | 80 行「段の切り方の規則」の 1 項目目（直後に 1 項目を追加）と 73 行 段の表の「決める項」の列の定義 | 置換（行全体 2 件。P2-a・P2-b。P2-a は 1 行を 2 行にする） | A（式で出せない量は測る段を前に置く） |
| P3 | `CLAUDE.md` | 87 行「実行後の照合」の段落（末尾に「未検証の事項」の段落を追加） | 置換（行全体。1 段落を 2 段落にする） | C・§1.2 項目 9 |
| P4 | `CLAUDE.md` | 97 行 粒度表 `roadmap.md` の行 | 置換（行の一部 2 箇所。P4-a・P4-b） | A・B |
| P5 | `CLAUDE.md` | 100 行 粒度表 `experiment_log.md` の行 | 置換（行全体） | C・E |
| P6 | `CLAUDE.md` | 107〜134 行「ロードマップの改訂（再計画）」節**全体** | 置換（節ごと） | B・C |
| P7 | `CLAUDE.md` | 55 行 ステップ6 の直前予測の文 | 置換（行の一部） | E |
| P8 | `CLAUDE.md` | 179 行「実験・検証の進め方」項目1 末尾の feat 読み替え | 置換（行の一部） | E |
| P9 | `CLAUDE.md` | 180 行 同 項目2 末尾 | 置換（行の一部） | E |
| P10 | `CLAUDE.md` | 181 行 同 項目3 末尾 | 置換（行の一部） | C |
| P11 | `CLAUDE.md` | 388〜389 行「行き詰まり検出」feat の項目と「発動したら」の項目 | 置換（行全体 2 件。P11-a・P11-b） | D・C |
| P12 | `docs/BACKLOG.md`・`docs/CHANGELOG.md`・本案件 `README.md` | 完了処理 | 追記・更新（Claude Code 本体がステップ6 で行う） | — |

**変更しないファイル**（理由つき）:

- `AGENTS.md`: 規定を再掲せず「ロードマップとマイルストーン文書の粒度」の表を名前で参照している。表の**内容**（P4・P5）が変わっても参照先の名前は変わらないため、対象文書単位の判定は自動的に新しい表に従う（update-008 `design.md` と同じ根拠）。式の無い予想行の不適合判定は P4-a の書式の規定で表に入る
- `docs/REVIEW_CRITERIA.md`: 「1.2 曖昧性の排除」（条件・閾値・パラメータが具体的数値で記述されているか）と「1.10 重要度分類」（必須条件の記述漏れ＝中）で、式の無い予想行は既存の基準で中以上として扱える。粒度表に書式を書けば足りる（README §5 判断 B の「足りる」）
- `docs/BUGFIX_STANDARD.md` §3・`docs/REQUIREMENTS_STANDARD.md`・`docs/DESIGN_STANDARD.md`: feat のステップ番号・文書の構成は変わらない
- `docs/PROJECT_KNOWLEDGE.md`: ファイルの追加・削除は案件フォルダのみで、ディレクトリ構成は `docs/issues/` を個別に列挙していない
- feat-024 の文書: 一時中断中（README §5 判断 F）。本案件の規定は feat-024 に遡及適用しない
- `.claude/handovers/`: 対象外

**保持するもの**（update-008 が保持した由来の目的と記入欄）: 段の単位（1 マイルストーン＝入力を与えて出力を確認できる単位）、完了条件は実行結果、最終出力の成分・入力の次元・段の表（処理単位・単位数・次元の式・手段・決める項・結合点）、「決める項が無しの段に検証以上を積まない」、マイルストーン ID の規則、粒度表の他の行（`requirements.md` の閾値の再掲・追加の禁止を含む）、「実行前の信号」の中断条件、改訂履歴の要求、ID を振り直さない、完了済みマイルストーンの記録を変えない。**削除するもの**: 想定値の「根拠の種別（実測／式から導出／推測）」、梯子の手 1〜4 と「手を飛ばす、または手を戻すには、ユーザーの指示を要する」、「中断してユーザーに報告する」（外れの後の動作としては、再検討に入り改訂を人レビューに出す形に変わる）。削除する文の由来は update-008 `README.md` §1.2 (d)・§5 判断 D と `design.md` P5・P9・P13 に残る。

---

## 2. 変更の内容（変更前 → 変更後）

### P1: 「パイプラインの切り方」ステップ2 で書くもの 4（76 行）

old:
```
4. **想定値**: 段ごとに、出力の値または割合の想定と、**根拠の種別**（実測／式から導出／推測）と、**外れとみなす幅**を数値で書く。値は当該段の実行直前に確定してよい（前段の実測を使う）が、想定する項目と幅の枠は `roadmap.md` の人レビュー通過で固定する（「実験・検証の進め方」の criteria lock）
```
new:
```
4. **想定値**: 段ごとに、出力の予想を **`予想値 = 式（入力値）`** の書式で書く。式は段の表の「出力の次元の式」または「決める項」の式から書き、入力値は前段の実測（出所の段を添える）と定数。出力に幅があるなら、**外れとみなす幅**と**幅を出した計算**（例: 予備測定 30 件からの二項分布の 95% 区間。計算の入力と方法）を書く。根拠を種別のラベル（実測／式から導出／推測）で代替しない。`roadmap.md` の人レビュー通過で固定するのは**式と幅の計算方法**であり（「実験・検証の進め方」の criteria lock）、値は前段の実測が確定してから代入する（当該段の実行直前。`experiment_log.md` の直前予測）。式に代入する実測が無く予想を出せない量は、「段の切り方の規則」に従いその量を測る段を前に置く。**式の無い想定値の行は、Codex レビュー・人（ユーザー）レビューとも不適合とする**
```

### P2: 「段の切り方の規則」と段の表の「決める項」（80 行・73 行。P2-a・P2-b の 2 件）

P2-a old（80 行。1 項目目の直後に 1 項目を追加）:
```
- 出力の次元を式で書けない作業のまとまりは1段にしない。式で書ける単位まで分ける
```
new:
```
- 出力の次元を式で書けない作業のまとまりは1段にしない。式で書ける単位まで分ける
- 出力の想定値を式で出せない段（式に代入する実測が無い）は、その量を測る段を前に置く。根拠の無い幅を持つ段はロードマップに置かない（「実験・検証の進め方」の「基準を測ることがゴールの実験」と同じ扱い。式で予想できるものを積み上げないと解が収束しないため）。測る段の決める項は「無し」とし、代わりに**測る項**（どの段のどの想定値の式のどの入力項か）を段の表に書く
```

P2-b old（73 行。段の表の「決める項」の列の定義。行全体）:
```
   - **決める項**（最終出力の式のどの成分を決めるか。無ければ「無し」と書く）
```
P2-b new:
```
   - **決める項**（最終出力の式のどの成分を決めるか。無ければ「無し」と書く。「段の切り方の規則」の測る段は「無し」に加えて測る項〔どの段のどの想定値の式のどの入力項か〕を書く）
```

### P3: 「実行後の照合」（87 行。段落を置換し、「未検証の事項」の段落を追加）

old:
```
**実行後の照合**: 段の実行後に想定値と実測を照合して `experiment_log.md` に記録する。想定の幅を外れたら、微修正で押し通さず「ロードマップの改訂（再計画）」の梯子に従う（「行き詰まり検出」と同じ扱い）。
```
new:
```
**実行後の照合**: 段の実行後に想定値と実測を照合して `experiment_log.md` に記録する。想定の幅を外れたら、微修正で押し通さず「ロードマップの改訂（再計画）」の再検討に入る（人の許可は要らない。「行き詰まり検出」の外れ 2 回はこれに重ねて適用する）。

**未検証の事項**: 式から出した幅の精度（外れの発生頻度、幅の広さ）は未検証である（update-009。ユーザー 2026-09-08「実際に運用してみないと分からない」）。運用では各段の照合結果（`experiment_log.md`）を材料に、幅が広すぎて外れが出ない段と、狭すぎて毎回外れる段を記録し、規定の見直しに使う。
```

### P4: 粒度表 `roadmap.md` の行（97 行。行の一部 2 箇所）

P4-a old（行の一部。この文字列は `CLAUDE.md` 内に 1 回出現する）:
```
**段ごとの想定値**（根拠の種別・外れの幅）
```
P4-a new:
```
**段ごとの想定値**（`予想値 = 式（入力値）` の書式。幅と幅を出した計算。式の無い行は不適合）
```

P4-b old（行の一部。この文字列は `CLAUDE.md` 内に 1 回出現する）:
```
改訂履歴（改訂理由）。任意: 保留リスト
```
P4-b new:
```
改訂履歴（改訂理由と式の連鎖。「ロードマップの改訂（再計画）」参照）。任意: 保留リスト
```

### P5: 粒度表 `experiment_log.md` の行（100 行。行全体）

old:
```
| `{マイルストーンID}-{slug}/experiment_log.md` | 予測と実測の記録（全マイルストーン） | 直前予測（`roadmap.md` の想定値の枠に値を入れる）。実測。照合と乖離の原因 | 判定基準（`roadmap.md` の想定値と外れの幅を参照） |
```
new:
```
| `{マイルストーンID}-{slug}/experiment_log.md` | 予測と実測の記録（全マイルストーン） | 直前予測（`roadmap.md` の想定値の式に前段の実測を代入した値。代入した実測の出所）。実測。照合（予測値・実測値・差）。外れた場合は「ロードマップの改訂（再計画）」の「書いてよいもの」に限る | 判定基準（`roadmap.md` の想定値と外れの幅を参照）。式に無い項目の予測、幅の無い点予測。外れの解釈・判定（「問題ない」「正常な範囲」「基準の欠陥」等）、外れの分類、自作の選択肢 |
```

### P6: 「ロードマップの改訂（再計画）」節の全体（107〜134 行）

old（節の全文。見出し行から、末尾の箇条書き行「- 改訂後の `roadmap.md` をステップ3（Codex 収束 → 人レビュー）に通してから、次のマイルストーンに着手する」まで。次の見出し `### 不具合修正フロー（bug-XXX 案件）` の前の空行は含めない）:
```
#### ロードマップの改訂（再計画）

ロードマップが真価を発揮するのは複雑な実装のときで、その場合はマイルストーンの途中で中断して計画を見直すことが多い。次のいずれかが起きたら、**その場で回避策を取らずに中断**し、何が起きたか・どこまで完了したかをユーザーに報告する。

- 設計書どおりに実行できない
- ドキュメントと実装の記述が矛盾する
- 完了条件を満たせない、または判定できない
- ロードマップの前提が実際と違うと判明した（前提の典拠が覆った）
- 段の実測が想定値の幅を外れた（「パイプラインの切り方」の照合）
- 実行前の信号が出た: 設計書が肥大する、Codex レビューが収束しない、実装中に不要な機能や設計の欠陥が判明する。不要と判明した機能は分割ではなく削除する

中断後は、次の**梯子**を 1 から順に上がって見直し、再レビューを経て再開する。**手を飛ばす、または手を戻すには、ユーザーの指示を要する。** ただし実行前の信号（外れた段が無い）では手 1・2 に対象が無いため、**手 3 から入る**（これは飛ばしではなく規定の入口である）。「行き詰まり検出」は案件全体に適用する。

| 手 | 見直し | 戻る場所 |
|---|---|---|
| 1 | 外れた段を見直す（手段・手順の修正。段の入出力の次元と決める項は変えない） | 当該 `design.md`（必要なら `requirements.md`）の修正 → ステップ5 |
| 2 | それでも外れる → 精度が悪い → 段を細かく分ける（枝番のマイルストーンとして挿入） | `roadmap.md` の改訂 → ステップ3 |
| 3 | 分けられない → パイプラインの構成を見直す（段の追加・削除・順序・入力・手段の変更） | `roadmap.md` の改訂 → ステップ3 |
| 4 | 見直しも無理 → 最終ゴールを変更する（妥協案・中止。ユーザーの決定） | `roadmap.md` の目標と最終出力の成分の改訂 → ステップ3 |

`roadmap.md` の改訂は次のとおり行う。

- 同じファイルを上書きで改訂し、改訂履歴に改訂理由（覆った前提、外れた段と実測、変更した段・順序、削除・吸収したマイルストーン、上がった手）を書く。旧版は git に残るため複製しない
- マイルストーン ID は振り直さない（「パイプラインの切り方」の ID 規則）。完了済みマイルストーンの記録（`README.md`・マイルストーンフォルダ）は変更しない
- 前提の典拠を更新する（実証された項目は「M-n で実証」に、覆った項目は新しい値と典拠に）
- 改訂で段を足す場合は、足す段の決める項を書く。決める項が「無し」の段を足せるのは手3 に限り、その理由を改訂履歴に書く
- 改訂後の `roadmap.md` をステップ3（Codex 収束 → 人レビュー）に通してから、次のマイルストーンに着手する
```
new:
```
#### ロードマップの改訂（再計画）

ロードマップが真価を発揮するのは複雑な実装のときで、その場合はマイルストーンの途中で計画を見直すことが多い。次のいずれかが起きたら、**その場で回避策を取らずに再検討に入る**。再検討に入るのに人（ユーザー）の許可は要らない（`roadmap.md` の人レビュー通過が、外れたら再検討に入ることの承認である）。再検討の結果（改訂した `roadmap.md`）は必ず人レビューに通す（ステップ3）。次段に進む・進まないの判断はエージェントがしない。

- 段の実測が想定値の幅を外れた（「パイプラインの切り方」の照合）
- ロードマップの前提が実際と違うと判明した（前提の典拠が覆った）
- 設計書どおりに実行できない／ドキュメントと実装の記述が矛盾する／完了条件を満たせない、または判定できない
- 実行前の信号が出た: 設計書が肥大する、Codex レビューが収束しない、実装中に不要な機能や設計の欠陥が判明する。不要と判明した機能は分割ではなく削除する

**再検討に入ったエージェントが書いてよいもの**は次の 4 つに限る。これ以外——「問題ない」「正常な範囲」「影響なし」「基準の欠陥」等の判定、外れの分類（成立条件／結果の予想、型1／型2 等）、自作の選択肢（案A〜D 等）、次段に進む判断、外れの重みを上げ下げする説明——は書かない。書けるものが無い場合はその欄を空欄のまま人に渡す。

1. **事実**: 予測値・実測値・差（`experiment_log.md` の照合）
2. **式の連鎖**: 予想を生んだ式、実測が式のどの項を決めたか、その項が最終出力の各成分にどう伝播するか（段の表と最終出力の成分の式を前向きに辿って式で書く）。後段の成分の値を計算する場合はこの連鎖の中で式として書き、段の外で計算した値を判断の根拠にしない
3. **再検討の手**（下記の手 1 / 手 2 のどちらを取ったか）と、その変更の diff
4. **推論の取り下げ**（「行き詰まり検出」の外れ 2 回が発動したときのみ）: 外れた予想を生んだ推論（式を立てた前提・入力の見立て）を名指しで取り下げる記録。取り下げた推論に代わる推論は文章で書かず、再検討の手の改訂の中で式として書く（手 1 では次元を固定した新しい式、手 2 では次元を変えた後の式）

**再検討の 2 手**（手 1 → 手 2 の順。飛ばさない。どちらも改訂後の `roadmap.md` をステップ3〔Codex 収束 → 人レビュー〕に通してから再実行する）:

| 手 | 見直し | 波及する範囲 | 戻る場所 |
|---|---|---|---|
| 1 | 段の入出力の**次元を固定**し、**式**を変える（予想を生む式、幅の計算、手段・手順） | 当該段の内側だけ。他の段の次元と想定値の式は動かない（後段の予測値は、当該段の再実行後の実測を式に代入して更新する） | `roadmap.md` の当該段の行（想定値・手段）の改訂 → ステップ3。当該 `design.md` / `requirements.md` も変える場合はステップ5 も通す |
| 2 | 式を変えても外れる → **次元**を変える（段の入力または出力の次元の式。段の追加・削除・分割〔枝番のマイルストーン〕・順序・入力・手段の変更、目標と最終出力の成分の変更を含む。最終出力の成分の変更はユーザーの決定を要する） | **入力**の次元を変えたら**前段**に、**出力**の次元を変えたら**後段**に波及する。波及した段の想定値（式に新しい実測を代入して再導出）と実装をやり直す。波及が無いなら次元を変えていない | `roadmap.md` の段の表・想定値・マイルストーン一覧の改訂 → ステップ3 |

実行前の信号（外れた段が無い）と前提の典拠が覆った場合は、式を変える対象となる外れた段が無いため、**手 2 から入る**（これは飛ばしではなく規定の入口である）。変更の diff（文書または手段の差分）を伴わない再検討は「手を取った」と書けない。変更なしの続行は「ユーザー指示による続行」としてのみ記録する。「行き詰まり検出」は案件全体に適用する（外れ 2 回で、外れた予想を生んだ推論を取り下げる）。

`roadmap.md` の改訂は次のとおり行う。

- 同じファイルを上書きで改訂し、改訂履歴に改訂理由と式の連鎖（外れた段と予測値・実測値、覆った項、最終出力の成分への伝播、変えた式または次元、波及した段、削除・吸収したマイルストーン、取った手〔1 / 2〕）を書く。旧版は git に残るため複製しない
- マイルストーン ID は振り直さない（「パイプラインの切り方」の ID 規則）。完了済みマイルストーンの記録（`README.md`・マイルストーンフォルダ）は変更しない
- 前提の典拠を更新する（実証された項目は「M-n で実証」に、覆った項目は新しい値と典拠に）
- 改訂で段を足す場合は、足す段の決める項と想定値（式）を書く。決める項が「無し」の段を足せるのは、手段の単体疎通と、他の段の想定値の式に代入する実測を得る測る段（いずれも「段の切り方の規則」。測る段は測る項を書く）に限る
- 改訂後の `roadmap.md` をステップ3（Codex 収束 → 人レビュー）に通してから、次のマイルストーンに着手する
```

### P7: ステップ6 の直前予測の文（55 行。行の一部）

old（この文字列は `CLAUDE.md` 内に 1 回出現する）:
```
すべてのマイルストーンで、実行の直前に `roadmap.md` の想定値の枠に値を入れ、実行後に実測と照合して `experiment_log.md` に記録する
```
new:
```
すべてのマイルストーンで、実行の直前に `roadmap.md` の想定値の式に前段の実測を代入して予測値を出し、実行後に実測と照合して `experiment_log.md` に記録する
```

### P8: 「実験・検証の進め方」項目1 末尾の feat 読み替え（179 行。行の一部）

old（この文字列は `CLAUDE.md` 内に 1 回出現する）:
```
feat 案件では、criteria 文書を別立てせず、段ごとの想定値と外れの幅を `roadmap.md` の段の表に書き、その `roadmap.md` の人（ユーザー）レビュー通過（機能追加フローのステップ3）を criteria lock とする（値は各段の実行直前に確定してよいが、想定する項目と幅の枠は変えない）
```
new:
```
feat 案件では、criteria 文書を別立てせず、段ごとの想定値を `予想値 = 式（入力値）` の書式と幅の計算で `roadmap.md` に書き、その `roadmap.md` の人（ユーザー）レビュー通過（機能追加フローのステップ3）を criteria lock とする（lock されるのは式と幅の計算方法。値は前段の実測が確定してから代入する。式・幅の計算の変更は「ロードマップの改訂（再計画）」に従う）
```

### P9: 同 項目2 末尾（180 行。行の一部）

old（この文字列は `CLAUDE.md` 内に 1 回出現する）:
```
予測する項目の枠（何を予測するか）は criteria 文書（feat 案件では `roadmap.md` の段の表）に事前定義してよい（値はフェーズ直前に確定する）
```
new:
```
予測する項目の枠（何を予測するか）は criteria 文書（feat 案件では `roadmap.md` の想定値の式）に事前定義してよい（値はフェーズ直前に確定する）。feat 案件では、直前予測は lock された式に前段の実測を代入した値であり、式に無い項目や幅の無い点予測を足さない
```

### P10: 同 項目3 末尾（181 行。行の一部）

old（この文字列は `CLAUDE.md` 内に 1 回出現する）:
```
乖離した場合はそのフェーズの前提・理解を疑い、原因を特定してから次フェーズに進む
```
new:
```
乖離した場合はそのフェーズの前提・理解を疑い、原因を特定してから次フェーズに進む（feat 案件では「ロードマップの改訂（再計画）」に従う。エージェントは原因の解釈を書かず、式の連鎖を示して改訂を人レビューに出す）
```

### P11: 「行き詰まり検出」feat の項目（388 行。行全体。P11-a・P11-b の 2 件）

P11-a old:
```
- **feat 案件では、段の実測が想定値の幅を2回続けて外れたら**（同じ段の再実行でも、連続する2つの段でも）、原因が同じかの分類を経ずに同じ扱いとする（「ロードマップの改訂（再計画）」の梯子に従う）
```
new:
```
- **feat 案件では、段の実測が想定値の幅を2回続けて外れたら**（同じ段の再実行でも、連続する2つの段でも）、原因が同じかの分類を経ずに同じ扱いとする。1 回目の外れで「ロードマップの改訂（再計画）」の再検討に入るのとは別にこの項目を置く理由: **エージェントは自分の事前の予想（推論）に執着し、外れても推論を保ったまま式の形だけを直しがちであるため、推論そのものを取り下げさせる外部トリガーが要る**（update-009。feat-024 の `roadmap.md` 第4版で観察）。発動したら、外れた予想を生んだ推論を明示的に取り下げる記録（「ロードマップの改訂（再計画）」の「書いてよいもの」4）を `experiment_log.md` に書く。それ以外の動作（何を見直すか・どの手を取るか・何を書いてよいか）は本項では定めず、すべて同節に従う
```

P11-b old（389 行。行全体）:
```
- 発動したら: いまの解決案を明示的に取り下げ、前提（原因の想定・アプローチ・そもそもの問い）に立ち返って代替案を挙げ、方針を選び直す。必要なら調査からやり直す
```
P11-b new:
```
- 発動したら: いまの解決案を明示的に取り下げ、前提（原因の想定・アプローチ・そもそもの問い）に立ち返って代替案を挙げ、方針を選び直す。必要なら調査からやり直す。feat 案件の段の外れでは、代替案と方針の選び直しは「ロードマップの改訂（再計画）」の再検討の 2 手（式の変更／次元の変更）に限り、同節の「書いてよいもの」以外（自作の選択肢・判定）を書かない
```

---

## 3. 実施手順（Sonnet サブエージェント）

1. `CLAUDE.md` を読み、P1〜P11（P2・P4・P11 はそれぞれ a・b の 2 件。計 14 件）の各 `old` が**ちょうど1回**出現することを確認する（Python の `str.count`）。1つでも 0 または 2 以上なら**中断して報告**する（本書の行番号は参考であり、一致判定は文字列で行う）
2. P1〜P11 を順に置換する（P6 は節全体。`old` の先頭行から末尾行までを1つの文字列として扱う）
3. 置換後、次を確認する
   - `grep -n "根拠の種別\|梯子\|手 3 から入る\|手を飛ばす\|上がった手\|想定値の枠に値を入れ" CLAUDE.md` が**無出力**であること（旧規定の語が残っていない）
   - `grep -c "予想値 = 式（入力値）" CLAUDE.md` が **3** であること（P1・P4-a・P8）
   - `grep -c "式の連鎖" CLAUDE.md` が **4** であること（P4-b・P6〔書いてよいもの 2・改訂履歴〕・P10）
   - `grep -c "再検討" CLAUDE.md` が **8 以上**であること（P3・P5・P6・P10・P11 と既存の「行き詰まり検出」の 1 件。模擬置換の実測は 12）
   - `CLAUDE.md` の行数が 426 → **434**（P2-a で +1 行、P3 で +2 行、P6 で +5 行、他は不変。2026-09-08 に置換を模擬して実測）
4. `git diff CLAUDE.md` を出力し、変更箇所が P1〜P11 の 14 件（P2・P4・P11 は各 2 件）だけであることを報告する
5. git 操作・`docs/BACKLOG.md`・`docs/CHANGELOG.md`・`docs/PROJECT_KNOWLEDGE.md`・本案件 `README.md` は**変更しない**（ステップ6 で Claude Code 本体が行う）

---

## 4. 反映後の検証（Claude Code 本体が `git diff` で行う）

| # | 確認項目 |
|---|---|
| 1 | 変更ファイルが `CLAUDE.md` のみである |
| 2 | 削除された行に、他所に存在しない情報が含まれていない（梯子の手 1〜4・根拠の種別の由来は update-008 `README.md` §1.2 (d)・§5 と `design.md` P5・P9 に残る。ID 規則・前提の典拠の更新・完了済み記録の不変・実行前の信号・改訂履歴の要求は保持されている） |
| 3 | 旧規定の語（「根拠の種別」「梯子」「手 3 から入る」「手を飛ばす」「上がった手」「想定値の枠に値を入れ」）が `CLAUDE.md` に残っていない |
| 4 | 「パイプラインの切り方」のステップ2 で書くもの 4 が `予想値 = 式（入力値）` の書式・幅の計算・lock の対象（式と計算方法）・式の無い行の不適合を含む（P1） |
| 5 | 「段の切り方の規則」が 5 項目で、「その量を測る段を前に置く」の項目があり、測る段の決める項は「無し」で測る項を書く旨がある（P2-a）。段の表の「決める項」の列の定義にも測る項がある（P2-b）。「ロードマップの改訂（再計画）」の改訂の箇条書きで、決める項が「無し」の段を足せる場合に測る段が含まれる（P6） |
| 6 | 「実行後の照合」が再検討への自動遷移（人の許可不要）を書き、「未検証の事項」の段落がある（P3） |
| 7 | 粒度表の `roadmap.md` 行に書式と「式の無い行は不適合」「式の連鎖」があり、`experiment_log.md` 行の書かないことに外れの解釈・判定・分類・自作の選択肢がある（P4・P5） |
| 8 | 「ロードマップの改訂（再計画）」節に「書いてよいもの」4 項目（4 は推論の取り下げ。外れ 2 回の発動時のみ）、「再検討の 2 手」の表（手 1 次元を固定して式を変える。他の段の次元と式は動かず後段の予測値は実測の代入で更新／手 2 次元を変えて前段・後段に波及）、手 2 から入る場合、diff を伴わない再検討の禁止、改訂履歴の式の連鎖がある（P6） |
| 9 | ステップ6・「実験・検証の進め方」項目1〜3 の feat 読み替えが「式に前段の実測を代入」「lock されるのは式と幅の計算方法」「式に無い項目・点予測を足さない」「解釈を書かず式の連鎖を示す」になっている（P7〜P10） |
| 10 | 「行き詰まり検出」の feat 項目に理由（推論への執着を断つ外部トリガー）があり、「発動したら」の項目が feat の段の外れでは再検討の 2 手と「書いてよいもの」に限る旨を含む（P11-a・P11-b）。P11-a は取り下げの記録以外の手順（見直す対象・手の選択・書いてよいもの）を定めず、同節と「ロードマップの改訂（再計画）」の間に相反する指示が無い |
| 11 | `AGENTS.md`・`docs/REVIEW_CRITERIA.md` が変更されていない |
| 12 | 「収束」の定義の適用先の列挙（「パイプラインの切り方」の実行後の照合、「ロードマップの改訂（再計画）」）が参照切れになっていない（両節の名前は不変） |

---

## 5. 完了処理（ステップ6。Claude Code 本体）

1. `docs/BACKLOG.md` の update-009 の行をステータス **Closed** にし、備考に完了日・レビュー回数・反映内容の要約を追記する
2. `docs/CHANGELOG.md` に 2026-09-08（または完了日）の項として、変更の要旨（想定値を式からの導出に変更し根拠の種別を廃止、criteria lock の対象を式と幅の計算方法に、外れ後の記述を 3 つに制限、梯子の手 1〜4 を再検討の 2 手に置換、再計画のトリガーを自動化し人レビュー必須、行き詰まり検出の外れ 2 回に理由を追記、式の精度を未検証として記録）と案件フォルダを記録する
3. 本案件 `README.md` のステータスを **Closed** に更新し、レビューの経過（`reviews/codex-NN`）と反映の検証結果（§4 の 12 項目）を記録する
4. `docs/PROJECT_KNOWLEDGE.md` は更新しない（ファイルの追加・削除が案件フォルダのみ）
5. Codex のレビューストリーム `rev-honocr-update-009` に `/quit` を送り、ペインを撤去する
6. **第 2 段**（独立項目 F1・F3・F6〜F9・F15・F18 の見直し）の起票はユーザーの判断を待つ（本案件の範囲外）

---

## 6. 設計判断の記録

| # | 判断 | 理由 |
|---|---|---|
| 1 | 欄の名前「想定値」は変えず、中身を `予想値 = 式（入力値）` の書式にする | 「想定値」は `CLAUDE.md` の 15 箇所以上（収束の定義・粒度表・ステップ6・行き詰まり検出等）で使われ、改名は置換箇所を増やして参照切れの危険を増やす。変えるのは中身（式と幅の計算）である |
| 2 | 根拠の種別を残さず削除する（「推測」を別欄に移さない） | README §1.4。予想の区分（停止条件／結果の予想）を作らないユーザー判断 A。種別ラベルは根拠の無い幅を許す穴になった（feat-024 第3版 §6 の「推測」） |
| 3 | 梯子の手 1〜4 を残さず 2 手に置換する（並存させない） | ユーザー判断 B。手 1〜4 は名目で選べた（第4版の「手3」）。2 手は「次元のどちら側を変えたか」で範囲が機械的に決まる |
| 4 | 「中断してユーザーに報告」を「再検討に入る（人の許可不要）」に変え、書いてよいものを列挙で制限する | ユーザー判断 C。規定は発火後にエージェントが書けることを列挙で制限しない限り拘束しない（F11〜F14 の共通点）。人の関与はロードマップの人レビューに集約する |
| 5 | 最終出力の成分の変更（旧 手4）は手 2 に含め、ユーザーの決定を要すると明記する | 最終出力の成分は最後の段の出力の次元であり、次元の変更は手 2。目標の変更は人の決定（旧規定の「ユーザーの決定」を保持） |
| 6 | 実行前の信号と前提の典拠の覆りは手 2 から入る | 外れた段が無いため式を変える対象が無い（旧規定の「手 3 から入る」と同じ構造） |
| 7 | 行き詰まり検出の「外れ 2 回」を残し、理由を本文に書く | ユーザー判断 D。「AIは事前の予想（推論）に執着があるので、それを断ち切るトリガーが欲しい」。理由が無いと Claude Code が廃止を提案する（本案件の壁打ちで実際に提案した） |
| 8 | 式の精度の未検証を「パイプラインの切り方」の末尾に置く | 想定値の規定（P1）と照合の規定（P3）の直後で、運用時に読む位置。改訂の典拠（ユーザー発言）を添えて第 2 段以降の見直しの材料にする |
| 9 | `docs/REVIEW_CRITERIA.md` と Codex の依頼文を変えない | ユーザー判断 B（書式の固定で「足りる」）。`AGENTS.md` の対象文書単位の判定が粒度表を参照するため、P4-a で表に書式を書けば Codex の判定対象になる |
| 10 | feat-024 の文書に遡及適用しない | ユーザー判断 F。feat-024 は一時中断。新規定の初適用は feat-025 で行う |
| 11 | 「行き詰まり検出」の「発動したら」（全作業共通）を残し、feat の段の外れに限って代替案を再検討の 2 手に限定する（P11-b） | codex-01 高1。全作業共通の「代替案を挙げ、方針を選び直す」と P6 の「自作の選択肢を書かない」が同じ発動で相反する。非 feat（レビュー対応・デバッグ等）には既存の文をそのまま残し、適用範囲を feat の段の外れで分離する |
| 12 | 「書いてよいもの」に 4「推論の取り下げ」を足し、外れ 2 回の発動時のみに限定する | codex-02 高1。P11-a が命じる記録が P6 の列挙に無く禁止されていた。取り下げの記録だけを許し、代わりの推論は再検討の手の改訂の中で式（手 1 は次元を固定した式、手 2 は次元を変えた後の式。codex-04 高1 で明記）として書かせることで、文章の解釈を書く余地を作らない |
| 13 | P11-a から手順の記述を除き、取り下げの記録以外はすべて「ロードマップの改訂（再計画）」に委ねる | codex-03 高1。codex-01〜03 の高はいずれも P11-a が P6 と別に手順（次項参照・記録先・手 2 の指定）を書いたことに起因し、同じ原因で 3 回続いた。「行き詰まり検出」に従い、P11-a に手順を書く方針を取り下げ、手順の所在を P6 の 1 か所に限定した（粒度の原則「各情報の所在を1つの文書に限定」と同じ構造） |
| 14 | 手 1 で固定するのは他の段の「次元と想定値の式」であり、予測値ではない | codex-06 高1。予測値は式に前段の実測を代入した結果なので、当該段の出力値が変われば後段の予測値は更新される（式は不変）。「他の段の予想は動かない」は式と値を混同していた |
| 15 | 決める項が「無し」の段として「測る段」（他の段の想定値の式の入力項を実測する段）を認め、測る項を書かせる | codex-06 高2。P2 が測る段を前に置くことを要求する一方、P6 が決める項「無し」の追加を単体疎通に限っていた。測る段は最終出力の成分を決めないが、どの想定値のどの入力項を決めるかは式で書けるので、その記入を条件に認める |
