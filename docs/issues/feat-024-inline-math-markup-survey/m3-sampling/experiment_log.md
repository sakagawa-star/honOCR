# feat-024 M3 実験ログ（段3: 層別標本抽出と原本ページの逆引き）

- **案件 / マイルストーン**: feat-024 / **M3**（`../roadmap.md` §5 の段3）
- **判定基準**: `../roadmap.md` §6 の**段3 の想定値と外れの幅**（criteria lock: `roadmap.md` 第3版の人レビュー通過 2026-09-07）。**本ログに閾値を再掲しない**
- **手順**: `design.md` §4・§5・§6
- **準拠**: `CLAUDE.md`「実験・検証の進め方（予測→実行→照合）」、`../m2-1-means-smoke/experiment_log.md` の**是正1〜7**
- **書籍本文を書かない**（`symbol`・`context` の値を記録しない。`../roadmap.md` 前提12-b）

**予測は層（＝段3 の処理単位）ごとに、その層の実行の直前に記録し、その時点でコミットする**
（是正4・6。3 層分をまとめて先に書かない）。層の順序は G → L → C（`design.md` ADR-M3-4）。

---

## 層G

### 予測（2026-09-08・実行前に記録。**コミット `23b75c2`**〔`23b75c2c57cb03197c1e037d6132af466325e992`〕に保存済み）

**予測の根拠**:

- 層G の母集団は 291 件、所在の内訳は `body` 250 / `footnote` 41（`footnote` は **14.1%**。`../survey_notes.md` §5）
- 逆引きの成功率は、`body` が 93.3%（M2 検査3 の実測 56/60）、`footnote` は 1 段階目の予備実測 75.7% を下回らないと見て 76% とする
  （`../survey_notes.md` §7.1）。層G の期待成功率は `0.859 × 0.933 + 0.141 × 0.76` = **0.908**

| 想定する項目（`../roadmap.md` §6 の段3 の枠） | 層G の予測 |
|---|---|
| 標本件数 | **50 件**（ヘッダ 1 行＋データ 50 行） |
| 再現性 | 同一シード（20260908）の再実行が**バイト一致** |
| 逆引きの成功率（`block` ＋ `page`） | **91%**（成功 **45 件** ± 4 件。二項分布 B(50, 0.908) の標準偏差 2.0 件の 2 倍） |
| 標本に含まれる `footnote` の件数 | **7 件**（期待値 50 × 0.141 = 7.05。二項分布 B(50, 0.141) の 95% 範囲は 3〜12 件） |

**参考（判定に使わない）**: `stage` の内訳は `block` 中心（M2 検査3 では `block` 50 / `page` 6 / `none` 4）と見込む。
章の分布は層G の母集団の構成（chap01 が 30.6% で最多）に従うと見込む。

**判定は 3 層の合計（150 件）に対して `../roadmap.md` §6 の幅で行う。**
層ごとの上記の値は、その層の実行に対する予測であって判定の閾値ではない。

**この予測が外れる場合に考えられること**:

- 標本件数が 50 でない → `--size` の指定が効いていない、または母集団の抽出条件が違う
- `footnote` が 3 件未満または 12 件超 → 抽出が母集団の所在構成を反映していない（無作為性の疑い）
- 成功率が大きく下回る → `footnote` の 2 段階目の成功率が予備実測より低い、
  または層G（ギリシャ文字）の候補が `body` でも逆引きしにくい性質を持つ

### 実測（2026-09-08）

**実行**: 抽出 `sampled: kind=G size=50 population=291 seed=20260908`（終了コード 0）／
逆引き `located: block=38 page=10 none=2 total=50`（終了コード 0）

| 項目 | 実測 |
|---|---|
| 標本件数 | **50 件**（ファイルは 51 行。列数はすべて 7） |
| 逆引きの成功率 | **96.0%**（`block` 38 ＋ `page` 10 = **48 件** / 50） |
| `stage` の内訳 | `block` **38** / `page` **10** / `none` **2** |
| `page` の `reason` | `page_both` 9 / `page_head` 1 / `page_tail` 0 |
| `none` の `reason` | `page_mismatch` **2** / `search_exhausted` 0 / `no_neighbor` 0 |
| 標本の `location` | `body` **44** / `footnote` **6** |
| 所在別の逆引き | `body` = `block` 33 / `page` 9 / `none` 2、`footnote` = `block` 5 / `page` 1 / `none` 0 |
| 章別（参考） | chap01 13 / chap03 10 / chap06 9 / chap05 6 / chap04 5 / chap09 4 / chap02 2 / chap08 1（chap00・chap07 は 0） |

**検証（`design.md` §5）**: 全 10 項目に合格。

| 項目 | 結果 |
|---|---|
| 5.1-1 行数 51 | **合格** |
| 5.1-2 列数がすべて 7 | **合格**（違反 0 行） |
| 5.1-3 `kind` がすべて `G` | **合格**（違反 0 行） |
| 5.1-4 `location` に `table` が無い | **合格**（0 行） |
| 5.1-5 `(chapter, offset)` の重複が無い | **合格**（0 件） |
| 5.1-6 全行が候補 TSV に存在 | **合格**（標本のみに現れる行 0） |
| 5.2-1 行数 51・列数がすべて 11 | **合格** |
| 5.2-2 先頭 7 列が標本と完全一致 | **合格**（`diff` 無出力） |
| 5.2-3 `stage` の値域 | **合格**（違反 0 行） |
| 5.2-4 成功行の `page_idx ≥ 0` | **合格**（違反 0 行） |
| 5.3 決定性（抽出・逆引きとも再実行がバイト一致） | **合格**（`diff` 無出力。再実行分は削除済み） |
| 5.5 既存コードの不変 | **合格**（`git diff --stat` が空） |

### 照合

| 予測 | 実測 | 判定 |
|---|---|---|
| 標本件数 50 | **50** | **一致** |
| 再現性（バイト一致） | バイト一致 | **一致** |
| 逆引きの成功 **45 件 ± 4**（成功率 91%） | **48 件**（96.0%） | **一致**（予測の幅 41〜49 件の内側。上寄り） |
| `footnote` **7 件**（95% 範囲 3〜12） | **6 件** | **一致** |

**乖離の説明（成功率が予測より 5 ポイント高い）**: 予測は `footnote` の成功率を 76%（1 段階目のみの
予備実測値）と置いたが、**実測では `footnote` 6 件のうち 6 件すべてが成功した**（`block` 5 / `page` 1）。
2 段階目（近傍候補によるページ推定）が `footnote` にも効いたためで、`../roadmap.md` 前提11 の
「`footnote` を含む標本での成功率は未検証」に対する最初の実測である。
**予測の幅の内側であり、外れではない**（判定は 3 層の合計に対して `../roadmap.md` §6 の幅で行う）。

**参考（判定に使わない）の項目**: `stage` は予測どおり `block` 中心（38/50）。
章の分布は chap01 が最多（13 件）で、母集団の構成（chap01 が 30.6%）と整合する。

**状態の更新（`design.md` 手順 A-6 の時点(2)）**: `README.md` の実施記録に層G の行を追加した。
`../roadmap.md` §7 の M3 の状態欄が実態どおり「**実施中**」であることを確認した（値は変えていない）。

---

## 層L

### 予測（2026-09-08・実行前に記録。**コミット `cd2d505`**〔`cd2d50543507756b9b59e6b0a332961a20ee4001`〕に保存済み）

**予測の根拠**（層G の実測を反映して直前に立てた。`CLAUDE.md`「実験・検証の進め方」手順2）:

- 層L の母集団は 1,408 件、所在の内訳は `body` 1,356 / `footnote` 52（`footnote` は **3.7%**。`../survey_notes.md` §5）
- `body` の逆引き成功率は、M2 検査3（56/60）と**層G の `body`（42/44）**を合わせて **98/104 = 94.2%** とする
- `footnote` は層G で 6/6 だったが n が小さいため、保守的に **90%** とする
- 層L の期待成功率は `0.963 × 0.942 + 0.037 × 0.90` = **0.940**

| 想定する項目（`../roadmap.md` §6 の段3 の枠） | 層L の予測 |
|---|---|
| 標本件数 | **50 件**（ヘッダ 1 行＋データ 50 行） |
| 再現性 | 同一シード（20260918）の再実行が**バイト一致** |
| 逆引きの成功率（`block` ＋ `page`） | **94%**（成功 **47 件** ± 3 件。二項分布 B(50, 0.94) の標準偏差 1.7 件の 2 倍） |
| 標本に含まれる `footnote` の件数 | **2 件**（期待値 50 × 0.037 = 1.85。二項分布 B(50, 0.037) の 95% 範囲は 0〜5 件） |

**参考（判定に使わない）**: `stage` は `block` 中心と見込む。章の分布は層L の母集団の構成
（chap05 269・chap02 213・chap08 181・chap03 173・chap09 171・chap04 167・chap06 107・chap07 68・chap01 56・chap00 3）に従うと見込む。

**この予測が外れる場合に考えられること**:

- 成功率が 87% を下回る → 層L（日本語に挟まれた半角ラテン英字）の候補が、層G と異なり
  逆引きのキー（前後 10 文字）で一意に定まりにくい性質を持つ（同じ文字列が章内に多数現れる等）
- `footnote` が 6 件以上 → 抽出が母集団の所在構成を反映していない（無作為性の疑い）

### 実測（2026-09-08）

**実行**: 抽出 `sampled: kind=L size=50 population=1408 seed=20260918`（終了コード 0）／
逆引き `located: block=37 page=8 none=5 total=50`（終了コード 0）

| 項目 | 実測 |
|---|---|
| 標本件数 | **50 件**（51 行・7 列） |
| 逆引きの成功率 | **90.0%**（`block` 37 ＋ `page` 8 = **45 件** / 50） |
| `stage` の内訳 | `block` **37** / `page` **8** / `none` **5** |
| `page` の `reason` | `page_both` 8 / `page_head` 0 / `page_tail` 0 |
| `none` の `reason` | `page_mismatch` **5** / `search_exhausted` 0 / `no_neighbor` 0 |
| 標本の `location` | `body` **47** / `footnote` **3** |
| 所在別の逆引き | `body` = `block` 34 / `page` 8 / `none` 5、`footnote` = `block` 3 / `page` 0 / `none` 0 |
| 章別（参考） | chap02 9 / chap09 9 / chap04 8 / chap05 8 / chap03 6 / chap08 6 / chap07 3 / chap06 1（chap00・chap01 は 0） |

**検証（`design.md` §5）**: 12 項目すべて合格（5.1-1〜6、5.2-1〜4、5.3 の抽出・逆引き、5.5）。
再実行分は削除済み。

### 照合

| 予測 | 実測 | 判定 |
|---|---|---|
| 標本件数 50 | **50** | **一致** |
| 再現性（バイト一致） | バイト一致 | **一致** |
| 逆引きの成功 **47 件 ± 3**（成功率 94%） | **45 件**（90.0%） | **一致**（予測の幅 44〜50 件の内側。下寄り） |
| `footnote` **2 件**（95% 範囲 0〜5） | **3 件** | **一致** |

**乖離の説明（成功率が予測より 4 ポイント低い）**: `none` 5 件はすべて `body` の候補で、
理由はすべて `page_mismatch`（前後の近傍候補が別ページを指し、ページを確定できない型）である。
`footnote` は 3 件とも成功しており、層G と同じ傾向である。
**予測の幅の内側であり、外れではない。** 層G（96.0%）と層L（90.0%）の差は
二項分布のばらつきの範囲にある（n = 50、p ≈ 0.93 のとき標準偏差は約 1.8 件）。

**参考（判定に使わない）の項目**: `stage` は予測どおり `block` 中心（37/50）。
章の分布は chap02・chap09 が最多（各 9 件）で、母集団の構成（chap05 が最多 269、chap02 213、chap09 171）
とおおむね整合する（chap05 は 8 件）。

**状態の更新（`design.md` 手順 A-6 の時点(2)）**: `README.md` の実施記録に層L の行を追加した。
`../roadmap.md` §7 の M3 の状態欄が実態どおり「**実施中**」であることを確認した（値は変えていない）。

---

## 層C

### 予測（2026-09-08・実行前に記録。コミット: {A-1 のコミットハッシュを実行前に追記する}）

**予測の根拠**（層G・層L の実測を反映して直前に立てた）:

- 層C の母集団は 2,925 件、所在の内訳は `body` 2,623 / `footnote` 302（`footnote` は **10.3%**。`../survey_notes.md` §5）
- `body` の逆引き成功率は、M2 検査3（56/60）＋層G（42/44）＋**層L（42/47）**を合わせて **140/151 = 92.7%** とする
- `footnote` は層G 6/6・層L 3/3 で合計 9/9 だが n が小さいため、保守的に **93%**（`body` と同等）とする
- 層C の期待成功率は **0.927**（所在によらず同率と置くため）

**層C は他の 2 層と性質が異なる点**: 層C の候補は**インライン数式そのもの**（`$…$`）であり、
逆引きのキー（候補の前後 10 文字を正規化したもの）に数式の記号列が含まれる。
`../survey_notes.md` §7 の予備実測は 3 層を混ぜた値であり、層C 単独の成功率は未実測である。

| 想定する項目（`../roadmap.md` §6 の段3 の枠） | 層C の予測 |
|---|---|
| 標本件数 | **50 件**（ヘッダ 1 行＋データ 50 行） |
| 再現性 | 同一シード（20260928）の再実行が**バイト一致** |
| 逆引きの成功率（`block` ＋ `page`） | **93%**（成功 **46 件** ± 4 件。二項分布 B(50, 0.927) の標準偏差 1.8 件の 2 倍） |
| 標本に含まれる `footnote` の件数 | **5 件**（期待値 50 × 0.103 = 5.16。二項分布 B(50, 0.103) の 95% 範囲は 1〜10 件） |

**参考（判定に使わない）**: `stage` は `block` 中心と見込む。章の分布は層C の母集団の構成
（chap04 548・chap09 531・chap05 495・chap08 431・chap06 286・chap03 276・chap07 156・chap02 133・chap01 63・chap00 6）に従うと見込む。

**この予測が外れる場合に考えられること**:

- 成功率が 85% を下回る → **数式を含むキーが md と content_list で表記差を持つ**
  （`norm()` が除去する記号〔`\`・`_`・`` ` ``・`-`・`:`〕は数式中に多く現れるため、
  正規化後の一致がかえって曖昧になる可能性がある）
- `footnote` が 0 件または 11 件以上 → 抽出が母集団の所在構成を反映していない（無作為性の疑い）

---

## 3 層の集計（`design.md` §6）

（3 層の実行後に記入）

---

## 判定

（3 層の集計後に記入。`../roadmap.md` §6 の段3 の想定値との照合結果と、手続き〔予測の記録〕の適合を書く）
