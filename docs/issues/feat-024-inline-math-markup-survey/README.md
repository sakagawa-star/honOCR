# feat-024: インライン数式のマークアップ欠落の影響範囲調査

- **種別**: feat（**調査案件**。md の修正は行わない）
- **ステータス**: **In Progress**（`roadmap.md` は**第3版**〔update-008「パイプラインの切り方」に伴う切り直し。2026-09-07 人レビュー承認済み〕。**M1 完了**／**M2 中止**／**M2-1 完了**〔出力値は合格・予測記録の手順逸脱あり〕。**M3 実施中**〔段3: 層別標本抽出と原本ページの逆引き。2026-09-08 文書の人レビュー承認、ステップ6 実行中〕）
- **起票日**: 2026-09-06（2026-09-07 に新フロー〔ロードマップ＋マイルストーン〕で再構成）
- **対象書籍**: 『プログラミングのための確率統計』（`{BASE2}` = `/home/sakagawa/work/確率統計`）**全10章**
- **前提案件**: feat-021（Q&A 見出し74件の原本突合）/ feat-023（意味に影響する数式記号の欠落2件の修正）

## 1. 概要

feat-021 が検出した `D4`（数式の不一致）18件のうち、feat-023 が意味に影響する2件を修正し、
**Q&A 見出し内の残件は 16 件**になった。しかし**本文（見出し以外）の同種の欠落は未調査**である
（feat-023 `design.md` §12-4 の引き渡し）。

本案件は、確率統計の全10章について「原本で数式として組まれている記号が md 上で数式マークアップを
失っている量」を、**機械的な候補抽出と標本抽出による母比率推定**で測定する。
**md の修正は行わない**（修正の要否はユーザーが本案件の報告を見て判断する）。

計画の内容は `roadmap.md` を、調査結果と実測値は `survey_notes.md` を参照する。

## 2. マイルストーン一覧と状態

`roadmap.md` 第3版の段（パイプラインの1段 = 1マイルストーン）に対応する。

| ID | 段 | 名称 | フォルダ | 状態 |
|---|---|---|---|---|
| **M1** | 段1 | 候補抽出 CLI の実装と母集団の確定 | `m1-scan-cli/` | **完了**（2026-09-07） |
| **M2** | — | （第1版・第2版）母集団の検証と逆引き可能性の判定 | `m2-population-check/` | **中止**（2026-09-07。理由は `roadmap.md` §7・§11.1） |
| **M2-1** | 段2 | 新規手段の単体疎通 | `m2-1-means-smoke/` | **完了**（2026-09-07。**出力値は合格／予測記録の手順逸脱あり**。`m2-1-means-smoke/experiment_log.md`） |
| **M3** | 段3 | 層別標本抽出と原本ページの逆引き | `m3-sampling/` | **実施中**（2026-09-08 文書の人レビュー承認。層G → 層L → 層C の順に実行） |
| **M4** | 段4 | 原本ページの切り出しと突合（判定） | `m4-collation/` | 未着手 |
| **M5** | 段5 | 母比率の推定と報告 | `m5-estimation/` | 未着手 |

## 3. 実施記録

| 日付 | 工程 | 結果（1行要約） | 判定 | 承認 | 詳細 |
|---|---|---|---|---|---|
| 2026-09-06 | 起票・調査 | 案 F を調査案件として起票。調査範囲・測定方法・ゴールをユーザーが決定 | — | ユーザー指示「案Fを着手してください」 | `survey_notes.md` §1.4 |
| 2026-09-06 | 旧構成での文書作成とレビュー | 案件フォルダ直下に `requirements.md` / `design.md` / `criteria.md` を作成し、Codex レビュー4回（A→B→**C**→B）で高・中ゼロに収束。**全文ゲートが母集団の source の誤り（content_list ではなく md であるべき）を検出** | 収束 | 人レビュー前に中断 | `reviews/codex-01〜04.result.md` |
| 2026-09-07 | 新フローでの再構成 | `CLAUDE.md`「マイルストーン分割」規定（update-006）に従い、`roadmap.md` と `survey_notes.md` に再編。旧3文書は削除（git 履歴に残る） | — | ユーザー指示「新ルールに則ってやり直して」 | `roadmap.md` の改訂履歴（第3版では §11） |
| 2026-09-07 | ステップ3: ロードマップのレビュー | Codex レビュー8回（A→B×5→**C**→B）で高・中ゼロに収束。高5件・中2件を反映。**06〜08 は同一原因の派生のため行き詰まり検出を適用して分岐を取り下げ**、**全文ゲート（11）が前提13 と M4 の出力先の非両立を検出** | **収束** | **2026-09-07 ユーザー承認**（「こちらはokです。ステップ4に進んでください」） | `reviews/codex-05〜12.result.md` |
| 2026-09-07 | M1 ステップ4〜5: 文書作成とレビュー | `m1-scan-cli/requirements.md`（217行）・`design.md`（555行）を作成。Codex レビュー3回（A→B→**C**）で高・中ゼロに収束。**高1件（`scripts/` 配置が規程に根拠を持たない）は規程側の割れが原因のため、別案件 update-007 で `CLAUDE.md` を明確化して解消**（コミット `12d7a1d`）。中1件（出力の原子性）は §5.1 と ADR-M1-7 を新設して解消 | **収束** | **2026-09-07 ユーザー承認**（「こちらはokです。実装を開始してください」） | `reviews/codex-13〜15.result.md` |
| 2026-09-07 | M1 ステップ6〜7: 実装と手動テスト | `scripts/scan_plain_math.py`（310行）と `tests/test_scan_plain_math.py`（T-01〜T-24・244行）を Sonnet サブエージェントが実装。**完了判定6項目すべて合格**: 260 passed（既存236＋新規24）／走査結果が `survey_notes.md` §5 の確定値と全10章×3層で一致／`(章名, オフセット)` の衝突0件／決定性・入力順非依存（本体が独立に再確認）／TSV の列数が全行7／**`{BASE2}` の既存データ 3,551ファイルが SHA-256 不変**（`DIFF_EXIT=0`）。**母集団が確定**（層G 291 / 層L 1,408 / 層C 2,925 = **4,624**、母集団外の表内 158）。想定外事象なし | **合格** | **2026-09-07 ユーザー承認**（「okでした」） | `tests/results/feat-024_test_result.txt`、`{BASE2}/ocr/collation/feat-024_candidates.tsv` |
| 2026-09-07 | M2 ステップ4〜5: 文書作成とレビュー | `m2-population-check/requirements.md`・`design.md` を作成。**M2 は数値判定を伴うため requirements のレビュー収束が criteria lock**。Codex レビュー**8段階**（A→B×4→**C**→B×2）で高・中ゼロに収束。高5件・中4件を反映。**全文ゲートが受け入れ基準4つの欠落（Claude Code の編集ミス）を検出** | **収束** | **2026-09-07 ユーザー承認**（「こちらはokです。実装を開始ししてください」） | `reviews/codex-16〜17.result.md` |
| 2026-09-07 | M2 ステップ6: 実装と検査 | スクリプト2本を Sonnet が実装（260 passed）。検査1 **合格**（全10章で対が偶数）。検査2 `covered` 7 / `not_covered` 9 / `heading_not_found` **0**（合格条件を満たす）。検査3 逆引き成功率 **93.3%**（≥90%）。**抜き取り確認で 1件 NG** | **No-Go** | **2026-09-07 ユーザー判断「No-Go として再計画」** | `m2-population-check/experiment_log.md` |
| 2026-09-07 | M2 の再計画（1回目） | NG の原因は**逆引きの欠陥ではなく確認手段の記述の不備**（`context` が md 上でブロック境界をまたぐため、ブロック単位の `text` に収まらない）。確認手段を読み替えれば合格するが、**実測後の基準変更は事後解釈の禁止に抵触する**ため No-Go とした。影響範囲は「マイルストーンの目的・完了条件の問題」で、**戻る場所は当該 `requirements.md` から作り直し → ステップ5**。`roadmap.md` の改訂は不要（前提・分割・順序に問題なし）。検査1〜3 の実測は有効で、やり直すのは抜き取り確認のみ。**この時点の「改訂は不要」という判断は同日の追加調査により更新された（次行）** | — | ユーザー判断 | `m2-population-check/experiment_log.md` の「判定」節 |
| 2026-09-07 | M2 の再計画（1回目・続き）: 検証手段の方針決定と `roadmap.md` の改訂 | 検証手段を設計し直すにあたり実データを調べたところ、**md と content_list の対応だけでは決着しない型が実在する**ことが判明した（原本の図キャプションのように content_list に対応テキストを持たない要素があり、抜き取り対象6件中1件がこの型）。**ユーザーが「機械による全件検査＋原本の切り出し画像による目視確認」を決定**。これにより M2 の入力に**原本 TIF** が加わるため、**`roadmap.md` を第2版に改訂**（前提14 の新設・M2 の入出力と完了条件(4)・判断の行き先・M3 の開始条件など）し、**ステップ3（Codex → 人）からやり直す**。前日の「`roadmap.md` の改訂は不要」という判断を更新した。原本ページ全体の切り出しは既存の `scripts/crop_blocks.py`（`--margin 1000`）で追加実装なしに行えることを実データで確認済み | — | ユーザー決定（方針の選択） | `roadmap.md` の改訂履歴 第2版の行（第3版では §11） |
| 2026-09-07 | ステップ3: ロードマップ（第2版）のレビュー | Codex レビュー4回（A→B→**C**→B）で高・中ゼロに収束。高2件・中1件を反映（M3 の開始条件を M2 の Go 条件に統一／再計画の戻り先の記録の矛盾を解消／保留リストの再測定の扱いを明確化）。**全文ゲートが `README.md`・`experiment_log.md` との矛盾を検出** | **収束** | **2026-09-07 ユーザー承認**（「こちらはok。次のステップに進んでください」） | `reviews/codex-18〜21.result.md` |
| 2026-09-07 | M2 ステップ4〜5: 文書（第2版）の作成とレビュー | `requirements.md` / `design.md` を第2版に改訂（**検査4 の新設**: 4-A 機械検査〔全件〕＋ 4-B 原本画像による目視）。Codex レビュー**7回**（A→B×2→**C**→B×3）で高・中ゼロに収束。高4件・中4件を反映。**全文ゲートが 4-B の同定の甘さ（同一ページの別の出現で OK にできる）と画像の同名衝突を検出**。最大の指摘は「**4-A は規則確定前に同じ56件で予備確認しており、合否条件にできない**」で、4-A を測定・対象選定に位置づけ直し、**判定は 4-B（原本）に置いた**（ADR-M2-8） | **収束** | **人レビュー通過**（2026-09-07）。ただし**実装は開始せず**、下記の方針転換により第3版へ移行 | `reviews/codex-22〜28.result.md` |
| 2026-09-07 | 方針転換（ユーザー主導） | ユーザーが「行き詰まっている。これだけの工数で判断基準も出ていないのは目標・マイルストーンの切り方がダメ」と判断。3日で最終出力の成分が **0 個**しか確定していない（M2 の大半が、最終出力の式に項を持たない段〔逆引き〕の検証の検証に費やされた）ことを確認し、**`CLAUDE.md` のロードマップ規定自体を直す**方針に転換。feat-024 をそのモデルケースと位置づけた | — | ユーザー決定 | `docs/issues/update-008-pipeline-stage-rule/README.md` §1.1〜1.2 |
| 2026-09-07 | update-008 の完了と第3版への移行の決定 | update-008（パイプラインの切り方の明文化）が Closed。その判断 E(1)「本案件の反映後、`roadmap.md` を第3版で切り直す」により、**`roadmap.md` 第2版と M2 の `requirements.md` / `design.md` 第2版（検査4）は実装せずに第3版へ移行する**。M1 の候補抽出 CLI と M2 の逆引きスクリプト・実測（検査1〜3）は段として再利用し、**完了済みの記録（M1）は変更しない** | — | **ユーザー決定**（update-008 §5 判断 E(1)） | `docs/issues/update-008-pipeline-stage-rule/README.md` §5 |
| 2026-09-07 | ステップ2: ロードマップ（第3版）の作成 | 新規定「パイプラインの切り方」に従って切り直し。**最終出力の成分 6**（独立: 層ごとの `p̂_i`・`n'_i`。従属は式）／**入力の次元 10,352,855,692**（md 517,974 ＋ 原本 TIF 10,351,879,984 ＋ content_list 457,734。実測）／**段の表 5 段**（段1 候補抽出〔完了〕・段2 新規手段の単体疎通・段3 標本抽出と逆引き・段4 切り出しと突合・段5 推定と報告）／段ごとの想定値と外れの幅を記載。**M2 は中止**（決める項を持たない段に判定を積む設計であったため）、**M2-1 を挿入**（新規手段の疎通）、標本数を `n_i = 50`（合計 150 件）に確定 | — | （レビュー前） | `roadmap.md` §11 |
| 2026-09-07 | ステップ3: ロードマップ（第3版）のレビュー | Codex レビュー4回（A→B→**C**→B）で高・中ゼロに**収束**。高2件・中2件を反映。**A が最終出力の計算関係の矛盾（段4 の `p̂_i` と `k_i` の取り違え、段5 の入力に `k_i` が無い）と、段4 の入力からの `bbox` の欠落を検出**し、独立成分を計数 `(k_i, n'_i)` に統一・入力の次元を `bbox` 込みで実測し直した（content_list 482,018 / 合計 10,352,879,976）。**全文ゲート（C）が `survey_notes.md` §8 との再測定方針の矛盾と、外れ時の参照先が履歴節である誤りを検出** | **収束** | **2026-09-07 ユーザー承認**（「こちらはokです。次のステップに進んでください」）。**§6 の想定値と外れの幅が criteria lock** | `reviews/codex-29〜32.result.md` |
| 2026-09-07 | M2-1 ステップ4〜5: 文書作成とレビュー | `m2-1-means-smoke/requirements.md`（113行）・`design.md`（450行）を作成（新規2本の CLI〔層別標本抽出・推定〕の仕様、既存 `locate_candidates.py --input-sample` の実行手順、疎通入力 `k=1, n'=2, N=291` と手計算値、ADR 4件）。Codex レビュー4回（A→B×2→**C**）で高・中ゼロに**収束**。高2件・中2件を反映（**A が NFR の 60 秒制約とロードマップの『性能測定は範囲外』の矛盾**と、**`--input-sample` の出力が入力標本を引き継ぐかを確認していない点**〔別の候補行が出ても疎通成功になる〕を検出。B が手計算値の参照先の誤りを検出）。**全文ゲートは指摘ゼロで通過** | **収束** | **2026-09-07 ユーザー承認**（「こちらはokです。実装を開始してください」） | `reviews/codex-33〜36.result.md` |
| 2026-09-07 | M2-1 ステップ6: 実装と実行 | Sonnet サブエージェントが `sample_candidates.py`・`estimate_rates.py`・`smoke_counts.tsv` を新設し、`design.md` §7 の手順1〜5 を実行（想定外事象なし）。**3 手段とも疎通**: 抽出 → 2行7列・`chap03`/offset 23628・`kind=G`・`location=body`／逆引き → 先頭7列が入力と完全一致・`stage=block`・`page_idx=20`・既存スクリプト不変／推定 → **6 値すべてが手計算値と完全一致**（相対差 0）。境界4通りは終了コード1・出力なし。再実行はいずれもバイト一致（本体が独立に再確認）。`uv run pytest -v` **260 passed**。`{BASE2}` の既存データ4ディレクトリは変更 0 件 | **出力値は合格** | — | `tests/results/feat-024_test_result.txt`、`m2-1-means-smoke/experiment_log.md` |
| 2026-09-07 | M2-1 ステップ7: 手動テストと実験ログの是正 | ユーザー指摘により、**実行前の予測に「事前の幅を持たない点予測」（`chapter`・`location`・`stage`）が混ざっており判定に使えないこと**、および**それを「確率的に正常」と述べたのが事後解釈であること**を認め、実験ログを書き換えた（予測そのものは改変せず追記で区別）。書き換えを Codex レビューに通し、**予測を3手段まとめて実装着手前に記録したのが `design.md` §8 違反であること**が判明。**ユーザー判断は案A（逸脱として記録し再実行しない。「再発させるな」）**。再発防止として是正4〜7 を定めた（実行単位ごとに直前に予測／実装と実行の分離／予測時点でのコミット／状態のその場更新）。書き換えの Codex レビューは6回（A→B×2→**C**→B×2）で高・中ゼロに**収束**（全文ゲートは高3件〔手順逸脱・不改変の証明不能・案件状態の記録の遅れ〕を検出） | **完了（出力値は合格／手順逸脱あり）** | **2026-09-07 ユーザー判断「案A」**（書き換えの人レビューは待ち） | `reviews/codex-37〜42.result.md`、`m2-1-means-smoke/experiment_log.md` |
| 2026-09-08 | M3 ステップ4〜5: 文書作成とレビュー | `m3-sampling/requirements.md`・`design.md` を作成（**新規実装なし**。既存の `sample_candidates.py` と `locate_candidates.py --input-sample` を層ごとに実行する設計。層 G→L→C の順に「予測→コミット→抽出→逆引き→検証→照合」を1巡。シードは層ごとに固定）。Codex レビュー4回（A→B×2→**C**→B）で高・中ゼロに**収束**。高1件・中1件を反映（**A・B が是正7〔状態のその場更新〕の反映不足**、**全文ゲートが「ユーザー承認前に完了へ更新する設計」**を検出し、状態更新を4時点に拡張した） | **収束** | **2026-09-08 ユーザー承認**（「こちらはok。次のステップに進んでください」） | `reviews/codex-45〜48.result.md` |
| 2026-09-08 | M3 ステップ6: 層G の実行 | 予測をコミット `23b75c2` に保存してから実行（是正6）。抽出 50 件（シード 20260908）、逆引き **成功 48/50 = 96.0%**（`block` 38 / `page` 10 / `none` 2〔すべて `page_mismatch`〕）、`location` は `body` 44 / `footnote` 6。**検証12項目すべて合格**（列数・`kind`・`table` 除外・重複なし・候補 TSV との一致・先頭7列の一致・`stage` の値域・`page_idx`・決定性・既存コード不変）。予測（成功 45±4 件・`footnote` 7 件）と照合し**いずれも幅の内側**。`footnote` 6 件が全件成功し、前提11 の未検証部分（`footnote` の成功率）の初の実測となった | **照合一致** | — | `m3-sampling/experiment_log.md` の層G |
| 2026-09-08 | M3 ステップ6: 層L の実行 | 予測をコミット `cd2d505` に保存してから実行。抽出 50 件（シード 20260918）、逆引き **成功 45/50 = 90.0%**（`block` 37 / `page` 8 / `none` 5〔すべて `page_mismatch`〕）、`location` は `body` 47 / `footnote` 3。**検証12項目すべて合格**。予測（成功 47±3 件・`footnote` 2 件）と照合し**いずれも幅の内側** | **照合一致** | — | `m3-sampling/experiment_log.md` の層L |

## 4. 関連ファイル

- `roadmap.md` — 案件全体の計画（**第3版**。目標と最終出力の成分・前提・入力の次元・段の表・段ごとの想定値・マイルストーン一覧）
- `survey_notes.md` — 調査記録（実測値・候補の定義・逆引きの実測・統計手法）
- `reviews/` — Codex レビューの結果（連番は案件通し）
- `docs/issues/feat-021-qa-heading-source-collation/collation_summary.md` — `D4` 18件の一覧
- `docs/issues/feat-023-qa-heading-math-semantics/design.md` §12 — 引き渡し事項
- `{BASE2}/ocr/collation/feat-021_qa_headings.md` — 74件の詳細記録（**リポジトリ外**）
- `scripts/crop_blocks.py` — 原本 TIF からブロック領域を切り出す CLI（feat-021 で新設）
