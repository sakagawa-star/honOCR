# codex-59: feat-024 ロードマップ第4版レビュー（A: 初回レビュー）

- **日付**: 2026-09-08 ／ **対象ファイル**: `roadmap.md`（第4版。関連: `README.md`・各マイルストーンの `experiment_log.md`）
- **ストリーム名**: `rev-honocr-feat-024`（pane `wH:p17`）
- **依頼種別**: **A（初回レビュー）**／**直前に `/new`**: **Yes**
- **ゲート状態**: **未実施**（本レビュー単位＝ロードマップ第4版は新しい単位。前単位の C は `codex-56.result.md`）
- **指摘数**: 高 **2** / 中 **0** / 低 0
- **収束判定**: **未収束（次: 全件反映 → B）**
- **トークン実測**: `total_tokens` 496,197（`input_tokens` 491,688 / `cached_input_tokens` 392,960 / `output_tokens` 4,509 / `reasoning_output_tokens` 2,576）
- **rollout jsonl**: `~/.codex/sessions/2026/09/08/rollout-2026-09-08T15-12-15-01a07fa5-4f40-7971-9cd4-f4ea01178d91.jsonl`

## レビュー単位について

本回から**新しいレビュー単位**である。ユーザー判断（2026-09-08「`roadmap.md` §6 の項目 4 は指針として間違っていた。調査・計画（ステップ 2〜3）からやり直せ」）により、人レビューを通過済みの第3版を改訂したため、`CLAUDE.md`「収束」の定義（人レビューを通過した文書一式が改版されて再びレビュー工程に入るときは、改版後を新しいレビュー単位とする）に従い、`/new` を送って A から開始した。

## 依頼内容

`docs/REVIEW_CRITERIA.md` の基準に従うロードマップのレビューに加え、次を判定させた: (1) `CLAUDE.md`「ロードマップの改訂（再計画）」が改訂に課す要件の充足（同一ファイルの上書き・改訂履歴の内容・ID を振り直さない・完了済み記録を変更しない・前提の典拠の更新）(2) 改訂後の §6 の段4 の想定値が「パイプラインの切り方」の要求（値・根拠の種別・**数値の**外れの幅）を満たすか (3) 遡及適用しない扱いが「実験・検証の進め方」手順4（事後解釈の禁止）と整合するか (4) §11.3 に申告した汚染が改訂の妥当性に与える影響 (5) 実施済み M1・M2-1・M3・M4 の記録との整合。

## 指摘（高 2 件）

### 高1: M4 の完了条件が、削除済みの旧 §6 の「4 項目」を参照したままである

- **箇所**: `roadmap.md:264`
- **内容**: 第4版の §6 の段4 は「候補規則の特異度」「`k_i` の予測精度」を含む 5 項目になったため、M4 の完了条件が一意に定まらない。
- **修正案（Codex）**: M4 の実施済み判定は第3版 lock 時点の 4 項目で固定し第4版を遡及適用しないこと、第4版の基準は将来の実行にのみ用いることを、M4 の完了条件に明記する。

### 高2: `README.md` が現行の計画を第3版としており、実体（第4版）と矛盾する

- **箇所**: `README.md:4`・`README.md:23`
- **内容**: M4 のログ・要求・設計が第3版の criteria lock を参照すること自体は正しいが、**案件の現行計画まで第3版とするのは誤誘導**である。
- **修正案（Codex）**: README の現行ロードマップ表記を第4版に更新し、**M4 の実施記録だけが第3版 lock に基づき保存される**と区別する。

## 指摘なしと確認された点

- 第4版は同一 `roadmap.md` の上書きであり、git 履歴にも連続して残っている
- §11.3 は覆った前提・外れた段と実測・変更内容・上がった手・ID を振り直していないこと・前提12 の典拠更新を記録している
- M1・M2-1・M3 の完了記録は今回の差分で変更されていない。M4 は未承認のため、実行記録の追記は「完了済み記録の変更」に当たらない
- 改訂後の §6 の段4 の `k_i` の予測精度は、実行直前に `p̂_pred` を確定する許容範囲内で、**出力想定・根拠の種別・95% 予測区間という数値的な外れの枠を固定**しており、要件を満たす
- 遡及適用しない扱いは事後解釈の禁止と整合する。**層G の Wilson 区間を見た汚染により、新基準を既実行の M4 の妥当性・合否の根拠にはできない**。一方 §11.3 はその限界を申告し、新基準を遡及判定に使わないため、**改訂自体を無効にするものではない**

## 反映（Claude Code 本体・2026-09-08）

| 指摘 | 反映内容 |
|---|---|
| 高1 | `roadmap.md` の M4 完了条件(2) を「**M4 の判定は、第3版で lock されていた §6 の段4 の 4 項目**（切り出しの生成率・判定不能率・有効標本数・各層の判定の内訳 `0 < k_i < n'_i`）**で固定する**」に改め、「第4版で改めた項目（候補規則の特異度・`k_i` の予測精度）は M4 に遡及適用せず、以後に実行する段にのみ用いる」ことを明記した |
| 高2 | `README.md` のステータス行を第4版（ステップ3 レビュー中）に更新し、「**M4 の実施記録と判定は第3版の criteria lock に基づく**——第4版の §6 は M4 に遡及適用しない」と区別した。マイルストーン一覧の前書きも「段の構成は第3版から第4版で変わっていない（第4版の改訂は §6 の想定値に限る）」に改めた |
