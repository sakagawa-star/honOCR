# codex-57: feat-024 要調査事項の記録レビュー（B: 解消確認・層C の追記）

- **日付**: 2026-09-08 ／ **対象ファイル**: `follow_up_items.md`（関連: `m4-collation/judgments.tsv`・`m4-collation/experiment_log.md`・`m3-sampling/experiment_log.md`）
- **ストリーム名**: `rev-honocr-feat-024`（pane `wH:p17`。既存エージェントを再利用）
- **依頼種別**: **B（解消確認）**／**直前に `/new`**: **No**
- **ゲート状態**: **実施済み**（当該 C は `codex-56.result.md`）
- **指摘数**: 高 **2** / 中 **0** / 低 0
- **収束判定**: **未収束（次: 全件反映 → B）**
- **トークン実測**: `total_tokens` 1,126,092（`input_tokens` 1,116,844 / `cached_input_tokens` 899,328 / `output_tokens` 9,248 / `reasoning_output_tokens` 5,886）
- **rollout jsonl**: `~/.codex/sessions/2026/09/08/rollout-2026-09-08T12-23-08-01a07f0a-79cc-7330-a5d9-01cdd0365960.jsonl`

## 依頼内容

層C の M4 実測完了に伴う 3 つの変更（(1) 冒頭の「層C は未実行」の記述を 3 層実行済みに改めた (2) §1 の `not_in_crop` の実績を 3 層で更新 (3) §8〜§10 の追加）について、変更点の問題、「この文書の扱い」の 4 原則への適合、典拠の整合、既存 §1〜§7 および「既に他の文書にある事項」との重複を確認させた。

## 指摘（高 2 件）

### 高1: §8 の「原本で立体と確認できた 3 件はいずれも `正当`」が判定 TSV と矛盾する

- **内容**: chap05 `off=41348` は `judgments.tsv` で **`劣化`** である。
- **修正案（Codex）**: `off=41348` は「`V` 自体の立体性は確認されたが、別の書体情報の欠落により標本全体のラベルは `劣化`」と分けて記述する。

### 高2: §8 の全章 `\mathrm` 出現数に典拠がない（層C 標本外の新規測定）

- **内容**: 提示された M3/M4 のログ・TSV に典拠がなく、層C の標本の外で新たに測った量である。本文書の「この文書の扱い」の原則1（本案件では調査しない）・原則3（実測された事実と典拠のみ）に適合しない。
- **修正案（Codex）**: 当該の全章集計を削除し、層C の標本で確認済みの識別子だけに限定する。全章集計を残すなら別案件で扱い、再現可能な典拠を保存する。

## 整合を確認できた点（指摘なし）

- §1 の `not_in_crop`: 層G 0 / 層L 1 / 層C 0、106 件中 1 件（0.9%）で既存記録と一致
- §9 の `page_mismatch` 14/15、§10 の `\boldsymbol` 集計・層C の標本 21 件・`劣化` 2 件は、既存の M3/M4 記録と整合
- 「この文書の扱い」の 4 原則: 新しい判定基準・手順・マイルストーンは導入されていない。既存の既知事項は参照に留まり、参照先・節番号の切れもない

## 反映（Claude Code 本体・2026-09-08）

| 指摘 | 反映内容 |
|---|---|
| 高1 | §8 の典拠の行を書き換え、3 件の**演算子の立体性の確認**と**標本のラベル**を分けて記述した（chap01 `off=1454`・chap04 `off=65180` が `正当`、chap05 `off=41348` が `劣化`。後者の理由は同じ数式内の `a`・`X` の太字の脱落）。あわせて、同じ誤りを含んでいた**観察の行**（「この型の書体情報の脱落は `正当` として計上される」→「ラベルの根拠にならない。§6.3 の 3 型の脱落が他に無ければ `正当`、有れば `劣化`」）と**本案件での扱いの行**（「§6.3 のとおり `正当` として計上する」→「ラベルは §6.3 の 3 型のみで決め、立体／イタリックの別は判定に使わない」）も修正した |
| 高2 | §8 の「後続案件が使える入力」から全章の `\mathrm` 出現数を**削除**し、層C の標本で確認できる識別子と参照先（`feat-024_m3_located_C.tsv` の `symbol` 列・`judgments.tsv`）に限定した。「この型の件数は本案件では測定していない。本書全体での出現頻度も測っていない」と明記した |
