# codex-31: feat-024 ロードマップ（第3版）レビュー（C: 全文ゲート）

- **日付**: 2026-09-07
- **対象ファイル**: `docs/issues/feat-024-inline-math-markup-survey/roadmap.md`（第3版）
- **ストリーム名**: `rev-honocr-feat-024`
- **依頼種別**: **C（全文ゲート。レビュー単位につき1回）**
- **直前に `/new` を送ったか**: **Yes**
- **ゲート状態**: **本回が全文ゲート**
- **指摘数**: 高 0 / 中 **2** / 低 0
- **収束判定**: **未収束（次: 全件反映 → B。2回目の `/new` はしない）**
- **トークン実測**: `total_tokens` 397,863（`input` 394,165 / `cached_input` 319,488 / `output` 3,698 / `reasoning` 1,994）
  - rollout jsonl: `~/.codex/sessions/2026/09/07/rollout-2026-09-07T17-13-58-01a07aee-6497-7251-9b9d-de5272fe04a5.jsonl`（`/new` 後の新しい会話）

---

## 依頼文

> docs/REVIEW_CRITERIA.md の基準に従い、以下のドキュメントをレビューせよ: docs/issues/feat-024-inline-math-markup-survey/roadmap.md 。本ドキュメントは対話ベースのレビューを経ている。今回は最終ゲートとして、文書全体の整合性（セクション間の矛盾、参照切れ、抜け）を含めて全文を確認して。

---

## 結果（[AGENTS.md適用] マーカーあり）

### 中

1. **`roadmap.md` §9 の保留方針が `survey_notes.md` §8 と矛盾している**
   - ロードマップは「数学記号の md 基準での再測定は行わない」とする一方、調査記録は「報告時に数え直す」としている。このままでは M5 が追加測定を行うか判断できない
   - 修正案: どちらかを正とし、他方も同じ方針へ更新する

2. **外れ値発生時の参照先が不適切である**
   - §6 は「§11.2 の梯子に従う」とするが、§11.2 は**今回の改訂に至った履歴**であり、将来の外れ値に対する一般的な再計画手順を定義していない
   - 修正案: `CLAUDE.md`「ロードマップの改訂（再計画）」を直接参照し、必要なら外れ時の戻り先を簡潔に明記する

### 高・低

なし。

### そのほかの確認事項（指摘ではない）

- 案件フォルダ構成、M2 中止と M2-1 挿入、最終出力から段5 までの計数・推定のデータフロー、主要な参照先の存在は整合している

---

## 反映（Claude Code 本体）

| 指摘 | 反映内容 |
|---|---|
| 中1 | **ロードマップ側（再測定しない）を正とした**（第2版の codex-20 で確定した方針であり、`roadmap.md` は人レビューを通過している）。`survey_notes.md` §8 の「報告時に数え直す」を「本案件では数え直さない（`roadmap.md` §9 の方針。M5 では content_list 基準の値を参考値として示し、基準の違いを明記する）」に更新し、§10 の未検証事項4 にも「本案件では測定しない」を追記した。`roadmap.md` §9 にも `survey_notes.md` を更新した旨を記した |
| 中2 | §6 の参照先を **`CLAUDE.md`「ロードマップの改訂（再計画）」の梯子**（手1 段の手段・手順の修正 → 手2 段の分割 → 手3 構成の見直し → 手4 目標の変更）に改め、**手を飛ばす・戻すにはユーザーの指示を要すること**と、**「行き詰まり検出」により想定値の外れが 2 回続いたら同じ扱いになること**を明記した。§11.2 は「本第3版に至った経路の記録であり将来の手順ではない」と位置づけを明示した |
