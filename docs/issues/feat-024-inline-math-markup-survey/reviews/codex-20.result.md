# Codex レビュー結果 codex-20

- **日付**: 2026-09-07
- **対象ファイル**: `docs/issues/feat-024-inline-math-markup-survey/roadmap.md`（第2版）
- **ストリーム名**: `rev-honocr-feat-024`
- **依頼種別**: **C（全文ゲート）**
- **直前に `/new` を送ったか**: **Yes**
- **ゲート状態**: **実施済み**（本ファイルが当該 C の結果）
- **指摘数**: 高 **1** / 中 **1** / 低 **0**
- **収束判定**: **未収束（次: B〔全件反映のうえ解消確認。2回目の `/new` はしない〕）**
- **トークン実測**: `total_token_usage` = input 243,275（cached 176,640）/ output 3,319（reasoning 1,794）/ total 246,594
- **rollout jsonl**: `~/.codex/sessions/2026/09/07/rollout-2026-09-07T12-08-18-01a079d6-8b32-7761-99b4-7c27728da22d.jsonl`

---

## 依頼文

```
docs/REVIEW_CRITERIA.md の基準に従い、以下のドキュメントをレビューせよ: docs/issues/feat-024-inline-math-markup-survey/roadmap.md 。本ドキュメントは対話ベースのレビューを経ている。今回は最終ゲートとして、文書全体の整合性（セクション間の矛盾、参照切れ、抜け）を含めて全文を確認して。
```

---

## 結果（原文）

[AGENTS.md適用]

### 高

- `roadmap.md` §8 は、M2 の No-Go を受けて「ロードマップを改訂し、原本画像を使う検証へ変更した」と
  しています。一方、案件の状態記録である `README.md` §3（行43）および M2 の実験ログは、影響範囲を
  「M2 の `requirements.md` から作り直し」とし、`roadmap.md` の改訂は不要と明記しています。
  再計画の戻り先が矛盾しており、次に行うべきレビュー工程が一意に決まりません。
  ユーザーが後から原本画像利用を決定してロードマップ改訂へ変更したのであれば、その決定と時系列を
  README の実施記録にも追記し、旧判断を更新してください。変更していないなら §8 の改訂理由を
  M2 文書の再作成に合わせて修正してください。

### 中

- §7 は「候補規則で拾えない数学記号」を M5 で件数のみ示すと定めていますが、`survey_notes.md` §8 の値は
  content_list 基準で、md 基準の再測定は未実施です。しかも M5 の入力概要（§5, M5）は M4 判定 TSV・
  M3 標本一覧・母集団サイズのみで、md 基準の再測定に必要な成果物 md が含まれていません。
  M5 の入力に成果物 md を追加するか、M1 の出力として当該集計を追加し、再測定結果をどの成果物へ
  保存するかも明確にしてください。

上記以外では、ロードマップ内のマイルストーン順序・Go/No-Go 分岐・参照先（`survey_notes.md`、
feat-021/023、`crop_blocks.py`）に致命的な参照切れや矛盾は見つかりませんでした。

---

## 対応

| # | 重要度 | 指摘 | 対応 |
|---|---|---|---|
| 1 | 高 | 再計画の戻り先が `roadmap.md` §8 と `README.md`・`experiment_log.md` で矛盾 | **反映**。事実は「No-Go 直後は改訂不要と判断したが、同日の追加調査（md と content_list の対応だけでは決着しない型の実在）とユーザーの方針決定により改訂が必要になった」である。`README.md` のステータスを更新し、実施記録に**決定と時系列の行を追加**、旧行には「この判断は次行で更新された」旨を明記。`experiment_log.md` の「次の行き先」表の該当行に更新の注記を付し、**追記節**（更新の理由・決定・分割は不変であること）を追加した |
| 2 | 中 | 保留リストの再測定に必要な入力が M5 に無い | **反映**。**md 基準での再測定は行わない**ことを §7 に明記した（`survey_notes.md` §8 の content_list 基準の値を典拠つきの参考値として示し、基準の違いを報告に明記する）。したがって **M5 の入力に成果物 md を加えない**ことも併記し、M5 の入力概要と整合させた |
