# codex-37: feat-024 M2-1 実験ログ（書き換え）レビュー（A: 初回レビュー）

- **日付**: 2026-09-07
- **対象ファイル**: `m2-1-means-smoke/experiment_log.md`（関連: `roadmap.md` §6、`m2-1-means-smoke/requirements.md`・`design.md`）
- **ストリーム名**: `rev-honocr-feat-024`
- **依頼種別**: **A（初回レビュー）**——実験ログの書き換えを対象とする新しいレビュー単位
- **直前に `/new` を送ったか**: **No**
- **ゲート状態**: **未実施**
- **指摘数**: 高 **1** / 中 **1** / 低 0
- **収束判定**: **未収束（高1 はユーザー判断を要するため保留。中1 は反映済み）**
- **依頼の契機**: ユーザー指示「書き換えは codex レビューを通せよ」（2026-09-07）

---

## 結果（[AGENTS.md適用] マーカーあり）

### 高

1. **3 手段の予測を実装着手前に一括記録しており、手段ごとの「実行直前」記録ではない**
   - ログ自身が明記しており（`experiment_log.md:9`）、`design.md` §8 の「各手段の実行直前」「一括で先に予測しない」と**直接矛盾**する。
     手段間の内容依存がないという説明も、この明示的な手順を免除しない
   - このままでは M2-1 の実行記録を、予測→実行→照合プロトコルに適合した完了記録として扱えない
   - 修正案: 既存の予測・実測を改変せず**手順逸脱として残し**、**ユーザー判断のうえ**、各手段の直前に予測を記録する形で M2-1 を再実行する

### 中

1. **実験ログが `roadmap.md` §6 の判定閾値を再掲している**
   - `experiment_log.md:185`・`:242` の「相対差 ≤ 1×10⁻⁹」、`:81`・`:254` の段3 の数値基準。
     ログの役割は基準への参照・予測・実測・照合であり、基準そのものは `roadmap.md` に一元化する規定（`CLAUDE.md`「ロードマップとマイルストーン文書の粒度」）に反する
   - 修正案: 数値基準を削除し「`roadmap.md` §6 の当該段の想定値と外れの幅を満たす」の参照へ置き換える。予測値・実測値・相対差 0 という実行記録は保持してよい

### 確認事項（指摘ではない）

- 事後追記は予測表の値を置換せず、追記として区別されている
- 点予測を判定から除外し、差異を正常化しない記述は**事後解釈の禁止に適合**している
- ただしファイルは未追跡（git 履歴が無い）ため、「追記前の予測が一切編集されていない」ことを外部証跡で独立に証明することはできない

---

## 反映（Claude Code 本体）

| 指摘 | 反映内容 |
|---|---|
| 高1 | **ユーザー判断待ち**（本体の一存で再実行しない）。ログ冒頭の「3 手段は互いの結果に依存しないため一括で差し支えない」という**正当化を撤回**し、`design.md` §8 に反する**手続き上の逸脱として記録**した（逸脱の事実・予測が実測に引きずられていない根拠・扱いはユーザー判断による旨） |
| 中1 | 4 箇所の数値基準の再掲を削除し、`../roadmap.md` §6 への参照に置き換えた（相対差 0 という実測は保持） |
