# codex-47: feat-024 M3 マイルストーン文書レビュー（C: 全文ゲート）

- **日付**: 2026-09-08
- **対象ファイル**: `m3-sampling/requirements.md`・`m3-sampling/design.md`（関連: `roadmap.md`、`m2-1-means-smoke/experiment_log.md` の是正1〜7）
- **ストリーム名**: `rev-honocr-feat-024`
- **依頼種別**: **C（全文ゲート。レビュー単位につき1回）**
- **直前に `/new` を送ったか**: **Yes**
- **ゲート状態**: **本回が全文ゲート**
- **指摘数**: 高 **1** / 中 0 / 低 0
- **収束判定**: **未収束（次: 反映 → B。2 回目の `/new` はしない）**
- **トークン実測**: `total_tokens` 507,026（`input` 502,765 / `cached_input` 420,352 / `output` 4,261 / `reasoning` 2,510）

---

## 結果（[AGENTS.md適用] マーカーあり）

### 高

1. **M3 をユーザー承認前に「完了」へ更新する設計になっている**（`design.md:131`）
   - 「3 層の集計と判定の直後」に `README.md` と `roadmap.md` の状態を完了へ更新するとしているが、
     機能追加フローのステップ7 では、**完了の状態記録はユーザーの手動テスト・承認後**である
   - このままでは、**M2-1 で問題となった状態記録と実態の不整合を再発する**
   - 修正案: 集計・自動検証後は「実施中（手動テスト・承認待ち）」を維持し、
     **ユーザー承認直後にのみ `README.md` と `roadmap.md` を「完了」へ更新する第4 時点**を A-6 と FR-M3-004 に追加する

### そのほか（指摘ではない）

- 要求・設計間、`roadmap.md` の目的・完了条件、参照先、および M2-1 の是正1〜6 との重大な不整合は無い

---

## 反映（Claude Code 本体）

| 指摘 | 反映内容 |
|---|---|
| 高1 | `design.md` 手順 A-6 の表を**4 時点**に拡張した。時点(3)（集計と検証の直後・手動テストより前）は「**実施中（手動テスト・承認待ち）**」に更新し、`roadmap.md` §7 は値を変えず確認するに留め、**この時点で「完了」と書かない**ことを明記。**時点(4)（ユーザーの手動テストと承認の直後）**を新設し、そこで初めて両文書を「完了」に更新することとした。「『完了』と書けるのはユーザーの承認後だけである」（`CLAUDE.md`「機能追加フロー」ステップ7）も明記。`requirements.md` FR-M3-004 受け入れ基準4 も 4 時点に展開し、(c) に「この時点で『完了』と書いていないこと」、(d) に承認直後の更新を加えた |
