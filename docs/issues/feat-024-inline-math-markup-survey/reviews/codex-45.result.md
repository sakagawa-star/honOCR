# codex-45: feat-024 M3 マイルストーン文書レビュー（A: 初回レビュー）

- **日付**: 2026-09-08
- **対象ファイル**: `m3-sampling/requirements.md`・`m3-sampling/design.md`（関連: `roadmap.md`、`m2-1-means-smoke/experiment_log.md` の是正1〜7）
- **ストリーム名**: `rev-honocr-feat-024`
- **依頼種別**: **A（初回レビュー）**
- **直前に `/new` を送ったか**: **No**
- **ゲート状態**: **未実施**
- **指摘数**: 高 **0** / 中 **1** / 低 0
- **収束判定**: **未収束（次: 反映 → B）**
- **トークン実測**: `total_tokens` 803,704（`input` 795,931 / `cached_input` 616,448 / `output` 7,773 / `reasoning` 4,607）

---

## 結果（[AGENTS.md適用] マーカーあり）

### 中

1. **是正7（案件状態を承認・実装・実行の都度更新）が設計に十分反映されていない**（`design.md:125`）
   - 状態更新を「層 `C` 完了時」に限定している。しかし是正7 は、**人レビュー承認時と実行のたびに** `README.md` の一覧・実施記録および `roadmap.md` の状態欄を更新するよう求めている
   - このままでは、M3 承認後または層 `G` / `L` 実行後に文書上の状態が実態より遅れ、**M2-1 で発生した不整合を再発させる**
   - 修正案: 人レビュー承認後、各層の実行・照合後、M3 完了時の**更新対象・記録内容・順序**を設計手順に明記し、対応する受け入れ基準を FR-M3-004 に追加する

### 高・低

なし。

### 確認できた事項（指摘ではない）

- ロードマップの段3 の目的・開始/完了条件・想定値への参照と整合している
- 要求と設計の対応、I/O、検証・中断規則、範囲外の定義は一貫している
- **是正1〜6 は反映されている**（予測枠の限定、枠外値の明示、層単位の直前記録、実装と実データ実行の分離、予測時点のコミット）

---

## 反映（Claude Code 本体）

| 指摘 | 反映内容 |
|---|---|
| 中1 | `design.md` 手順 A-6 を、**3 時点（(1) 人レビュー承認の直後〔各層の実行より前〕／(2) 各層の照合の記録の直後／(3) 3 層の集計と判定の直後）それぞれで更新する表**に書き換え、各時点の更新対象・記録内容・順序を明記した。「(1) を怠ると文書上は未着手のまま実行済みの記録が存在する状態になる（M2-1 で実際に生じた）」ことも記した。`requirements.md` の FR-M3-004 に**受け入れ基準4**（3 時点それぞれで `README.md` と `roadmap.md` §7 が更新されていること）を追加した |
