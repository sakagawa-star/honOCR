# codex-40: feat-024 M2-1 実験ログ（書き換え）レビュー（C: 全文ゲート）

- **日付**: 2026-09-07
- **対象ファイル**: `m2-1-means-smoke/experiment_log.md`（関連: `roadmap.md` §6・`requirements.md`・`design.md`）
- **ストリーム名**: `rev-honocr-feat-024`
- **依頼種別**: **C（全文ゲート。レビュー単位につき1回）**
- **直前に `/new` を送ったか**: **Yes**
- **ゲート状態**: **本回が全文ゲート**
- **指摘数**: 高 **3** / 中 0 / 低 0
- **収束判定**: **未収束（次: 全件反映 → B。2回目の `/new` はしない）**

---

## 結果（[AGENTS.md適用] マーカーあり）

### 高

1. **直前予測の手順違反**（`experiment_log.md:9`）
   - ログ自身が、3 手段の予測を実装前に一括記録したと明記しており、各実行直前に記録する `design.md` §8 および `CLAUDE.md` 手順2 に不適合
   - 修正案: 再実行しない判断を維持するなら、現状どおり「出力値は満たすがプロトコル適合の完了記録ではない」とし、**M2-1 を完了扱いにしない**

2. **実行前予測が改変されていないことを判定できない**
   - 対象ログは未追跡で git 履歴がなく、事後追記前の予測内容を独立証跡で比較できない。ログ中の説明や既存レビューは状況証拠にはなるが、不改変の検証には不足する
   - 修正案: 今後は予測記録を実行直前にコミット済みファイルへ保存するか、内容ハッシュ・時刻つきの外部証跡を残す。今回は「**不改変は判定不能**」と明記し、完了判定の根拠に含めない

3. **案件状態と実行記録が矛盾し、着手条件にも適合しない**（`roadmap.md:7`・`README.md:29`・`README.md:54`）
   - `roadmap.md` と `README.md` は M2-1 を「未着手」、README の実施記録も人レビュー待ちとしている一方、対象ログは実装・実行済みである
   - 修正案: 実行が人レビュー前だった事実を手続き逸脱として記録し、状態を事実に合わせる

### 観点(c)（指摘ではない）

- ログは判定閾値を `roadmap.md` §6 に参照させ、閾値そのものを再掲していない。実測値・照合結果の記録に留まっており、粒度規定に適合している。参照切れも無い

---

## 反映（Claude Code 本体）

| 指摘 | 反映内容 |
|---|---|
| 高1 | 「完了扱いにしない」という勧告と、**ユーザー判断（案A: 逸脱として記録し完了扱いにする）が分かれた事実**を実験ログの「判定」節に記録した。**判断はユーザーに従い完了扱いとし、状態欄・実施記録に「出力値は合格／予測記録の手順逸脱あり」を併記**した（`README.md`・`roadmap.md`） |
| 高2 | 実験ログの逸脱の記録に「**本ファイルは予測記録時点でコミットされておらず、追記前の予測の不改変を独立の証跡で証明できない。この点は完了判定の根拠に含めない**」を明記。再発防止として**是正6（予測を記録した時点でコミットする）**を追加 |
| 高3 | **事実関係の訂正**: 実行は人レビューの**後**である（2026-09-07 ユーザー「こちらはokです。実装を開始してください」→ その後に実装・実行）。矛盾の原因は**承認と実行を `README.md` に記録しなかったこと**（記録の遅れ）であり、着手条件の違反ではない。`README.md` の実施記録に承認・実装実行・ステップ7 の 3 行を追加し、マイルストーン一覧と `roadmap.md` §7 の状態欄を「完了（出力値は合格／手順逸脱あり）」に更新した。`roadmap.md` の改訂履歴に「状態欄のみの更新であり計画の改訂ではない」旨を記した。再発防止として**是正7（状態は承認・実装・実行のたびにその場で更新する）**を追加 |
