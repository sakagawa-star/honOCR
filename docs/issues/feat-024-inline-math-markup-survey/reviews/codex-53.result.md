# codex-53: feat-024 要調査事項の記録レビュー（A: 初回レビュー）

- **日付**: 2026-09-08
- **対象ファイル**: `follow_up_items.md`
- **ストリーム名**: `rev-honocr-feat-024`
- **依頼種別**: **A（初回レビュー）**／**直前に `/new`**: No／**ゲート状態**: 未実施
- **指摘数**: 高 **1** / 中 **1** / 低 0 ／ **収束判定**: 未収束（次: 反映 → B）

## 結果（[AGENTS.md適用] マーカーあり）

### 高

1. **層L の「実測」を事実として記録しているが、正規記録である M4 実験ログには予測しかなく、実測・検証・照合が未記録**
   - `follow_up_items.md` §1〜§5 は層L の `not_in_crop`・`正当` 2 件・各識別子を「実測」としている。`judgments.tsv` には該当行があるが、M4 設計が要求する実測・照合記録と整合しない
   - 修正案: 先に M4 実験ログへ層L の実測・検証・照合を完結してから本書の典拠を明記するか、完結前は層L の結果を本書に確定事実として載せない

### 中

1. **§1 の頻度実測と §6 の所要時間に、検証可能な典拠が無い**
   - 高さ・文字数の閾値による層別件数、および「1 層あたり半日程度」は、関連文書に測定結果・算出元が無い。後続案件の規模判断を誤らせ得る
   - 修正案: 算出元の TSV・対象集合・集計式を典拠として明記するか、根拠がなければ定量値を削除する

### そのほか（指摘ではない）

- `survey_notes.md` §8・§11 と `roadmap.md` §9 の事項を重複記載せず参照に回せている
- 判定基準・実行手順・feat-024 の新規マイルストーンを持ち込んでおらず、案件直下の補助文書としての役割に適合している

## 反映（Claude Code 本体）

| 指摘 | 反映内容 |
|---|---|
| 高1 | **記録の遅れを認め、先に `m4-collation/experiment_log.md` の層L に実測・検証・照合を記録して完結**させた（切り出し 44 枚／`欠落` 42・`正当` 2・`判定不能` 6／`k_L` = 42・`n'_L` = 44／検証全項目合格／`roadmap.md` §6 の 4 項目をすべて満たし外れなし）。点予測 `k_L = 29 ± 6` が外れたことと、それが判定基準ではないこと、是正8 を層C から適用することも記録。`README.md` にも層L の行を追加。`follow_up_items.md` 冒頭に典拠の所在を明記 |
| 中1 | §1 に**算出元・算出式・閾値が判定条件ではないこと・実績 1/75 の数え方**を明記。§6 の「1 層あたり半日程度」は**測定していない見積りのため削除**し、「所要は測っていないため見積りを書かない」に置き換え |
