# update-004 反映設計書: CLAUDE.md 内部の規定矛盾2件の解消

対象案件: `docs/issues/update-004-fix-review-rule-contradictions/`
調査記録: 同フォルダの `README.md`
反映担当: **Claude Code 本体**（3箇所の文字列置換のみ。Sonnet 委任のオーバーヘッドに見合わない）
テスト: **不要**（コード変更なし。§7）

本書は自己完結している。`/clear` 後でも本書だけで反映作業ができるよう、置換の旧文・新文はすべて全文で示す。

## 1. 変更の概要

| # | ファイル | 変更方式 | 箇所 |
|---|---|---|---|
| 1 | `CLAUDE.md` | 文字列置換 ×3（§3） | 194行目・203行目・77行目 |
| 2 | `docs/BACKLOG.md`・`docs/CHANGELOG.md`・案件 `README.md` | 完了処理のみ（§6） | — |

**上記以外のファイルは変更しない。** 特に `docs/PROJECT_KNOWLEDGE.md`・`AGENTS.md`・`docs/` 直下の
基準書（`REVIEW_CRITERIA.md` 等）・`README.md`（リポジトリルート）は変更対象外である
（変更が不要であることの根拠は `README.md` §2.5・§3.1・§4）。

行番号はすべて **反映直前の HEAD 版 `CLAUDE.md`**（コミット `1074216` 時点）に基づく。
行番号は所在の説明のためだけに使い、**置換の同定には行番号を使わず文字列一致を使う**（§3）。

## 2. 反映前の事前検査（必須。1つでも不一致なら中断してユーザーに報告する）

```bash
# S0-1: 作業ツリーの CLAUDE.md が HEAD と同一であること（出力が空であること）
git -C /home/sakagawa/git/honOCR diff HEAD -- CLAUDE.md

# S0-2: 置換前文字列がそれぞれちょうど1回であること（3行とも 1 が出ること）
f=/home/sakagawa/git/honOCR/CLAUDE.md
grep -c '^\*\*全文ゲート（`/new` による「新しい目」）は、案件（細分化して並列に進める場合は細分化単位）につき1回だけ行う。\*\* ゲートを経ていない段階のゼロ（初回レビューのゼロ、C より前の解消確認のゼロ）は収束ではない。$' "$f"
grep -c '\*\*案件につき1回だけ\*\* |$' "$f"
grep -c '\*\*`docs/PROJECT_KNOWLEDGE.md` は運用ドキュメントではなく、内容の更新は各案件の完了処理で行う（update 案件の対象外）。ただし同ファイルの構成変更（分割・セクション再編）は update 案件とする。\*\*' "$f"
```

期待値: S0-1 は無出力、S0-2 は `1` が3つ。

## 3. `CLAUDE.md` の3箇所の置換（全文）

### 3.1 変更 1-A: 「収束」の定義（194行目）

**置換前**（1行全体。行頭から行末まで）:

```
**全文ゲート（`/new` による「新しい目」）は、案件（細分化して並列に進める場合は細分化単位）につき1回だけ行う。** ゲートを経ていない段階のゼロ（初回レビューのゼロ、C より前の解消確認のゼロ）は収束ではない。
```

**置換後**（1行）:

```
**全文ゲート（`/new` による「新しい目」）は、レビュー単位につき1回だけ行う。** レビュー単位とは、初回レビュー（A）で対象ファイルとして指定した文書一式である: 案件のフローのステップ4 のレビュー対象（requirements.md / design.md 等）は案件につき1単位、案件を細分化して並列に進める場合は細分化単位ごとに1単位、実験・検証の criteria 文書のレビュー（criteria lock）は criteria 文書ごとに1単位とする。同一の文書一式は、指摘を反映して内容を修正した後も同一のレビュー単位である（同じ一式を A に指定し直すこと・分割し直すこと・ストリームを変えることによって新しいレビュー単位にはならない）。新しいレビュー単位のレビューは遷移表の「（開始時）」の行から始める（同一案件のストリームで続けてよい。結果の保存は案件の連番を進める）。ゲートを経ていない段階のゼロ（初回レビューのゼロ、C より前の解消確認のゼロ）は収束ではない。
```

### 3.2 変更 1-B: 「依頼は3種類」表の C 行の末尾（203行目）

**置換前**（部分文字列。行末の `|` を含む）:

```
**案件につき1回だけ** |
```

**置換後**:

```
**レビュー単位につき1回だけ** |
```

置換後の 203行目の全文は次のとおりになる（検証用）:

```
| **C** | 全文ゲート | **`/new` を送って会話をクリアしてから**（「新しい目」の確保。ペイン・エージェント名はそのまま）、全文・全基準のレビューを依頼する（依頼文は後述）。**レビュー単位につき1回だけ** |
```

### 3.3 変更 2-A: ドキュメント更新フロー冒頭の但し書き（77行目）

**置換前**（部分文字列。**行全体ではない**。この文字列の前後は変更しない）:

```
**`docs/PROJECT_KNOWLEDGE.md` は運用ドキュメントではなく、内容の更新は各案件の完了処理で行う（update 案件の対象外）。ただし同ファイルの構成変更（分割・セクション再編）は update 案件とする。**
```

**置換後**:

```
**`docs/PROJECT_KNOWLEDGE.md` は運用ドキュメントではないため、その内容の改訂を目的とする update 案件は起票しない（知見・データ状態の追記は各案件の完了処理で行う）。update 案件の完了処理でファイルの追加・削除をディレクトリ構成へ反映することは、本フローのステップ6 のとおり行う。同ファイルの構成変更（分割・セクション再編）は update 案件とする。**
```

置換後の 77行目の全文は次のとおりになる（検証用）:

```
開発プロセスを定める運用ドキュメント（`CLAUDE.md`、`docs/` 直下の基準書・BACKLOG・CHANGELOG、`.gitignore` 等）の改訂は **update-XXX 案件**として扱い、以下のフローを**厳守**する。**`docs/PROJECT_KNOWLEDGE.md` は運用ドキュメントではないため、その内容の改訂を目的とする update 案件は起票しない（知見・データ状態の追記は各案件の完了処理で行う）。update 案件の完了処理でファイルの追加・削除をディレクトリ構成へ反映することは、本フローのステップ6 のとおり行う。同ファイルの構成変更（分割・セクション再編）は update 案件とする。** 典型例:
```

## 4. 反映手順

置換は **Python スクリプトで一括実行**する（エディタでの手編集はしない。旧文字列がちょうど1回で
あることをスクリプト内で検査し、1回でなければ何も書き換えずに異常終了させるため）。

作業用スクリプトはスクラッチパッド（`/tmp/claude-.../scratchpad/`）に置き、リポジトリには残さない。

```python
# apply_update_004.py
from pathlib import Path

PATH = Path("/home/sakagawa/git/honOCR/CLAUDE.md")

REPLACEMENTS = [
    # (ラベル, 置換前, 置換後)
    (
        "1-A",
        "**全文ゲート（`/new` による「新しい目」）は、案件（細分化して並列に進める場合は細分化単位）につき1回だけ行う。** ゲートを経ていない段階のゼロ（初回レビューのゼロ、C より前の解消確認のゼロ）は収束ではない。",
        "**全文ゲート（`/new` による「新しい目」）は、レビュー単位につき1回だけ行う。** レビュー単位とは、初回レビュー（A）で対象ファイルとして指定した文書一式である: 案件のフローのステップ4 のレビュー対象（requirements.md / design.md 等）は案件につき1単位、案件を細分化して並列に進める場合は細分化単位ごとに1単位、実験・検証の criteria 文書のレビュー（criteria lock）は criteria 文書ごとに1単位とする。同一の文書一式は、指摘を反映して内容を修正した後も同一のレビュー単位である（同じ一式を A に指定し直すこと・分割し直すこと・ストリームを変えることによって新しいレビュー単位にはならない）。新しいレビュー単位のレビューは遷移表の「（開始時）」の行から始める（同一案件のストリームで続けてよい。結果の保存は案件の連番を進める）。ゲートを経ていない段階のゼロ（初回レビューのゼロ、C より前の解消確認のゼロ）は収束ではない。",
    ),
    (
        "1-B",
        "**案件につき1回だけ** |",
        "**レビュー単位につき1回だけ** |",
    ),
    (
        "2-A",
        "**`docs/PROJECT_KNOWLEDGE.md` は運用ドキュメントではなく、内容の更新は各案件の完了処理で行う（update 案件の対象外）。ただし同ファイルの構成変更（分割・セクション再編）は update 案件とする。**",
        "**`docs/PROJECT_KNOWLEDGE.md` は運用ドキュメントではないため、その内容の改訂を目的とする update 案件は起票しない（知見・データ状態の追記は各案件の完了処理で行う）。update 案件の完了処理でファイルの追加・削除をディレクトリ構成へ反映することは、本フローのステップ6 のとおり行う。同ファイルの構成変更（分割・セクション再編）は update 案件とする。**",
    ),
]

text = PATH.read_text(encoding="utf-8")
original_lines = text.count("\n")

for label, old, new in REPLACEMENTS:
    n = text.count(old)
    if n != 1:
        raise SystemExit(f"ABORT: {label} の置換前文字列の出現回数が {n} 件（期待 1 件）。書き換えずに中断した。")
    if text.count(new) != 0:
        raise SystemExit(f"ABORT: {label} の置換後文字列が既に存在する。書き換えずに中断した。")
    text = text.replace(old, new, 1)

if text.count("\n") != original_lines:
    raise SystemExit("ABORT: 行数が変化した。書き換えずに中断した。")

PATH.write_text(text, encoding="utf-8")
print("OK: 3件を置換した")
```

手順:

| # | 作業 |
|---|---|
| S0 | §2 の事前検査を実行し、期待値どおりであることを確認する（不一致なら中断） |
| S1 | 上記スクリプトをスクラッチパッドに保存し、`uv run python {スクラッチパッド}/apply_update_004.py` で実行する。`OK: 3件を置換した` 以外の出力（`ABORT:` を含む）が出たら中断してユーザーに報告する |
| S2 | §5 の検証 V1〜V4 をすべて実行する |
| S3 | §6 の完了処理を行う |

**設計にない変更が必要になったら、その場で対処せず中断し、ドキュメント更新フロー ステップ2（調査）に戻る。**

## 5. 検証（S2）

| # | 検査 | コマンド | 期待値 |
|---|---|---|---|
| V1 | 変更が `CLAUDE.md` の3行だけであること | `git -C /home/sakagawa/git/honOCR diff --numstat -- CLAUDE.md` | `3	3	CLAUDE.md`（3行追加・3行削除） |
| V2 | 変更内容が意図どおりであること | `git -C /home/sakagawa/git/honOCR diff -- CLAUDE.md` | §3 の3箇所の旧文→新文のみ。他の行に変更がない |
| V3 | テンプレートの当該範囲と一致すること（1-A で追加した1文を除く） | 下記 V3 コマンド | V3a は**無出力**、V3b は `15c15` の1件のみ、V3c は `OK` |
| V4 | 旧表現が残っていないこと | `grep -n "案件につき1回だけ\|運用ドキュメントではなく" /home/sakagawa/git/honOCR/CLAUDE.md` | **無出力**（一致0件） |

V3 のコマンド（`README.md` §5.2 と同じ範囲。置換後は行数が変わらないため範囲もそのまま使える）:

```bash
h=/home/sakagawa/git/honOCR/CLAUDE.md
t=/home/sakagawa/git/DEV_TEMPLATE/template/CLAUDE.md
diff <(sed -n '75,101p'  "$h") <(sed -n '79,105p'  "$t")   # V3a: ドキュメント更新フロー節 → 無出力
diff <(sed -n '180,270p' "$h") <(sed -n '184,274p' "$t")   # V3b: Codex レビュー節 → 194行目の1行のみ相違
```

**V3b の期待値が「無出力」ではない理由**: 1-A の置換後文字列には、codex-02（全文ゲート）の指摘を受けて
レビュー単位の同一性を定める1文を追加している（§8.1）。この1文はテンプレート側には無いため、
Codex レビュー節の diff は 194行目（テンプレート側 198行目）の1行だけが相違する（`15c15` の1件）。
**それ以外の行が相違した場合は中断する。**

V3c: 相違が「追加した1文だけ」であることの機械検査（`OK` が出ること）:

```bash
python3 - <<'PY'
from pathlib import Path
ADD = "同一の文書一式は、指摘を反映して内容を修正した後も同一のレビュー単位である（同じ一式を A に指定し直すこと・分割し直すこと・ストリームを変えることによって新しいレビュー単位にはならない）。"
h = Path("/home/sakagawa/git/honOCR/CLAUDE.md").read_text(encoding="utf-8").splitlines()
t = Path("/home/sakagawa/git/DEV_TEMPLATE/template/CLAUDE.md").read_text(encoding="utf-8").splitlines()
assert h[193].replace(ADD, "") == t[197], "NG: 追加1文を除いてもテンプレートと一致しない"
print("OK")
PY
```

V4 の検索語について: `案件につき1回だけ` は 1-B の旧表現に完全一致し、1-A の旧表現（`…単位につき1回だけ行う`）
にも部分一致する。置換後の新表現（`レビュー単位につき1回だけ`）は「案件につき1回だけ」を含まないため、
一致0件が正しい期待値である。なお置換後の 194行目には「案件につき1単位」という別語が現れるが、
これは検索語に一致しない。

**V1〜V4 のいずれかが期待値と異なる場合は、`git -C /home/sakagawa/git/honOCR checkout -- CLAUDE.md` で
復元し、中断してユーザーに報告する。**

## 6. 完了処理（S3）

| # | 対象 | 内容 |
|---|---|---|
| 1 | `docs/BACKLOG.md` | update-004 の行のステータスを **Closed** に更新し、備考に完了日（2026-09-04）と完了内容の要約を記録する |
| 2 | `docs/CHANGELOG.md` | 完了内容を記録する（既存の記載様式に合わせる） |
| 3 | 案件 `README.md` | 冒頭のステータスを **In Progress → Closed** に更新する（完了日を併記） |
| 4 | `docs/PROJECT_KNOWLEDGE.md` | **更新しない**。本案件でファイルの追加・削除は発生しない（案件フォルダ配下のドキュメントはディレクトリ構成の記載対象外。update-002・update-003 でも同様に更新していない） |
| 5 | Codex エージェント | レビュー収束・人レビュー通過後、`/quit` を送って終了し、`herdr pane close {pane_id}` でペインを撤去する |
| 6 | DEV_TEMPLATE への逆還元 | §8.1 の1文はテンプレート側に無いため、**完了時の検討事項として `docs/CHANGELOG.md` に記録する**（テンプレート側の update 案件として起票するかはテンプレート側の判断。本案件では起票しない） |

## 7. テスト

**不要**（自動・手動とも）。本案件の変更は `CLAUDE.md` の文言のみであり、ソースコード・テストコードの
変更を含まない。`tests/results/` への出力も行わない。

## 8. レビュー（ドキュメント更新フロー ステップ4）

### 8.1 全文ゲート（C）の指摘への対応（2026-09-04・ユーザー決定）

codex-02（全文ゲート C。`reviews/codex-02.result.md`）が重要度「**高**」1件を指摘した。

> 「レビュー単位」の同一性が規定されておらず、1回ゲート型を迂回できる。同じ文書一式を A に指定し直せば
> 「新しいレビュー単位」を名乗れてしまい、C を何度でも実施できる。

**対応（ユーザー決定）**: 1-A の置換後文字列に、レビュー単位の同一性を定める次の1文を追加する。

```
同一の文書一式は、指摘を反映して内容を修正した後も同一のレビュー単位である（同じ一式を A に指定し直すこと・分割し直すこと・ストリームを変えることによって新しいレビュー単位にはならない）。
```

挿入位置は「…criteria 文書ごとに1単位とする。」の直後、「新しいレビュー単位のレビューは…」の直前
（§3.1 の置換後文字列に反映済み）。

**この1文は DEV_TEMPLATE 側には無い**ため、`CLAUDE.md` 194行目はテンプレートより1文長くなる。
これは意図した乖離であり、検証 V3b・V3c でその範囲（追加した1文だけ）に収まっていることを確認する。
テンプレートへの逆還元は完了時の検討事項として記録する（§6 の 6）。

なお codex-02 が併記した「結果メタにレビュー単位の識別子を加える」という追加提案は**採用しない**。
`CLAUDE.md`「結果の保存」の冒頭メタには既に「対象ファイル」と「ゲート状態（未実施 / 実施済み: 当該 C の
結果ファイル名）」があり、レビュー単位（＝対象ファイル一式）と C の実施状態は既存メタから特定できる。
本案件のスコープ（3箇所の置換）を超える追加でもある。

### 8.2 レビューの実施条件

- ストリーム名: `rev-honocr-update-004`
- レビュー対象: 本案件の `README.md` と `design.md`
- レビュー観点（update 案件の定型3点）:
  1. 反映計画の自己完結性（design.md だけで作業ができるか）
  2. 情報の喪失（削除・置換対象に、他所に存在しない情報が含まれていないか）
  3. 変更後のドキュメント間整合性（参照切れ、矛盾、案件の漏れ・重複）
- 結果の保存先: `docs/issues/update-004-fix-review-rule-contradictions/reviews/codex-NN.result.md`（連番は 01 から）
- 手順は `CLAUDE.md`「Codexによるレビューの実行方法（Herdr 対話方式）」の遷移表に従う。
  **本案件のレビュー単位は「`README.md` + `design.md` の一式」1つであり、全文ゲート（C）は1回だけ行う。**
