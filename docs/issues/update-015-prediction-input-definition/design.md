# update-015 反映設計書: 想定値の式の「入力」の定義、前のステージの由来の語、3 種に当たらない入力が出たときの動作

- **案件**: update-015（`docs/issues/update-015-prediction-input-definition/`）
- **作成日**: 2026-09-09
- **位置づけ**: ドキュメント更新フロー ステップ3「設計・保存」。調査と判断は `README.md`（§1〜§5）
- **反映担当**: Sonnet サブエージェント（ステップ5。読み替えは CLAUDE.md「ドキュメント更新フロー」ステップ5 に従う）
- **テスト**: コード変更が無いため自動・手動とも**不要**

**本書だけで反映作業ができる**。変更方式はすべて**完全一致の文字列置換**（`old` は当該ファイル内で**ちょうど1回**出現する。出現回数が 0 または 2 以上なら中断して報告する）。W1 は行の一部、W2 は行全体。コードブロックのフェンス（```）自体は `old` / `new` に含めない。行数はどちらも変わらない。

---

## 1. 変更対象ファイルと変更の一覧

| # | ファイル | 箇所（行番号は 2026-09-09 時点。コミット `2eba441`。`CLAUDE.md` 437 行、`docs/REVIEW_CRITERIA.md` 77 行） | 方式 | README の判断 |
|---|---|---|---|---|
| W1 | `CLAUDE.md` | 78 行「パイプラインの切り方」ステップ2 の 4「想定値」の、3 種の列挙から「3 種以外のラベルを作らない」まで | 置換（行の一部） | A・B・C |
| W2 | `docs/REVIEW_CRITERIA.md` | 57 行 1.11 項目 3 | 置換（行全体） | A・B・C |
| W3 | `CLAUDE.md` | 55 行 機能追加フロー ステップ6（行の一部） | 置換（行の一部） | B |
| W4 | `CLAUDE.md` | 78 行「想定値」の criteria lock の文（行の一部。W1 と同じ行の別の箇所） | 置換（行の一部） | B |
| W5 | `CLAUDE.md` | 105 行 粒度表 `experiment_log.md` の行（行の一部） | 置換（行の一部） | B |
| W6 | `CLAUDE.md` | 189 行「実験・検証の進め方」項目 1（行の一部） | 置換（行の一部） | B |
| W7 | `CLAUDE.md` | 190 行 同 項目 2 の前半（行の一部） | 置換（行の一部） | B |
| W8 | `CLAUDE.md` | 190 行 同 項目 2 の feat 読み替え（行の一部） | 置換（行の一部） | B |
| W9 | `CLAUDE.md` | 336 行 依頼文の基準部分（行の一部） | 置換（行の一部） | B |
| W10 | `docs/REVIEW_CRITERIA.md` | 65 行 1.11 の実験ログの項（行の一部） | 置換（行の一部） | B |
| W11 | `docs/BACKLOG.md`・`docs/CHANGELOG.md`・本案件 `README.md` | 完了処理 | 追記・更新（Claude Code 本体がステップ6 で行う） | — |

**変更しないファイル・箇所**（理由つき）:

- `AGENTS.md`: 対象の記述なし
- `CLAUDE.md`「ステージの切り方の規則」・「ロードマップの改訂（再計画）」: W1 の分岐 (1)(2) はこれらの既存の規定を参照するだけで、本文は変えない
- `docs/REVIEW_CRITERIA.md` 1.11 の他の項目と重要度: 変更なし（項目 3 の不適合は引き続き高）
- feat-025 の文書: 範囲外（README §5 判断 D）

**保持するもの**: 3 種の閉じた列挙、調査で数えた値の置き場（前提の定数）、3 種以外のラベルの禁止、不適合の例示、想定値の書式と幅の計算。**削除するもの**: 無し（語の置換と追記のみ）。「前のステージの実測」の語は両ファイルから全て消え、「前のステージの出力（`experiment_log.md` に記録した値）」に統一される（W1〜W10。codex-01 高1）。

---

## 2. 変更の内容（変更前 → 変更後）

### W1: `CLAUDE.md` 78 行（行の一部）

old（この文字列は `CLAUDE.md` に 1 回出現する）:
```
**入力ごとにどれかを名指しする**: **前のステージの実測**（出所のステージを添える）、**測るステージの測る項**、**ロードマップで固定した定数**。定数には `z`・`e` のような設計値のほか、**ロードマップ作成時の調査で数えた値**を含める。後者は前提の表に置き、いつ・何に・どの規則を当てて数えたかを典拠に書き、式からは前提の番号で参照する（最初のステージのように前のステージが無い場合はこれを使う）。3 種以外のラベル（「調査時点の実測」「事前の測定」等）を作らない。出力に幅があるなら、
```
new:
```
**入力ごとにどれかを名指しする**: **前のステージの出力**（`experiment_log.md` に記録した値。実測値と、そのステージで計算して確定した値の両方。出所のステージを添える）、**測るステージの測る項**、**ロードマップで固定した定数**。定数には `z`・`e` のような設計値のほか、**ロードマップ作成時の調査で数えた値**を含める。後者は前提の表に置き、いつ・何に・どの規則を当てて数えたかを典拠に書き、式からは前提の番号で参照する（最初のステージのように前のステージが無い場合はこれを使う）。3 種以外のラベル（「調査時点の実測」「事前の測定」等）を作らない。**入力とは、そのステージの外から入る量をいい、3 種はその全部である。同じステージの式で先に計算する量（`n` のような途中の量）は入力ではなく式の途中の値であり、由来を書かない**（展開して書く必要もない）。3 種のどれにも当たらない入力が出たら、ラベルを作らず、**次の順に判定し、最初に当てはまった 1 つを行う**: (1) その量が測れるものなら、それを測るステージを前に置く (2) 1 に当たらず、その量が自分のステージの前回の実行結果なら、繰り返しを別のステージに分ける（「ロードマップの改訂（再計画）」の次元の変更） (3) 1 にも 2 にも当たらないなら、止まって報告する。その行は「3 種のどれにも当たらない」と明記して不適合のままにし、規定を直すまで `roadmap.md` は収束しない。出力に幅があるなら、
```

### W2: `docs/REVIEW_CRITERIA.md` 57 行（行全体）

old:
```
3. **想定値の導出**: 各ステージの想定値が `予想値 = 式（入力値）` の形で書かれ、式の**入力ごとに**「前のステージの実測」「測るステージの測る項」「ロードマップで固定した定数」の**いずれかが名指しされている**。判定はこの閉じた 3 種への該当で行う。出所が書かれていて辿れることだけでは適合としない（3 種のいずれかの名指しと、その種に定めた出所の要件〔前のステージの実測は出所のステージ、測る項は測るステージ、定数は典拠〕の両方が要る）。3 種以外のラベル（「調査時点の実測」「事前の測定」等）が付いた入力は、値が正しく出所が辿れても不適合。ロードマップ作成時の調査で数えた値は、前提の表に定数として置かれ（いつ・何に・どの規則を当てて数えたかの典拠つき）、式から前提の番号で参照されていれば「ロードマップで固定した定数」として適合。推測・根拠の書かれていない目視の値・出所の無い定数が式に入っている行、式そのものが無い行も不適合
```
new:
```
3. **想定値の導出**: 各ステージの想定値が `予想値 = 式（入力値）` の形で書かれ、式の**入力ごとに**「前のステージの出力（`experiment_log.md` に記録した値。実測値と計算して確定した値の両方）」「測るステージの測る項」「ロードマップで固定した定数」の**いずれかが名指しされている**。入力とはそのステージの外から入る量であり、同じステージの式で先に計算する量（途中の値）は入力に数えず、由来の記載を求めない（展開して書くことも求めない）。判定はこの閉じた 3 種への該当で行う。出所が書かれていて辿れることだけでは適合としない（3 種のいずれかの名指しと、その種に定めた出所の要件〔前のステージの出力は出所のステージ、測る項は測るステージ、定数は典拠〕の両方が要る）。3 種以外のラベル（「調査時点の実測」「事前の測定」等）が付いた入力は、値が正しく出所が辿れても不適合。ロードマップ作成時の調査で数えた値は、前提の表に定数として置かれ（いつ・何に・どの規則を当てて数えたかの典拠つき）、式から前提の番号で参照されていれば「ロードマップで固定した定数」として適合。3 種のどれにも当たらない入力が「3 種のどれにも当たらない」と明記されて報告されている行は、不適合（高）として報告する（書き手が止まって報告した状態であり、規定の改訂まで収束させない）。推測・根拠の書かれていない目視の値・出所の無い定数が式に入っている行、式そのものが無い行も不適合
```

### W3〜W10: 「前のステージの実測」の語の統一（行の一部）

### W3: `CLAUDE.md` 55 行 機能追加フロー ステップ6（行の一部）

old（この文字列は当該ファイルに 1 回出現する）:
```
想定値の式に前のステージの実測を代入して予測値を出し
```
new:
```
想定値の式に前のステージの出力（`experiment_log.md` に記録した値）を代入して予測値を出し
```

### W4: `CLAUDE.md` 78 行「想定値」の criteria lock の文（行の一部。W1 と同じ行の別の箇所）

old（この文字列は当該ファイルに 1 回出現する）:
```
値は前のステージの実測が確定してから代入する（当該ステージの実行直前
```
new:
```
値は前のステージの出力が確定してから代入する（当該ステージの実行直前
```

### W5: `CLAUDE.md` 105 行 粒度表 `experiment_log.md` の行（行の一部）

old（この文字列は当該ファイルに 1 回出現する）:
```
直前予測（`roadmap.md` の想定値の式に前のステージの実測を代入した値。代入した実測の出所）
```
new:
```
直前予測（`roadmap.md` の想定値の式に前のステージの出力〔`experiment_log.md` に記録した値〕を代入した値。代入した値の出所）
```

### W6: `CLAUDE.md` 189 行「実験・検証の進め方」項目 1（行の一部）

old（この文字列は当該ファイルに 1 回出現する）:
```
値は前のステージの実測が確定してから代入する。式・幅
```
new:
```
値は前のステージの出力が確定してから代入する。式・幅
```

### W7: `CLAUDE.md` 190 行 同 項目 2 の前半（行の一部）

old（この文字列は当該ファイルに 1 回出現する）:
```
予測は前のステージの実測結果を使って直前に立てる
```
new:
```
予測は前のステージの出力（`experiment_log.md` に記録した値）を使って直前に立てる
```

### W8: `CLAUDE.md` 190 行 同 項目 2 の feat 読み替え（行の一部）

old（この文字列は当該ファイルに 1 回出現する）:
```
直前予測は lock された式に前のステージの実測を代入した値であり
```
new:
```
直前予測は lock された式に前のステージの出力（`experiment_log.md` に記録した値）を代入した値であり
```

### W9: `CLAUDE.md` 336 行 依頼文の基準部分（行の一部）

old（この文字列は当該ファイルに 1 回出現する）:
```
直前予測が `roadmap.md` の式に前のステージの実測を代入した値であること
```
new:
```
直前予測が `roadmap.md` の式に前のステージの出力を代入した値であること
```

### W10: `docs/REVIEW_CRITERIA.md` 65 行 1.11 の実験ログの項（行の一部）

old（この文字列は当該ファイルに 1 回出現する）:
```
想定値の式に前のステージの実測を代入した値であり
```
new:
```
想定値の式に前のステージの出力（`experiment_log.md` に記録した値）を代入した値であり
```

---

## 3. 実施手順

**手順 0（Claude Code 本体。Sonnet 委任の前）**: 反映前の基準を、リポジトリ外の固定ディレクトリに次のコマンドで保存する（シェル変数 `B` にディレクトリを代入して使う。`--untracked-files=all` により未追跡のディレクトリは配下の全ファイルに展開される）。

```
B='/tmp/claude-1000/-home-sakagawa-git-honOCR/60d897aa-7762-4115-808e-079a082730bc/scratchpad/update-015'
mkdir -p "${B}"
git -C /home/sakagawa/git/honOCR status --short --untracked-files=all > "${B}/status_before.txt"
awk '{print $2}' "${B}/status_before.txt" | while read f; do sha256sum "/home/sakagawa/git/honOCR/$f"; done > "${B}/sha_before.txt"
```

`status_before.txt` は `git status --short` の形式（状態 2 文字＋パス）、`sha_before.txt` は `sha256sum` の形式（ハッシュ＋絶対パス）。これらが §4 項目 1 の基準になる。保存が済むまで Sonnet に委任しない。反映後は本案件 `README.md` を更新する前に §4 項目 1 を照合する。

**手順 1〜5（Sonnet サブエージェント）**:

1. `CLAUDE.md` と `docs/REVIEW_CRITERIA.md` を読み、W1〜W10 の各 `old` が当該ファイルに**ちょうど1回**出現することを確認する（Python の `str.count`）。0 または 2 以上なら**中断して報告**する
2. W1〜W10 を置換する（改行を含めて完全一致。W1 と W4 は同じ行の別の箇所）
3. 置換後、次を確認する
   - `grep -c "入力とは、そのステージの外から入る量をいい" CLAUDE.md` が **1**、`grep -c "次の順に判定し、最初に当てはまった 1 つを行う" CLAUDE.md` が **1**
   - `grep -c "入力とはそのステージの外から入る量であり" docs/REVIEW_CRITERIA.md` が **1**、`grep -c "3 種のどれにも当たらない」と明記されて報告されている行" docs/REVIEW_CRITERIA.md` が **1**
   - `grep -c "前のステージの実測" CLAUDE.md docs/REVIEW_CRITERIA.md` が両ファイルとも **0**（語が全て「前のステージの出力」に統一されている）
   - `CLAUDE.md` の行数が **437**、`docs/REVIEW_CRITERIA.md` の行数が **77** のまま
4. 自分が書き込んだファイルの一覧を報告する（git コマンドは使わない）
5. git 操作（diff を含む）・`docs/BACKLOG.md`・`docs/CHANGELOG.md`・`docs/PROJECT_KNOWLEDGE.md`・本案件 `README.md` は**変更しない**

---

## 4. 反映後の検証（Claude Code 本体が行う）

| # | 確認項目 |
|---|---|
| 1 | 反映で変化したファイルが `CLAUDE.md` と `docs/REVIEW_CRITERIA.md` の 2 つのみ。判定方法（§3 手順 0 と同じ値を `B` に代入してから行う。本案件 `README.md` の更新前に行う）: (a) `git -C /home/sakagawa/git/honOCR status --short --untracked-files=all > "${B}/status_after.txt"` を取り、`diff "${B}/status_before.txt" "${B}/status_after.txt"` の増分（`>` 行）が ` M CLAUDE.md` と ` M docs/REVIEW_CRITERIA.md` の 2 行だけであること (b) `sha256sum -c "${B}/sha_before.txt" --quiet` が失敗なく終わること（基準の全ファイルの内容が不変）。行数は `wc -l` で 437 / 77 |
| 2 | 反映後の両ファイルが、本書の W1〜W10 をコミット `2eba441` の版に適用した模擬置換の結果とバイト一致する |
| 3 | `CLAUDE.md` の想定値の項に、3 種の 1 つ目「前のステージの出力（記録した値。実測値と計算して確定した値の両方）」、入力の定義（外から入る量。途中の値は由来を書かない。展開不要）、3 種に当たらない入力の順序つきの分岐 (1)〜(3) と「止まって報告。規定を直すまで収束しない」がある |
| 4 | 1.11 項目 3 に、同じ語の 3 種、入力の定義（途中の値に由来を求めない。展開も求めない）、出所の要件の括弧書きが「前のステージの出力は出所のステージ」、「3 種のどれにも当たらないと明記されて報告されている行は不適合（高）。収束させない」がある。既存の不適合の例示が残っている |
| 5 | 「前のステージの実測」が両ファイルに残っていない（W3〜W10）。`CLAUDE.md` の想定値の項と 1.11 項目 3 で 3 種の語が一致する。1.11 の項目数 7 と重要度の文が不変。`AGENTS.md` が変更されていない |

---

## 5. 完了処理（ステップ6。Claude Code 本体）

1. `docs/BACKLOG.md` の update-015 の行をステータス **Closed** にし、備考に完了日・レビュー回数・反映内容の要約を追記する
2. `docs/CHANGELOG.md` に完了日の項として、変更の要旨と契機（Opus の 4 箇所の報告、最初の案の却下、例外時の動作を分岐で書くこと）と案件フォルダを記録する
3. 本案件 `README.md` のステータスを **Closed** に更新し、レビューの経過と検証結果（§4 の 5 項目）を記録する
4. `docs/PROJECT_KNOWLEDGE.md` は更新しない
5. Codex のレビューストリーム `rev-honocr-update-015` に `/quit` を送り、ペインを撤去する
6. feat-025 `roadmap.md` は展開版のまま人レビューへ（README §5 判断 D。展開を戻すかは Opus に任せる）

---

## 6. 設計判断の記録

| # | 判断 | 理由 |
|---|---|---|
| 1 | 「入力」を「そのステージの外から入る量」と定義し、途中の値に由来を書かせない（W1・W2） | README §5 判断 A。入力・出力の語だけで書く。「末端の記号」「式を持つ記号を辿る」はユーザー却下（規定に無い概念を持ち込み、判断を読み手に委ねる） |
| 2 | 展開の併記を求めない | Opus 案は不要。入力の定義があれば途中の値に名前を付けた式のままでよい。展開して書くことは不適合ではない |
| 3 | 3 種の 1 つ目を「前のステージの出力（記録した値）」に（W1・W2） | README §5 判断 B。S4 で計算して確定した `n` を S5 が使うとき「実測」の語で読み手が割れる。当初は他の箇所の「実測を代入」を手順の記述として残す設計だったが、codex-01 高1（1.11 で適合になる計算確定値を実験ログの規定・依頼文が「実測」と限定して不適合にしうる）を受け、両ファイルの全出現（W3〜W10）を「前のステージの出力（`experiment_log.md` に記録した値）」に統一した |
| 4 | 3 種に当たらない入力が出たときの動作を、順に判定し最初に当てはまった 1 つを行う分岐で書く（W1） | README §5 判断 C。例外のラベルを置くと Opus がそこに何でも入れる。動作を固定し、分岐であることを最初に書く（ユーザー「条件分岐の文なら最初にそう書け」） |
| 5 | 検査側は「3 種に当たらないと明記された行」を不適合（高）として報告し収束させない（W2） | 止まって報告した状態を Codex が「適合」と誤判定しないため。規定を直すまで lock できないのが正しい状態 |
| 6 | ステージ表の単位数を定数の例に加えない | 今回の 3 点の範囲外。現行の語で読める（feat-025 の `U_smoke`）。必要になれば次の改訂で |
