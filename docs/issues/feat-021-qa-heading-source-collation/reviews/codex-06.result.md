# codex-06: feat-021 解消確認（B・実装フェーズでの設計修正後）

- **日付**: 2026-09-03
- **対象ファイル**: `docs/issues/feat-021-qa-heading-source-collation/requirements.md`, `docs/issues/feat-021-qa-heading-source-collation/design.md`
- **ストリーム名**: `rev-honocr-feat-021`
- **依頼種別**: B（解消確認）
- **直前に `/new` を送ったか**: No（**全文ゲート C は案件につき1回**の規定により、2回目の `/new` は送らない）
- **ゲート状態**: 実施済み（`codex-04.result.md`）
- **指摘数**: 高 1 / 中 0 / 低 0
- **収束判定**: 未収束（次: 高1件を反映 → B 解消確認）
- **トークン実測**: total 886,649（input 880,236 / cached 677,888 / output 6,413 / reasoning 3,696）
- **rollout jsonl**: `~/.codex/sessions/2026/09/02/rollout-2026-09-02T17-03-35-01a06125-175d-73b2-a56f-72b9ec68ccf7.jsonl`

## 依頼の背景

実装フェーズ（2026-09-03、chap00 の突合）で当初の比較規則が**実行不能**と判明したため、
`CLAUDE.md`「実装の実行方法」の想定外事象の規定に従い実装を中断し、
調査・計画に戻って requirements.md / design.md を修正したうえで再レビューを依頼した。
修正内容は見出しを**プレフィックス部**と**質問文部**に分け、`D1`/`D4` を質問文部に限定し、
プレフィックス部は `D2` でのみ判定して空白の有無・量は判定しない、というもの（ADR-10）。

---

[AGENTS.md適用]

## 提示した2つの欠陥の解消判定

| 欠陥 | 判定 | 確認内容 |
|---|---|---|
| `##` を含むプレフィックス部が文字単位比較に入り全件不一致になる | **解消** | プレフィックス部を `D1` の対象から外し、`D1`/`D4` を質問文部だけに限定している（`requirements.md:149`） |
| `?` と番号の間の空白の有無を原本側から確定できない | **解消** | プレフィックス部の空白を判定対象外とし、`D2` にも含めないことが明文化されている（`requirements.md:153`） |
| 文書間の整合 | **問題なし** | 設計側の実施手順・詳細記録・ADR-10 も同じ方針に更新されており、文書間の矛盾はない（`design.md:547`） |

## 新規指摘

### 高

#### 1. 番号が欠落した場合の質問文部の切り分け手順が未定義

質問文部は「番号（およびその直後の空白）より後ろ」と定義されていますが、`D2` はまさに
md 側の番号欠落を対象に含めています。番号がないケースでは `D1`/`D4` の比較範囲を確定できず、
体裁欠落と質問文誤読が併発した際に判定が実装者依存になります。

- **該当**: `requirements.md:68`、`requirements.md:143`
- **修正案**: 番号欠落時の境界決定規則、または `D2` のみを記録して `D1`/`D4` を評価しない旨と
  その記録方法を明記してください。

## 中

なし

## 低

なし
