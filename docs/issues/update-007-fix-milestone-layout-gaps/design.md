# update-007 反映設計書: マイルストーン規定の適用上の欠落2件の解消

- **案件**: update-007（honOCR / `docs/issues/update-007-fix-milestone-layout-gaps/`）
- **作成日**: 2026-09-07
- **調査記録**: 同フォルダの `README.md`（契機・目的・採らなかった案・人レビューの記録）
- **準拠**: `CLAUDE.md`「ドキュメント更新フロー（update-XXX 案件）」

**本書は自己完結している。** `/clear` 後でも本書だけで反映作業ができる。
変更は**すべて文字列の置換**であり、変更前の文字列は反映時点の HEAD（`dbfaf58`）の内容と一致する。

---

## 1. 反映対象と変更方式の一覧

| # | ファイル | 行（HEAD 時点） | 変更方式 | 後処理 |
|---|---|---|---|---|
| P1 | `CLAUDE.md` | 209 | **部分置換**（1語） | なし |
| P2 | `CLAUDE.md` | 187 | **部分置換**（文の後半を差し替え、文を追加） | なし |
| P3 | `CLAUDE.md` | 95 | **部分置換**（表セル内の1句） | なし |
| P4 | `CLAUDE.md` | 101 | **部分置換**（文の一部） | なし |
| P5 | `CLAUDE.md` | 204 | **行の置換＋1行追加** | なし |
| P6 | `docs/issues/update-006-adopt-template-milestone-layout/README.md` | §1.3 の表の案 D の行 | **行の置換** | なし |

**全置換（ファイル全体の書き換え）は行わない。** すべて指定した文字列の1箇所置換である。
**置換対象が見つからない場合は中断して報告する**（HEAD と異なる状態で反映しない）。

---

## 2. P1: 旧配置の対象範囲を feat-001〜feat-023 にする

**ファイル**: `CLAUDE.md`
**セクション**: 「案件ディレクトリ構成」の、ツリー直後の但し書き段落（HEAD の 209 行）

### 変更前（この文字列を検索する）

```
本規定の導入（update-006、2026-09-07）より前に起票した feat 案件（feat-001〜feat-024）は、
```

### 変更後

```
本規定の導入（update-006、2026-09-07）より前に起票した feat 案件（feat-001〜feat-023）は、
```

**変更はこの1語（`024` → `023`）のみ。同じ段落の他の記述は変更しない。**

**理由**: feat-024 は 2026-09-07 にユーザー判断で新フロー（`roadmap.md` ＋ マイルストーンフォルダ）へ
移行しており、旧配置の案件ではない（`README.md` §1.1 欠落1）。

---

## 3. P2: スクリプトの置き場を「ドキュメント作成ルール」で区別する

**ファイル**: `CLAUDE.md`
**セクション**: 「ドキュメント作成ルール」の箇条書きのうち、ドキュメントの置き場を定める項目（HEAD の 187 行）

### 変更前（この文字列を検索する）

```
feat 案件の `roadmap.md` は案件フォルダ直下、マイルストーンごとの `requirements.md` / `design.md` / `experiment_log.md` とスクリプトは `docs/issues/{案件フォルダ}/{マイルストーンID}-{slug}/` に置く（「案件ディレクトリ構成」参照）
```

### 変更後

```
feat 案件の `roadmap.md` は案件フォルダ直下、マイルストーンごとの `requirements.md` / `design.md` / `experiment_log.md` と**そのマイルストーンのためのジョブスクリプト・実験コード**は `docs/issues/{案件フォルダ}/{マイルストーンID}-{slug}/` に置く（「案件ディレクトリ構成」参照）。**プロダクトコード**（案件の完了後も他の入力・他の案件で使う CLI・モジュール）は、マイルストーンフォルダではなく `scripts/` 配下に置き、そのテストは `tests/` 配下に置く（「テスト」節の規定に従う）。**どちらに当たるかは「その案件が終わった後も使うか」で判定する**——使うならプロダクトコード、その案件の実行・実験のためだけのものならジョブスクリプト・実験コードとする。判定と理由は当該 `design.md` の ADR に記録する
```

**変更点は3つ**: (a)「スクリプト」を「そのマイルストーンのためのジョブスクリプト・実験コード」に限定、
(b) プロダクトコードの置き場（`scripts/` と `tests/`）を追加、(c) 判定基準と ADR 記録の義務を追加。

**この項目の先頭部分**（「ドキュメントは `docs/issues/{案件フォルダ}/` に置く。ファイル名は…」）
**は変更しない。**

---

## 4. P3: 粒度表の `design.md` 行の「書かないこと」を書き分ける

**ファイル**: `CLAUDE.md`
**セクション**: 「ロードマップとマイルストーン文書の粒度」の表、`{マイルストーンID}-{slug}/design.md` の行（HEAD の 95 行）

### 変更前（この文字列を検索する）

```
| スクリプト本体（マイルストーンフォルダ配下のファイルとして置く）。コードの全文。他のマイルストーンの設計。`roadmap.md` / `requirements.md` の再掲 |
```

### 変更後

```
| スクリプト本体（ジョブスクリプト・実験コードはマイルストーンフォルダ配下、プロダクトコードは `scripts/` 配下のファイルとして置く）。コードの全文。他のマイルストーンの設計。`roadmap.md` / `requirements.md` の再掲 |
```

**同じ行の「書くこと」欄は変更しない。**

---

## 5. P4: 「細かさの上限」を書き分ける

**ファイル**: `CLAUDE.md`
**セクション**: 「ロードマップとマイルストーン文書の粒度」の表の直後の箇条書き（HEAD の 101 行）

### 変更前（この文字列を検索する）

```
- **細かさの上限**: コードの全文は書かない。ジョブスクリプト・実験コードの本体はマイルストーンフォルダ配下のファイルとして置き、`design.md` はその仕様と参照を書く（本体と設計書の二重管理を避ける）
```

### 変更後

```
- **細かさの上限**: コードの全文は書かない。ジョブスクリプト・実験コードの本体はマイルストーンフォルダ配下のファイルとして、プロダクトコードは `scripts/` 配下のファイルとして置き、`design.md` はいずれもその仕様と参照を書く（本体と設計書の二重管理を避ける）
```

---

## 6. P5: 案件ディレクトリ構成のツリーのコメントに注記する

**ファイル**: `CLAUDE.md`
**セクション**: 「案件ディレクトリ構成」のコードブロック内（HEAD の 204 行）

### 変更前（この文字列を検索する）

```
    │   └── （スクリプト等）     # ジョブスクリプト・実験コード（design.md から参照）
```

### 変更後（**2行になる**）

```
    │   └── （スクリプト等）     # そのマイルストーンのためのジョブスクリプト・実験コード（design.md から参照）
    │                          #   プロダクトコードは scripts/ 配下（テストは tests/ 配下）に置く
```

**追加する2行目の行頭は半角スペース4個 ＋ `│` ＋ 半角スペース26個 ＋ `#` である**
（ツリーの縦線の位置を1行目と揃える）。**ツリーの他の行は変更しない。**

---

## 7. P6: update-006 の案 D に撤回の経緯を追記する

**ファイル**: `docs/issues/update-006-adopt-template-milestone-layout/README.md`
**セクション**: 「1.3 採らなかった案と不採用の理由」の表の、案 D の行

### 変更前（この文字列を検索する）

```
| D | 進行中の feat-024 を新フローへ移行する（`roadmap.md` を後付けする） | 不採用 | feat-024 は調査案件で criteria lock（codex-01〜04）を終えており、後付けのロードマップは実施済みの工程と食い違う。旧配置のまま残工程（実装・手動テスト・完了）を進める |
```

### 変更後（**1行のまま。セル内の文字列を差し替える**）

```
| D | 進行中の feat-024 を新フローへ移行する（`roadmap.md` を後付けする） | 不採用 → **2026-09-07 にユーザー判断で撤回（採用）** | 起票時の不採用理由: feat-024 は調査案件で criteria lock（codex-01〜04）を終えており、後付けのロードマップは実施済みの工程と食い違う。旧配置のまま残工程（実装・手動テスト・完了）を進める。**撤回の経緯（2026-09-07）**: 本案件の完了後、ユーザーが「feat-024 を新ルールに則ってやり直す」と決定した。feat-024 は人（ユーザー）レビュー前で実装に入っておらず、実施済みの工程は Codex レビューの収束のみであり、新フローで作り直す方が旧配置の例外を1件減らせる。これに伴う `CLAUDE.md` の但し書きの変更は **update-007** で行った（本案件では変更していない） |
```

**理由**: update 案件の README は、テンプレート還元時に「契機・目的・採らなかった案」を典拠として
精査される（`CLAUDE.md` ドキュメント更新フロー ステップ2）。判断が後で覆ったことを記録しないと、
還元時に誤った経緯が伝わる。

**update-006 の `design.md`・`reviews/`・BACKLOG の行は変更しない**（完了済み案件の反映記録であり、
事実として正しいため）。

---

## 8. 反映の実施手順

**Sonnet サブエージェントに委任する**（`CLAUDE.md` ドキュメント更新フロー ステップ5）。

### 8.1 事前確認

```bash
git -C /home/sakagawa/git/honOCR status --short
git -C /home/sakagawa/git/honOCR diff --stat CLAUDE.md
```

**`CLAUDE.md` に変更が無い（HEAD と同一である）ことを確認してから着手する。**
変更がある場合は中断して報告する。

### 8.2 反映

P1 → P2 → P3 → P4 → P5 → P6 の順に、§2〜§7 の「変更前」の文字列を検索し「変更後」に置換する。

- **各置換の前に、変更前の文字列がファイル内に「ちょうど1箇所」あることを確認する。**
  0 箇所または2箇所以上のときは**その場で中断して報告する**（回避策を実装しない）
- 置換は指定した文字列のみに行う。前後の空白・改行を変えない

### 8.3 反映後の検証（Claude Code 本体が行う）

```bash
git -C /home/sakagawa/git/honOCR diff -- CLAUDE.md docs/issues/update-006-adopt-template-milestone-layout/README.md
```

**確認項目**:

| # | 項目 | 期待 |
|---|---|---|
| 1 | 変更ファイル | `CLAUDE.md` と update-006 の `README.md` の**2ファイルのみ** |
| 2 | `CLAUDE.md` の変更行数 | **追加6行・削除5行**（P1・P3・P4 が各1行の置換、P2 が1行の置換、P5 が1行→2行） |
| 3 | update-006 `README.md` の変更行数 | 追加1行・削除1行 |
| 4 | `feat-001〜feat-024` の残存 | `grep -c 'feat-001〜feat-024' CLAUDE.md` が **0** |
| 5 | `feat-001〜feat-023` の存在 | `grep -c 'feat-001〜feat-023' CLAUDE.md` が **1** |
| 6 | プロダクトコードの記述 | `grep -c 'プロダクトコード' CLAUDE.md` が **4**（P2 に2回、P3・P4・P5 に各1回のうち、P2 の1回目は太字表記のため一致は計4箇所）|
| 7 | 意図しない変更 | `git diff` に §2〜§7 以外の変更が現れない |
| 8 | 保持対象 | 「テスト」節・「コーディング規約」節・機能追加フローのステップ1〜8 の本文が**無変更**である |

**確認項目6 の件数が期待と異なる場合は、置換が重複または欠落しているため中断して報告する。**

---

## 9. テスト

**不要。** 本案件はドキュメントのみを変更し、ソースコード・テストコードを変更しない
（`CLAUDE.md` ドキュメント更新フロー ステップ7）。自動テスト・手動テストとも実施しない。

---

## 10. 完了処理（ステップ6）

1. `docs/BACKLOG.md` の update-007 のステータスを **Closed** に更新する
2. `docs/CHANGELOG.md` に完了内容を記録する（2026-09-07 の項に追記）
3. 本案件の `README.md` のステータスを **Closed** に更新する
4. `docs/PROJECT_KNOWLEDGE.md` は**更新しない**（ファイルの追加・削除がないため。
   案件フォルダの追加はディレクトリ構成の「`docs/issues/` — 案件ディレクトリ」に包含される）
5. `docs/TECH_STACK.md` は**更新しない**（ライブラリの変更がないため）
6. ルートの `README.md` は**更新しない**（コマンド・CLI オプション・入出力形式・既定値・
   実行環境のいずれも変わらないため）

---

## 11. 本案件の完了後に再開すること（本案件では実施しない）

**feat-024 の M1 文書のレビュー（機能追加フローのステップ5）**を、`scripts/scan_plain_math.py` の
配置のまま再開する。あわせて、M1 の `design.md` に未反映の Codex 指摘（**中**: 出力失敗時に
既存 TSV を破損しない原子的書き込みの設計）を反映する。
