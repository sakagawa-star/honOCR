# feat-025: インライン数式のマークアップ欠落の影響範囲調査（再実施）

- **種別**: feat（**調査案件**。md の修正は行わない）
- **ステータス**: **In Progress**（2026-09-08 起票。機能追加フローの**ステップ2 完了**〔`roadmap.md` 第1版を作成・保存〕、**ステップ3 の Codex レビューは収束済み**〔codex-11 = 全文ゲート codex-10 後の解消確認が高・中ゼロ。update-012 のレビュー基準改訂を受けた再実施分〕・**人レビュー待ち**）
- **起票日**: 2026-09-08
- **対象書籍**: 『プログラミングのための確率統計』（`{BASE2}` = `/home/sakagawa/work/確率統計`）**全10章**
- **前身案件**: feat-024（On Hold。**目的のみを引き継ぐ**。詳細は §2）

## 1. 概要（目的）

確率統計の全10章について、**原本で数式として組まれている記号が、成果物の Markdown 上で数式マークアップを失っている量**を測定する。

**ゴールは調査のみ**。md の修正は行わない（修正の要否と修正案は後続案件で扱い、本案件では「修正すべきか」を判定しない）。

### ユーザー確定事項（feat-024 起票時 2026-09-06 の決定を引き継ぐ）

| 項目 | 決定 |
|---|---|
| 調査範囲 | 『プログラミングのための確率統計』**全10章**の本文＋Q&A 見出し |
| 対象外 | PRML |
| ゴール | **調査のみ**（修正は後続案件。本案件では修正の要否を判定しない） |

上記以外（測定の手段、母集団の定義、標本設計、ステージの分割）は本案件でゼロから設計する（§2）。

## 2. feat-024 からの引き継ぎ範囲

feat-024 は update-008 以前の旧規定下で計画したため、update-009〜011 で改訂された現行規定（想定値を `予想値 = 式（入力値）` の書式で式から導出する、再検討の 2 水準〔式の変更／次元の変更〕、外れた後の記述制限等）に計画全体が適合しない。このため feat-024 を On Hold とし、本案件で最初からやり直す（ユーザー指示 2026-09-08）。

**引き継ぐもの**: §1 の目的とユーザー確定事項**のみ**。

**引き継がないもの**（いずれも本案件で作り直す。ユーザー確認 2026-09-08）:

- feat-024 の `roadmap.md`（第4版）とマイルストーン構成（M1〜M5）
- 母集団の定義（3層 4,624 件）・標本設計・推定手法
- M1〜M4 の実測値（逆引き成功率等）
- `scripts/scan_plain_math.py` および `tests/test_scan_plain_math.py`（既存コードとして流用せず、本案件で設計し直す）
- `survey_notes.md`・`rule_feedback_notes.md` 等の補助文書

feat-024 のフォルダは削除・改名せず記録として残す。feat-025 の完遂をもって feat-024 は中止とする。feat-024 のドキュメント内に残る記述の矛盾（ステータス行の再開手順）は、feat-025 完了後に整理する（ユーザー指示 2026-09-08）。

## 3. マイルストーン一覧と状態

計画の内容は `roadmap.md` を参照する（本表は ID・名称・フォルダ名・状態のみ）。

| ID | 名称 | フォルダ | ステージ | 状態 |
|---|---|---|---|---|
| M1 | 抽出 CLI の単体疎通 | `m1-extract-smoke/` | S1 | 未着手 |
| M2 | 全10章の候補抽出（`N` の確定） | `m2-population/` | S2 | 未着手 |
| M2-1 | 逆引き処理の単体疎通 | `m2-1-locate-smoke/` | S2-1 | 未着手 |
| M3 | 予備標本 30 件の逆引きと突合（`r`・`p₀` の測定） | `m3-pilot-measure/` | S3 | 未着手 |
| M4 | 本標本の抽出・逆引き・突合 | `m4-sample-collate/` | S4 | 未着手 |
| M5 | 推定と報告 | `m5-estimate-report/` | S5 | 未着手 |

## 4. 実施記録

| 日付 | 工程 | 結果 | 判定・承認 | 詳細 |
|---|---|---|---|---|
| 2026-09-08 | ステップ1 案件作成 | 案件フォルダと `README.md` を作成し、`docs/BACKLOG.md` に追加 | ユーザー指示により実施 | 本ファイル |
| 2026-09-09 | ステップ3 人レビュー（1回目） | ユーザーの指摘 4 件（`e` が要求か設計値か文面が両方に読める／`k` の式が前提とする判定規則の同一性が書かれていない／S4 の幅が広く発火しにくい／改訂の残りかす）を全件反映 | 反映後 Codex に差し戻し | `roadmap.md` 改訂履歴「反映5」 |
| 2026-09-09 | ステップ3 Codex レビュー（新基準で再実施） | **レビュー基準の改訂（update-012。`REVIEW_CRITERIA.md` 1.11 の新設）を受け、ユーザー指示で codex を再起動し新しいレビュー単位として A から再実施。** A（高1・中1）→ B（高2）→〔人レビュー〕→ B（高1）→ B（ゼロ）→ `/new` → **C**（高1）→ B（ゼロ）の6回で**収束**。`e` を最終出力への要求として維持する設計を撤回（行き詰まり検出により推論を取り下げ） | **収束**（人レビュー待ち） | `reviews/codex-06〜11.result.md` |
| 2026-09-09 | ステップ3 Codex レビュー | A（高2・中4）→ B（高2・中1）→ B（ゼロ）→ `/new` → **C**（中3）→ B（ゼロ）の5回で**収束**。統計設計の欠陥を計 10 件是正（標本数 0 の不成立・判定不能の扱い・Wilson 区間の端点・`n` の成分分類・S2 の処理単位ほか） | **収束**（人レビュー待ち） | `reviews/codex-01〜05.result.md` |
| 2026-09-08 | ステップ2 調査・計画 | 最終出力の成分（独立 4・従属 6）・入力の次元・ステージ表（S1〜S5）・想定値を定め、`roadmap.md` 第1版を作成。最終出力の形と対象範囲はユーザーが確定（推定総件数＋信頼区間／ギリシャ文字＋数学記号のみ） | ステップ3 のレビュー待ち | `roadmap.md` |
