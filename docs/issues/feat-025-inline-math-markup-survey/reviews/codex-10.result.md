# codex-10: feat-025 ロードマップの全文ゲート（C・新基準）

- **日付**: 2026-09-09
- **対象ファイル**: `docs/issues/feat-025-inline-math-markup-survey/roadmap.md`
- **ストリーム名**: `rev-honocr-feat-025`
- **依頼種別**: **C（全文ゲート）**
- **直前に `/new` を送ったか**: **Yes**
- **ゲート状態**: **実施済み（本ファイルが当該 C）**
- **指摘数**: 高 1 / 中 0 / 低 0
- **収束判定**: **未収束（次: B〔全件反映後の解消確認。同じ会話・2 回目の `/new` はしない〕）**
- **トークン実測**: total 240413 (input 236254 / cached_input 184832 / output 4159)
- **rollout jsonl**: `/home/sakagawa/.codex/sessions/2026/09/09/rollout-2026-09-09T08-44-20-01a08368-84c5-7e51-b1a3-27166e3db112.jsonl`
- **AGENTS.md 適用マーカー**: あり
- **所要**: 1 分 24 秒

## 指摘

### 1. 【高】S5 の想定値が `予想値 = 式（入力値）` の書式になっていない

`roadmap.md:217`

S5 の想定値が、基準 1.11(3) が必須とする `予想値 = 式（入力値）` になっていない。「10 成分を載せる」という説明のみで、§2.2 の各式と S2/S4 の実測値を入力として明示的に接続していない。

**修正案**: S5 に、`n_eff = n − u`、`p̂ = k/n_eff`、`M̂ = N × p̂`、`CI_lo = N × Wilson(k,n).lo`、`CI_hi = N × Wilson(k+u,n).hi`、`u/n` を、入力値が S2 の `N`・S4 の `n`・`k`・`u` である形で列挙すること。これにより外れ判定も実行可能になる。

## 適合と判定された項目

1.11 の他の項目は確認済み。独立成分は S2/S4 で被覆され、`q`・`p₀` は先行する S3 で測定され、`e` は最終受入要件ではない設計パラメータとして明記されている。確率的な幅と決定的な幅 0 の根拠も追跡できた。

## 対応（反映済み。2026-09-09）

修正案のとおり、§8 の S5 を書き直した。計算する 6 成分（`n_eff`・`p̂`・`M̂`・`CI_lo`・`CI_hi`・判定不能率）それぞれを `予想値 = 式（入力値）` の書式で列挙し、各式の入力値の出所（S2 の `N`、S4 の `n`・`k`・`u`、および同ステージ内の先行する式の値）を明示した。転記する 4 成分（`N`・`n`・`k`・`u`）については、予想値が当該ステージの実測そのものであることを明記した。幅は 6 成分に共通で 0 とし、その根拠（四則演算と平方根のみで乱数・外部状態・順序依存を含まない。不一致は計算誤りか転記誤り）を記載した。
