# feat-020: Q&A コラム見出しが見出しとして認識されない19件の修正

- **ステータス**: Open（2026-09-02 起票。**着手は後回し**。ユーザー決定）
- **対象書籍**: 『プログラミングのための確率統計』（`{BASE2}` = `/home/sakagawa/work/確率統計`）
- **対象章**: chap00・01・02・04・05・06・07・08・09 の9章、計19件
- **種別**: 機能追加（feat）

## 0. 起票の経緯

feat-014（Q&A コラム見出しの「？」アイコン分離8件の修正）の手動テスト中に、ユーザーが
**「`? 2.7` が無い」**ことに気づいたのが端緒である。調査したところ、`? 2.7` は氷山の一角で、
**Q&A コラム見出し全74件のうち19件が md 上で見出しになっていない**ことが判明した。

feat-014 は当初スコープ（アイコンが画像ブロックとして切り出された8件）のまま完了させ、
本19件は別案件として扱うことをユーザーが決定した（2026-09-02）。

### feat-014 の調査での見落としについて

feat-014 の調査で章ごとの Q&A 番号の連番を確認した際、検索式を
`^(## )?N\.M[ 　]` としたため、**行頭が `? 5.6 ` のように「?」で始まる本文段落を拾えなかった**。
その結果「md に痕跡なし」と誤判定した箇所があった。正しくは
`^(##\s+)?([？?][ 　]*)?([0-9A-C]+)\.([0-9]+)(?=[ 　])` のように、
`##` の有無と `?` の有無をいずれも省略可能として走査する必要がある。

`docs/PROJECT_KNOWLEDGE.md` に feat-019 で記した教訓（**正しい語を起点に、表記ゆれ・脱落・挿入の
バリアントを正規表現で網羅的に洗い出す**）は、語彙だけでなく**番号や見出し記号の検索にも
適用すべき**であった。この点は feat-014 の完了時に同ファイルへ追記済みである。

## 1. 全体像

確率統計の Q&A コラム見出しは全 **74件**ある。

| 状態 | 件数 |
|---|---|
| 正常な見出し（`## ? N.M …`。feat-014 で修正した8件を含む） | 55 |
| **見出しになっていない（本案件の対象）** | **19** |

### 章ごとの内訳

| 章 | Q&A 総数 | 正常 | 異常 |
|---|---|---|---|
| chap00 | 3 | 2 | 1 |
| chap01 | 10 | 8 | 2 |
| chap02 | 12 | 6 | 6 |
| chap03 | 8 | 8 | 0 |
| chap04 | 9 | 6 | 3 |
| chap05 | 10 | 8 | 2 |
| chap06 | 5 | 4 | 1 |
| chap07 | 4 | 2 | 2 |
| chap08 | 7 | 6 | 1 |
| chap09 | 6 | 5 | 1 |
| **計** | **74** | **55** | **19** |

## 2. 型の定義と対象19件

行番号は **2026-09-02 時点**（feat-014 適用後）の
`{BASE2}/ocr/final/chapNN/chapNN_gray300.md` のものである。

### 型α: 見出しにはなっているが「?」が欠落（3件）

`## N.M 質問文` の形。見出しレベルは正しいが、先頭の ？アイコンが完全に失われている。
**feat-014 の A 型と症状は同じだが、アイコンが画像ブロックとしても出ていない**点が異なる
（そのため feat-014 の「長辺 80px 以下の画像」走査では検出されなかった）。

| # | 章 | 番号 | 行 | 現状 |
|---|---|---|---|---|
| 1 | chap00 | 0.2 | 67 | `## 0.2 なぜ線形代数本の次が確率統計本になったのですか？` |
| 2 | chap01 | 1.3 | 145 | `## 1.3 翻訳って言うけど、元の話は確率的だったのに…そんなので翻訳と言っていいのか？` |
| 3 | chap06 | 6.2 | 142 | `## 6.2 パラレルワールドを横断して観察することはできないんだから、…じゃないの？` |

### 型β: 「?」は付いているが見出しでなく本文段落（13件）

`? N.M 質問文` の形の**通常の段落**。`##` が付いていない。

| # | 章 | 番号 | 行 | 現状（冒頭） |
|---|---|---|---|---|
| 4 | chap01 | 1.10 | 456 | `? 1.10 確率論の本格的な教科書を読んでみたら Ω をどうとるのかが…` |
| 5 | chap02 | 2.1 | 454 | `?2.1 P(Y = ウ, Z = ア) は周辺確率なんですか？…`（`?` と番号の間に空白なし） |
| 6 | chap02 | 2.9 | 1287 | `? 2.9 「▲▲は○○から独立」と言うほうが適切じゃないですか？…` |
| 7 | chap02 | 2.11 | 1451 | `? 2.11 なぜ（オ）が独立を表すのでしょうか？` |
| 8 | chap02 | 2.12 | 1690 | `?2.12 P(○○, △△, □□) = P(○○) P(△△) P(□□) だけで…`（空白なし） |
| 9 | chap04 | 4.6 | 758 | `? 4.6  $f_{X,Y}(x,y)$  と  $f_{Y,X}(y,x)$  は同じですか？` |
| 10 | chap04 | 4.7 | 844 | `? 4.7 積分する順番を変えて、たとえば $f_{Z}(z)=\int…$` |
| 11 | chap04 | 4.8 | 1733 | `? 4.8 なぜ $f_{X}(x)=$ 定数 $\cdot\exp(x$ の 2 次式) なら X は正規分布だと言えるの？` |
| 12 | chap05 | 5.6 | 1213 | `? 5.6 なぜ □ exp(x の成分の 2 次式) なら多次元正規分布なの？`（**`□` は要調査**） |
| 13 | chap05 | 5.10 | 1736 | `? 5.10 いまの図 5.38 は変です。…` |
| 14 | chap07 | 7.4 | 172 | `? 7.4 整数値の擬似乱数 $Z$ ( $≥ 0$ ) を使って、…` |
| 15 | chap08 | 8.6 | 520 | `? 8.6 ツキには波があるということが理論的にも証明されたわけだな。…` |
| 16 | chap09 | B.1 | 940 | `? B.1 3σ 以上離れた値が出る確率は 0.3% 以下だと習ったんだけど違うんですか?` |

### 型γ: 「?」も無く本文段落（1件）

| # | 章 | 番号 | 行 | 現状（冒頭） |
|---|---|---|---|---|
| 17 | chap02 | 2.7 | 734 | `2.7 さっきの例題 2.4 は、一枚目の色 X がまず決まって、…`（**ユーザーが気づいた件**） |

### 型δ: 別ブロックに取り込まれ「?」も欠落（2件）

**最も深刻な型。** 質問文が本文ですらない別種のブロックに吸収されている。

| # | 章 | 番号 | 行 | 現状 |
|---|---|---|---|---|
| 18 | chap02 | 2.5 | 573 | **数式ブロック `$$ … $$` の中**に `\text{}` で埋没:<br>`\text {2.5 もし} \mathrm{P} (X = a) = 0 \text {だったら} \mathrm{P} (Y = b \| X = a) \text {はどうなるの？}` |
| 19 | chap07 | 7.3 | 141 | **コードブロック ` ```txt ` の中**:<br>`7.3 [0,1) って何ですか？` |

## 3. 調査方法

### 3.1 Q&A 見出しの網羅的な走査

```bash
cd /home/sakagawa/work/確率統計/ocr/final
uv run python -c "
import re, glob
pat = re.compile(r'^(##\s+)?([？?][ 　]*)?([0-9A-C]+)\.([0-9]+)(?=[ 　])')
for p in sorted(glob.glob('chap*/chap*_gray300.md')):
    ch = p.split('/')[0]
    for i, line in enumerate(open(p, encoding='utf-8'), 1):
        line = line.rstrip(chr(10))
        m = pat.match(line)
        if m and m.group(2):
            kind = '見出し' if m.group(1) else '★本文段落'
            print(f'{ch} L{i} [{kind}] {m.group(3)}.{m.group(4)} | {line[:60]}')
"
```

`##` の有無と `?` の有無をいずれも省略可能にすることが要点である。

### 3.2 番号の連番の穴と、本文中の相互参照による裏づけ

各章の Q&A 番号を集めて連番の穴を探し、穴の番号について md 全文を検索する。
さらに**本文中の相互参照**（`？N.M` / `?N.M` の形で他の Q&A を指す記述）を集計すると、
**見出しが失われた Q&A が原本に実在することを裏づけられる**。

```bash
cd /home/sakagawa/work/確率統計/ocr/final
grep -ho '[？?][ 　]*[0-9A-C]\.[0-9]\+' chap0*/chap0*_gray300.md | sort | uniq -c
```

実際この方法で、`? 2.9` `? 4.6` `? 4.7` `? 4.8` `? 5.6` `? 8.6` などへの参照が本文に存在し、
これらの Q&A が実在することを確認した。

### 3.3 型δ の発見

型δ の2件は、番号で検索しても行頭にヒットしないため上記だけでは見つからない。
**連番の穴として残った番号を、行頭に限定せず全文検索する**ことで発見した。

```bash
grep -n '2\.5' chap02/chap02_gray300.md
grep -n '7\.3' chap07/chap07_gray300.md
```

## 4. 未調査の事項（着手時に行うこと）

本案件は**起票と調査記録のみ**で、以下は未実施である。

1. **原本 TIF による全19件の確認**（feat-014 と同様に、原本の見出し体裁と質問文を目視確認する）
2. 型δ の2件について、**どこからどこまでが質問文で、周囲のブロックがどう壊れているか**の精査
   （chap02 2.5 は数式ブロック全体が質問文なのか、他の数式が混在しているか。
   chap07 7.3 のコードブロックがどこで閉じているか）
3. #12（chap05 5.6）の `□` が何の誤認識か（原本確認が必要）
4. `content_list.json` 側の状態（`text_level` の有無、ブロック分割の様子）
5. 修正方針の決定とユーザー確認
   - 型α: 見出しに `? ` を補うだけでよいか
   - 型β: `## ` を付けて見出しに昇格させるか。`?` と番号の間の空白の有無をどう揃えるか
     （#5・#8 は空白なし。既存の正常な見出しにも `## ?N.M` が8件あり、
     この表記ゆれの扱いは feat-014 でスコープ外としたまま）
   - 型γ: `## ? ` を補う
   - 型δ: 数式ブロック／コードブロックからの取り出しが必要。
     `apply_fixes.py` の複数行 `old`/`new` で表現できるかの検討
6. `apply_fixes.py` での実現可能性と、`old`/`new` の一意性の検証

## 5. 想定される対処

feat-014・feat-015・feat-016・feat-017・feat-019 と同じく、
`{BASE2}/ocr/fixes/chapNN.json` への追記 ＋ `apply_fixes.py` → `build_final.py` の再適用で
対処できる見込みである（**リポジトリ内のコード変更なしの見込み**）。

ただし型δ の2件は、削除する範囲が数式・コードブロックの区切り記号（`$$` / ` ``` `）に及ぶため、
`old`/`new` の設計に注意を要する。

## 6. スコープ外（想定）

- `content_list.json` と `images/` の変更（feat-010 以来の既存ポリシー。feat-014 ADR-3）
- MinerU が Q&A コラム見出しを正しく見出しとして認識するようにする根本対処
  （layout 解析の挙動であり、本プロジェクトから制御できない）
- PRML（`{BASE}`）側の同種調査（PRML には Q&A コラムの体裁がない。feat-014 案件 README §7）

## 7. 関連案件

- **feat-014**: Q&A コラム見出しの「？」アイコン分離8件の修正 — **本案件の直接の前段**。
  アイコンが独立した画像ブロックとして切り出された8件を扱った。
  本19件にはアイコン画像が出ておらず、別の現象である
- feat-015: chap02 の Q&A コラム見出しの分断修復 — 見出しテキストが2ブロックに分断された1件
- feat-010: `apply_fixes.py`（修正定義ファイルの機械適用）— 想定される修正手段
- feat-012: `build_final.py`（final 構築）— 想定される再構築手段
