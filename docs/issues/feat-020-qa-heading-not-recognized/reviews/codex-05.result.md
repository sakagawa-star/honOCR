# codex-05 レビュー結果（依頼 B: 解消確認・実装フェーズでの設計修正）

| 項目 | 内容 |
|---|---|
| 日付 | 2026-09-05 |
| 対象ファイル | `docs/issues/feat-020-qa-heading-not-recognized/requirements.md`、`docs/issues/feat-020-qa-heading-not-recognized/design.md` |
| ストリーム名 | `rev-honocr-feat-020` |
| 依頼種別 | **B（解消確認）** |
| 直前に `/new` を送ったか | **No**（codex-03 = C と同一会話の継続） |
| ゲート状態 | **実施済み: `reviews/codex-03.result.md`**（依頼 C） |
| 指摘数 | 高 **0** / 中 **0** / 低 **0** |
| 収束判定 | **収束**（根拠: ゲート実施済み（`reviews/codex-03.result.md`）の B で高・中ゼロ） |
| トークン実測（累積） | {'input_tokens': 973501, 'cached_input_tokens': 883712, 'cache_write_input_tokens': 0, 'output_tokens': 4771, 'reasoning_output_tokens': 318, 'total_tokens': 978272} |
| rollout jsonl | `/home/sakagawa/.codex/sessions/2026/09/05/rollout-2026-09-05T19-23-57-01a07118-adbc-70d3-bce3-576101879529.jsonl` |

## 経緯: 実装フェーズで受け入れ基準の誤りが判明した

**実装（ステップ6）で9章すべての適用に成功したが、検証で FR-002 基準5 が満たせないことが判明した。**
基準は「適用後の chap07 に ` ```txt ` が **0 件**」としていたが、実際は **4 件**残る。

**原因**: chap07 には Q&A 見出しとは無関係の正当なコードブロックが**4つ**ある
（md 283 行のプログラム例、544・565・576 行の端末セッション）。基準を書いた際にこれを数えず、
「7.3 のブロックが唯一の ` ```txt ` である」と誤って仮定した。

**データ側は正しい**。適用前 5 件 → 適用後 4 件で、**意図した 7.3 のブロックだけが1つ解体**されている。
実装を担当した Sonnet サブエージェントは、書き込み自体は正しいと判断して**ロールバックせず中断・報告**した。

CLAUDE.md 機能追加フローのステップ6 の規定（想定外事象が起きたら中断し、
「調査・計画 → ドキュメントの修正」に戻ってレビューを経る）に従い、本依頼を送った。

## 修正内容

1. `requirements.md` **FR-002 基準5 を3項目に分割**:
   - chap04: `mineru-algorithm` の出現回数が **0**（適用前は 1）
   - chap07: ` ```txt ` フェンスの出現回数が **5 → 4**。**0 にはならない**（理由を明記）
   - chap07: 残る4つのフェンスについて、**直後の行が Q&A 見出しの形（`N.M ` で始まる）である
     ブロックが 0 件**（＝コードブロックに取り込まれた Q&A 見出しが残っていない）
2. `design.md` **§7 手順1 の検証スクリプトを同じ判定に変更**（フェンス数 == 4 かつ
   フェンス直後が Q&A 見出し形式のブロック == 0）。期待値の表も3行に分割
3. `design.md` **§4.4 に注記を追加**（「chap07 の ` ```txt ` が 0 件になることを
   受け入れ基準にしてはならない。本設計の初版はこれを誤っており、実装フェーズで発覚して修正した」）

## codex の判定

**「今回の修正は妥当です。変更に伴う新たな高・中の問題は認めません。前回、この誤った期待値を
見落としていました。」**（原文）

確認内容（原文の要旨）:

- **FR-002 基準5 と設計の一致**: 要求・§4.4 の注記・§7 の検証式と期待値が、いずれも
  「txt フェンス4件、直後が Q&A 見出し形式のブロック0件」で一致している
- **実データでの確認**: 更新された §7 手順1 の Python 部分を実行し `ALL_OK` を確認した。
  残る4ブロックの位置も記載どおり。**修正定義から適用前をメモリ上で復元するとフェンスは5件になる**
- **他の基準との整合性**: 見出し74件・本文参照1件、全9章の文字数・行数は合格。
  **FR-005 が保護する 7.3 の本文も変更されていない**。§9 に旧「0件」を要求する記述はなく矛盾もない

## Codex レビューの記録（本案件の全5往復）

| # | 依頼種別 | `/new` | 高 | 中 | 低 | 結果 |
|---|---|---|---|---|---|---|
| codex-01 | A（初回レビュー） | No | 2 | 0 | 0 | 配列初期化の失敗／書き込み前の不一致でも復元を指示 → 全件反映 |
| codex-02 | B（解消確認） | No | 0 | 0 | 0 | 解消。ゲート未実施のため未収束 |
| codex-03 | **C（全文ゲート）** | **Yes** | 2 | 0 | 0 | 差分ハンク数の基準が実際と不一致／退避失敗時に既存 JSON を削除しうる → 全件反映 |
| codex-04 | B（解消確認） | No | 0 | 0 | 0 | **一度収束**し人レビューを通過 → 実装へ |
| codex-05 | B（実装中の設計修正の確認） | No | **0** | **0** | **0** | **再収束**（本ファイル） |
